#!/bin/sh
# Copyright 2025 The Wardyn Authors
# SPDX-License-Identifier: Apache-2.0

# Grader for interactive-repl — INTENTIONAL PLACEHOLDER.
#
# This task is graded from the PTY OUTPUT STREAM by the Go orchestrator (see
# task.yaml `expects:` and README.md), not from final workspace state. There is
# nothing to inspect under /ws, so this grader is a no-op that always passes.
# This is the one acknowledged exception to the "grade.sh inspects /ws" contract.
exit 0
