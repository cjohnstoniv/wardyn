# Wardyn environment variables

Registry of every `WARDYN_*` environment variable **read** by non-test Go in
this repo, with its type, default, and the binary that reads it.

The variable list is generated by grep (so it stays complete); a ratchet test
(`cmd/wardynd/envdoc_guard_test.go`) runs it **both ways** — a `WARDYN_*` literal
read in non-test Go but missing here fails the build, and so does a row here whose
last Go reader was deleted. To regenerate the raw list:

```sh
grep -rhoE '"WARDYN_[A-Z0-9_]+"' --include='*.go' --exclude='*_test.go' . \
  | tr -d '"' | sort -u
```

**What the ratchet does not check: the Default column.** It compares *names* only,
so a default that drifts from its `flagEnv`/`flagBool`/`flagDuration` call site goes
red nowhere — check the call site when you edit one. This file is deliberately
hand-written rather than generated: the generated columns are the ones already
correct, and generating it would cost the Notes column, where every load-bearing
caveat lives.

Most `wardynd` / `wardyn-tetragon-ingest` variables are the env form of a CLI
flag: the flag wins when both are set, and the env value only moves the flag's
default (`internal/cliutil`). Every parseable read through `internal/cliutil`
(bool / duration / int) is **loud** — an unparseable value exits 2 rather than
silently reinstating the default. Unset or empty (`docker run -e VAR` / compose
`VAR=`) keeps the default quietly.

Values that are secrets (tokens, DSNs, keys, base64 blobs) are marked 🔒 — never
log them.

## `wardynd` (control plane)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_LISTEN` | string | `:8080` | HTTP listen address (flag `-listen`) |
| `WARDYN_ADMIN_TOKEN` 🔒 | string | **binary: empty**; compose: `demo-admin-token` | admin bearer gating the public API (flag `-admin-token`). Two layers, and they differ: `wardynd`'s own default is the **empty string**, and an empty token with no OIDC issuer on a loopback bind **auto-enables no-auth LOCAL HOST MODE** (`cmd/wardynd/boot_flags.go`) — so leaving it unset does not leave the API bearer-gated, it turns the gate off. The compose stack supplies `demo-admin-token` (`deploy/compose/docker-compose.yaml`) — nominally authenticated: that token is published here, so wardynd warns at boot when it is in use and refuses to start outright if the listen address binds a specific non-loopback interface. Note the recommended `make setup` containerized path additionally writes `WARDYN_LOCAL_MODE=true` into the compose env, so the flagship install runs the no-auth loopback local mode; the bare compose file's own default is the token. There is no `WARDYN_TOKEN` fallback here; that variable is CLI-only |
| `WARDYN_PG_DSN` 🔒 | string | (unset, required) | Postgres DSN (flag `-dsn`). pgx's own `pool_max_conns` DSN param, not a `WARDYN_*` var, so it is not its own row here — but operators should size it **at least 2, and at least 4 with the ground-truth rotator enabled**: the single-instance lock (`db.SingleInstanceLockKey`) holds one connection for the whole process lifetime (wardynd refuses to start while another instance holds it; `-allow-multi-instance` lifts that), the lifecycle reaper borrows one per tick (transient, released immediately after), and the ground-truth rotator's leader-election lock (`db.GroundTruthRotatorLockKey`) holds one for the whole process lifetime once acquired — both on top of whatever the pool needs for its own request-serving queries. At 2, one person's concurrent sign-ins are not serialized — each such pass writes an `auth.signin_unserialized` audit row — so give it at least 3 to serialize them. An organisation that enrols laptops (hybrid enrolment) should add headroom for them: each enrolled device's audit push holds one connection while its claimed hashes are recomputed, a device has at most one push in flight (a second concurrent push is answered 429 with `Retry-After`), so budget one connection per device you expect to push at the same moment |
| `WARDYN_PG_MIGRATE_DSN` 🔒 | string | (unset) | optional owner/migrator DSN; splits migrate vs runtime pool (flag `-migrate-dsn`). Migrations run as this role, `WARDYN_PG_DSN` becomes the least-privilege runtime role, and `audit_events` gains DDL protection — a compromised app role cannot `DROP TRIGGER` or `ALTER TABLE … DISABLE TRIGGER` its way past the append-only guard. **Adopting it on an existing database has exactly one correct direction**: the role you already use OWNS every table and function, so it becomes THIS one, and the role you create becomes `WARDYN_PG_DSN`. A migrator that owns nothing cannot run the `ALTER TABLE` and `CREATE OR REPLACE FUNCTION` migrations an upgrade applies, and wardynd fails closed rather than half-migrating. Procedure, grants and the boot check that verifies the split actually took: [OPERATIONS.md](OPERATIONS.md) → "Splitting the migrator and app roles" |
| `WARDYN_MIGRATE_TIMEOUT` | duration | `5m` | how long `db.Migrate` may run before boot fails closed — separate from the fixed 30s connect budget, so a slow migration (e.g. an index build on a large table) doesn't crash-loop the upgrade (flag `-migrate-timeout`) |
| `WARDYN_IDENTITY` | string | `embedded` | identity provider seam (flag `-identity`) |
| `WARDYN_TRUST_DOMAIN` | string | `wardyn.local` | SPIFFE trust domain (flag `-trust-domain`) |
| `WARDYN_SECRET_STORE` | string | `pg` | secret store seam (flag `-secret-store`): `pg` seals each credential in Postgres under a local key (`WARDYN_AGE_KEY`); `vaultkv` is **store mode**: each value lives in the organisation's Vault KV v2 (OpenBao is a supported endpoint), the Postgres row is a pointer to it, and wardynd holds no key and does no at-rest cryptography; `azurekv` is store mode in the organisation's Azure Key Vault. Reads follow each row, so either setting reads the other's rows while both are configured; move rows with `wardynd -migrate-secrets -to=vaultkv\|azurekv\|local`. One external store is configured at a time. See [OPERATIONS.md](OPERATIONS.md) → "Store mode: credentials in Vault" and "Store mode: credentials in Azure Key Vault" |
| `WARDYN_SECRET_STORE_TIMEOUT` | duration | `5s` | timeout of each call to an external secret store (flag `-secret-store-timeout`); a 429, a 5xx or a network failure is retried three times with backoff before the call counts as transient; a store-mode write, the wait for its credential's lock included, is bounded at six times this |
| `WARDYN_VAULT_ADDR` | string | (unset) | Vault address for the `vaultkv` store (flag `-vault-addr`). `https://`; `http://` is refused except to a loopback host. Set, wardynd logs in at boot and refuses to start if it cannot; unset, there is no Vault client and a pointer row refuses by name |
| `WARDYN_VAULT_NAMESPACE` | string | (unset) | Vault Enterprise/HCP namespace, sent as `X-Vault-Namespace` (flag `-vault-namespace`) |
| `WARDYN_VAULT_AUTH` | string | `kubernetes` | `kubernetes` (a projected service-account token) or `token-file` (flag `-vault-auth`). There is no token-in-env option, by design |
| `WARDYN_VAULT_AUTH_MOUNT` | string | `kubernetes` | mount of Vault's Kubernetes auth method (flag `-vault-auth-mount`) |
| `WARDYN_VAULT_ROLE` | string | (unset) | Kubernetes-auth role wardynd logs in as (flag `-vault-role`); required with `WARDYN_VAULT_AUTH=kubernetes` |
| `WARDYN_VAULT_ROLE_PLATFORM` | string | (unset) | optional second Kubernetes-auth role (flag `-vault-role-platform`, chart `secretStore.vault.rolePlatform`): wardynd reads and writes its own signing, session and SSH host keys (`<prefix>/platform/`) as this role and everything else as `WARDYN_VAULT_ROLE`, holding two tokens. **Recommended**: with policies split the same way, a leak of the credentials token reaches no platform key. Refused with `WARDYN_VAULT_AUTH=token-file` and when it equals `WARDYN_VAULT_ROLE`. See [OPERATIONS.md](OPERATIONS.md) → "Two Vault roles" |
| `WARDYN_VAULT_K8S_TOKEN_FILE` | string | (unset; the chart sets `/var/run/secrets/wardyn-vault/token`) | the projected service-account token (audience `vault`) for Kubernetes auth (flag `-vault-k8s-token-file`); re-read at every login, since the kubelet refreshes it in place |
| `WARDYN_VAULT_TOKEN_FILE` | string | (unset) | a file holding a Vault token, for `WARDYN_VAULT_AUTH=token-file` (a Vault Agent sink or a CSI file; flag `-vault-token-file`); re-read on every 403 |
| `WARDYN_VAULT_CACERT_FILE` | string | (unset) | PEM bundle added to the system roots for the Vault client only (flag `-vault-cacert-file`); unset, `WARDYN_TRUSTED_CA_FILE` is used, else the system roots. The Vault client's TLS config is its own, never shared |
| `WARDYN_VAULT_KV_MOUNT` | string | `wardyn` | the KV v2 mount (flag `-vault-kv-mount`) |
| `WARDYN_VAULT_KV_PREFIX` | string | `wardyn`; the chart sets the release namespace | this install's path prefix under the mount (flag `-vault-kv-prefix`). Values live at `<prefix>/platform/<name>` (Wardyn's boot keys), `<prefix>/operator/<name>` and `<prefix>/people/<owner in base32hex>/<name>` |
| `WARDYN_VAULT_KV_MAX_VERSIONS` | int | `1` | `max_versions` set on each secret the store creates (flag `-vault-kv-max-versions`); 1 means a replaced value does not linger |
| `WARDYN_KEK` | string | `local` | the key that wraps each stored credential's data key in Postgres (flag `-kek`): `local` (derived from `WARDYN_AGE_KEY`) or `transit`, the Vault Transit key `WARDYN_VAULT_TRANSIT_KEY` names, over the same Vault client (`WARDYN_VAULT_ADDR`, its auth and TLS) as the `vaultkv` store. With `transit`, no age key is needed or generated once no row is sealed under it (boot refuses by name until `wardynd -rewrap` reports none left). wardynd proves the key at boot, and refuses to start if a probe does not round-trip or if the key unwraps a probe under another row's `associated_data` (a key type that does not bind it). Reads follow each row's `kek_id`, so a row sealed under either key stays readable while both are configured. Anything else refuses boot. See [OPERATIONS.md](OPERATIONS.md) → "Key service: Vault Transit" |
| `WARDYN_VAULT_TRANSIT_MOUNT` | string | `transit` | mount of Vault's Transit engine (flag `-vault-transit-mount`) |
| `WARDYN_VAULT_TRANSIT_KEY` | string | (unset) | the Transit key (type `aes256-gcm96`) that wraps data keys (flag `-vault-transit-key`); required with `WARDYN_KEK=transit`. Set with `WARDYN_KEK=local`, it only reads the rows sealed under it, which is how `wardynd -rewrap` moves them back to the local key. Needs `WARDYN_VAULT_ADDR`. Its row `kek_id` is `transit:<mount>/<key>` |
| `WARDYN_AZURE_KV_URL` | string | (unset) | Azure Key Vault base URL for the `azurekv` store, `https://<vault>.vault.azure.net` (flag `-azure-kv-url`); `http://` is refused except to a loopback host. Set, wardynd gets an Entra token at boot and refuses to start if it cannot; unset, there is no Key Vault client and an `azurekv` pointer row refuses by name. Not together with `WARDYN_VAULT_ADDR` |
| `WARDYN_AZURE_AUTH` | string | `workload-identity` | how wardynd gets its Entra token (flag `-azure-auth`): `workload-identity` exchanges a projected Kubernetes token for one (a federated credential, no client secret); `managed-identity` asks a VM's instance metadata service, never through a proxy. There is no client-secret option, by design |
| `WARDYN_AZURE_TENANT_ID` | string | (unset) | Entra tenant id (flag `-azure-tenant-id`); required with `workload-identity` |
| `WARDYN_AZURE_CLIENT_ID` | string | (unset) | client id of the app registration or user-assigned managed identity wardynd runs as (flag `-azure-client-id`); required with `workload-identity`, optional with `managed-identity` (unset: the VM's system-assigned identity) |
| `WARDYN_AZURE_FEDERATED_TOKEN_FILE` | string | the value of `AZURE_FEDERATED_TOKEN_FILE` | the projected token exchanged for an Entra token (flag `-azure-federated-token-file`). Unset, the path the AKS workload identity webhook sets in `AZURE_FEDERATED_TOKEN_FILE` is used. Re-read at every exchange, since the kubelet refreshes it in place |
| `WARDYN_AZURE_AUTHORITY_HOST` | string | `https://login.microsoftonline.com` | Entra authority host (flag `-azure-authority-host`), for a sovereign cloud |
| `WARDYN_AZURE_KV_PREFIX` | string | `wardyn`; the chart sets the release namespace | the start of every secret name this install writes (flag `-azure-kv-prefix`): 1-63 lowercase letters, digits and inner `-`. Names are `<prefix>-<platform\|operator\|people>-<32 hex of a hash of the owner and name>-g<generation>` |
| `WARDYN_AZURE_KV_MAX_VERSIONS` | int | `100` | versions a secret holds before the next write starts a new name and deletes the old one (flag `-azure-kv-max-versions`); 1-500. Key Vault cannot delete old versions, so every write disables the ones before it |
| `WARDYN_AZURE_KV_PURGE` | string | `auto` | `auto` purges a removed secret right after its soft delete, when the vault allows it (a vault with purge protection, or a role without purge, leaves it soft-deleted and the `secret.delete` audit row says `purged: false`); `never` always leaves it soft-deleted for the vault's retention (flag `-azure-kv-purge`) |
| `WARDYN_RECORDING_STORE` | string | `pg` | recording store seam — `pg` (Postgres-backed, visible to every replica), `fs` (legacy per-pod on-disk store) or `off` (no recording, no replay) (flag `-recording-store`). Recording is **on** with the default `pg`: every PTY asciicast is persisted into the control-plane database, and `WARDYN_RECORDING_RETENTION_DAYS` defaults to keep-forever. **The off switch is `WARDYN_RECORDING_STORE=off`** (what the Helm chart ships when `persistence.enabled=false`). An EMPTY env value keeps the compiled default here as everywhere (0.7), so the pre-0.7.1 recipe `fs` + `WARDYN_RECORDING_DIR=""` disables nothing — it kept `./data/recordings` and crash-looped the stock chart on its read-only root FS |
| `WARDYN_RECORDING_DIR` | string | `./data/recordings` | PTY recording directory; `fs` store only, **ignored by `pg`** (flag `-recording-dir`). An EMPTY value keeps the default (0.7, like every env var here) — it disables nothing; turn recording off with `WARDYN_RECORDING_STORE=off` |
| `WARDYN_RECORDING_RETENTION_DAYS` | int | `0` | delete stored session recordings older than N days; `0` (the default) keeps them forever. A recording is governance evidence, so nothing is deleted unless you set a window; each sweep that removes anything emits a `recording.retention.sweep` audit event. fs and pg stores (flag `-recording-retention-days`) |
| `WARDYN_RUNNER` | string | `none` | runner substrate: `none` or a registered confinement substrate (`docker` in `-tags docker` builds, `k8s` in `-tags k8s` builds — both may be registered in a `-tags docker,k8s` build; unknown/uncompiled names fail closed at boot) (flag `-runner`) |
| `WARDYN_RUNNER_TARGET` | `docker` \| `k8s` | (unset = the resolved target, `none` when no runner is configured) | **test harnesses only.** The substrate name **stored objects validate against** while `WARDYN_RUNNER` is `none`. A runner-less daemon resolves the target `none`, and a user drive names exactly one target that can mount its backend (`docker` for `docker_volume`/`host_path`, `k8s` for `k8s_pvc`/`k8s_pvc_static`), so without this every drive backend is refused with a 400 and no drive can be registered at all — which is the daemon `scripts/e2e-backend.sh` boots for the Playwright suite. It changes what may be **registered**, never what is **dispatched**: the runner is still nil, so every run still stays `PENDING`. **Ignored** whenever a runner *is* configured (the resolved substrate's own name is the only truthful target), and any other value fails boot closed (flag `-runner-target`) |
| `WARDYN_INTERNAL_NETWORK` | string | `wardyn-internal` | the docker network the runner attaches each run's proxy sidecar to; must match the compose control-plane network name (both derive from the compose namespace) or the sidecar lands on the wrong bridge |
| `WARDYN_ALLOW_UNENFORCEABLE_CAPS` | `1` only | off | escape hatch: allow a run to start on a host where the resource caps (memory/pids/cpu) can't actually be enforced. Off by default — the gate fails closed, since an unenforceable cap is a silently-broken guarantee. **Not a `cliutil` bool**: `register.go` compares against the literal string `1`, so `true`/`yes`/`on` do **not** enable it and a garbage value is silently ignored rather than exiting 2 |
| `WARDYN_K8S_NAMESPACE` | string | the pod's own namespace (read from `/var/run/secrets/kubernetes.io/serviceaccount/namespace`), else `default` | Kubernetes namespace the `k8s` substrate (`-tags k8s`) creates every sandbox's Secret/NetworkPolicies/pods in |
| `WARDYN_K8S_IMAGE_PULL_SECRET` | string | (unset) | optional pre-existing Secret name threaded onto `imagePullSecrets` for every pod the `k8s` substrate creates (agent, proxy, and the boot-time egress canary) (`-tags k8s`) |
| `WARDYN_K8S_ALLOW_UNENFORCED_NETPOL` | `1` only | off | escape hatch: allow the `k8s` substrate to boot when its boot-time egress canary proves the cluster's CNI does NOT enforce NetworkPolicy — CreateSandbox then proceeds with UNCONFINED egress instead of refusing to boot. Off by default (fail closed). The opt-out is never an invisible downgrade: it logs a loud warning at construction naming what is going unconfined, and `ClassSupport.NetworkPolicy`/`StructuralEgress` both still report `false` regardless — an opted-out substrate never reads identical to a genuinely confined one on `/healthz` (`-tags k8s`) |
| `WARDYN_K8S_ACK_AMBIENT_DEFAULT_DENY` | `1` only | off | on a managed cluster where a platform team already applies a baseline default-deny NetworkPolicy, phase A of the boot-time egress canary (which applies no policy of its own) fails in exactly that shape — the canary pod reaches Running and its own connect exits `1` — and construction refuses to boot with no override at all. This flag acknowledges that shape as an expected platform baseline rather than a Wardyn misconfiguration and lets boot proceed; phase B is skipped (behind an existing ambient deny it could only ever also exit 1, proving nothing). Never proof of enforcement: `ClassSupport.NetworkPolicy` stays `false`, `NetworkPolicyAcknowledged` reports `true`, and the setup checklist's `k8s_egress_containment` row grades `warn` ("acknowledged, not proven"), never `ok`. Any OTHER canary failure shape (never reached Running, a different exit code, apiserver unreachable) stays indeterminate regardless of this flag — it is scoped exactly to the one shape it explains (`-tags k8s`) |
| `WARDYN_DEFAULT_POLICY` | string | `examples/policies/default.json` | default RunPolicy spec path (flag `-default-policy`) |
| `WARDYN_UI_DIR` | string | (unset) | built web UI directory (flag `-ui-dir`) |
| `WARDYN_HOST_PROXY_B64` | string (base64 JSON) | (unset) | host-side proxy detection, captured by `scripts/up.sh` on the host and consumed by the Getting-started "Host proxy" step. **Diagnostics only.** Unset ⇒ the step honestly reports it could not look at the host. See [Four variables that need more than a table cell](#four-variables-that-need-more-than-a-table-cell) |
| `WARDYN_AUDIT_SINKS` 🔒 | string (JSON) | (unset) | audit sink config file/webhook/syslog (flag `-audit-sinks`). A webhook `bearer_token` requires an `https://` url — boot fails rather than replay the SIEM credential in cleartext on every POST. Schema: [Audit sinks](#audit-sinks-wardyn_audit_sinks) below |
| `WARDYN_AUDIT_SPOOL` | string | `./data/audit-spool.jsonl` | append-only JSONL fallback for failed audit writes (flag `-audit-spool`); a sibling `<path>.quarantine` appears if the store permanently refuses a spooled line |
| `WARDYN_AUDIT_SOURCE` | string | (unset) | #10: optional static string stamped as an extra top-level `"source"` field on every event a configured sink (file/webhook/syslog) serializes — never written to Postgres, sink payloads only (flag `-audit-source`). Lets one SIEM index ingesting from several wardynd instances/environments (staging vs prod, cluster A vs B) tell them apart without per-sink config. Empty (the default) is byte-identical to before this field existed. See [Audit sinks](#audit-sinks-wardyn_audit_sinks) below |
| `WARDYN_AGE_KEY` 🔒 | string | (unset) | age X25519 identity (flag `-age-key`). **Unset is not a safe default**: wardynd then mints a throwaway identity on every boot, so boot 1 encrypts its own signing key into a persistent Postgres under identity A and boot 2 cannot decrypt it and fails closed (`loadOrCreateSecret`, `cmd/wardynd/main.go`) — the daemon crash-loops on its SECOND start and those rows are unrecoverable. All three front doors refuse rather than boot that way: `scripts/up.sh` dies if `-gen-age-key` mints nothing, `install.sh` dies on an upgrade whose `.env` has no `AGE-SECRET-KEY-` line, and the Helm chart fails the render unless `secrets.allowEphemeralAgeKey=true`. **Store mode** (`WARDYN_SECRET_STORE=vaultkv` or `azurekv`) is the exception: there no key is generated when it is unset, and unset is the goal. It is needed only while local rows remain (boot refuses by name until `wardynd -migrate-secrets -to=vaultkv` (or `-to=azurekv`) reports none left). So is **`WARDYN_KEK=transit`**, where Vault's Transit key wraps every new row: unset is the goal there too, and once `wardynd -rewrap` reports no row left under the age key it must be unset — wardynd refuses to start while it is set with no stored row sealed under it, since it could then only let whoever also holds the database forge Wardyn's boot keys |
| `WARDYN_PLATFORM_KEY_FILE` 🔒 | path | (unset) | local mode only (flag `-platform-key-file`): a file holding a SECOND age identity (`wardynd -gen-age-key`) from which alone wardynd derives the key that wraps its own signing, session, UI-session and SSH host keys, so `WARDYN_AGE_KEY` protects only credentials and a leak of it forges no run identity or console session. Unset, both derive from `WARDYN_AGE_KEY` and `/setup/status` shows `platform_shared`. Refused without `WARDYN_AGE_KEY`, when it holds the same key, or a published one. Setting it on an install that already holds boot keys needs `wardynd -rewrap` once (boot refuses by name until then). With `WARDYN_KEK=transit` the boot keys are wrapped by Transit like every other row, and the file only reads the ones `wardynd -rewrap` has not moved yet. See [OPERATIONS.md](OPERATIONS.md) → "Separating the platform keys" |
| `WARDYN_PG_DSN_FILE` | string (path) | (unset) | file holding `WARDYN_PG_DSN`'s value, read once at boot — for a Vault Agent / Secrets Store CSI / Secret-volume file instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_PG_DSN` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_PG_MIGRATE_DSN_FILE` | string (path) | (unset) | file holding `WARDYN_PG_MIGRATE_DSN`'s value, read once at boot — for a Vault Agent / Secrets Store CSI / Secret-volume file instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_PG_MIGRATE_DSN` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_ADMIN_TOKEN_FILE` | string (path) | (unset) | file holding `WARDYN_ADMIN_TOKEN`'s value, read once at boot — for a Vault Agent / Secrets Store CSI / Secret-volume file instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_ADMIN_TOKEN` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_AGE_KEY_FILE` | string (path) | (unset) | file holding `WARDYN_AGE_KEY`'s value, read once at boot — for a Vault Agent / Secrets Store CSI / Secret-volume file instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_AGE_KEY` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). Every `*_FILE` resolves while flags are parsed, before `-gen-age-key` and `-print-groundtruth-token` run, so a stale `_FILE` naming a missing file makes those exit 1 too. See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_OIDC_CLIENT_SECRET_FILE` | string (path) | (unset) | file holding `WARDYN_OIDC_CLIENT_SECRET`'s value, read once at boot — for a Vault Agent / Secrets Store CSI / Secret-volume file instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_OIDC_CLIENT_SECRET` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_DIRECTORY_CLIENT_SECRET_FILE` | string (path) | (unset) | file holding `WARDYN_DIRECTORY_CLIENT_SECRET`'s value, read once at boot — for a Vault Agent / Secrets Store CSI / Secret-volume file instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_DIRECTORY_CLIENT_SECRET` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_AUDIT_SINKS_FILE` | string (path) | (unset) | file holding `WARDYN_AUDIT_SINKS`'s value, read once at boot — for a Vault Agent / Secrets Store CSI / Secret-volume file instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_AUDIT_SINKS` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_GEN_AGE_KEY` | bool | `false` | generate a fresh age identity and exit (flag `-gen-age-key`) |
| `WARDYN_LOCAL_MODE` | bool | `false` | LOCAL HOST MODE: bypass public-API auth (flag `-local-mode`) |
| `WARDYN_LOCAL_OPERATOR` | string | `local:<os-user>` | operator principal stamped in local-mode (flag `-local-operator`) |
| `WARDYN_LOCAL_TRUST_FORWARDER` | bool | **binary: `false`; compose: `true`** | accept a non-loopback peer under local-mode (flag `-local-trust-forwarder`) |
| `WARDYN_ALLOW_LOCAL_MODE_WITH_OIDC` | bool | `false` | override: allow boot with `-local-mode` explicitly set alongside a configured `WARDYN_OIDC_ISSUER` — i.e. silently disable the configured OIDC/SSO admin-member RBAC deployment and attribute every request to the fixed local operator — normally refused (`resolveLocalMode`, `cmd/wardynd/boot_flags.go`). The auto-enable heuristic (no explicit flag, no admin token, loopback bind) is unaffected — it already excludes a configured issuer on its own (flag `-allow-local-mode-with-oidc`) |
| `WARDYN_ALLOW_SHARED_SUBSCRIPTION` | bool | `false` | override: allow ONE operator's Anthropic subscription credential to be injected into runs on a daemon that is **not** `-local-mode` — e.g. the compose demo stack, which authenticates with a shared admin token rather than local mode. Does **not** waive the refusals on the k8s runner or a configured `WARDYN_OIDC_ISSUER`: those are multi-user by definition, and sharing one subscription there breaches the harness vendor's requirement that each end user authenticate with their own credential — putting the OPERATOR in breach, not Wardyn. Single-user demo boxes only; prefer a per-user API key (`wardyn secret set anthropic-api-key`) or Bedrock. See `subscriptionInjectPosture` in `cmd/wardynd/boot_posture.go` (flag `-allow-shared-subscription`) |
| `WARDYN_USER_DESKTOP` | bool | `false` | **member-mode desktop**: assert that the human using this daemon is a **member** and the operator authority is elsewhere (an org IdP / MDM-managed config). It adds no middleware — the admin/member split in `internal/api` already enforces everything — it makes the assumption *checkable*: boot is **refused** unless local mode is off (it bypasses public-API auth and would make the loopback developer an admin) **and** OIDC is configured (with no signed-in identity there is no role to derive, so every caller is an admin). See `validateMemberModePosture` (`cmd/wardynd/boot_posture.go`) and [docs/design/member-role-desktop.md](design/member-role-desktop.md) (flag `-member-mode`) Deprecated alias `WARDYN_MEMBER_MODE` still works and WARNs at boot through 0.8.x; removed in 0.9. |
| `WARDYN_ORG_URL` | string | (unset) | the org control plane a managed laptop belongs to — the missing half of member-mode desktop, which asserts the human is a member but never said **which** org. Unset (the default) is no hybrid posture at all. Set, boot is **refused** unless `WARDYN_USER_DESKTOP` is also on, and unless the URL is `https://` **or** names a loopback host (`WARDYN_ALLOW_PLAINTEXT_LISTEN=true` overrides the scheme check, the same escape hatch `-listen`'s own plaintext refusal uses) — the enrolment token below travels with every request to it. See `validateHybridPosture` (`cmd/wardynd/boot_posture.go`) (flag `-org-url`) |
| `WARDYN_ORG_ENROLMENT_TOKEN` 🔒 | string | (unset) | secret enrolment token this device presents to `WARDYN_ORG_URL`. Setting it with `WARDYN_ORG_URL` unset is **refused** at boot — a token with nowhere to send it is a misconfiguration, not a no-op (flag `-org-enrolment-token`) |
| `WARDYN_ORG_ENROLMENT_TOKEN_FILE` | string (path) | (unset) | file holding `WARDYN_ORG_ENROLMENT_TOKEN`'s value, read once at boot — for an MDM-delivered or otherwise file-backed token instead of an env var. One trailing newline is trimmed. Setting it together with `WARDYN_ORG_ENROLMENT_TOKEN` refuses boot, and so does an unreadable, empty, group- or world-writable file, or one wardynd's own non-root uid owns that others can read (`resolveSecretFiles`, `cmd/wardynd/secret_file.go`). See [OPERATIONS § Secrets from files](OPERATIONS.md#secrets-from-files-vault-agent--csi) |
| `WARDYN_ORG_DEVICE_NAME` | string | (unset) | human-readable name this device registers under at `WARDYN_ORG_URL` (e.g. a hostname or asset tag). Carries no posture of its own — empty is fine even with `WARDYN_ORG_URL` set (flag `-org-device-name`) |
| `WARDYN_USER_WORKSPACE_ROOTS` | CSV (absolute paths) | (unset = **members may not mount host directories**) | the absolute host directories a **member's own** `local_dir` workspace source may live under. A member source is allowed only if its **canonicalized real path** (`filepath.EvalSymlinks`) is inside one of these — so a symlink *inside* a root pointing *out* of every root is refused — and never if it is or traverses a credential dotfile path (`.ssh`, `.aws`, `.claude`, `.wardyn`, `.gnupg`, `.docker`, `.kube`, `.config/gh`, `.netrc`, `.git-credentials`, `.git/config`), which holds even when a root is set too wide. Checked at onboarding, again at run-create on the resolved spec, and a **third** time in the docker driver immediately before the bind. Unset (the default) fails closed: a member can still onboard repos and use operator-owned workspaces. Point it at a dedicated projects directory — **never `$HOME` or `/`**, which boot WARNs about but permits, and never a tree `WARDYN_USER_DRIVE_HOST_ROOTS` names (see that row: the overlap lets a member bind a whole share one person's home at a time was the point of, and boot WARNs about the pair). Operator mounts are never narrowed by it. (`internal/runner/member_mount.go`; flag `-member-workspace-roots`) Deprecated alias `WARDYN_MEMBER_WORKSPACE_ROOTS` still works and WARNs at boot through 0.8.x; removed in 0.9. |
| `WARDYN_USER_WORKSPACE_ROOTS_MAP` | JSON | (unset) | per-member override of the list above, `{"<principal>": ["/abs/root", ...]}` keyed by lowercased OIDC `sub` **or** email (the same dual-key identity a `capability_grants` `user` subject uses). A principal with an entry uses **only** that entry — per-member **REPLACES** the shared list rather than unioning with it, because per-member exists to *narrow* and a union would make adding a row widen. An entry with an empty list means that member mounts nothing. Malformed JSON, an empty key, or a non-absolute/uncleaned path fails boot closed (flag `-member-workspace-roots-map`) Deprecated alias `WARDYN_MEMBER_WORKSPACE_ROOTS_MAP` still works and WARNs at boot through 0.8.x; removed in 0.9. |
| `WARDYN_USER_WRITABLE_ROOTS` | CSV (absolute paths) | (unset = **no writable member mounts at all**) | where a member may mark **their own** mount writable, matched on the same canonicalized real path as the roots above. Unset means every member mount is read-only regardless of the source's `writable` flag — a writable bind widens the residual, so it is opt-in per subtree. Operators keep their unrestricted per-source writable opt-in (flag `-member-writable-roots`) Deprecated alias `WARDYN_MEMBER_WRITABLE_ROOTS` still works and WARNs at boot through 0.8.x; removed in 0.9. |
| `WARDYN_USER_WRITABLE_DENY` | CSV (absolute paths) | (unset) | carve-outs from `WARDYN_USER_WRITABLE_ROOTS`. **Deny wins** over allow and is checked first, so a subtree inside a writable root can be pinned read-only for members (flag `-member-writable-deny`) Deprecated alias `WARDYN_MEMBER_WRITABLE_DENY` still works and WARNs at boot through 0.8.x; removed in 0.9. |
| `WARDYN_USER_DRIVE_HOST_ROOTS` | CSV (absolute paths) | (unset = **no `host_path` user drive may be registered**) | the absolute host directories a **user drive** of backend `host_path` may be registered inside — typically the mount point of an NFS/SMB share the operator mounted **host-side** (fstab / systemd; Wardyn never performs the share mount and never holds a share credential). One level up from the member roots above: a drive's `host_root` is authored in the **database by an admin**, and its per-person subdirectories are bound into **other people's** sandboxes, so the ceiling over it is operator/MDM-set here rather than in a table the product itself writes — a console compromise then widens nothing. A `host_root` is accepted only if it passes the same host bind-mount deny-list every authored source does **and** its **canonicalized real path** (`filepath.EvalSymlinks`) is inside one of these — and, unlike a member mount source, it must actually **exist on this host** (fail-closed: authoring a drive over a path that is not there defers the failure to somebody else's run). Only that person's **subdirectory** is ever bound, never the root. Unset (the default) fails closed: Wardyn-managed volume drives are unaffected. Point it at the share's mount point. Two values boot WARNs about and still permits, for opposite reasons: `$HOME` is far too **wide** (a drive could then be authored over any dotfile tree the deployment's own home holds), while `/` is **dead** — the within-root match is `real == root` or `real` under `root + "/"`, and no absolute path is under `//`, so a ceiling of `/` matches nothing at all and refuses every `host_path` drive. Neither is the value you want; name the mount point. **And it must not name a tree `WARDYN_USER_WORKSPACE_ROOTS` also names, contains, or sits inside.** The two ceilings are one level apart by design: Wardyn binds ONE person's subdirectory of a drive's root, while a member names a directory under the member roots and binds it **whole**, writable where `WARDYN_USER_WRITABLE_ROOTS` allows it, through a surface that consults no drive allocation at all — so point the two at one tree and a member onboards the share as a workspace and mounts every other person's home. Each list is valid on its own and neither parser can see the other, so the PAIR is compared at boot and WARNed about (`wardynd: mount ceilings overlap — …`, one line per overlapping pair, naming both variables and the tree). It is a warning and **not** a refusal — an operator may have opened a tree to both deliberately, and refusing at boot would take a running deployment down on upgrade over a posture it already has. Every member ceiling is compared, the shared list and each `WARDYN_USER_WORKSPACE_ROOTS_MAP` per-principal override alike (an override REPLACES the shared list, so it is a ceiling in its own right), lexically, on the values as configured. Malformed or non-absolute/uncleaned entries fail boot closed (`MountCeilingOverlapWarnings` and `ParseUserDriveHostRoots`, `internal/runner/user_drive_mount.go`; flag `-user-drive-host-roots`) |
| `WARDYN_TLS_CERT` | string | (unset) | TLS certificate PEM path (flag `-tls-cert`) |
| `WARDYN_TLS_KEY` 🔒 | string | (unset) | TLS private key PEM path (flag `-tls-key`) |
| `WARDYN_TLS_TERMINATED` | bool | `false` | TLS terminates upstream; mark cookies Secure (flag `-tls-terminated`) |
| `WARDYN_ALLOW_PLAINTEXT_LISTEN` | bool | `false` | override: allow boot on a specific non-loopback bind serving plain HTTP with no TLS posture configured — normally refused (`refusePlaintextListen`, `cmd/wardynd/main.go`), for **both** listeners: `WARDYN_LISTEN` and `WARDYN_UI_SANDBOX_LISTEN`, which share one TLS posture; loopback and the unspecified bind (`:8080`, the compose topology) stay warn-only either way (flag `-allow-plaintext-listen`) |
| `WARDYN_OIDC_ISSUER` | string | (unset) | OIDC public issuer URL; enables human SSO (flag `-oidc-issuer`) |
| `WARDYN_OIDC_INTERNAL_ISSUER` | string | (unset) | server-reachable OIDC issuer (flag `-oidc-internal-issuer`) |
| `WARDYN_OIDC_CLIENT_ID` | string | (unset) | OIDC client id (flag `-oidc-client-id`) |
| `WARDYN_OIDC_CLIENT_SECRET` 🔒 | string | (unset) | OIDC client secret (flag `-oidc-client-secret`). **Optional**: PKCE S256 is sent on every login regardless, so leaving this unset registers a **public client** (a SPA/native-app registration with no secret at all — some IdPs refuse to issue one for a confidential client) and works exactly the same as a confidential one |
| `WARDYN_OIDC_REDIRECT_URL` | string | (unset) | OIDC redirect URL — the URL the **browser** is sent back to, so it must name the host/port the console is reachable on, and it must be registered with the IdP. Unset in a bare wardynd; the compose stack defaults it to `http://localhost:${WARDYN_UP_PORT:-8080}/auth/callback` so it follows the published port. The bundled Dex (`deploy/compose/dex.yaml`) registers the `8080` form as a **literal** (env expansion is off in that file) and matches `redirect_uri` exactly, so running the `sso` profile on another `WARDYN_UP_PORT` means editing that file too (flag `-oidc-redirect-url`) |
| `WARDYN_OIDC_EMAIL_DOMAINS` | CSV | (unset = any) | allowed email domains, exact match (list subdomains separately) The domain is matched **printable-ASCII only** (checked on the raw claim domain, before lowercasing, so a look-alike character cannot fold onto one of your ASCII domains — the same guard the `groups` snapshot uses below), and an address with no domain at all (`user@`) is refused; the local part is not restricted. (flag `-oidc-email-domains`). Entra ID tokens typically **omit `email_verified` entirely**, and this option fail-closes on that (every login denied) against an Entra tenant — prefer Entra App Roles (`WARDYN_OIDC_ROLE_MAP` against the `roles` claim) plus the app registration's "assignment required" setting there instead. An absent claim and an explicit `email_verified: false` are two distinct denials (`auth_error=email_verified_absent` vs `email_unverified`), each logged separately server-side; no opt-in flag relaxes either — allowlisting a self-asserted, unverifiable email would defeat the point of this list |
| `WARDYN_OIDC_OPERATOR_EMAILS` | CSV | (unset = **refused at boot** when OIDC is configured) | operator (admin) allowlist of the minimal user/admin role gate: a signed-in human whose session email is NOT listed is a **user** — owner-scoped: reads their OWN runs/approvals/audit and may launch/kill runs, 403 on configuring the deployment (harness-credential, policy, workspace, site-config **writes** — and since R1 the site-config, source-library and base-image **reads** too, which return the operator's internal proxy/SCM/registry hostnames, local_dir host paths and integration credential refs), on **secret** writes/deletes, and on admin-only **credential**/**tool_call** approvals. A user still **decides** `egress_domain` approvals on, and **attaches** to, their OWN runs — both PTY lanes: the ticket mint (`POST /runs/{id}/attach-ticket`) and the attach WebSocket itself (`GET /runs/{id}/attach`), which falls back to session-cookie auth when no ticket is presented; a foreign run is a **404** (no existence oracle). `GET /secrets` (names only) stays readable. Full addresses, case-insensitive. Admin-token and local-mode callers are always admins (one shared credential, no human to key a role off). Leaving it empty with OIDC configured makes every SSO human admin-equivalent and is refused at boot — see `WARDYN_ALLOW_OIDC_NO_OPERATOR_LIST` (flag `-oidc-operator-emails`). Also doubles as a **legacy admin source** for the separate `WARDYN_OIDC_ROLE_MAP` role gate below: an email on this list always derives role `admin` there too, so an existing deployment adopting the role map keeps its operators as admins with zero re-configuration |
| `WARDYN_ALLOW_OIDC_NO_OPERATOR_LIST` | bool | `false` | override: allow boot with OIDC SSO configured but `WARDYN_OIDC_OPERATOR_EMAILS` empty — i.e. every signed-in human admin-equivalent — normally refused (`validateOperatorPosture`, `cmd/wardynd/main.go`). Unconditional, unlike the plaintext rule: a loopback bind bounds who can reach the port, not who the IdP authenticates. No effect without OIDC (flag `-allow-oidc-no-operator-list`) |
| `WARDYN_SSO_ONLY` | bool | `false` | declare SSO the **only** way into the console. Boot is refused unless OIDC is configured and `WARDYN_ADMIN_TOKEN`, `WARDYN_LOCAL_MODE`, `WARDYN_USER_DESKTOP` and `WARDYN_ALLOW_OIDC_NO_OPERATOR_LIST` are all unset — each refusal names the one to remove. Member mode is refused because it relies on the admin token as a **process credential** ([deploy/desktop/wardyn.env.m-prime.example](../deploy/desktop/wardyn.env.m-prime.example)); refusing the no-operator-list override is what makes the "every signed-in human is an admin" role-derivation branch unreachable, which is why `/healthz`'s `sso_only` bit lets the sign-in screen drop that caveat. Publishes `sso_only` on `/healthz`, and forces its sibling `token_login` bit false, so the sign-in screen renders only what can work. See `validateSSOOnlyPosture` (`cmd/wardynd/boot_posture.go`) (flag `-sso-only`) |
| `WARDYN_OIDC_ROLE_MAP` | CSV (`value=role` pairs) | (unset = role derivation off unless a console-managed mapping actually contributes to the merged map) | maps an Entra App Role (`roles` claim — the priority path), a `groups` claim entry, or an email — matched case-insensitively — to a Wardyn session role — `admin`, `security_admin` or `user` — or to a **user type id** (0.8), which is the `user` tier on that type, e.g. `Wardyn.Admin=admin,Wardyn.Security=security_admin,eng-team=user,pm-group=portfolio-manager,alice@corp.com=admin`. `user` is the built-in `standard` type (Standard user). When the values a person matches name more than one type, the custom type with the highest priority wins and `standard` never wins against a custom type; two custom types at the same top priority refuse the sign-in (`auth_error=user_type_ambiguous`), and a value naming a type that does not exist refuses it too (`auth_error=user_type_unknown`, and a boot warning per such type) — never a wider default. An admin tier's matched type is recorded as well, but an `admin` sign-in (a map row or `WARDYN_OIDC_OPERATOR_EMAILS`) is never refused over a type: no type decides anything for that tier, so a tie or a missing type puts it on `standard` with a warning instead. A `security_admin` sign-in is refused like a `user` one. The pre-0.8 value `member` is still accepted as `user` (the built-in `standard` user type), with a boot warning per entry, until 0.9. A `groups`-keyed row depends on the IdP actually EMITTING the claim: Wardyn's authorization request is fixed at `openid profile email` and deliberately does not ask for a `groups` scope (Entra defines none and rejects unrecognised ones, and an IdP that merely advertises one may not have granted it to this client registration — asking would be `invalid_scope`, i.e. every human locked out). On an IdP that gates its group claim behind that scope the claim simply does not arrive, which is indistinguishable from "this human is in no groups", so group-keyed rows decide nothing. Wardyn says so rather than guessing: a boot warning when the CHART map is keyed on such values, and a once-per-process login warning when the MERGED map (chart plus console-managed rows) is — both only on a provider whose `scopes_supported` advertises `groups`, so an Entra tenant never sees either. This map (merged with the console-managed rows below) is the **only** way a session reaches `security_admin`: there is no allowlist twin of `WARDYN_OIDC_OPERATOR_EMAILS` for it, and `WARDYN_OIDC_DEFAULT_ROLE` refuses it at boot. If more than one entry matches, the **highest rank wins** — `user` < `security_admin` < `admin` — regardless of which claim produced it. An email on `WARDYN_OIDC_OPERATOR_EMAILS` (above) always resolves to `admin` too — same top precedence. Console-managed mappings (Getting Started → People) merge with this map as additional entries at each login — same matching rules, and a chart entry here always wins on a duplicate key — this var remains the boot-time bootstrap those console rows merge on top of. No match at all falls through to `WARDYN_OIDC_DEFAULT_ROLE`, or denies the login when that is also unset. A bad role value, a non-ASCII key (matching is ASCII-only, so it could never match), a duplicate key, or non-empty input with no valid entry is a boot error naming the var. Derived once, at `/auth/callback` time (`internal/auth/oidc`'s `deriveRole`), and stamped into the signed session cookie — a cookie signed before this field existed decodes as no session, forcing a re-login. **Prefer `roles`/`groups` keys (IdP-signed) over email keys**: an email key is a second email-keyed privilege source riding an unverified IdP claim, same caveat as `WARDYN_OIDC_OPERATOR_EMAILS` above — `email_verified` is enforced only when `WARDYN_OIDC_EMAIL_DOMAINS` is also set (flag `-oidc-role-map`). **The same two claims do double duty**, with or without this var set: the union of `roles` and `groups` is also snapshotted into the session at callback as the caller's group identity, which is what a `subject_type=group` capability grant matches ([OPERATIONS.md](OPERATIONS.md) → "Capabilities: what one member, or one group, may do"). Deduped, sorted, **printable-ASCII only** (checked on the raw claim value, before lowercasing, so a look-alike character cannot fold onto one of your ASCII group names), lowercased, and capped at 2048 payload bytes — dropped from the alphabetical END so the same human loses the same groups every login — which keeps the signed cookie under the ~4096 bytes a browser silently discards whole (~100 typical group names; past that, grant the user directly or prefer App Roles). A claim value the snapshot cannot carry marks the snapshot **partial**, exactly as the byte cap and an Entra overage do, so a group deny or a group-tier ceiling written against it fails closed instead of evaporating. It is a **login-time snapshot**: a directory change needs a re-login, while grants themselves resolve per request. `groups_snapshot_stale` on `GET /me/capabilities` reports "can't tell yet" distinctly from "holds no groups" — but never for a pre-0.7 cookie, which is refused outright by the codec-version check named earlier in this row, not carried as a stale snapshot |
| `WARDYN_OIDC_DEFAULT_ROLE` | string (`admin`\|`user`\|a user type id; `security_admin` is refused at boot; the pre-0.8 `member` is accepted as `user`, with a boot warning, until 0.9) | (unset = **deny the login**, this applies once the merged map — chart or console — is non-empty) | role assigned when a signed-in human's roles/groups/email matched none of the merged map's entries — the merged map is `WARDYN_OIDC_ROLE_MAP` (chart) plus console-managed mappings (Getting Started → People), so this var applies whenever EITHER source is non-empty, not only when the chart var itself is set. Unset denies that login with a message pointing the user at `WARDYN_OIDC_ROLE_MAP`. Ignored only when the merged map is itself empty — the chart var is empty AND no console mapping actually contributes to it: a console row that's empty/non-canonical or shadowed by the chart or `WARDYN_OIDC_OPERATOR_EMAILS` contributes nothing, the same as having no console rows at all, so the merged map can still be empty even with saved console rows present (flag `-oidc-default-role`). **Not applied when the IdP withheld the claims:** on an Entra groups/roles OVERAGE (`_claim_names`, see `WARDYN_OIDC_ROLE_MAP` above) the map is keyed on a claim nobody read, so a fallthrough to a role WIDER than `user` — today exactly `admin` — is refused with `auth_error=claims_overage` rather than serving it; `user` is served as before, since no hidden claim could have produced less |
| `WARDYN_OIDC_ALLOW_EMAIL_MAPPINGS` | bool | `false` | opt-in for an email-shaped (contains `@`) value on a **console** People-step role mapping (`POST /access/mappings`) only — `WARDYN_OIDC_ROLE_MAP`'s own email-keyed entries above are unaffected either way (legacy, still boot-warned separately when `WARDYN_OIDC_EMAIL_DOMAINS` is unset). Refused by default (`EMAIL_KEY_REFUSED`, `internal/api/access.go`) steering an admin toward an App Role or group key instead — an Entra ID token typically **omits `email_verified` entirely**, so an email-keyed mapping rides an unverifiable claim the same way an email-keyed `WARDYN_OIDC_ROLE_MAP` entry does. Set it to allow the console write when this deployment has no usable App Role/group claim (flag `-oidc-allow-email-mappings`) |
| `WARDYN_DIRECTORY_PROVIDER` | string (`entra`) | (unset = **off**) | identity-directory connector behind the console's "who" autocomplete — the governance assignment **subject** and the People-step mapping **value** — so an admin picks a name and Wardyn stores the claim value (a group's `groups` claim carries its **object GUID**, not its name, and a hand-typed name binds nothing). Unset is the whole feature OFF: no connector, no directory read, `GET /access/directory/search` answers `503 {"code":"directory_unconfigured"}` and every field stays free text, byte-identical to a deployment without it. **Setting it grants wardynd READ OF THE WHOLE DIRECTORY** (users + groups always; App Roles only where `Application.Read.All` was consented) and adds **daemon-side** outbound HTTPS to `graph.microsoft.com:443` — not sandbox egress, not proxied by the egress sidecar, the same class as the daemon's existing IdP discovery/token/JWKS calls. Suggestions are cached in memory for 60s and never persisted; unsetting this fully retracts the capability. Any value other than `entra` or empty is a **boot refusal** — a typo must not read as "off". See [OPERATIONS.md](OPERATIONS.md) → "Directory autocomplete" (flag `-directory-provider`) |
| `WARDYN_DIRECTORY_TENANT` | string | (unset = derived from `WARDYN_OIDC_ISSUER`) | Entra tenant id or verified domain for the connector. Unset, it is derived from the issuer's first path segment — commercial-cloud issuers only (`https://login.microsoftonline.com/<tenant>/v2.0`, or the v1.0 `https://sts.windows.net/<tenant>/`), since the connector talks to `graph.microsoft.com`; any other issuer is a **boot refusal** naming this var rather than a runtime failure against a tenant that does not exist. Set it together with `WARDYN_DIRECTORY_CLIENT_ID` and `WARDYN_DIRECTORY_CLIENT_SECRET` (flag `-directory-tenant`) |
| `WARDYN_DIRECTORY_CLIENT_ID` | string | (unset = reuses `WARDYN_OIDC_CLIENT_ID`) | client id of a **dedicated, least-privilege** app registration for the directory read. The default reuses the OIDC app, which then needs admin-consented Graph **application** permissions — `User.Read.All` + `Group.Read.All` (`Directory.Read.All` works but is broader; add `Application.Read.All` only if App Roles should appear in suggestions). The consent is performed by a **tenant admin in Entra** — Wardyn cannot grant itself any of it. All three `WARDYN_DIRECTORY_TENANT`/`_CLIENT_ID`/`_CLIENT_SECRET` are set together or not at all; a half-configured set is a **boot refusal**, never a silent fallback to the OIDC credentials (flag `-directory-client-id`) |
| `WARDYN_DIRECTORY_CLIENT_SECRET` 🔒 | string | (unset = reuses `WARDYN_OIDC_CLIENT_SECRET`) | client secret of that dedicated app registration. **`WARDYN_DIRECTORY_PROVIDER=entra` is REFUSED AT BOOT** when neither this nor `WARDYN_OIDC_CLIENT_SECRET` is set: Microsoft Graph needs the client-credentials flow, and a **public** OIDC client (PKCE, no secret — a supported registration shape, see `WARDYN_OIDC_CLIENT_SECRET` above) cannot perform it at all. Same refusal when no OIDC is configured at all (an admin-token or local-mode deployment has no issuer to derive a tenant from). The connector itself can only answer `503` lazily at the first keystroke, so the check lives at boot where it can name what to set (`resolveDirectoryConfig`, `cmd/wardynd/boot_posture.go`) (flag `-directory-client-secret`) |
| `WARDYN_APPROVAL_EXPIRY_AFTER` | duration | `24h` | PENDING approvals older than this expire (flag `-approval-expiry-after`) |
| `WARDYN_ENDED_RUN_GRACE` | duration | `168h` (7 days) | how long a run whose end (`ends_at`, from its owner's run limits) has passed keeps its files. At the end the run is stopped and KEPT: its agent container is stopped but not removed, its proxy sidecar is removed so it has no network, its broker credentials are revoked and its pending approvals cancelled. Past the grace it is torn down (`STOPPED`). `0` tears a run down at its end. A substrate that cannot keep a stopped sandbox (Kubernetes) always tears down at the end (flag `-ended-run-grace`) |
| `WARDYN_APPROVAL_EXPIRY_INTERVAL` | duration | `10m` | sweep interval for stale approvals; 0 disables (flag `-approval-expiry-interval`) |
| `WARDYN_AUTOSTOP_INTERVAL` | duration | `1m` | lifecycle reaper scan interval; 0 disables (flag `-autostop-interval`) |
| `WARDYN_AUDIT_COALESCE_WINDOW` | duration | `5m` | fold IDENTICAL consecutive `auth.failed` audit rows — same refusing boundary, same `reason`, same path (the peer is deliberately NOT in the key: a caller that rotates its source address would otherwise write one row per request) — into the FIRST row plus one **new** summary row carrying `count`, `peers` (distinct peer IPs, port stripped, saturating at 100), `peer_ips` (those addresses, capped at 100) and `peers_truncated`, `first_seen` and `last_seen`. Never an update of the first row: `audit_events` is append-only and hash-chained, so a summary is always another append. **This is the maximum GAP between two consecutive identical rows, not a cap on how long a streak may run:** the flood it exists for was one row a minute, forever, from a single sidecar retrying a renew the control plane would never grant, which the `auth.failed` rate limiter (1/sec, burst 5) never trips — and which still evicted every real security event out of the console's 1000-row window in minutes. A streak is also closed at 1000 rows, so one summary row can never stand for unbounded volume, and folded rows are counted in `wardyn_auth_failed_suppressed_total` exactly like rate-limited ones. **The summary row pays the `auth.failed` rate limiter too**, exactly like a first row — a streak closes on every key change, so an unmetered summary emit would hand a client alternating two paths one free audit row per two requests; a summary the limiter refuses is dropped and counted in that same series. The same window folds the hybrid device routes' `device.enrol` and `device.audit.ingest` failure rows (`docs/AUDIT-ACTIONS.md`, Devices). `0` disables the folding entirely (one row per refusal, pre-0.7.2 behaviour). See `threatmodel/THREAT-MODEL.md` (flag `-audit-coalesce-window`) |
| `WARDYN_SUBSCRIPTION_INJECT` | bool | **binary: ON** (unset); compose: `off` | proxy-side subscription OAuth injection; `off`/`0`/`false`/`no` disable, garbage exits 2. Two layers, and they differ: the compose stack pins it `off` because the distroless `wardynd` image carries no `claude` binary to inject with — see [deploy/compose/README.md](../deploy/compose/README.md). **Scope: the resident-mount subscription path only** — a run with `~/.claude` mounted falls back to its own OAuth creds over the tunnel, unMITMed, when this is `off`. It has NO effect on the separate Wardyn-managed lane (but `subscriptionInjectPosture` gates BOTH lanes — see `WARDYN_ALLOW_SHARED_SUBSCRIPTION`) (a connected managed setup-token, no resident mount): that path still injects proxy-side and still MITMs `api.anthropic.com` regardless of this flag (`internal/api/runs_dispatch_llm.go`'s `managed` gate) |
| `WARDYN_SCAN_AI_ADVISOR` | bool | `false` | opt-in advisory AI workspace-scan fallback (flag `-scan-ai-advisor`) |
| `WARDYN_REQUIRE_OPERATOR_SET_EGRESS` | bool | `true` (**on since 0.7**) | gate an `egress:<host>` workspace requirement the SAME way a `secret:<name>` one has ALWAYS been gated: only `operator_set` provenance is auto-added at launch; a `scan_seeded` host — the workspace scanner reading **untrusted repo content** — is skipped, and the run's warnings name it. **Flipped ON in 0.7**: the asymmetry with the secret side (which calls this boundary *"security-critical — do not relax"*) was the anomaly. Upgrade impact: a workspace whose egress requirements are `scan_seeded` stops having them auto-added — an operator declaring the host (making it `operator_set`) is the intended fix, and is the action the gate exists to require. Set `false` to restore pre-0.7 behavior (flag `-require-operator-set-egress`) |
| `WARDYN_GIT_PAT_BROKER` | string | `on` | never-resident **git_pat** lane. `on` (default since 0.7) mints a non-GitHub forge's PAT **proxy-side** and injects it on the outbound leg, so the credential never enters the sandbox — the posture `github_token` has always had. `agent-run` rewrites those hosts to the broker path (`url.<broker>/git/<host>/.insteadOf`), and the grant ids are **withheld** from the sandbox env so the in-sandbox helper cannot mint the PAT anyway. `off` restores pre-0.7 behaviour (the helper mints the PAT into the agent's process, where it is resident for the run). Only the literal `off` disables it — a typo leaves the broker on. An escape hatch for a forge that misbehaves under the rewrite, not a supported posture (flag `-git-pat-broker`) |
| `WARDYN_GIT_PAT_BROKER_HOSTS` | string (space-separated) | (set by dispatch) | **Sandbox-side, set by wardynd, never by an operator.** The hosts `agent-run` must route through the git_pat broker. Host names only — it carries no grant id, so it cannot be used to mint anything |
| `WARDYN_GITHUB_REQUIRE_REF_RULESET` | bool | `false` (**off**) | opt in to refusing a `github_token` mint whose repo is not confined by a GitHub repository ruleset. On, for every repo in the grant, the broker reads `GET /repos/{owner}/{repo}/rules/branches/{branch}` for a ref outside `refs/heads/wardyn/**/*` (`creation`, `update` and `deletion` must all be in force) and one inside it (`creation`/`update` may not be, or the run's own pushes would be refused), then `GET /repos/{owner}/{repo}/rulesets/{id}` for each ruleset behind those rules (`current_user_can_bypass` must be `never`) — and fails the mint if any answer is wrong **or cannot be obtained**. Note the `**/*`: a bare `refs/heads/wardyn/**` exclude matches nothing a run pushes, fails this gate, and blocks governed pushes at GitHub too. **Default off** because the ruleset must be created per repo by someone with admin on it, which Wardyn never has, so defaulting on would break every existing deployment; the `github_ref_ruleset` setup-checklist row is what keeps the off state visible rather than silent. Recipe: [docs/POLICIES.md](POLICIES.md) → "Bound the token itself: a GitHub ruleset". Read once at broker construction, so a garbage value exits at boot. No flag |
| `WARDYN_ENVBUILD` | bool | **binary: `false`**; compose: `true` | enable devcontainer image builds for create-run (`-tags docker`) (flag `-envbuild`). Gates BOTH the BYOI wrap and the devcontainer-build lane — see [OPERATIONS.md](OPERATIONS.md#recommended-builds-on-compose) and `threatmodel/THREAT-MODEL.md` residual #13 |
| `WARDYN_ENVBUILD_IMAGE` | string | (unset = upstream) | envbuilder OCI image override (flag `-envbuild-image`) |
| `WARDYN_ENVBUILD_CACHE_REPO` | string | **binary: (unset)**; compose: `registry:5000/wardyn/devcontainers` | OCI registry ref for layer cache / daemonless push (flag `-envbuild-cache-repo`). The compose value is the build container's OWN reach to the registry sidecar, by compose service name over the `wardyn-internal` bridge — see `WARDYN_ENVBUILD_PUSHED_REF` below for the HOST daemon's separate loopback address to the same registry |
| `WARDYN_CONFINEMENT_MAP` | string | (unset) | per-class substrate/runtime pins, e.g. `CC2=runsc;CC3=kata-qemu` (flag `-confinement-map`) |
| `WARDYN_CONTROL_PLANE_URL` | string | `https://wardynd:8443` | the URL every run's proxy sidecar dials to reach this daemon's **internal TLS listener** (`WARDYN_INTERNAL_LISTEN`) — credential resolves, mints, token renewals, decisions, approvals and uploads all ride it. Its host is the name wardynd's internal CA certifies at each boot, and dispatch hands that CA to every proxy as the only root it trusts for these calls (never the system roots, never `WARDYN_TRUSTED_CA_FILE`). **`http://` is refused at boot** (and by the proxy at start) unless the host is loopback — `localhost`, `127.0.0.0/8` or `::1`, matched literally with no DNS lookup — which is the one "local" install. Chart: rendered as `https://<release>.<ns>.svc.cluster.local:<service.internalPort>`; compose/Desktop: `https://wardynd:8443`; host mode (`scripts/run-host.sh`): `https://host.docker.internal:8443`. See [OPERATIONS.md](OPERATIONS.md#control-plane-to-proxy-tls) (flag `-control-plane-url`) |
| `WARDYN_INTERNAL_LISTEN` | string | `:8443` | listen address of the proxy-facing TLS listener: `/api/v1/internal/*` and `/healthz` only, served with a certificate from wardynd's own internal CA (secret-store row `wardyn-internal-ca`, minted on first boot, kept across restarts and upgrades). Runs whenever `WARDYN_CONTROL_PLANE_URL` is `https://`; a bind failure ends the daemon. Never publish it outside the network your proxies share with wardynd (flag `-internal-listen`) |
| `WARDYN_PROXY_IMAGE` | string | (unset) | OCI image for the wardyn-proxy sidecar (flag `-proxy-image`). **Bump it in lockstep with wardynd.** The sidecar is pinned here by the operator, independently of the daemon, and every run's proxy config is written by whatever wardynd is running — so a sidecar left at an older tag/digest meets config keys its binary does not know. Since 0.7 that is a loud failure rather than a silent one: the sidecar decodes its config strictly (`LoadConfigBytes`, `internal/egress/proxy/config.go`) and refuses to start on a key it cannot honour, naming the key. Before 0.7 the same skew was accepted with no error and the unknown keys were discarded — a routing document half in force |
| `WARDYN_PROXY_URL_OVERRIDE` | string | `http://wardyn-proxy:3128` | sandbox `WARDYN_PROXY_URL` override (flag `-proxy-url`) |
| `WARDYN_GROUNDTRUTH_TOKEN_FILE` 🔒 | string | (unset) | file holding the host-sensor token |
| `WARDYN_PRINT_GROUNDTRUTH_TOKEN` | bool | `false` | mint+print a host-sensor token and exit (flag `-print-groundtruth-token`) |
| `WARDYN_AGENT_IMAGES` | string (JSON) | (unset) | agent-name → OCI image ref map (flag `-agent-images`). The `base` key is what the setup connectivity probe resolves — it needs no coding agent, only `curl` |
| `WARDYN_AGENT_ANTHROPIC_MODEL` | string | (unset) | pin `ANTHROPIC_MODEL` inside claude-code sandboxes (flag `-agent-anthropic-model`) |
| `WARDYN_ALLOW_AGENT_TELEMETRY` | bool | (unset) | re-enable the agent CLI's own telemetry inside sandboxes. Default-unset SUPPRESSES it (`CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1`, `DISABLE_TELEMETRY=1` in the sandbox env, `runs_dispatch_mounts.go`): the CLI's first-run phone-home is otherwise the FIRST pending egress approval a Confined pilot sees, before the host their task actually needs |
| `WARDYN_ALLOW_USER_ENV_SECRET` | bool | (unset) | let MEMBERS hold `env_secret` grants. Default-unset DROPS a member's `env_secret` grant even when the operator's ceiling lists the exact `(name, secret)` pairing (`dropAdminOnlyEnvSecretGrants`, `inline_policy.go`) — on EVERY route a run policy arrives by (an inline body, a stored row the member selected, or the deployment default) and whatever the caller's governance assignment, because the rule is a role check plus this switch rather than a ceiling check: every other member-reusable kind is bounded after delivery (`api_key` never leaves the broker, `git_pat` reaches git through the helper, `ssh_key` is wiped after the clone) while an `env_secret` is a raw value in the process env for the run's whole life, with no mint, no TTL and nothing to revoke. An operator's own runs are unaffected either way — the ceiling authority is never clamped by its own ceiling Deprecated alias `WARDYN_ALLOW_MEMBER_ENV_SECRET` still works and WARNs at boot through 0.8.x; removed in 0.9. |
| `WARDYN_EGRESS_SECOND_HUMAN` | bool | (unset) | four-eyes on egress approvals: the human who DECIDES an `egress_domain` approval must not be the human who created the run (`requireSecondHuman`, `approvals.go`). Default-unset keeps single-human approval — turning it on unprompted would deadlock every single-operator deployment. **Local mode REFUSES it** with a `503` naming the incompatibility, and says so at BOOT (a warning naming the consequence and the remedy) rather than leaving you to discover it when the first approval hangs: that mode authenticates nobody, so the decider and the run's `created_by` both come from the client-supplied `X-Wardyn-Principal` dev override and no request can prove a second human decided — configure SSO to use this switch. **The `admin-token` principal BYPASSES it** — a bare `WARDYN_ADMIN_TOKEN` caller is attributed `system`/`admin-token` precisely because a shared token carries no per-human identity to compare, so it is the deliberate break-glass; each one writes `approval.second_human.bypass`, on the decision's own outcome — `success` when the decision it was spent on was made, `failure` (with an `error` class of `already_decided`, `not_found` or `error`) when the bypass got past the gate and the decision then failed. A deployment that wants this gate to bind must treat the admin token accordingly (SSO configured, token held out of band). Scoped to `egress_domain` only. See `docs/OPERATIONS.md` and `threatmodel/THREAT-MODEL.md` §5 |
| `WARDYN_BEDROCK_MODEL` | string | (unset) | Bedrock inference-profile id, **or the profile's full ARN** — `arn:aws:bedrock:<region>:<acct>:inference-profile/<id>` or `arn:aws:bedrock:<region>:<acct>:application-inference-profile/<id>`, which is how an enterprise attaches quota, logging and guardrails to the profile rather than the bare model. Reaches the agent verbatim as `ANTHROPIC_MODEL`; Wardyn never parses or validates the shape (flag `-bedrock-model`) |
| `WARDYN_BEDROCK_REGION` | string | (unset) | AWS region for the Bedrock transport (flag `-bedrock-region`); falls back to the standard `AWS_REGION` / `AWS_DEFAULT_REGION` when empty |
| `WARDYN_BEDROCK_BASE_URL` | string (URL) | (unset) | Bedrock **data-plane** (`bedrock-runtime`) base URL, re-pointed at a VPC/PrivateLink endpoint so inference traffic never traverses the public internet (flag `-bedrock-base-url`; `ValidateBedrockBaseURL`, `internal/api/llm_gateway.go`). Unset = every Bedrock run dials `bedrock-runtime.<region>.amazonaws.com`, byte-identical to today. Same seven rules as `WARDYN_ANTHROPIC_BASE_URL` below — `https://`, no userinfo, no query/fragment, non-empty host, not the regional public host itself, and an IP literal accepted OUTSIDE loopback/link-local/metadata/unspecified/multicast/NAT64 (an RFC1918/CGNAT literal IS accepted — that is exactly what a PrivateLink endpoint resolves into). A malformed value refuses boot naming the var. The ONE rule `WARDYN_ALLOW_TEST_ENDPOINTS=true` relaxes is the first: with that acknowledgement (and only with it) a plain `http://` data-plane host is accepted, so a test deployment can point this at a local stub — see the AWS SSO walk in OPERATIONS.md, and **every boot that actually takes it logs a `TEST HATCH ACTIVE` WARN naming the plaintext target** (`BedrockPlainHTTPWarn`), because in bearer mode this value is the credential-injection target and the API key would ride `Authorization: Bearer` in cleartext on every model call. Without it, `http://` refuses boot and the refusal names both variables; every other rule applies identically in both postures. One override moves everything together: the egress allow-list entry, the bearer mode's TLS-MITM + `Authorization` injection target, the record-mode skip list, and the sandbox's `ANTHROPIC_BEDROCK_BASE_URL` + `AWS_ENDPOINT_URL_BEDROCK_RUNTIME` (the AWS SDK's *service-specific* knob — the global `AWS_ENDPOINT_URL` is deliberately never set, since it would re-point STS and SSO too). **Ceiling: ONE data-plane host per deployment** — it wins for EVERY region, a workspace's own region override included, so a multi-region estate must not set it. The **control plane** (`bedrock.<region>.amazonaws.com`) is deliberately NOT overridden; pin an inference-profile ARN with `WARDYN_BEDROCK_MODEL` instead. A private endpoint also needs its address range lifted past the proxy's SSRF guard — that is `SiteConfig.internal_hosts`, not an env var. **Set this ONLY when the endpoint's TLS cert names that hostname**: with private DNS enabled the cert usually names the *public* host, so you leave this unset and instead route the public host through `upstream_proxy_no_proxy` + `internal_hosts` — pointing it at the `vpce` hostname then fails the handshake on an SNI/cert mismatch. See OPERATIONS.md "Bedrock on a private endpoint". Not a `SiteConfig` field: in bearer mode this value IS the MITM and credential-injection target, so it is boot-time-only, never a live API write |
| `WARDYN_BEDROCK_AWS_DIR` | string | (unset) | host `~/.aws` mounted read-only into Bedrock runs (flag `-bedrock-aws-dir`) |
| `WARDYN_BEDROCK_AWS_PROFILE` | string | (unset) | `AWS_PROFILE` to select from the mounted `~/.aws` (flag `-bedrock-aws-profile`); falls back to the standard `AWS_PROFILE` when empty |
| `WARDYN_BEDROCK_AWS_SSO_REGION` | string | (= bedrock-region) | AWS SSO region for the sandbox token exchange (flag `-bedrock-aws-sso-region`) |
| `WARDYN_AWS_SSO_ENDPOINT_OVERRIDE` | string (URL) | (unset) | **TEST ONLY.** Re-points **both** AWS IAM Identity Center services — `sso-oidc` and the `sso` portal — at this base URL (flag `-aws-sso-endpoint-override`; `ValidateAWSSSOEndpointOverride`, `internal/api/awssso_endpoint.go`). **REFUSED unless `WARDYN_ALLOW_TEST_ENDPOINTS=true`**, and every boot carrying it logs a WARN naming it a test hatch. One value moves five derivations together: the containerized login sandbox's `AWS_ENDPOINT_URL_SSO` + `AWS_ENDPOINT_URL_SSO_OIDC`, the captured-credential (`ssoInject`) sandbox's same pair, the SSO egress allow-list entries (`oidc.<r>` + `portal.sso.<r>`), the login flow's own `device.sso.<r>` entry, and the dispatch-time `CreateToken` URL. `http://` is accepted (the fake serves no TLS, and the AWS SDK accepts a plain-HTTP endpoint override); an embedded credential, a query string, a fragment, a path, an empty host or a missing scheme refuses boot. With `WARDYN_AWS_SSO_PROXY_INJECT=on` (the default), the sandbox's SDK still CONNECTs to the portal host and the proxy still terminates that tunnel to substitute the real session (the sandbox holds only an inert placeholder) — so a plain-HTTP override also makes the proxy's MITM entry for that host re-originate its upstream leg in plain HTTP rather than TLS; every other MITM entry, including the real `portal.sso.<region>.amazonaws.com`, stays TLS regardless. It exists so the AWS SSO walk (`scripts/kind-sso-walk.sh`) can run against `test/awsssofake` on a throwaway cluster with **no AWS tenant** — see OPERATIONS.md "Testing AWS SSO without an AWS tenant". It is **not** `WARDYN_BEDROCK_BASE_URL` (a different service, and a supported production posture) and **not** the global `AWS_ENDPOINT_URL` (which would re-point every AWS service). Unset (the default, and every real deployment) = every SSO derivation is byte-identical to a build without this knob. A boot flag, never a `SiteConfig` field |
| `WARDYN_AWS_SSO_PROXY_INJECT` | enum | `on` | whether a captured AWS SSO session (the `bedrock_sso` lane, `ssoInject`) is injected **proxy-side** or written into the sandbox (flag `-aws-sso-proxy-inject`; `api.ResolveAWSSSOProxyInject`). `on` (the default): the sandbox's SSO token cache holds an inert placeholder, the real access token is set on the wire as `x-amz-sso_bearer_token` at the run's own `portal.sso.<region>` host (TLS-MITM, paired injection grant), and a session that lapses **mid-run HOLDS** the sandbox's next `GetRoleCredentials` call while its owner signs in again (`WARDYN_CREDENTIAL_REAUTH_TIMEOUT`, below) instead of failing the run. `off`: the pre-0.7.6 behaviour — the token is written into the sandbox, no `portal.sso` MITM, no hold, no sign-in requests. **`off` applies to NEW dispatches only**: the placeholder cache, the injection grant and the MITM entry are all authored at dispatch, so a run already running keeps the lane it was authored with until it ends — flip it, then relaunch the runs that matter or wait them out. `on`/`true`/`1` and `off`/`false`/`0` are the accepted spellings; anything else takes the default rather than refusing boot, because this switch exists to be reached in a hurry. It is the rollback for an SDK or corporate-MITM surprise without a downgrade; the approval rows a previous `on` period created stay readable either way (migration `0064`). **Corporate-proxy stopgap:** on a private-endpoint estate whose corporate proxy is also TLS-intercepting, the MITM re-origination fails closed exactly like every other dial failure (`builtin:dial-failed`, no detail) unless `WARDYN_TRUSTED_CA_FILE` below is ALSO staged onto the `wardyn-proxy` sidecar; flipping this switch `off` is the sanctioned stopgap while that is sorted out, at the cost of the sandbox residency this switch exists to avoid — see [docs/adoption/aws-sso-mitm-upstream-proxy.md](adoption/aws-sso-mitm-upstream-proxy.md) |
| `WARDYN_ALLOW_TEST_ENDPOINTS` | bool | `false` | override: acknowledge that this deployment is a **test** deployment (flag `-allow-test-endpoints`). It unlocks exactly two things, and on its own changes nothing: (1) `WARDYN_AWS_SSO_ENDPOINT_OVERRIDE` may re-point AWS IAM Identity Center at a server of your choosing (`resolveAWSSSOEndpointOverride`, `cmd/wardynd/boot_flags.go`), and (2) `WARDYN_BEDROCK_BASE_URL` may be plain `http://` (`ValidateBedrockBaseURL`, `internal/api/llm_gateway.go`) so the Bedrock data plane can point at a local stub that serves no TLS. Both refuse boot without it, naming both variables, and **both log a `TEST HATCH ACTIVE` WARN at every boot that takes them** — the relaxation is never silent. It relaxes nothing else — every other validation rule is identical in both postures — and it is never set on a deployment holding a real credential |
| `WARDYN_RECORDING_MOUNT` | string | (unset) | recording mount override read on the docker runner path (`runner_docker.go`) |
| `WARDYN_SSH_LISTEN` | string | daemon: (unset); **one-line install and desktop envelope: `:2222`** | SSH gateway listen address, e.g. `:2222` (flag `-ssh-listen`). **Empty = off = no listener, no new surface** — the host key is not even generated/persisted unless this is set (`buildOptionalFeatures`, `cmd/wardynd/boot_deps.go`). The DAEMON default is off, but two shipped deployments turn it on: `install.sh` writes `WARDYN_SSH_LISTEN=:2222` into every `.env` it creates **and backfills it on upgrade** (an install predating the listener vars would otherwise publish a port that refuses every connection), and `deploy/desktop/wardyn.env.example` ships it on too. So on those two paths the pre-auth listener exists by default, bound to loopback via the compose host-port publish — see [SSH.md](SSH.md) and `threatmodel/THREAT-MODEL.md` B9. Registered-public-key auth only (no passwords), owner-or-admin authorization (`run.created_by` equals the key's principal, or the key was stamped `role=admin` and re-checked within `WARDYN_SSH_ROLE_TTL`); see `docs/SSH.md` Bounds |
| `WARDYN_SSH_ADVERTISE` | string | (unset) | externally-reachable `host[:port]` for the SSH gateway, shown in the run-detail "Attach from your terminal" pane's `ssh` command and on `/healthz`'s `ssh.advertise_addr` (flag `-ssh-advertise`). Purely advisory copy — the gateway itself binds `WARDYN_SSH_LISTEN`, not this; a container/NAT deployment's bind and its externally-reachable address routinely differ, so set this whenever the gateway is enabled (unset logs a boot warning) |
| `WARDYN_SSH_ROLE_TTL` | duration | `24h` | how stale a registered SSH key's admin-override stamp (`ssh_public_keys.role_checked_at`, migration `0046`) may be before `sshAuth` refuses the override (flag `-ssh-role-ttl`). Refreshed on every OIDC login for that key's owning principal (`oidc.Config.OnLogin`) and at registration time; a key whose stamp is older than this (or was never stamped — `NULL`, a pre-`0046` row) is treated as **bounded-stale**, never live — see `docs/SSH.md` Bounds |
| `WARDYN_UI_SANDBOX_LISTEN` | string | (unset) | UI-sandbox gateway listen address, e.g. `:8081` (flag `-ui-sandbox-listen`). **Empty = off = no listener, no new surface** — the relay cookie key is not even generated unless this is set. It **must differ from `WARDYN_LISTEN`** (and from `-ssh-listen`) and boot refuses if it does not: relayed pages are the sandbox's own code, and the separate browser origin is the only thing keeping that code away from the console's session. Boot also applies the `WARDYN_ALLOW_PLAINTEXT_LISTEN` rule to **this** address, not only to `WARDYN_LISTEN`: the relay session cookie is a bearer credential for a run, good for `WARDYN_UI_SANDBOX_SESSION_TTL` (default 8h), so a specific non-loopback bind with no TLS posture is refused here too |
| `WARDYN_UI_SANDBOX_ADVERTISE` | string | (unset) | externally-reachable base URL of that gateway, e.g. `https://wardyn-ui.example.com` (flag `-ui-sandbox-advertise`), published on `/healthz`'s `ui_sandbox.enter_url_template` for the console's **Open** button. Purely advisory copy — the gateway binds `WARDYN_UI_SANDBOX_LISTEN`, not this; unset falls back to the raw bind address and logs a boot warning |
| `WARDYN_UI_SANDBOX_ORIGIN_TEMPLATE` | string | (unset) | optional **per-run** origin for the UI-sandbox gateway, e.g. `https://run-{run}.ui.example.com` (flag `-ui-sandbox-origin-template`); needs wildcard DNS and a wildcard certificate. Set, each run's apps get their own browser origin and an enter served on any other host is refused; unset, every run shares one origin separated only by a path-scoped cookie (a stated residual, logged at boot). Boot refuses a template with no `{run}` placeholder |
| `WARDYN_UI_SANDBOX_SESSION_TTL` | duration | `8h` | how long a UI-sandbox relay session (the `wardyn_ui_sess` cookie minted at the ticket handoff) stays usable (flag `-ui-sandbox-session-ttl`) — the relay's sibling of `WARDYN_SSH_ROLE_TTL`, and the same kind of bound: neither lane has a live console session to re-read a current role from. The cookie carries its own issued-at, so **shortening this binds the cookies already in browsers**, not just the next ones minted. Independent of the per-connection re-checks, which run regardless: every NEW relay connection re-asserts owner-or-admin against the freshly-loaded run and consults the revoke cutoff (`POST /sessions/revoke`). A session already established keeps its connection until it closes, and at least every 30 s on a reused (pooled) one (`uiReassertInterval`) |
| `WARDYN_TRUSTED_CA_FILE` | string (path) | (unset) | path to a PEM bundle of additional trusted roots (certificates only — a private key or CSR in the file is refused at boot) — e.g. a corporate TLS-inspecting middlebox's CA — ADDITIVE to the system roots, read once at boot (flag `-trusted-ca-file`; `loadTrustedCA`, `cmd/wardynd/trusted_ca.go`). A missing file or content with no parseable certificate refuses boot naming the var; unset is byte-identical to today (system roots only). Reaches three trust boundaries: `wardynd`'s OWN outbound TLS (mutates the shared `http.DefaultTransport` — GitHub App, OIDC, the audit webhook sink all ride it), the `wardyn-proxy` sidecar's forwarding transport (forwarded per run as `trusted_ca_pem` on `WARDYN_PROXY_CONFIG_JSON`), and every sandbox's own CA trust (appended to `WARDYN_MITM_CA_PEM`, below). **The re-originating proxy needs this too:** the Phase B AWS-SSO MITM lane (`WARDYN_AWS_SSO_PROXY_INJECT`, above) terminates and re-originates `portal.sso.<region>` on that SAME sidecar forwarding transport, so behind a TLS-intercepting corporate proxy this var is what lets the re-origination trust it — unset, the re-origination fails `x509: certificate signed by unknown authority`, filed as the same bare `builtin:dial-failed` as any other dial failure. Installing a CA is one action PER TOOLCHAIN, so dispatch sets five vars — grep for whichever yours reads: `NODE_EXTRA_CA_CERTS` (Node — ADDS to its bundled roots, so it gets the bare CA) plus `SSL_CERT_FILE` / `REQUESTS_CA_BUNDLE` / `CURL_CA_BUNDLE` / `AWS_CA_BUNDLE` (OpenSSL-Python, `requests`, curl, AWS CLI v2 — each REPLACES its trust store, so all four get the combined bundle; AWS CLI v2 needs its own because it ships its own Python and CA store and reads none of the other four). **Java is reached by none of them**: the JVM's `cacerts` keystore sits behind no environment variable, so a Java toolchain in a custom image needs its own build-time `keytool -importcert` step — Wardyn does not do it for you (same for Deno's `DENO_CERT`). Per-toolchain table: [deploy/images/README.md](../deploy/images/README.md). **Named ceiling**: on a BYOI base image missing every system CA-bundle path, OpenSSL-shaped clients (curl, Python, Ruby) inside the sandbox lose public trust entirely once this is set — see [docs/ENVBUILD.md](ENVBUILD.md#base-image-trust-byoi) and [docs/adoption/corp-image-authoring.md](adoption/corp-image-authoring.md). Not a `SiteConfig` field: posture is operator-set at process boot, never a live API write, never agent-reachable. See [OPERATIONS.md § Network](OPERATIONS.md#network-upstream-proxy-and-egress-redirects) Needs an agent image that installs the bundle (`install_mitm_ca` in `agent-run-lib.sh`; every published image from 0.7 does, including `agent-base`); a BYOI image that never calls it sees the REPLACE-vars point at a missing file |
| `WARDYN_DAEMON_PROXY_URL` | string (URL) | (unset = direct) | the forward proxy **wardynd's own** outbound HTTP calls traverse: OIDC discovery/JWKS, audit webhooks, GitHub App token minting, AWS SSO `CreateToken` renewal, and Entra directory sync (flag `-daemon-proxy-url`; `installDaemonProxy`, `cmd/wardynd/daemon_proxy.go`). The supported replacement for setting `HTTPS_PROXY` on wardynd, which stays unsupported (documented and discouraged, not runtime-rejected): the standard names are also read by the Kubernetes client and by every library that calls `http.ProxyFromEnvironment`, and getting `NO_PROXY` wrong there takes the control plane's own API access with it. Applied to `http.DefaultTransport` only — the Kubernetes client builds its own transport and the Docker client speaks a unix socket. Deliberately not `SiteConfig.upstream_proxy_url`, which is the sandbox's egress hop and is admin-editable at runtime. Must not embed `user:pass@`. A malformed value refuses boot. Separate from `WARDYN_HOST_PROXY_B64` (diagnostics only) |
| `WARDYN_DAEMON_NO_PROXY` | string (CSV) | (unset) | destinations `WARDYN_DAEMON_PROXY_URL` must not be used for, in `NO_PROXY` spelling (flag `-daemon-no-proxy`; `x/net/http/httpproxy` semantics). Wardynd appends three hosts itself: `KUBERNETES_SERVICE_HOST`, any `WARDYN_AWS_SSO_ENDPOINT_OVERRIDE` host, and any `WARDYN_OIDC_INTERNAL_ISSUER` host. Ignored when the proxy URL is unset |
| `WARDYN_DAEMON_PROXY_SECRET` | string (path) | (unset) | path to a file holding **one** forward-proxy URL that MAY embed `user:pass@` — the credentialed form of `WARDYN_DAEMON_PROXY_URL`, for an estate whose egress proxy requires a credential (flag `-daemon-proxy-secret-file`; `installDaemonProxySecret`, `cmd/wardynd/daemon_proxy.go`). A **file, not a secret-store reference**: this transport is installed before the database connects and before the secret store exists, so a store reference cannot be resolved at this point in boot — same ordering constraint `WARDYN_TRUSTED_CA_FILE` is under. Read once at boot, never deferred. The file's mode must be `0600` or tighter (readable/writable by group or other refuses boot); a missing file, an empty file, or content that is not a single `http://`/`https://` proxy URL also refuses boot, naming the var — never the raw path content, which may carry the credential. **Refuses boot if `WARDYN_DAEMON_PROXY_URL` is ALSO set** — the two name the same knob two different ways, and Wardyn never picks one silently. `WARDYN_DAEMON_PROXY_URL`'s own userinfo refusal is unchanged: it still refuses a credential in the process environment; this file is the escape hatch for a credential instead. The boot log names only the proxy's scheme and host, exactly as `WARDYN_DAEMON_PROXY_URL` does — never the credential |
| `WARDYN_ANTHROPIC_BASE_URL` | string (URL) | (unset) | operator-set internal model gateway base URL re-pointing Anthropic's brokered upstream (flag `-anthropic-base-url`; `ValidateLLMGateways`, `internal/api/llm_gateway.go`). Unset = every lane dials `api.anthropic.com` directly, byte-identical to today. Must be `https://`, carry no userinfo, no query/fragment, a non-empty host, and must not equal `api.anthropic.com` itself; an IP literal is accepted OUTSIDE loopback/link-local/metadata/unspecified/multicast/NAT64 (RFC1918/CGNAT IS accepted — that is the point; prefer a hostname over an IP literal — a literal must also be allow-listed by that literal, which exposes the gateway host to the sandbox on every port, see OPERATIONS). A malformed value refuses boot naming the var. **Scope: the api-key lane, plus subscription and Wardyn-managed runs** — setting this sends the operator's live subscription/managed OAuth token to the configured gateway instead of `api.anthropic.com` proxy-side, exactly as it would to the public host (`(*Server).anthropicBaseURL`, `runs_dispatch_llm.go`); turning it on is an operator trust decision. The harness-login (`claude setup-token`) lane is unaffected and always stays on `api.anthropic.com` — that flow mints the OAuth token itself. Boot warns when `WARDYN_DEFAULT_POLICY`'s `allowed_domains` does not list the gateway host, and separately when `SiteConfig.upstream_proxy_url` is set but `upstream_proxy_no_proxy` has no entry covering it — a boot-time read only, since `SiteConfig` is admin-editable afterwards and both settings can change without a restart. See [OPERATIONS.md § Internal model gateway](OPERATIONS.md#internal-model-gateway) |
| `WARDYN_OPENAI_BASE_URL` | string (URL) | (unset) | same as `WARDYN_ANTHROPIC_BASE_URL`, for OpenAI's api-key lane (`api.openai.com`; flag `-openai-base-url`) |
| `WARDYN_DEMO_VIDEO_BASE_URL` | string (URL) | (unset) | operator-run mirror base URL re-pointing the Getting Started demo episodes (flag `-demo-video-base-url`; `ValidateDemoVideoBaseURL`, `internal/api/llm_gateway.go`) for an air-gapped deployment, where `github.com` is unreachable. Unset = the console downloads episodes from GitHub directly, byte-identical to today. Same rule shape as `WARDYN_ANTHROPIC_BASE_URL` minus its rule 4/5 (no IP-literal floor, no "must not equal a public host" — there is no sandbox-reachability or public-provider-host concern for this knob): must be `https://`, carry no userinfo, no query/fragment, a non-empty host. A malformed value refuses boot naming the var. Published on the anonymous `/healthz` as `demo_video_base_url` beside `ui_sandbox`; the console's `episodeUrl` (`ui/src/app/lib/demo-videos.ts`) reads it off that poll to build each episode's download URL, and the CSP's `media-src` (`cspMediaSrc`, `internal/api/security_headers.go`) names ONLY this origin instead of the two hardcoded GitHub hosts once it is set — both re-derive from the SAME validated value, run through `cspSafeHost` before interpolation like every other CSP source |
| `WARDYN_ANTHROPIC_GATEWAY_HEADER` | string | (unset) | injection header name the operator's Anthropic gateway wants instead of the harness catalog's compile-time convention (`x-api-key`; flag `-anthropic-gateway-header`; `ValidateLLMGateways`, `internal/api/llm_gateway.go`). Unset = `x-api-key`, byte-identical to today — setting nothing never changes the vendor default. Independent of `WARDYN_ANTHROPIC_GATEWAY_FORMAT` (either may be set alone) and of `WARDYN_ANTHROPIC_BASE_URL` (the gateway host override). Must be a valid HTTP header token (`egress.ValidHeaderName`); a malformed value refuses boot naming the var, rather than surfacing later as a confusing upstream dial error. Applied in `(*Server).llmProviderFor`, the same seam that resolves the gateway host, onto the `InjectionRule.Header` the api-key grant carries |
| `WARDYN_ANTHROPIC_GATEWAY_FORMAT` | string | (unset) | injection value format the operator's Anthropic gateway wants instead of the harness catalog's compile-time convention (the bare key, `%s`; flag `-anthropic-gateway-format`; `ValidateLLMGateways`, `internal/api/llm_gateway.go`), e.g. `Bearer %s`. Unset = the bare key, byte-identical to today. Independent of `WARDYN_ANTHROPIC_GATEWAY_HEADER` and of `WARDYN_ANTHROPIC_BASE_URL`. Must contain exactly one `%s` (where the secret goes) and no other `%` verb (`validInjectionFormat`, the same rule an `api_key` eligible-grant scope and an integration's `proxy_header` delivery already enforce); a malformed value refuses boot naming the var. Applied in `(*Server).llmProviderFor` onto `InjectionRule.Format` |
| `WARDYN_OPENAI_GATEWAY_HEADER` | string | (unset) | same as `WARDYN_ANTHROPIC_GATEWAY_HEADER`, for OpenAI's gateway (default `Authorization`; flag `-openai-gateway-header`) |
| `WARDYN_OPENAI_GATEWAY_FORMAT` | string | (unset) | same as `WARDYN_ANTHROPIC_GATEWAY_FORMAT`, for OpenAI's gateway (default `Bearer %s`; flag `-openai-gateway-format`) |

Standard AWS environment variables are honored as **fallbacks only** — the
Wardyn-specific Bedrock names above always win when set. Compose forwards them from
the operator's shell (`docker-compose.yaml`); in host mode wardynd reads them
directly. Region alone cannot enable Bedrock (that also needs
`WARDYN_BEDROCK_MODEL`), and no AWS *credentials* are read from the environment —
only these selectors.

| Variable | Type | Falls back into | Notes |
|---|---|---|---|
| `AWS_REGION` | string | `WARDYN_BEDROCK_REGION` | preferred over `AWS_DEFAULT_REGION`, matching AWS SDK precedence |
| `AWS_DEFAULT_REGION` | string | `WARDYN_BEDROCK_REGION` | used only when `AWS_REGION` is absent |
| `AWS_PROFILE` | string | `WARDYN_BEDROCK_AWS_PROFILE` | selects a profile from the mounted `~/.aws` |

### Audit sinks (`WARDYN_AUDIT_SINKS`)

The value is the JSON blob itself, not a path to one — so a webhook
`bearer_token` lands in the process environment unless you deliver the blob
through `WARDYN_AUDIT_SINKS_FILE` instead. Omitting a top-level key
disables that sink; the whole variable unset disables fanout entirely (Postgres
stays the source of truth either way).

```json
{
  "syslog":  {"network": "udp", "addr": "localhost:514"},
  "webhook": {"url": "https://siem.example.com/ingest", "bearer_token": "...", "batch_size": 50},
  "file":    {"path": "/var/log/wardyn/audit.log", "max_bytes": 52428800, "keep": 3}
}
```

Defaults for the fields not shown: `file` rotates at `max_bytes` 100 MiB keeping
`keep` 5; `webhook` also takes `flush_interval` (`5s`), `buffer_size` (`4096`),
`max_retries` (`3`) and `retry_base_delay` (`200ms`) — see
`internal/audit/sinks/webhook.go`. The compose stack ships the `file` sink
pointed at `/data/audit/audit.log`.

Every event is one JSON object (NDJSON on the webhook sink — one line per
event per POST batch); `Data` is the per-action payload documented in
[AUDIT-ACTIONS.md](AUDIT-ACTIONS.md). `WARDYN_AUDIT_SOURCE` above adds a
`"source"` field to every one of them, for a multi-instance SIEM index.

#### SIEM recipes (`webhook`)

Two concrete `webhook` configs — swap in your real collector URL/token. Both
are just `WARDYN_AUDIT_SINKS`'s `webhook` block; nothing else changes.

**Splunk HTTP Event Collector (HEC).** HEC wants each line wrapped in an
`{"event": ...}` envelope, which this sink does not do — point it at a
lightweight HEC-shaped relay (or Splunk's own NDJSON-to-HEC forwarder) rather
than `services/collector/event` directly, and pass the HEC token as the
bearer:

```json
{
  "webhook": {
    "url": "https://splunk-hec-relay.corp.example:8443/ingest",
    "bearer_token": "<hec-token>",
    "batch_size": 100
  }
}
```

**Generic JSON webhook** (Datadog, Elastic, Chronicle, an in-house collector
— anything that accepts a bearer-authed NDJSON POST):

```json
{
  "webhook": {
    "url": "https://siem.example.com/ingest",
    "bearer_token": "<collector-token>",
    "batch_size": 50,
    "flush_interval": "5s"
  }
}
```

Both refuse to boot with a `bearer_token` set on a non-`https://` `url` (the
credential would replay in cleartext on every POST — see `NewWebhookSink`).
Watch `wardyn_audit_sink_drops_total{sink="webhook"}` on `/metrics`: a rising
count means the collector is slow/down and events are being shed after
`max_retries` — see [Monitoring](OPERATIONS.md#monitoring).

## `wardyn-proxy` (egress sidecar)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_PROXY_CONFIG_JSON` 🔒 | string (JSON) | (unset) | full proxy config incl. run egress policy, delivered at container create |
| `WARDYN_LLM_SCAN` | enum | (unset = per policy) | per-proxy kill-switch for outbound content inspection; `off`/`0`/`false`/`no`/`disable`/`disabled`/`none` disable, `on`/`1`/`true`/`yes`/`enable`/`enabled` (or unset) leave as policy, garbage exits 2 |
| `WARDYN_GIT_BROKER_ENFORCE_BRANCH_NS` | enum | (unset = **on**) | push branch-namespace confinement on the git-broker route: a `git-receive-pack` may update only refs under `refs/heads/wardyn/<run-id>/` (403 otherwise, before any mint). `1`/`true`/`yes`/`on`/`enable`/`enabled`/`enforce` (or unset) enforce; `0`/`false`/`no`/`off`/`disable`/`disabled`/`none` opt out; garbage fails CLOSED (enforces + logs). **On by default**: `agent-run` checks each cloned repo out onto `wardyn/$WARDYN_RUN_ID/work` and sets `push.default=current`, so a stock run already pushes inside its namespace — set `=false` only for an image whose `agent-run` predates that. Binds the brokered App lane only (a `git_pat`/`ssh_key` push never traverses this route). Since 0.7 that is a scoping DECISION for `git_pat`, not an impossibility: the never-resident `git_pat` lane is brokered cleartext smart-HTTP too (`WARDYN_GIT_PAT_BROKER`, default on), so the same parser can read its pushes — and since 0.7.2 it does, behind the separate, **default-off** `WARDYN_GIT_PAT_BROKER_ENFORCE_BRANCH_NS` below; this variable never turns that one on or off. An `ssh_key` push remains structurally unreadable. A brokered repo has no `git_pat` path beside its App lane in any case: policy-write refuses the two grants for the same forge. Both container runners forward it (and `WARDYN_LLM_SCAN`, and the `git_pat` switch below) from wardynd's environment to every proxy sidecar — docker as an `Env` entry, Kubernetes on the proxy pod; host-run proxies read their own env. **Opting out is visible, not inferred:** the proxy logs a one-shot `slog` WARN at boot when enforcement is OFF, and every push it then forwards unparsed carries the decision-log `rule_source` `brokered:git:branch-ns-off` instead of `brokered:git`, so the audit trail distinguishes an opted-out run from a confined one. **This variable is WHERE-only:** a run's `push_rules` (`deny_paths`, `max_inspect_pack_mib`) is WHAT a push may touch, enforced on both brokered lanes independently of either branch-namespace switch — opting out of this one, or of `WARDYN_GIT_PAT_BROKER_ENFORCE_BRANCH_NS` below, never opts a push out of its content rules. See [docs/POLICIES.md](POLICIES.md#push_rules--pushrulesspec). |
| `WARDYN_GIT_PAT_BROKER_ENFORCE_BRANCH_NS` | enum | (unset = **off**) | the same push branch-namespace confinement as `WARDYN_GIT_BROKER_ENFORCE_BRANCH_NS`, applied to the brokered `git_pat` route (`/wardyn/git/<host>/`): with it on, a `git-receive-pack` through that route may update only refs under `refs/heads/wardyn/<run-id>/` (403 otherwise, before the PAT is minted), a non-identity `Content-Encoding` is refused as unparseable (415), and the run's own `git_push_any_branch` still opts out. Same word list as the App-lane switch (`1`/`true`/`yes`/`on`/`enable`/`enabled`/`enforce` on; `0`/`false`/`no`/`off`/`disable`/`disabled`/`none` off; garbage fails CLOSED to **on**, since a typo must not undo a control you deliberately enabled) — only the unset default differs. **Off by default**, unlike the App lane: a PAT carries whatever scope the operator issued it with and Wardyn cannot narrow it, and forges other than GitHub do not share GitHub's push-ref conventions, so the namespace is a convention Wardyn asks for rather than a bound it can enforce. Turning it on changes no decision-log vocabulary: the refusals reuse `brokered:git:branch-ns` / `brokered:git:branch-ns-encoding` and a `git_push_any_branch` push reuses `brokered:git:branch-ns-off`, against the FORGE's host (an unconfined PAT push keeps the ordinary `brokered:git-pat` allow). The proxy logs a one-shot `slog` INFO at boot when it is ON — the mirror of the App lane's OFF warning. **Both container runners forward it** from wardynd's environment into every proxy sidecar — the docker driver's `Env` slice and the Kubernetes driver's proxy-pod `Env` (one shared list, `runner.ProxySidecarEnvKnobs`); host-run and custom-image proxies read their own environment. |
| `WARDYN_CREDENTIAL_REAUTH_TIMEOUT` | duration | `600s` | how long the proxy **HOLDS** a request while the credential's owner signs in again, instead of failing it (`credhold.go`). Applies to the two lanes whose sign-in the proxy injects: the captured-AWS-SSO Bedrock lane (a sandbox's `GetRoleCredentials` call, `WARDYN_AWS_SSO_PROXY_INJECT`) and, from 0.7.10, the per-person Azure DevOps lane (a request made after the person's Azure DevOps sign-in ended or was blocked by Conditional Access); every other credential is unaffected and every other grant resolves byte-identically to before this existed. Clamped to `[10s, 1800s]`; unparseable or non-positive values keep the default. One hold per re-auth REQUEST, and every call that discovers the same lapse joins it: the hold belongs to the request, not to whichever call opened it, so a client that gives up does not end it and a client that arrives later does not start a second one. At most 8 such holds open per run, counted per request rather than per call. Lower it if your agent's SDK gives up before the hold does — on expiry the call fails with a modelled `UnauthorizedException` exactly as it did before this existed, and the sign-in request stays open. Forwarded into every proxy sidecar from wardynd's environment by both container runners (`runner.ProxySidecarEnvKnobs`) |
| `WARDYN_GIT_APPROVAL_TIMEOUT` | duration | `120s` | overrides how long the proxy's git broker blocks polling a `wait_for_review` git-mint approval before failing (`waitForGitApproval`). Unparseable or non-positive values are silently ignored and the 120s default is kept. |

## `wardyn-git-helper` (git credential broker)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_APPROVAL_TIMEOUT` | duration | `120s` | approval wait budget; unparseable exits 2 (via `cliutil.EnvDuration`) |
| `WARDYN_GIT_HELPER_SECRET` 🔒 | string | (unset) | per-run caller-auth secret gating token emission |
| `WARDYN_GITHUB_GRANT_ID` | string | (unset) | this run's GitHub **App** grant id. The helper mints it for a GitHub host **unless** `WARDYN_GIT_BROKER_REPOS` says the run is brokered. Unset ⇒ a GitHub host falls through to `WARDYN_GIT_PAT_GRANTS` as before. |
| `WARDYN_GIT_BROKER_REPOS` | string (space-separated) | (unset) | the `<org>/<repo>` set this run's GitHub access is **brokered** for — written by dispatch from the same map `confineGitBrokerEgress` keys on. Non-empty ⇒ the `/wardyn/gh/` route serves those repos, the broker-managed GitHub hosts are denied, and the helper REFUSES every GitHub host so the installation token (minted proxy-side) never enters the sandbox. Empty ⇒ the run has a grant but no brokered repo: no broker route, no injected deny, so the helper stays its credential path. This is the ONE predicate — refusal and confinement must never drift apart. |
| `WARDYN_GIT_PAT_GRANTS` 🔒 | string (JSON) | (unset) | brokered PAT grants |
| `WARDYN_PROXY_URL` | string | (unset) | broker/proxy address the helper mints against |

## `wardyn-tetragon-ingest` (host sensor → ground-truth)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_TETRAGON_EXPORT` | string | `/var/log/tetragon/tetragon.log` | Tetragon JSON export path (flag `-export`) |
| `WARDYN_CONTROL_PLANE_URL` | string | `http://wardynd:8080` | control plane base URL (flag `-control-plane-url`) |
| `WARDYN_GROUNDTRUTH_TOKEN` 🔒 | string | (unset) | host-sensor bearer (aud=wardyn-groundtruth) (flag `-token`) |
| `WARDYN_GROUNDTRUTH_TOKEN_FILE` 🔒 | string | (unset) | file source for the sensor token |
| `WARDYN_GROUNDTRUTH_HEARTBEAT` | duration | `30s` | sensor heartbeat interval (flag `-heartbeat`) |
| `WARDYN_GROUNDTRUTH_REFRESH` | duration | `15s` | container→run index refresh (flag `-refresh`) |
| `WARDYN_GROUNDTRUTH_STATS` | duration | `60s` | throughput/drop stats log interval (flag `-stats`) |
| `WARDYN_GROUNDTRUTH_BATCH` | int | `64` | max events per POST batch (flag `-batch`) |
| `WARDYN_GROUNDTRUTH_BUFFER` | int | `4096` | event buffer before backpressure drops (flag `-buffer`) |
| `WARDYN_GROUNDTRUTH_BLIND_RUNS` | CSV | (unset) | run ids the host sensor is blind to, CC3/Kata (flag `-blind-runs`) |
| `WARDYN_GROUNDTRUTH_FORWARD_UNMAPPED_HOST_EVENTS` | bool | `false` | opt-in: forward kernel exec/connect/write events the host sensor could not correlate to a Wardyn-managed run (other containers, the bare host) to the audit sink/SIEM; OFF by default so the undeletable audit log stays sandbox-scoped (flag `-forward-unmapped-host-events`) |

## `wardyn` (CLI)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_URL` | string | `http://localhost:8080` | control plane URL (flag `-url`) |
| `WARDYN_ADMIN_TOKEN` 🔒 | string | (unset) | admin bearer for CLI calls |
| `WARDYN_TOKEN` 🔒 | string | (unset) | fallback source for the CLI's admin bearer (`cmd/wardyn/main.go`) — read by the CLI only, never by `wardynd` |
| `WARDYN_ALLOW_PLAINTEXT` | bool | `false` | silence the warning the CLI prints when the admin token would travel in cleartext (`http://` to a non-loopback wardynd). Advisory only — the call is never refused; set it when TLS terminates in front of the control plane |

## `wardyn-scan` (workspace-needs scanner, run INSIDE a sandbox)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_WORKSPACE_DIR` | string (path) | `/home/agent/work` | the directory `wardyn-scan` scans, **inside the run container** — the agent images' workspace mount point (`defaultWorkspaceDir`, `cmd/wardyn-scan/main.go`). Not a host path and not read by the `wardyn` CLI at all |

## `wardyn-rec` (recording uploader)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_RUN_TOKEN` 🔒 | string | (unset) | bearer for `-upload-url` auth (flag `-run-token`) |

## `internal/envbuild` (devcontainer builder, invoked by `wardynd`)

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_ENVBUILD_BUILD_NETWORK` | string | **binary: (builder default, `none`)**; compose: the envbuild bridge (`${WARDYN_NS:-wardyn}-envbuild`) | build network: `bridge`/`host`/a named network. **Never `host`** on a deployment that also loopback-publishes anything for its own convenience (compose's control-plane Postgres and admin API) — see `threatmodel/THREAT-MODEL.md` residual #13 |
| `WARDYN_ENVBUILD_BUILD_CPUS` | float | (builder default) | build CPU limit |
| `WARDYN_ENVBUILD_BUILD_MEMORY_MB` | int | (builder default) | build memory limit (MB) |
| `WARDYN_ENVBUILD_MAX_CONTEXT_MB` | int | (builder default) | max build-context size (MB) |
| `WARDYN_ENVBUILD_TOOLS_DIR` | string | **binary: (unset)**; compose: `/wardyn-tools` | host tools dir injected into the built image |
| `WARDYN_ENVBUILD_PUSHED_REF` | string | (unset) | pushed image ref recorded after a daemonless push |
| `WARDYN_ENVBUILD_REGISTRY_INSECURE` | bool | `false` | skip TLS verification against CacheRepo (`ENVBUILDER_INSECURE`); only for a registry that never spoke TLS |

## Sidecar / sandbox-injected (read inside the run container)

These are set by `wardynd` (`internal/api/runs_dispatch.go`, runner driver) into
the sidecar/sandbox environment and read there.

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_PROXY_URL` | string | (unset) | egress proxy address inside the sandbox (`internal/sidecar`, `internal/runner/docker`) |
| `WARDYN_RUN_ID` | string | (unset) | this run's id, injected into sidecar/sandbox |
| `WARDYN_REPO_URL` | string | (unset) | repo clone URL for the run |
| `WARDYN_REPO_SLUG` | string | (unset) | repo slug for the run |
| `WARDYN_REPOS` | string (JSON) | (unset) | multi-repo spec for the run |
| `WARDYN_EPHEMERAL_DIRS` | string (CSV) | (unset) | scratch targets a workspace's ephemeral sources ask for: `agent-run` mkdirs them in the sandbox. No host path is involved — they die with the run |
| `WARDYN_USER_DRIVE` | string (`<target>:ro\|rw`) | (unset) | the run's USER DRIVE, present ONLY when one is mounted: the in-container target and the mode, e.g. `/home/agent/drive:ro`. An ANNOUNCEMENT, never the mechanism — the mount is made by the runner from the resolved drive (`applyUserDriveEnv`, `internal/api/runs_dispatch_mounts.go`), so an agent that ignores this still has the drive and one that fabricates it still has nothing. It names the target and the mode and nothing else: the storage object, the host path and the drive's name are admin-facing, and the member's own request carried a flag, never a path |
| `WARDYN_SSH_GRANTS` 🔒 | string (JSON) | (unset) | brokered SSH grants |
| `WARDYN_MITM_CA_PEM` | string (PEM bundle) | (unset) | CA certificate(s) the sandbox trusts — the per-run TLS-MITM CA when `intercept_tls`/subscription injection is active, with the operator's `WARDYN_TRUSTED_CA_FILE` bundle appended when configured (`installSandboxTrustedCA`, `internal/api/runs_dispatch_llm.go`); a run with neither active never has this key at all. `install_mitm_ca` (`agent-run-lib.sh`) treats it as an opaque PEM bundle, so multiple concatenated certificates work |
| `WARDYN_TASK_MODE` | string | (unset) | run task mode (e.g. exec) |
| `WARDYN_INTERACTIVE_START` | string | (unset) | `agent` makes an INTERACTIVE run's attach shell open in the image's agent CLI (`claude`/`codex`) in the prepared workspace; unset/`shell` is a bare shell. With no boot seed (`WARDYN_INTERACTIVE_SEED` below, unset) this fires on first attach, as before; with a seed, the session already starts at boot and attach just joins it. Read by `attach-bashrc.sh` (the image's `~/.bashrc`), so it covers the console terminal and the SSH gateway alike. Only ever set for interactive runs |
| `WARDYN_INTERACTIVE_SEED` | string | (unset) | the run's optional boot seed — its `task`, exported verbatim for an interactive run only, and interpreted per `WARDYN_INTERACTIVE_START` (an initial prompt for `agent`, a startup command for `shell`). Fires once at sandbox boot, in the same persistent session `wardyn attach` joins. Excluded for the server-reserved Tasks (`workspace record`/`workspace verify`/harness login) — those are interactive runs too, but seeding them would boot the agent into their own control task |
| `WARDYN_SEED_AUTO_TOOLS` | bool | (unset) | `1` starts the boot seed above with `--dangerously-skip-permissions` (opt-in, for that pre-attach span only); unset/absent is supervised — the seed parks at its first tool-approval prompt until someone joins. Today `1` still parks on the CLI's own *Bypass Permissions mode* confirmation until a human attaches and answers it — Wardyn does not pre-answer that prompt ([THREAT-MODEL.md](../threatmodel/THREAT-MODEL.md) §4.7). Only ever set alongside a non-empty `WARDYN_INTERACTIVE_SEED` |
| `WARDYN_TOOL_APPROVALS` | string | (unset) | `hold` routes an AUTONOMOUS (non-interactive) Claude Code run's own tool calls to a Wardyn approval instead of running unsupervised; unset/`auto` is today's `--dangerously-skip-permissions` behavior. Never set for an interactive run (that run's supervised posture is `WARDYN_SEED_AUTO_TOOLS`'s job) or for codex-cli (rejected at create time — no external tool-approval contract) |
| `WARDYN_AUTONOMY_LEVEL` | string | (unset) | the run's RESOLVED autonomy level (`L0` attended, `L1` gated, `L2` unattended, `L3`), frozen on the run row by the launch gate and announced here so the image's launcher can log which level it came up under. An ANNOUNCEMENT, never the mechanism — what actually constrains the agent is its launch branch (`WARDYN_TOOL_APPROVALS` / `WARDYN_SEED_AUTO_TOOLS` above) plus the managed-settings file generated from this same level and delivered root-owned, so an agent that ignores this key still has both and one that fabricates it still has neither. Absent for every run no governance rubric bound, which is every run on a deployment that has authored none |
| `WARDYN_RECORDING` | bool | (unset) | `1` tells the sandbox that session recording is configured — stamped into the agent container's env by the docker driver when its Record config is on. `boot_seed_rec_wrap` (agent-run-lib.sh) keys the boot-seed pane's wardyn-rec wrap on this; env because the earlier signal (the driver's post-start `/var/log/wardyn` root exec) races the boot pane and losing silently skipped recording. Absent — recording off, k8s (no Record config), or an older driver — the seed runs unrecorded rather than not at all |
| `WARDYN_PROBE_TO_URL` | string | (unset) | site-config test-redirect: the stored egress redirect's `to` URL the probe fetches through wardyn-proxy |
| `WARDYN_PROBE_FROM_URL` | string | (unset) | site-config test-redirect: the stored egress redirect's `from` URL the probe fetches directly (`--noproxy`), to check whether the mirror is actually enforced |
| `WARDYN_PROBE_TO_CONNECT` | string | (unset) | site-config test-redirect: the `host:port` the probe dials for the `to` URL while still presenting the `from` hostname for TLS (curl `--connect-to`). Set when the redirect target is a literal IP, whose certificate is scoped to the public hostname — dialing it directly would present the IP as SNI and fail verification against a mirror that a real run reaches fine, since a CONNECT tunnel takes SNI from the sandbox. Empty leaves the probe's first leg byte-identical |
| `WARDYN_SCAN_ONLY` | bool | (unset) | scan-only run flag |
| `WARDYN_ARTIFACT_CONFIG_B64` 🔒 | base64 | (unset) | artifact config blob |
| `WARDYN_CLAUDE_MANAGED_B64` 🔒 | base64 | (unset) | managed Claude credential blob |
| `WARDYN_AWS_SSO_CONFIG_B64` 🔒 | base64 | (unset) | captured AWS SSO session, delivered as a minimal synthetic `~/.aws` (config + SSO token cache) and materialized by `agent-run`; the sandbox SDK exchanges it for short-lived Bedrock role credentials |
| `WARDYN_AWS_SSO_ACCOUNT_ID` | string | (unset) | the AWS account a sign-in in the **container-login** sandbox may capture, seeded from the agent roster row's `sso_account_id`. `wardyn-aws-sso` VERIFIES it against the SSO portal (the account must be one this session reaches, and the pinned role must exist in it) and refuses the sign-in rather than falling back — without it the helper would take `AccountList[0]`, which a cloud team granting an unrelated entitlement can silently change. Set together with the role below, or not at all; NOT a credential (an account id is configuration). Present only in an aws-sso login run, never in a workspace run |
| `WARDYN_AWS_SSO_ROLE_NAME` | string | (unset) | the IAM role the same sign-in may capture, seeded from the roster row's `sso_role_name` and verified in the pinned account (`ListAccountRoles` is scoped to the account it is asked about, so a role that exists in some OTHER account the person reaches is not a match). Set together with the account above |

## Compose / scripts (shell-only — not read by Go)

Read by `deploy/compose/docker-compose.yaml` and the operator scripts, never by
Go, so they carry no flag and are exempted from the reverse ratchet by name
(`envDocShellOnly` in `cmd/wardynd/envdoc_guard_test.go`). The first table is the
compose stack itself — mostly scoping, so several stacks can coexist on one host
(see [docs/CI.md](CI.md#concurrent-jobs-on-a-shared-host)); the second is the
`make setup` installer.

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_NS` | string | `wardyn` | namespace prefix for every *explicitly named* compose object — containers, the control-plane network, the recordings volume. Leave unset for a single interactive stack. See [below](#four-variables-that-need-more-than-a-table-cell) |
| `WARDYN_VAULT_TOKEN_DIR` | string | (unset; required by the overlay) | the host directory a Vault Agent sink (or CSI driver) writes `token` into, mounted read-only at `/run/wardyn-vault` by `deploy/compose/docker-compose.vault.yaml`, the store-mode overlay, and by `deploy/compose/docker-compose.transit.yaml`, the Transit key-service overlay; wardynd reads `/run/wardyn-vault/token` as `WARDYN_VAULT_TOKEN_FILE` |
| `WARDYN_UP_PORT` | int | `8080` | host port the control plane/UI publishes on (loopback-only). `0` = OS-assigned ephemeral port, which is what `scripts/ci-run.sh` uses (it reaches wardynd inside the container, not over the host port) |
| `WARDYN_PG_PORT` | int | `5432` | host port the compose Postgres publishes on (loopback-only, so a host-mode wardynd can reach the same database). `0` = OS-assigned ephemeral port — used by CI, which reaches Postgres in-network by service name and never needs a predictable host port |
| `WARDYN_DEX_PORT` | int | `5556` | host port the compose Dex publishes on (loopback-only; Dex runs only under the `sso` profile). `0` = OS-assigned ephemeral port, so concurrent jobs on one host don't collide |
| `WARDYN_REGISTRY_PORT` | int | `5010` | host port the compose devcontainer-build registry sidecar publishes on (loopback-only; this is the HOST DAEMON's own reach to it at image-finalize time — the build container instead reaches it by compose service name, see `WARDYN_ENVBUILD_CACHE_REPO` above). `0` = OS-assigned ephemeral port — like `WARDYN_UP_PORT`/`WARDYN_PG_PORT`, both `scripts/ci-run.sh` and `scripts/test-concurrent.sh` already default it to `0`; a caller only needs to set it by hand outside those two scripts, or it collides with another job's registry on the shared default; see [docs/CI.md](CI.md#concurrent-jobs-on-a-shared-host) |
| `WARDYN_SSH_PORT` | int | `2222` | host port the compose wardynd service publishes for the SSH gateway (loopback-only, mirroring `WARDYN_UP_PORT`; see [docs/SSH.md](SSH.md)). The mapping is always present, but is inert until `WARDYN_SSH_LISTEN` is ALSO set — that is the variable that actually turns the gateway on (empty = off = no listener, not even a generated host key) |
| `WARDYN_UI_SANDBOX_PORT` | int | `8081` | host port the compose wardynd service publishes for the UI-sandbox gateway (loopback-only, mirroring `WARDYN_SSH_PORT`; see [docs/UI-SANDBOXES.md](UI-SANDBOXES.md)). Always mapped, inert until `WARDYN_UI_SANDBOX_LISTEN` is ALSO set. A different port is a different browser **origin**, which is the separation this gateway exists for — keep it distinct from `WARDYN_UP_PORT` (wardynd refuses to boot when the two listen addresses are equal) |
| `WARDYN_WARDYND_IMAGE` | string | `wardyn/wardynd:local` | image tag the compose `wardynd` service is built to and run from. As of 0.6.2 `scripts/up.sh` PULLS the published image for the daemon's version and retags it to this name, so the tag is usually a pulled artifact rather than a local build (`WARDYN_BUILD_LOCAL=1` forces the build). Paired with `WARDYN_PROXY_IMAGE` (above), which the same stack passes to wardynd for its per-run proxy sidecar and which the build-only `proxy-image` stanza is tagged with. Both exist so a job on a shared daemon can build its OWN copies instead of racing another job's writes to the mutable `:local` tags — `scripts/run-e2e-ssh.sh` and `scripts/run-e2e-ui-sandbox.sh` set them to `wardyn/<name>:${COMPOSE_PROJECT_NAME}`. Leave unset and every caller behaves exactly as before |
| `WARDYN_UI_STAGE` | string | `ui-build` | which Dockerfile stage feeds the image's `/srv/ui`: `ui-build` (builds from source with pnpm) or `ui-prebuilt` (consumes a host-built `ui/dist`). **Normally you never set this** — `scripts/up.sh` retries with `ui-prebuilt` on its own. See [below](#four-variables-that-need-more-than-a-table-cell) |
| `WARDYN_BUILD_LOCAL` | bool | `false` | `make setup` / `scripts/up.sh`: skip pulling the published, cosign-signed images and build every image from this working tree instead. Set it when testing your own changes — otherwise the pull would silently run a released binary rather than your code. The pull already falls back to building on its own when an image is missing (a version whose images are not pushed yet, an air-gapped host, an unreachable registry), so this is for forcing the build, not enabling it |
| `NPM_REGISTRY` | string | (unset ⇒ npmjs) | alternate npm registry for the in-image `pnpm install`. A transparent-cache corporate mirror works; a strict allowlist mirror that hasn't onboarded pnpm 404/403s its tarballs, which is what triggers the `ui-prebuilt` fallback |
| `GOPROXY` | string | (unset ⇒ `https://proxy.golang.org,direct`) | build arg forwarded into every Go builder stage's `ARG GOPROXY=` / `ENV GOPROXY=${GOPROXY}` — an alternate Go module mirror (Athens/Artifactory/Nexus) for `go mod download`. Empty is identical to unset: the go command falls back to `$GOROOT/go.env` |
| `HTTP_PROXY` / `HTTPS_PROXY` / `NO_PROXY` | string | (unset) | forwarded from the operator's shell as **build args** so image builds traverse a corporate proxy, and read by `scripts/up.sh` for its corp-CA preflight. Never set as wardynd runtime env — see `WARDYN_HOST_PROXY_B64` above for why. For wardynd's OWN outbound calls behind a corporate proxy, use `WARDYN_DAEMON_PROXY_URL` / `WARDYN_DAEMON_NO_PROXY` above instead: the standard names are also honored process-wide by the Kubernetes client and by every library that calls `http.ProxyFromEnvironment`, so setting them here would silently reroute the control plane's own API access too |
| `WARDYN_HOST_GOOS` / `WARDYN_HOST_GOARCH` | string | derived from `uname` | build target for the host-native `wardyn` used by host-side proxy detection. Override only when cross-building for a different host |
| `WARDYN_CI_PROJECT` | string | `wardyn-ci-$$` (unique per invocation) | `scripts/ci-run.sh` only: the one name used for **both** `COMPOSE_PROJECT_NAME` and `WARDYN_NS`. Pin it to the CI job id for a greppable stack name. See [below](#four-variables-that-need-more-than-a-table-cell) |
| `WARDYN_CI_TOOLS_DIR` | string (path) | (set per invocation) | **Not operator-settable — an output, not a knob.** `scripts/ci-run.sh` `export`s it to a fresh `mktemp -d` it stages the runner tools into, overwriting any value you export; `deploy/compose/docker-compose.ci.yaml` consumes it (`${WARDYN_CI_TOOLS_DIR:?exported by scripts/ci-run.sh}`) |
| `WARDYN_DOCKER_SOCK` | string (path) | `/var/run/docker.sock` | which host Docker socket `wardynd` binds and drives. Matters on a two-daemon host (Docker Desktop's socket vs. a native in-distro `dockerd`): the driver probes CC2/CC3 runtimes against *this* socket's daemon and fails closed when they are absent, so pointing at a runc-only daemon caps the whole stack at CC1. Persisted into `deploy/compose/.env` by setup |
| `WARDYN_WORKSPACES_ROOT` | string (path) | (unset = no host path exposed) | compose only — bind-mounts this host directory READ-ONLY at the *same path* inside the `wardynd` container so `local_dir` workspace onboarding (`internal/api/workspaces.go`) can see host source. Point it at the narrowest project directory you have — **never** your home directory (wardynd could then read `~/.ssh`, `~/.aws`, `~/.claude`, and staged Claude creds). The interactive prompt below enforces this itself — typing your home directory (or `~`) is refused with a warning and reverts to whatever was set before, not merely discouraged in copy. Unset means local-directory import stays unavailable rather than silently exposing anything. `scripts/setup.sh`'s containerized path prompts for it interactively (TTY only, and only when it is unset) and persists a non-empty answer into `deploy/compose/.env`; an already-exported env var wins silently and is persisted UNCHECKED — skipping the prompt (and its guard) entirely — and a bare Enter with no prior `.env` value leaves it unset/sealed — never the current directory. It is singular for the same reason `WARDYN_USER_DRIVE_HOST_ROOT` below is: compose cannot expand a CSV into volume lines, so a `WARDYN_USER_WORKSPACE_ROOTS` naming more than one tree — `deploy/desktop/wardyn.env.m-prime.example` names both `/Users/Shared/src` and `/srv/src` — has a bind for only whichever one path this variable is set to, and every other root needs its own same-path `:ro` volume line added by hand in `deploy/compose/docker-compose.yaml` (a commented example sits next to the existing bind); the read-only-ness of this bind is unrelated to whether a member's mount ends up writable, which `WARDYN_USER_WRITABLE_ROOTS` alone decides |
| `WARDYN_USER_DRIVE_HOST_ROOT` | string (path) | (unset = nothing mounted) | compose only, and **not the ceiling** — the ceiling is `WARDYN_USER_DRIVE_HOST_ROOTS` above, which `wardynd` reads. This bind-mounts one host tree READ-ONLY at the *same path* inside the `wardynd` container, the way `WARDYN_WORKSPACES_ROOT` does, because the drive's ceiling check resolves symlinks **in wardynd's own process** and fails closed on a path it cannot stat: without it a containerised wardynd refuses every `host_path` drive. Set both, to the same path. It is singular because compose cannot expand a CSV into volume lines — a ceiling naming several roots needs one extra volume line per root in `deploy/compose/docker-compose.yaml`, copied from this one (or run wardynd on the host / on k8s, where no bind is involved). Read-only is enough — wardynd stats the tree and never writes to it — but it does need search (`x`) permission down to each person's directory |
| `WARDYN_MANAGED_DIR` | string (path) | (unset = nothing mounted) | compose only — bind-mounts this host directory READ-ONLY at the fixed path `/etc/wardyn` inside the `wardynd` container: the desktop tier's MDM payload (`deploy/desktop/wardyn-desktop.sh` exports it as `/etc/wardyn` so the MDM-managed `WARDYN_DEFAULT_POLICY`, a HOST path wardynd reads off disk at boot, resolves inside the container too), OR a corporate CA bundle on **any** compose install (point `WARDYN_TRUSTED_CA_FILE` above at a file under this mount, e.g. `/etc/wardyn/corp-ca.pem`). Unlike `WARDYN_WORKSPACES_ROOT` above, only the HOST side varies — the container side is the literal `/etc/wardyn`. A deployment needing neither leaves it unset and mounts a harmless empty path |

### Setup / operator scripts (shell-only)

Read by `install.sh`, `scripts/setup.sh`, `scripts/up.sh`, `scripts/up-reset.sh`,
`scripts/ci-run.sh` and the Makefile. For the rest of the CI knobs see
[docs/CI.md](CI.md). This table is the registry for the rest — there is no
second list of shell knobs anywhere else.

| Variable | Type | Default | Notes |
|---|---|---|---|
| `WARDYN_SETUP_MODE` | string | (unset = interactive prompt; headless `container`) | `make setup` installer path: `container` (this compose stack — the default and the recommended mode) or `local`/`host` (advanced host mode). Read by `scripts/setup.sh`, not by `wardynd` |
| `WARDYN_SUBSCRIPTION_TOKEN` 🔒 | string | (unset) | **Neither compose path honours it, despite the name.** `scripts/up.sh` *ignores* it with a warning — a shared subscription credential is a single-user desktop setting (`WARDYN_ALLOW_SHARED_SUBSCRIPTION` is that escape hatch, and the Helm chart refuses the whole family outright) — and `scripts/ci-run.sh` **exits non-zero** on it, because a subscription belongs to one person while a pipeline runs on behalf of everyone who can trigger it (pass an `anthropic-api-key` CI secret, or use Bedrock — [docs/CI.md](CI.md) has the variable). The one reader that honours it is `scripts/record-demo.sh`, which pipes it into `wardyn subscription connect --token-stdin`. Everywhere else: connect it in the console, Settings → Model provider → Claude subscription |
| `WARDYN_IMPORT_AWS` 🔒 | bool | (unset = ask; headless without it = do not import) | **Imports STATIC AWS CREDENTIALS, not "selectors".** When `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` (and optionally `AWS_SESSION_TOKEN`) are in the environment, `scripts/setup.sh` offers to write them into Wardyn's age-encrypted secret store as `aws-access-key-id` / `aws-secret-access-key` / `aws-session-token`; `WARDYN_IMPORT_AWS=1` is the headless YES for exactly that decision (a non-interactive run never stores keys silently). These are long-lived cloud credentials and become **resident in every sandbox that uses Bedrock** at run time. This is a different thing from the Bedrock *selectors* (`WARDYN_BEDROCK_REGION` / `WARDYN_BEDROCK_MODEL`) above, which are not credentials and which this variable does not control. Safer alternative, and what `setup.sh` recommends: AWS SSO with a read-only `~/.aws` mount — short-lived, auto-rotating, and Wardyn stores nothing |
| `WARDYN_IMPORT_SCM` | bool | (unset = ask, default no) | import host SCM credentials during setup |
| `WARDYN_FORCE_RESET` | bool | (unset = ask) | answer the destructive-reset confirmation non-interactively |
| `WARDYN_FORCE_STOP_HOST` | bool | (unset = ask) | `WARDYN_FORCE_RESET`'s sibling, gated **separately** because it is the bigger act: `scripts/up.sh reset-all` stops a HOST-mode wardynd only when this is set too (`_confirm_host_stop`, `scripts/up-reset.sh`). `WARDYN_FORCE_RESET` alone answers the volume-wipe confirmation and leaves a host daemon running |
| `WARDYN_UP_NO_BROWSER` | bool | (unset = open one) | `1` stops `scripts/up.sh` and `scripts/setup.sh` opening the console in a browser when they finish. Set it on headless boxes, in recorded demos, and anywhere `up` is driven by another script |
| `WARDYN_UP_SKIP_RUN_IMAGES` | bool | (unset = build them) | `1` makes `scripts/up.sh` skip building the agent + proxy run images, for a console-only look. Runs then fail until you build them: `make agent-images-core && docker compose -f deploy/compose/docker-compose.yaml --profile build-only build proxy-image` |
| `WARDYN_SCM_SSH_HOSTS` | string (space-separated) | `github.com` | hosts `scripts/setup.sh` fetches SSH host keys for while importing SCM credentials, e.g. `"github.com dev.azure.com"` |
| `WARDYN_GEN_DEPLOY_KEY` | bool | (unset = ask) | `1` makes `scripts/setup.sh` generate a per-repo deploy key headlessly instead of prompting for one |
| `WARDYN_DEPLOY_KEY_HOST` | string | `github.com` | the host that deploy key is generated for (`scripts/setup.sh`); read only when a key is actually being generated |
| `WARDYN_KATA_VERSION` | string | (unset = the Kata Containers GitHub API's latest release tag) | `wardyn setup vault`'s Kata install script (`cmd/wardyn/setup.go`) only: override the resolved Kata Containers version. FLOORED at the security floor (`kataMinVersion`, `sort -V`) either way — an old mirror, a GitHub API hiccup, or an explicit downgrade override below the floor refuses the install (exit 1) rather than silently reintroducing CVE-2026-44210/-47243 (virtio-fs guest-root → host-root via virtiofsd) |
| `WARDYN_VERSION` | string | (unset = newest release) | `install.sh` only: which release the one-line installer resolves, e.g. `v0.6.6`. Unset asks the GitHub releases API |
| `WARDYN_HOME` | string (path) | `~/.wardyn` (`~/.<WARDYN_NS>`) | `install.sh` only: where the one-line install puts `.env`, the compose file and the data. Re-running the installer at a newer version reads the same directory — that is the upgrade path ([OPERATIONS.md](OPERATIONS.md#upgrading-a-one-line-install)) |
| `WARDYN_PORT` | int | `8080` | `install.sh` only: the loopback host port the one-line install publishes the console on. It is written once, as `WARDYN_UP_PORT` in `.env`; an upgrade re-run deliberately does **not** rewrite the ports, so change the port in `.env` rather than here |
| `WARDYN_DEFAULT_POLICY_AUTO` | bool | (unset) | **not operator-settable — an internal marker, not a knob.** `scripts/up.sh` writes `1` into `deploy/compose/.env` next to `WARDYN_DEFAULT_POLICY` whenever it auto-picked that value itself; `0` once an explicit `WARDYN_DEFAULT_POLICY` override is seen. Lets `up` safely re-run the auto-pick on a later invocation without ever clobbering a value the operator set by hand — including a model path `host_llm_key_present` cannot see in THIS process's env at all (a subscription connected in a prior `up`, or a key added through the UI): `up` asks the running daemon's own `/setup/status` `llm_ready` for that, not just its own env |
| `WARDYN_INSTALL_IMAGE` | string | `ghcr.io/cjohnstoniv/wardynd:latest` | `deploy/desktop/install.sh` only: the `wardynd` image it runs `-gen-age-key` against to mint a new device's `age.key`. Set it to a pinned digest or a corporate mirror ref; the desktop tier's compose stack itself still resolves its own image from `WARDYN_WARDYND_IMAGE` in the managed `wardyn.env` (`wardyn-desktop.sh` defaults that one too — see [docs/DESKTOP.md](DESKTOP.md)) |

## Four variables that need more than a table cell

- **`WARDYN_HOST_PROXY_B64`** — a containerized wardynd is blind to every host
  detection tier (the OS/PAC tier dispatches on the *process's* `GOOS`, `git` is
  absent from the distroless image, `HOME` is unset so no shell profile or tool
  config is reachable), so without this seed the step would assert a false negative.
  It is deliberately **not** named `HTTP_PROXY`: Go's `net/http` honors the standard
  names process-wide, which would silently reroute wardynd's own OIDC discovery,
  audit webhooks, GitHub App minting and AWS credential chain through the corporate
  proxy. `WARDYN_DAEMON_PROXY_URL` above is the supported knob for actually routing
  those calls; this variable only feeds the Getting-started diagnostic step and
  routes nothing itself. Values are credential-masked before capture; base64 because compose
  interpolates `$` inside `.env` values.
- **`WARDYN_NS`** — compose sets wardynd's `WARDYN_INTERNAL_NETWORK` from the same
  value; they **must** stay equal or each run's proxy sidecar joins the wrong bridge.
- **`WARDYN_UI_STAGE`** — when the in-image build fails, `scripts/up.sh` builds the UI
  on the host and retries with `ui-prebuilt` automatically. Setting it explicitly
  forces one stage *and disables that retry*.
- **`WARDYN_CI_PROJECT`** — the per-PID default guarantees concurrent invocations
  never collide, and that one job's `down --volumes` cannot tear down another's.

## Test / internal-only (not operator configuration)

Read only by test scaffolding, the fake-docker harness, or the ground-truth
negative-control tooling; deliberately excluded from the registry above and
allowlisted in the ratchet test: `WARDYN_TEST_BOOL`, `WARDYN_TEST_DUR`,
`WARDYN_TEST_STR`, `WARDYN_TEST_PG`, `WARDYN_TEST_VAULT` and
`WARDYN_TEST_VAULT_TOKEN_FILE` (the live Vault/OpenBao case,
`internal/secretstore/vaultkv/live_test.go`: the server's address and a file
holding a token that can mount and write policies),
`WARDYN_TEST_VAULT_K8S_JWT_FILE` (the same file's Kubernetes-auth case: a
projected service-account token with audience `vault`), `WARDYN_TEST_AZURE_KV`
(the live Key Vault case, `internal/secretstore/azurekv/live_test.go`: the
vault's URL; the identity comes from the Key Vault settings above), `WARDYN_TEST_DOCKER`,
`WARDYN_TEST_CACHE_REPO`, `WARDYN_TEST_TOOLS_DIR`, `WARDYN_ENVBUILD_TEST_FLOAT`,
`WARDYN_ENVBUILD_TEST_INT`, `WARDYN_FAKE_MARKER`, `WARDYN_NEGCTL`,
`WARDYN_FLOOR_PROBE` (the skip-floor probe sentinel `internal/testfloor.Mark`
logs, which `scripts/test-report.sh` greps from `go test -json` output — never
read via `os.Getenv`), `WARDYN_E2E_BASE_URL`, `WARDYN_E2E_CLAUDE_CREDS`, `WARDYN_E2E_REAL_MODEL`,
`WARDYN_E2E_TASKS_DIR`, `WARDYN_E2E_WORK_ROOT`, `WARDYN_E2E_EXPECT_INJECT`,
`WARDYN_TEST_K8S`, `WARDYN_TEST_K8S_AGENT_IMAGE` (the k8s conformance suite's
gate + built-agent-image override, `test/conformance/conformance_k8s_test.go`
— see `make test-conformance-k8s`), `WARDYN_TEST_AGENT_IMAGE` (the agent image
whose BOOT egress `TestBootEgress_NoFirstUseApproval`
(`test/conformance/boot_egress_docker_test.go`) measures; default
`wardyn/agent-claude-code:local` — point it at another agent image to measure
that one instead, and the claude-specific screen assertions stand down while the
host measurement still applies), `WARDYN_E2E_UI_ADDR` (the UI-sandbox
gateway's second listener on the Playwright e2e backend, default `:8089`;
`scripts/e2e-backend.sh` — it must differ from `WARDYN_E2E_ADDR`, which the
daemon itself enforces). `WARDYN_E2E_ADDR` is the backend's console listener,
default `:8088`. `scripts/run-ui-e2e.sh` picks a free port for each of the two
that is unset, so two runs on one host never share one.

The rest of the Playwright e2e backend's knobs (`scripts/e2e-backend.sh`,
`scripts/run-ui-e2e.sh`, `scripts/screenshots.sh`, `test/e2e/e2e.sh`) are
shell-only too — no Go file reads them, so neither ratchet direction above
sees them; a dedicated shell-side scan
(`TestEnvDoc_E2EShellVarsAreDocumented`) covers this table instead (F063 —
until it landed, ten of these thirteen were undocumented and unenforced in
*both* directions):

| Var | Type | Default | Meaning |
|---|---|---|---|
| `WARDYN_E2E_DSN` | string (DSN) | `postgres://wardyn:wardyn@localhost:55432/wardyn_e2e?sslmode=disable` | The Postgres DSN `wardynd` itself connects with. `run-ui-e2e.sh`/`screenshots.sh` compose this FOR you from `WARDYN_E2E_PG_HOSTPORT` + the DB name — set it directly only when calling `e2e-backend.sh` outside those wrappers |
| `WARDYN_E2E_PG_HOSTPORT` | string (`host:port`) | `localhost:55432` | Where `run-ui-e2e.sh`/`screenshots.sh` point `WARDYN_E2E_DSN` at. **The one var to set on a shared box** where `:55432` is held by another job's Postgres — pair it with a `WARDYN_E2E_PG_CONTAINER` that actually publishes that port, or `e2e-backend.sh` refuses the mismatch loudly (F062) |
| `WARDYN_E2E_PG_CONTAINER` | string | `wardyn-test-pg` | The container `e2e-backend.sh` runs `pg_isready`/`psql`/seed SQL against via `docker exec` — independent of the DSN's host:port, which is why the two must agree |
| `WARDYN_E2E_PG_DBNAME` | string | `wardyn_e2e` (`run-ui-e2e.sh`: `wardyn_e2e_<pid>`) | The e2e database name. Unset, `run-ui-e2e.sh` names one after its own PID and drops it on exit, so concurrent runs never share a database; a name you set is kept. `screenshots.sh` overrides it to `wardyn_shots` so its own run never collides with a concurrent `run-ui-e2e.sh` |
| `WARDYN_E2E_TOKEN` | string | `wardyn-e2e-token` | The fixed admin bearer token the seeded backend accepts, so specs never need a real sign-in flow |
| `WARDYN_E2E_AGE_KEY` | string | (unset = mint a fresh one) | Pins the backend's secret-store age identity instead of minting one per `up` via `wardynd -gen-age-key`. Leave unset — a committed value would be a publicly-known key, and `wardynd` fail-closed refuses those |
| `WARDYN_E2E_SKIP_BUILD` | bool | (unset = build) | `1` reuses the already-built `.e2e-bin/wardynd` instead of rebuilding it. `run-ui-e2e.sh`/`screenshots.sh` set this themselves after their own one-time build, so later `e2e-backend.sh up` calls in the same run don't rebuild per spec |
| `WARDYN_E2E_NO_UI_BUILD` | bool | (unset = build) | `1` reuses an existing `ui/dist` and **refuses** (exit 1) if anything under `ui/` is newer. For a backend-only iteration; a UI change needs the knob unset or `cd ui && pnpm build` |
| `WARDYN_E2E_KEEP` | bool | (unset = tear down) | `test/e2e/e2e.sh` only: `1` leaves the compose stack up on exit (success or failure) instead of tearing it down, for post-mortem inspection |
| `WARDYN_E2E_NO_BUILD` | bool | (unset = build) | `test/e2e/e2e.sh` only: `1` reuses existing images instead of building fresh ones |
| `WARDYN_E2E_ANTHROPIC_KEY` | string (credential) | (unset = skip) | `test/e2e/e2e.sh` only: when set, the real-LLM path records against a live Anthropic key at boot instead of skipping that leg. Credential-shaped — never commit a value, and it never appears in captured output |
| `WARDYN_E2E_LIVE_BASE_URL` | string (URL) | (unset = hermetic) | `run-ui-e2e.sh` LIVE mode: run specs from `ui/e2e/live/` against an ALREADY-RUNNING external Wardyn at this URL — today the kind SSO cluster (`scripts/kind-sso-walk.sh`, which exports it) — instead of booting and re-seeding the hermetic `-runner none` backend. It also becomes `WARDYN_E2E_BASE_URL`, which is the `live` Playwright project's base URL. Unset (every other caller) leaves the default path byte-identical: the hermetic backend is built, brought up per spec and torn down as before. Every live spec ALSO self-skips without `WARDYN_TEST_K8S=1` |
| `WARDYN_E2E_ALLOW_ALL_SKIPPED` | string (space-separated spec basenames) | (unset = none allowlisted) | `run-ui-e2e.sh` only (F061): names spec files allowed to report zero executed tests (every test in the file skipped) without failing the gate. Empty by default — a spec that skips its whole file is a red flag until named here on purpose |
| `WARDYN_SCREENSHOTS` | bool | (unset = skip) | Set by `screenshots.sh` itself (never by hand): gates `ui/e2e/screenshots/docs.spec.ts` so a bare `pnpm e2e` never regenerates the doc PNGs — only `make screenshots` does |
| `WARDYN_LOG_TAG` | string | `==>` | Console prefix for `scripts/lib/common.sh`'s `log()`. Set by the sourcing script itself (`e2e-backend.sh` → `[e2e]`, `run-ui-e2e.sh` → `[e2e-ui]`, `screenshots.sh` → `[screenshots]`), never by hand |

The kind quickstart (`deploy/kind/quickstart.sh`) and the kind SSO walk
(`scripts/kind-sso-walk.sh`, `deploy/kind/sso/overlay.sh` — see OPERATIONS.md
"Testing AWS SSO without an AWS tenant") share these shell-only knobs:

| Var | Type | Default | Meaning |
|---|---|---|---|
| `WARDYN_QUICKSTART_CLUSTER` | string | `wardyn-quickstart` | The kind cluster name `quickstart.sh` creates and the SSO walk/overlay target — set it to run a second quickstart cluster beside the default one |
| `WARDYN_QUICKSTART_HTTP_PORT` | int | `8080` on a plain `quickstart.sh`; the SSO walk and overlay default it to `8280` so the SSO cluster coexists with a compose stack on `:8080` | Host port the console's HTTP NodePort publishes on. Pass the SAME value to `make kind-quickstart` and to `make kind-sso`/the walk — a mismatch is refused by `overlay.sh`'s `/healthz` guard before anything is installed. With the SSO overlay use `8280`: Dex's `redirectURIs` and `WARDYN_OIDC_REDIRECT_URL` in `deploy/kind/sso/` are pinned to it, so another port passes the guard and still breaks the OIDC callback |
| `WARDYN_QUICKSTART_SSH_PORT` | int | `2222` on a plain `quickstart.sh`; the SSO recipe uses `2322` | Host port the SSH NodePort publishes on |
| `WARDYN_QUICKSTART_IMAGE_TAG` | string | `quickstart` (`kind-ado` for the Azure DevOps profile) | Tag `quickstart.sh`, `overlay.sh` and the SSO walk build and load `wardyn/wardynd` and `wardyn/wardyn-proxy` under. Give each kind cluster its own so building one never retags the images another cluster's overlay reloads; the Azure DevOps profile's overlay always builds under its tag and pins the release to it |
| `WARDYN_KIND_SSO_SERVICE_CIDR` | string (CIDR) | (unset = read off the apiserver's `--service-cluster-ip-range`) | Overrides the Service CIDR the walk lifts past the proxy sidecar's SSRF guard for the fake AWS endpoint Service — set it when the apiserver read fails (a set value also wins over a successful read) — never a pod IP or the pod CIDR, both of which `liftInternalHost` refuses |
| `WARDYN_KIND_SSO_REBUILD` | bool | (unset) | `1` rebuilds all five images the walk judges (`wardynd`, `wardyn-proxy`, `agent-aws-sso`, `agent-claude-code`, the SSO fake) from this tree before loading them into the cluster, instead of reusing the quickstart's already-built images; retags the two `:local` agent images on that Docker daemon, which a compose stack sharing it adopts for new runs |
| `WARDYN_KIND_SSO_DEX_PORT` | int | `5557` | Host port `overlay.sh` port-forwards Dex onto and renders into `dex.yaml`'s issuer and the chart's `WARDYN_OIDC_ISSUER` (with `WARDYN_QUICKSTART_HTTP_PORT` rendered into the callback), for the browser leg of the split-horizon OIDC flow (the cluster reaches Dex by its Service; your browser reaches it through this port) |
| `WARDYN_KIND_SSO_EVIDENCE` | path | `local/v074/evidence/kind-sso` | Directory the walk writes its `PUT /site-config` response and `walk.log` evidence to |
| `WARDYN_KIND_SSO_SEEN_PORT` | int | `8390` | Host port the walk port-forwards the fake AWS endpoint's read-only `/_seen` page onto (what the fake was asked to mint and which Bedrock models the stub answered); the walk reads it after the spec and copies it into the evidence directory |
| `WARDYN_LIVE_SEEN_URL` | URL | `http://127.0.0.1:8390/_seen` | Set by the walk for `ui/e2e/live/sso-member.spec.ts` (the port-forward above); the spec asserts the member's pinned account/role and the healed pin against it |
| `WARDYN_KIND_SSO_ADMIN_TOKEN` | string (credential) | (unset = mint a random one) | Admin bearer token the walk authenticates its own site-config PUT with |
| `WARDYN_KIND_SSO_NODE` | string | `<cluster>-control-plane` | The kind NODE the walk hands to `ui/e2e/live/sso-member-recovery.spec.ts`, whose cold-start case taints it to hold a run pod in `Pending`. Override it only on a multi-node kind cluster |
| `WARDYN_KIND_SSO_TOKEN_TTL` | duration | `12m` | The fake's `AWSSSOFAKE_TOKEN_TTL`, set via `kubectl set env` before the walk's hold cases (finding 4): how long a captured AWS SSO access token lives before the SDK's role-credential refresh re-resolves and can discover it dead. Short enough (relative to the stock 1-hour default) that case K's hold opens inside a runnable window instead of ~55 minutes in |
| `WARDYN_KIND_SSO_ROLE_CRED_TTL` | duration | `3m` | The fake's `AWSSSOFAKE_ROLE_CRED_TTL`, set alongside the token TTL above: how long each `GetRoleCredentials` answer lasts, stamped per call. Short enough that a prompt typed after the window opens forces a fresh fetch (the only way the proxy's injector is reached at all), long enough that an ordinary turn does not spend itself re-fetching |
| `WARDYN_KIND_SSO_PROXY_INJECT` | `on` / `off` | `on` | The kill-switch posture the kind SSO walk pins on the deployment (`helm upgrade --set env.WARDYN_AWS_SSO_PROXY_INJECT=…`), printed in the step line, read back off the Deployment and recorded in the walk's `MANIFEST.json`; a mismatch stops the walk. `off` is for a diagnostic run of the 0.7.5 resident-token posture (finding 4's rollback); the walk's certification runs with the daemon's own default, `on`. |
| `WARDYN_KIND_SSO_PROFILE` | `default` / `ado` | `default` | Which walk `scripts/kind-sso-walk.sh` runs and which overlay `deploy/kind/sso/overlay.sh` installs. `ado` is the Azure DevOps profile (`scripts/lib/kind-sso-walk-ado.sh`): the console signs in against a fake Entra tenant (`test/adofake/cmd`, reached as a forward proxy with a walk CA) instead of Dex, and the walk proves the member's login captures their Azure DevOps credential and their run redeems it. It pins `WARDYN_QUICKSTART_HTTP_PORT` to `8580`, and reads `WARDYN_KIND_SSO_SEEN_PORT` (default `8590` there) as the port-forward to the fake |
| `WARDYN_KIND_SSO_SKIP_REAUTH_HOLD` | bool | (unset = run it) | `1` drops `ui/e2e/live/sso-reauth-hold.spec.ts` from the walk's invocation — its case K spends about ten minutes of wall clock waiting for the injector's own re-resolve window by design, which `.github/workflows/nightly.yml`'s hosted job excludes for exactly that reason. Unset (every interactive or release walk) runs all three specs |

The walk also EXPORTS `WARDYN_LIVE_ADMIN_TOKEN`, `WARDYN_LIVE_FAKE_URL`,
`WARDYN_LIVE_PIN_ACCOUNT`, `WARDYN_LIVE_PIN_ROLE`, `WARDYN_LIVE_SSO_START_URL`,
`WARDYN_LIVE_SSO_REGION`, `WARDYN_LIVE_KUBE_CONTEXT`,
`WARDYN_LIVE_KUBE_NAMESPACE` and `WARDYN_LIVE_KUBE_NODE` as its own internal
handoff to `ui/e2e/live/sso-member.spec.ts` and
`ui/e2e/live/sso-member-recovery.spec.ts` (which read them via `process.env`) —
outputs of the walk, not operator inputs; nothing external should set them. The
last three are the cluster coordinates the recovery spec's cold-start case needs
to taint the node and read the run pod's phase back: passing them rather than
letting the spec guess is what makes that case RED on a renamed cluster instead
of vacuous. The Azure DevOps profile (`scripts/lib/kind-sso-walk-ado.sh`) exports `WARDYN_LIVE_ADO_MEMBER_EMAIL` (the walk's member login, default `member@wardyn.test`) and `WARDYN_LIVE_ADO_PROXY_URL` (its port-forward to the fake, the browser's only route to `login.microsoftonline.com`) the same way, to `ui/e2e/live/ado-connect.spec.ts`. `WARDYN_LIVE_ROLES_RENDER` (`sso` / `sso-only` / `mprime` / `compose-sso`) is the same kind of handoff to `ui/e2e/live/sso-roles.spec.ts`: the render that role leg runs on. `scripts/compose-sso-roles.sh` runs that spec on the two compose shapes (the desktop member-mode envelope and `--profile sso`) and self-skips unless `WARDYN_TEST_SSO_ROLES=1`; `WARDYN_ROLES_WARDYND_IMAGE` / `WARDYN_ROLES_PROXY_IMAGE` (default the `:quickstart` tags) and `WARDYN_ROLES_EVIDENCE` (default `local/evidence/compose-sso-roles`) are its only other inputs.

`WARDYN_LIVE_SANDBOX_UP_MS` / `WARDYN_LIVE_LOGIN_DONE_MS` (int ms; default
`300000`, unset) are the one pair of operator inputs in this group:
`ui/e2e/live/helpers.ts`'s `SANDBOX_UP`/`LOGIN_DONE` poll ceilings for a fresh
sign-in sandbox coming up and its capture landing. They exist for SLOWER CI
HARDWARE, not flakiness — a member's own sign-in sandbox is a pod (image pull,
schedule, proxy sidecar, a real device-code flow) created while the cluster is
concurrently scheduling the CNI, Postgres, the daemon, Dex and every earlier
sandbox, and the 300s default was tuned on a developer box (#285: the nightly
walk's first hosted-runner dispatch timed out here). `scripts/kind-sso-walk.sh`
raises both to `720000` (12m) itself when `GITHUB_ACTIONS` is set, unless the
caller already exported one; nothing else in this file changes.

### Live-local harness (opt-in, never in CI)

Read only by the live-local suites (`internal/testlive/` under `-tags live`,
and `ui/playwright.live-local.config.ts`), which reach a real Entra tenant,
Azure DevOps organisation and AWS account. [LIVE-TESTS.md](LIVE-TESTS.md) is
the setup and run guide. Every suite skips unless its gate is `1`. Secrets are
never env values: the `_FILE` variables hold a path to a file outside the
repository.

| Var | Type | Default | Meaning |
|---|---|---|---|
| `WARDYN_LIVE_ENTRA` | bool | (unset = skip) | Gate for LL1, the Entra roles suite (Playwright) |
| `WARDYN_LIVE_ADO` | bool | (unset = skip) | Gate for LL2, the Azure DevOps suite (Go) |
| `WARDYN_LIVE_ADO_WRITE` | bool | (unset = skip) | Gate for LL2b, bounded access: pushes and deletes one scratch branch (Go) |
| `WARDYN_LIVE_ADO_PAT_PROBE` | bool | (unset = skip) | Gate for LL2c, the personal access token mint probe (Go, needs a browser sign-in) |
| `WARDYN_LIVE_BEDROCK` | bool | (unset = skip) | Gate for LL3, the Bedrock suite (Go) |
| `WARDYN_LIVE_AWS_SSO` | bool | (unset = skip) | Gate for LL4, the AWS SSO through Entra suite (Playwright) |
| `WARDYN_LIVE_BASE_URL` | URL | (none) | The running Wardyn console the suites sign in to |
| `WARDYN_LIVE_IDENTITIES_FILE` | path | (none) | JSON file of test identities: each role's recorded storage state, and the member's API token |
| `WARDYN_LIVE_ADO_ORG` | string | (none) | Azure DevOps test organisation |
| `WARDYN_LIVE_ADO_PROJECT` | string | (none) | Project in that organisation |
| `WARDYN_LIVE_ADO_REPO` | string | (none) | Repository the member can read |
| `WARDYN_LIVE_ADO_SPACED_PROJECT` | string | `Payments Platform` | Project whose name carries a space, for LL2's second run |
| `WARDYN_LIVE_ADO_SPACED_REPO` | string | `Card Auth (v2).Service` | Repository in that project, for LL2's second run |
| `WARDYN_LIVE_ADO_PAT_PROBE_TENANT_ID` | string | (none) | Entra tenant of the LL2c probe app |
| `WARDYN_LIVE_ADO_PAT_PROBE_CLIENT_ID` | string | (none) | The LL2c probe's own throwaway public-client app registration, never Wardyn's app |
| `WARDYN_LIVE_ADO_PAT_PROBE_SCOPE` | string | `499b84ac-1321-427f-aa17-267ca6975798/vso.pats` | Scope LL2c signs in for; `…/user_impersonation` is the other one worth measuring |
| `WARDYN_LIVE_AWS_SSO_START_URL` | URL | (none) | IAM Identity Center start URL (LL4) |
| `WARDYN_LIVE_AWS_SSO_REGION` | string | (none) | IAM Identity Center region (LL3, LL4) |
| `WARDYN_LIVE_AWS_SSO_TOKEN_FILE` | path | (none) | The AWS CLI `sso login` cache file for the member-account profile (LL3). An expired sign-in is a skip |
| `WARDYN_LIVE_BEDROCK_ACCOUNT_ID` | string | (none) | The capped member account. LL3 refuses unless STS places its credentials in exactly this account |
| `WARDYN_LIVE_BEDROCK_ROLE_NAME` | string | (none) | Identity Center permission set whose role credentials LL3 uses |
| `WARDYN_LIVE_BEDROCK_REGION` | string | (none) | Bedrock and STS region |
| `WARDYN_LIVE_BEDROCK_MODEL` | string | Claude Haiku 4.5 (`us.` profile) | Refused unless the base model is Claude Haiku 4.5 or Amazon Nova Micro |
| `WARDYN_LIVE_BEDROCK_MAX_CALLS` | int | `5` | Model calls allowed per test process; `1`–`20`, anything else is refused |
