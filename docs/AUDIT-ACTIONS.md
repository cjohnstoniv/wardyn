# Audit action vocabulary

Every row in the append-only audit log (`internal/types/types.go`'s
`AuditEvent`) carries a dotted `action` string. There is no enum or schema
file for it — call sites are the source of truth — so this page is the
generated-by-hand vocabulary reference: every action literal live in the tree
today, grep-curated from `internal/api`, `internal/broker`, `internal/lifecycle`,
`internal/approval`, `internal/identity/embedded`, and `internal/groundtruth`,
with its `Data` field names read from that call site. **OPERATIONS.md and
SSH.md point here** rather than re-listing actions themselves.

To reproduce or extend this table: `grep -rn 'auditEvent(\|"[a-z_]\+\.[a-z_.]\+"' internal/api/*.go`
(excluding `_test.go`) finds every call site; each site's `mustJSON(map[string]any{...})`
or `Data:` literal is the field list. CI enforces doc↔emit parity in both
directions: `TestAuditActionsDocCitationsAreLive` checks every `path:line`
citation in this table still names what it claims
(`cmd/wardynd/audit_actions_doc_guard_test.go`), and
`TestAuditActionsDoc_EveryEmitHasRow` plus
`TestAuditActionsForwardGuardCoversEveryEmitShape` check the other direction —
every emitted action literal has either a row here or an entry in
`auditActionAllow` (`cmd/wardynd/audit_actions_forward_guard_test.go`'s own
escape hatch for a literal deliberately outside the registry — empty today,
so nothing is currently excluded this way); all three run in
`cmd/wardynd` and gate `make ci` via `test-race`. The residual gap is
narrower now than "no CI check" — a `Data:` field can still go undocumented or
drift unguarded (see `internal/api/audit.go`'s `parseAuditFilter` for the
filterable envelope fields, which are stable regardless of action): the
action SET is guarded, the per-action field LISTS are not.

**Stable vs internal**, as used below: *stable* means another Wardyn doc
already commits to the name and its fields as an integration point (a SIEM
rule, a filter, an operator runbook) — treat it as a compatibility surface.
*Internal* means today's implementation detail: the name and fields exist
because that's what the call site happened to write, not because anything
outside `wardynd` depends on the shape. Nothing here is versioned; an
internal action can rename across releases without notice.

**Envelope fields added by the hash chain.** Since migration `0047` every event
also carries `prev_hash` and `row_hash` (hex SHA-256, computed by Postgres —
see [OPERATIONS.md](OPERATIONS.md#the-hash-chain--what-a-rewritten-row-looks-like)).
They are action-independent, and they ride the **audit-sink stream**
(webhook/syslog/file) whenever the Postgres write that computes them succeeded,
which is what puts a head hash in your SIEM. An event spooled during a store
outage streams *without* them and is not re-streamed when the spool drains
(OPERATIONS.md, "The audit log can't quietly rot"). `GET /audit`
and `GET /audit/export` do not select them, so they are absent there, and
neither is filterable; the chain is checked through
`GET /api/v1/audit/chain/verify`, not by reading rows back. The sweep emits no
audit action of its own — writing an integrity finding into the log the finding
is *about* would record it in the one place already under suspicion.

## Runs

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `run.create` | Two families. **success**: a run row is created (`POST /runs`, or system-created for a follow-on workspace-step run). **failure**: the dispatch-failure family re-uses this action rather than adding one — sandbox create, a lost `sandbox_ref`, MITM CA provisioning, the subscription and Bedrock inject-grant authors, the `require_inspectable_llm` refusal, the declared-mechanism refusal (an `AgentProviders` row names how this agent reaches its model and the transport that resolved is not that one), the stored-AWS-identity refusal (the captured AWS SSO session this run would carry names an account/role the roster row no longer pins — the pin is bound at capture time, so a capture that predates one is never otherwise compared), and an `api_key` grant whose scope would not compile | success: `agent`, `repo`, `policy_id`, `confinement_class`, `confinement_source` (`requested`/`defaulted` — 0.7.8: an unspecified request's enforced class is now the strongest the runner advertises at or above the policy floor, live-probed, so this is the one field that still says whether the caller named `confinement_class` or a runtime resolved it for them), `jti`, `inline_policy`; conditional: `interactive_start`, `seed_auto_tools`, `task_mode`, `tool_approvals`, `clamp_warnings` (every way launch narrowed what the caller asked for — the run-bound record the run-detail "Effective policy" widget reads). failure: `error`, or `sandbox_ref` + `set_sandbox_ref_error`; the declared-mechanism refusal also carries `reason` (`model_credential` — the machine-readable class the console grades a run's ending by) and `mechanism` (the DECLARED lane, so a console door binds to the refused run's own lane) | `internal/api/runs.go:354` (create); `internal/api/runs_dispatch.go:536,578`, `internal/api/runs_dispatch_llm.go:377,517,568,623`, `internal/api/runs_dispatch_sso_inject.go:247`, `internal/api/runs_dispatch_llm_mechanism.go:368`, `internal/api/runs_create.go:472` (failure — and that last one stamps the **grant uuid** as Target, not the run id) | internal |
| `run.dispatch` | Sandbox dispatch attempted or completed | `note`, `sandbox_ref` | `internal/api/runs_dispatch.go:158` | internal |
| `run.build` | BYOI/devcontainer image build for a run, and the workspace-image resolve/build | `byoi_base`, `devcontainer_repo`, `error`, `image`; the workspace path also carries `workspace_id` | `internal/api/runs_create.go:706,726,740`, `internal/api/workspace_run_image.go:157` | internal |
| `run.complete` | Run reaches a terminal state | `error`, `exit_code`, `panic`, `state` | `internal/api/runs_lifecycle.go:67` | internal |
| `run.fail` | Run fails before the agent starts | `error`, `from` | `internal/api/runs_lifecycle.go:507` | internal |
| `run.kill` | Operator or owner kills a run — or WARDYN ITSELF supersedes one: starting a new harness-login run (AWS SSO or the Claude container login) ends that same person's older login sandboxes for that agent (one live sign-in per person, `supersedeCallerLoginRuns`), because an abandoned one completes its login unattended and its late capture overwrites the one the person just made. A supersede row is actor `system`/`wardynd` and carries `reason` = `superseded_by_new_login`, `superseded_for` (whose sandbox it was) and `superseded_by_run` (the sign-in that replaced it); it audits `success` like any clean kill. **`superseded_for` is the run's `created_by`, which is not always a PERSON:** `admin-token` is the literal name of the shared admin bearer, and a local-mode caller's is the operator name (or whatever `X-Wardyn-Principal` said), so two humans sharing one of those supersede each other and the row names the CREDENTIAL they shared, not which of them clicked. Nothing widens — both are already full operators who can kill any run through the kill route — but a reviewer counting sign-ins per person must read an SSO `sub` as a person and those two as a credential — the reason rides the row's data and is deliberately NOT in the per-step error map, which is what decides `outcome`. **A FAILED supersede reads differently from a failed kill:** the row goes out `outcome=failure` carrying the failing step (`identity_error`, `runner_error`, `broker_error`) BESIDE the supersede fields, and the supersede adds no `run.revoke` row for that failed teardown step and no 500 — the new sign-in proceeds rather than locking the person out over the old run's teardown, and the capture upload's `run_killed` refusal is what contains it. (The cascade's approval-cancel half can still write its own `run.revoke` / `approval_cancel_error` on a supersede, as on any terminal transition — see the `run.revoke` row below.) A `run.revoke` row carrying `identity_error`/`runner_error`/`broker_error` therefore never belongs to a supersede: it is a requested kill, or a stop/completion/sweep/reaper revoke | (run state transition), `broker_error`, `identity_error`, `reason`, `runner_error`, `superseded_by_run`, `superseded_for` | `internal/api/runs_lifecycle.go:744` | internal |
| `run.exec` | Exec into a dispatched sandbox. TWO failure sites, not one: the `Exec` call itself, and the `SetRunAgentExecID` write that follows it — a lost exec-id write leaves the boot reconciler unable to tell a healthy run from one whose dispatcher died, so it fails the run here rather than letting the trail say `success` | `argv`, `error` | `internal/api/runs_dispatch.go:692,728,737` | internal |
| `run.files` | A workspace file-browse operation on a run fails | `error` | `internal/api/run_files.go:368` | internal |
| `run.interactive` | `interactive_start` path taken | `note`, `sandbox_ref` | `internal/api/runs_dispatch.go:663` | internal |
| `run.resources` | A run-resources query/update fails | `error` | `internal/api/run_resources.go:188` | internal |
| `run.selftest` | Post-dispatch selftest gate (agent-image liveness check before a run is usable) | `confinement_class`, `detail`, `error`, `exit_code`, `fail_closed` | `internal/api/runs_dispatch.go:683` | internal |
| `run.record.start` | A Record Mode session starts or fails to start. Carries the O5 cross-user marker `workspace_owner` when the launching admin is not the workspace's owner — recording a MEMBER's workspace mounts their directory and injects their credentials, so it is exactly the act that marker makes queryable | `allow_all_egress`, `confined`, `confinement`, `confinement_class`, `detail`, `label`, `mode`, `record_run_id`, `workspace_owner` | `internal/api/record.go:352` | internal |
| `run.record.synthesize` | Record→Promote profile synthesis | `allowed_domains`, `anomalies`, `eligible_grants` | `internal/api/profile.go:168` | internal |
| `run.revoke` | Credential/identity revoked on stop (API path), or the lifecycle reaper's revoke attempt fails. `approval_cancel_error` is the terminal cascade's approval half failing (`cancelRunApprovals`, `internal/api/runs_lifecycle.go:266`) — the run is already terminal by then, so like every other field here it is reported rather than propagated, and the stale-PENDING sweeper is the backstop | `approval_cancel_error`, `broker_error`, `identity_error`, `runner_error` | `internal/api/runs_lifecycle.go:231`; reaper failure at `internal/lifecycle/lifecycle.go:393` | internal |
| `run.autostop` | Idle-timeout autostop fires | `idle_for_sec`, `reason`, `threshold_sec` | `internal/lifecycle/lifecycle.go:365` | internal |
| `run.reconcile` | The orphan-sandbox reconciler acts on a run at boot | (reconcile outcome) | `internal/api/reconcile.go:729` | internal |
| `sandbox.orphan_sweep` | The boot-time orphan reconciler (`reconcileOrphanedSandbox`) FAILS to tear down a terminal run's still-live sandbox — emitted on failed teardown only; a still-failing teardown leaves the ref set for the next boot to retry | `sandbox_ref`, `teardown_error` | `internal/api/reconcile.go:252` (`stopSandboxOrAudit`, `internal/api/reconcile.go:252`) | internal |
| `sandbox.sweep_requested` | An OPERATOR triggered a sandbox sweep via `POST /api/v1/admin/sandboxes/sweep` — emitted on SUCCESS, recording who asked and how many were swept. Distinct from `sandbox.sweep`, which the primitive emits on failed teardown only. The boot pass runs the same primitive without this row, since nobody asked for it | `swept` | `internal/api/runs_lifecycle.go:803` | operator |
| `sandbox.sweep` | `SweepTerminalSandboxes` (the callable sweep primitive over terminal runs with a live probed sandbox) FAILS to tear one down — emitted on failed teardown only, like its boot-time sibling | `sandbox_ref`, `teardown_error` | `internal/api/runs_lifecycle.go:368` (`stopSandboxOrAudit`, `internal/api/runs_lifecycle.go:368`) | internal |
| `run.artifact.redirect` | Package-registry (artifact) redirect configured for a run | `detail`, `ecosystem`, `error`, `header`, `host`, `integration_id`, `port`, `secret_name` | `internal/api/artifact_redirect.go:307` | internal |
| `run.policy.effective` | The effective (post-merge) policy snapshot is recorded at dispatch | (full policy snapshot); plus `disk_mib_filled` when the scratch size was filled in from the org's `storage.ephemeral.default_disk_mib` rather than authored on the policy — the provenance a reader needs to know a host that cannot keep that size runs the sandbox uncapped instead of refusing it at create | `internal/api/runs_dispatch.go:505` | internal |
| `run.ceiling.reassert` | A governance profile applies to the run's creator, so dispatch re-asserts the profile's denied hosts AFTER the phases that add corporate hosts and credential lanes. Emitted on EVERY walled run from EVERY dispatch lane — an ordinary create, a record/verify session, a workspace source scan, a site-config probe and the managed-harness login all hand dispatch a resolved ceiling, so the absence of this row means "no profile applies", never "this door skipped the ceiling" — including one with nothing to drop — "which ceiling did this run actually run under" is the question `run.policy.effective` cannot answer, and the drops themselves are invisible there because injections and broker grants ride `ProxyConfig`, not the spec. `actor_type` `system` (`wardynd`) | `denied_added`, `dropped_broker_lanes` (the git/PAT broker keys withheld, plus `bedrock` when the profile denies a host this run's Bedrock lane reaches — the resident SigV4 keys, the captured-SSO env, the host `~/.aws` bind-mount and the per-run MITM entry all go with it), `dropped_injection_hosts`, `note`, `profile`; and when the profile sets `max_ephemeral_disk_mib` (0.7.2), that limit plus the `ephemeral_disk_mib` the run ended up with — the size half of the same ceiling, absent for a profile that sets no size | `internal/api/runs_dispatch_ceiling.go:521` (`reassertCeilingDenies`) | internal |
| `run.upstream_proxy.resolve` | Upstream (corporate) proxy resolution for a run | `reason` | `internal/api/runs_dispatch_mounts.go:151` (`resolveRunUpstreamProxy`) | internal |
| `run.llm.bedrock` | Bedrock LLM credential/config wired into a run | `detail`, `hosts`, `mode`, `model`, `region` | `internal/api/runs_dispatch_llm.go:351` | internal |
| `run.llm.subscription_inject` | Subscription OAuth proxy-side inject wired into a run (see `subscription-proxy-inject` — the operator's live OAuth is injected proxy-side, never resident in the sandbox) | `detail`, `host`, `source`, `tls_mitm` | `internal/api/runs_dispatch_llm.go:525` | internal |
| `run.llm_inspection.secrets_resolve` | LLM egress content-inspection secret names resolved for a run | `missing`, `names`, `reason`, `resolved`; conditional: `reserved_skipped` (names refused by the sink guard) | `internal/api/runs_dispatch_secrets.go:52` | internal |
| `llm.scan.*` | An outbound LLM content-inspection pass completes for a run's egress — the literal suffix is `sc.Action` (`llm.scan.alert`, `llm.scan.block`, `llm.scan.skipped`, `llm.scan.blind`, or `llm.scan.error` when the scanner failed or the body did not parse, per `egress.ScanSummary.Action`); security-relevant, and deliberately CONTENT-FREE — `findings` carries detector names, field paths, offsets, counts and masked samples only, never the matched bytes. `finding_count` is the pre-cap total the proxy COUNTS as it produces findings and sends as `findings_total` — it is NOT `len(findings)`. `findings_reported` is `len(sc.Findings)`, the count the proxy itself reported to the daemon after the proxy's own cap; `finding_count` exceeds `findings_reported` ONLY when `findings_capped` is true — the converse does NOT hold, since block mode's severity keep-backs can leave the two equal on a capped scan, so `findings_capped` (not the comparison) is the truncation signal. The embedded `findings` array is separately truncated to the first 100 records (`maxAuditFindings`) by this handler, so on a capped scan `findings` is SHORTER than `findings_reported`, and neither `finding_count` nor `findings_reported` equals `len(findings)`. `findings_past_cap` is an upper bound on what was pushed out past the cap, because block mode's severity keep-backs are counted past the cap and still reported alongside it. `llm.scan.error` records `outcome=failure`; every other suffix follows the usual success/denied rule | `channel`, `coverage`, `finding_count`, `findings`, `findings_capped`, `findings_past_cap`, `findings_reported`, `host`, `mode`, `scanned`, `skipped`, `skip_reason` | `internal/api/internal.go:139` (`recordLLMScanAudit`) | internal |
| `run.env_secret.resolve` | One `env_secret` grant resolved store→sandbox env at dispatch (one event per grant). `outcome=failure` means the grant was SKIPPED — `reason` says why (unreadable scope, reserved secret name, no secret store, unresolvable secret, or a refusal to overwrite a platform-authored variable that is already set to a non-empty value) — and the variable is left ABSENT, never blank-set. Carries the variable name and the SECRET NAME only; the value is never logged, and is mask-registered for the run | `name`, `reason`, `secret_name` | `internal/api/runs_dispatch_secrets.go:196` | internal |
| `run.git_pat.egress` | A `git_pat` grant adds egress domains at launch | `added_domains` | `internal/api/runs_create.go:622` | internal |
| `run.git_pat.brokered_forge` | A `git_pat` grant is withheld at dispatch because the run is brokered for that forge — the git-broker route is its only route to it by name, so withholding the PAT is load-bearing, not belt-and-braces | `dropped_hosts`, `note` | `internal/api/runs_dispatch.go:259` (`auditBrokeredGrantDrop`, `internal/api/runs_dispatch.go:259`) | internal |
| `run.site_config.egress` | Site-config-derived egress domains added at launch | `added_domains` | `internal/api/runs_create.go:613` | internal |
| `run.ssh.egress` | SSH gateway egress domains added at launch | `added_domains` | `internal/api/runs_create.go:618` | internal |
| `run.ssh.brokered_forge` | An `ssh_key` grant is withheld at dispatch for the same brokered-forge reason as `run.git_pat.brokered_forge` | `dropped_hosts`, `note` | `internal/api/runs_dispatch.go:254` (`auditBrokeredGrantDrop`, `internal/api/runs_dispatch.go:254`) | internal |
| `run.ssh.unsupported_agent` | SSH-enabled run launched against an agent image without SSH support; the SSH host set is dropped | `dropped_hosts` | `internal/api/runs_create.go:531` | internal |
| `run.provider.lane_dropped` | A git credential lane the deployment's workspace-provider policy does not PERMIT, so the run was wired without it. Providers mint nothing — they veto — and this is the veto: the `applySSHLaneWarnings` shape, drop the wiring and say so on the 201. The lane is decided by the row that ADMITTED the repository (two rows of one kind on one host are a supported configuration, so "whichever row claims the host" is not an answer); only a grant whose scope names no repository at all falls back to the union of the rows claiming its host, permitting any lane any of them permits. The eligibility GRANT ROW stays where the policy authored one (it records what the policy asked for, not an issued credential); what is dropped is the sandbox wiring, and for the `pat` lane the ADO egress bundle that arm would have added with it — nothing else adds those domains, so a veto that left them would hand the run reachability for a forge it can no longer authenticate to. **`Target` is the provider KIND, never the row id**: `auditScope` lets a run's creator read their own run's rows, and a row id is on the far side of the disclosure rule (the operator-facing 201 warning names it; this durable row a member can read does not). Emitted from all five grant sites: the three `persistRunGrants` arms (`github_token`/`git_pat`/`ssh_key`) and the two launcher-side helpers (`maybeGitHubReadGrant`, `maybeSSHKeyGrant`), which have no warnings channel — for those this row IS the record. `actor_type` `system` (`wardynd`) | `grant`, `kind`, `lane` | `internal/api/workspace_admission.go:506` (`laneVetoed`) | internal |
| `run.provider.ssh_host_level` | A repository was admitted over SSH at the HOST level by a row whose base URLs carry an ORG PATH — the documented SSH scoping ceiling, recorded the one time it actually costs something. An SSH clone URL carries no path a base URL can be compared against (`git@github.com:acme/x.git` is not `/acme/x`), so `https://github.com/acme` admits an SSH clone of ANY org on github.com with the deployment's `ssh-key-github-com` secret. This is the ONLY admission outcome that is WIDER than the policy reads, so it is never silent: run create also puts it on the 201 as a warning, and the remedy is the row's own `lanes` list (drop `ssh` and the addresses bind again, over the one transport that carries a path). The `run.provider.lane_dropped` SHAPE exactly — outcome `failure`, **`Target` is the provider KIND, never the row id**, for the same disclosure reason: a run's creator can read their own run's rows. Emitted only from run create, the one door with a warnings channel to pair it with; a source onboarded at another door is re-asked here at launch. Absent on a deployment with no provider rows, and on a row whose base URLs bound the whole host anyway — nothing was widened. `actor_type` `system` (`wardynd`) | `host`, `kind`, `lane` | `internal/api/workspace_admission.go:345` (`sshHostLevelWarnings`) | internal |
| `run.workspace.clone_egress` | Workspace-clone egress domains added at launch | `added_domains` | `internal/api/runs_create.go:587` | internal |
| `run.workspace.collision` | Launch refused because the workspace is already in use by another run | `other_runs` | `internal/api/runs.go:136` | internal |
| `run.workspace.creds` | Workspace-scoped integration credential resolved for a run | `integration_ref`, `type` | `internal/api/runs_create_fields.go:127` | internal |
| `run.workspace.egress` | Workspace-derived egress domains added at launch | `added_domains` | `internal/api/runs_create.go:570` | internal |
| `run.workspace.requirement.egress` | A workspace-needs-scanner egress requirement is satisfied at launch | `added_domains`, `workspace_id` | `internal/api/runs_create_requirements.go:212` | internal |
| `run.workspace.requirement.integration` | A workspace-needs-scanner integration requirement is satisfied at launch | `header`, `injected_hosts`, `integration_id` | `internal/api/integrations_run.go:91` | internal |
| `run.workspace.requirement.secret` | A workspace-needs-scanner secret requirement is satisfied at launch | `host`, `secret_name` | `internal/api/runs_create_requirements.go:272` | internal |

## Workspaces & sources

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `workspace.create` | `POST /workspaces` | `name`, `owned_by`, `sources` | `internal/api/workspaces.go:528` | internal |
| `workspace.update` | `PUT /workspaces/{id}` | `image_changed`, `name`, `rescan_required`, `sources` | `internal/api/workspaces.go:696` | internal |
| `workspace.delete` | `DELETE /workspaces/{id}` | — | `internal/api/workspaces.go:909` | internal |
| `workspace.scan` | A workspace directory/repo scan (needs-scanner) runs | `detail`, `reason`, `scan_run_ids`, `sources`, `workspace_id` | `internal/api/workspace_run.go:316`, `internal/api/source_scan.go:374,423,440` | internal |
| `workspace.record` | The "workspace record" onboarding-import run completes | `anomalies`, `domains`, `kernel_sensor_blind`, `minted_grants`, `mode`, `task` | `internal/api/workspace_run.go:495` | internal |
| `workspace.requirement.write` | An admin/member edits a workspace-needs requirement from the approval flow | (requirement diff) | `internal/api/approvals_writeback.go:272` | internal |
| `workspace.requirements.write` | An admin/member replaces the workspace's whole requirements contract (`PUT /workspaces/{id}/requirements`) — distinct from the singular `workspace.requirement.write` above (a single-requirement edit from the approval flow); this is the map-wide replace | `count` | `internal/api/workspace_requirements.go:137` | internal |
| `workspace.envcode.write` | The onboarding "env code" (devcontainer/setup snippet) is written for a workspace | `files`, `skipped`, `skipped_files`, `written_files` | `internal/api/workspace_envcode.go:70` | internal |
| `workspace.egress.approve` | Operator/member approves a pending workspace egress decision (`always`/`session` scope write-back), **or the boot heal replays one** — `source` tells them apart (`approval:<id>` vs `boot-heal`), and the heal also emits `failure` for a verdict the live path would now refuse | `domains`, `source` | `internal/api/approvals_writeback.go:184`, `internal/api/approvals_reconcile.go:263`, `internal/api/record.go:845` | internal |
| `workspace.egress.deny` | Operator/member denies a workspace egress decision, **or the boot heal replays one** (same `source` split as the approve row above) | `domains`, `source` | `internal/api/approvals_writeback.go:182`, `internal/api/approvals_reconcile.go:261`, `internal/api/workspaces.go:814` | internal |
| `workspace.llm_cred.set` | An operator binds (or clears) the model/harness credential for a workspace/container (`PUT /workspaces/{id}/llm-cred`) | `integration_ref` | `internal/api/workspaces.go:843` | internal |
| `workspace.reassign` | An admin returns a member-owned workspace to the operator, `owned_by=""` (`POST /workspaces/{id}/reassign`) — the offboarding path for a member who has left | `from_owner` (the departed member's principal; `""` when the row was already operator-owned) | `internal/api/workspace_owner.go:63` | internal |
| `workspace.provider.legacy_host` | A repository was admitted ONLY because `site_config.scm_hosts` still names its host and NO provider row claims it — the one-release grace that keeps an onboarded GitLab/Bitbucket repo launchable the day the first GitHub provider row is added, and the state an admin has until 0.8 to close. Emitted at EVERY admission door (`admitRepoSources`, `admitLauncherRepo`), not only run create: the two server-side launchers carry no warnings field, so a record/scan clone on such a host would otherwise pass in silence. Every door that ANSWERS A BODY also says it out loud as a warning on the success response — run create's 201 and the three onboarding doors, `POST`/`PUT /workspaces` and `POST /sources` (V2/F1: onboarding a GitLab workspace through the wizard used to succeed silently, so the admin who must close the state first heard about the host from whoever launched the first run). One row per HOST rather than per repo, because the fact is about the host; no run id, because most of these doors have none. Absent on a deployment with no provider rows — nothing is admitted "only because" of the legacy list when the legacy list is the whole policy. `actor_type` `system` (`wardynd`) | `host` | `internal/api/workspace_admission.go:291` (`auditLegacyHostAdmissions`) | internal |
| `source.write` | A workspace source (dir/repo) is added or updated | `kind`, `locator`, `ref`, `writable` | `internal/api/sources.go:297` | internal |
| `source.delete` | A workspace source is removed | `detached_from`, `forced` | `internal/api/sources.go:387` | internal |
| `source.scan` | A single source's onboarding scan runs | `ai_advisor`, `ai_changed`, `confidence`, `detail`, `leak_findings`, `reason`, `scan_run_id`, `secret_reqs` | `internal/api/workspace_run.go:282`, `internal/api/source_scan.go:62` | internal |

**`workspace_owner` — the cross-user marker on every workspace-scoped write.**
Any event in this section whose target is a MEMBER-OWNED workspace
(`workspaces.owned_by` non-empty, migration `0048`) carries one extra Data
field, `workspace_owner`, **whenever the actor is not that owner** — i.e.
whenever an admin acts on a member's workspace for support or offboarding. That
includes the approval write-backs (`workspace.egress.approve`,
`workspace.egress.deny`, `workspace.requirement.write`), which are the most
common cross-user path of all: deciding an approval is owner-OR-admin, so an
admin choosing `always` on a member's run durably rewrites that member's
workspace. One rule stamps them all, in `internal/api/workspace_owner.go` —
`auditWorkspaceData` for a handler that holds the request,
`auditWorkspaceDataFor` for a writer that holds the acting principal as a
string — and it is what makes cross-user admin access QUERYABLE rather than
merely present in the log: filter `?actor=<admin>` and keep the rows where
`workspace_owner` is set and differs.

The deliberate exception is two EMIT SITES, not two actions: the `wardynd`-actor
emits that settle a governed run — `reconcileWorkspaceRun`'s `workspace.scan` and
`reconcileRecordRun`'s `workspace.record`, both in `internal/api/workspace_run.go`
— have no acting human to compare the owner against, so they never stamp. The
human who launched that run is recorded on the run, not on these events. The
`workspace.scan` ACTION is not exempt: emitted from `scanAttachedSources`
(`internal/api/source_scan.go`) it carries the requesting human as actor and
stamps `workspace_owner` like every other row above.

The actor is never rewritten to match. An admin acting on a member's workspace
is recorded as the ADMIN — there is no impersonation anywhere on this path, and
`TestWorkspaceOwner_NoImpersonation` (`internal/api/workspace_reassign_test.go`)
pins it across the write surface. An operator-owned workspace (`owned_by=""`,
every pre-0.6 row) never stamps the field, so an admin-only deployment's audit
Data is unchanged.

## Sessions, SSH & UI relay

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `session.attach` | A human attaches to a run's tmux session (console or CLI) | `cols`, `error`, `lane`, `reason`, `rows`, `sandbox_ref`, `transport` | `internal/api/attach.go` | internal |
| `session.detach` | A session detaches | `read_only`, `reason`, `transport` | `internal/api/attach.go` | internal |
| `session.takeover` | A second viewer takes over a held session (the write-lane single-holder rule) | `held_since`, `previous_holder`, `previous_source`, `taken_over` | `internal/api/attach_holder.go:419` | internal |
| `session.recording` | A recording is attached to / detached from a session | — | `internal/api/attach.go:730` | internal |
| `recording.upload` | A sandbox uploads an asciinema-cast chunk for a run's recording session — audited on both outcomes, like every sibling recording lane, since a full store or an over-cap upload is exactly how a long session's provenance gets lost | `error` (failure only) | `internal/api/recording.go:119` | internal |
| `ssh.auth` | Every SSH-gateway connection attempt, success or failure — `docs/SSH.md` names this one **stable** and documents it as the residual-#19 correlate | `override`, `reason` | `internal/api/sshgateway.go:309` (failure), `internal/api/sshgateway.go:345` (success); documented `docs/SSH.md:383,435,440` | **stable** (documented) |
| `ssh.channel_rejected` | An SSH channel was REFUSED by the per-run concurrent-channel cap (`maxSSHSessionsPerRun`). Emitted for BOTH channel types, and `channel_type` distinguishes them — `session` (shell/exec/sftp) and `direct-tcpip` (`-L` forwards) draw on ONE shared counter, inverting the OpenSSH model where `MaxSessions` scopes to session channels only. Before 0.7 a refusal was invisible to the deployment: the client saw `ResourceShortage` and nothing was recorded — which matters now that the desktop envelope ships `WARDYN_SSH_LISTEN` on by default | `channel_type`, `reason`, `max` | `internal/api/sshgateway.go:446` | human |
| `ssh.exec` | A command executed over the SSH gateway (argv + exit code only — no content) | `argv`, `error`, `exit` | `internal/api/sshgateway_channels.go:662`; documented `docs/SSH.md:478` | **stable** (documented) |
| `ssh.sftp` | An sftp transfer over the SSH gateway (byte count only — no payload/filenames) | `bytes`, `error` | `internal/api/sshgateway_channels.go`; documented `docs/SSH.md:478` | **stable** (documented) |
| `ssh.forward` | An `ssh -L` port-forward session | `bytes`, `error`, `port` | `internal/api/sshgateway_channels.go`; documented `docs/SSH.md:479` | **stable** (documented) |
| `ssh_key.add` | A human registers an SSH public key (`POST /me/ssh-keys`) | `name` | `internal/api/sshkeys.go:160` | internal |
| `ssh_key.delete` | A human removes a registered SSH public key | — | `internal/api/sshkeys.go:193` | internal |
| `ui.auth` | A UI-sandbox relay session is authorized or denied | `app`, `host`, `port`, `reason` | `internal/api/uigateway.go:219`, `internal/api/uigateway_session.go:297` | internal |
| `ui.open` | A UI-sandbox relay session is opened | `app`, `port` | `internal/api/uigateway.go:625` | internal |
| `ui.close` | A UI-sandbox relay session closes | `app`, `duration_sec`, `port` | `internal/api/uigateway.go:631` | internal |
| `ui.start` | The relay starts (or fails to start) the sandbox-side app process | `app`, `launcher`, `port`, `reason` | `internal/api/uigateway.go:737` | internal |

## Secrets, credentials & identity

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `secret.write` | An admin or member writes a secret (0.7, migration `0050`: self-service, own namespace — see OPERATIONS.md's "Secret ownership"). `owner_known:false` marks the one write that lands in a namespace nobody is yet known to own: an admin's `?owner=` value that matched no principal on this deployment is taken VERBATIM, which is the documented pre-provisioning affordance for a member who has not signed in — and is byte-identical, from the outside, to a typo whose namespace will never be read. The status is 204 either way; the marker is what lets the log tell the two apart | `secret_owner` (the non-"" namespace written; absent for the operator's own "" namespace), `owner_known` (present, and `false`, only for an unresolved `?owner=`) | `internal/api/secrets.go:207` | internal |
| `secret.delete` | An admin or member deletes a secret, same ownership rule as `secret.write` | `secret_owner` (absent for ""), `owner_known` (as on `secret.write`) | `internal/api/secrets.go:265` | internal |
| `secret.list` | An admin enumerates a namespace through the admin-only `?owner=` surface (`GET /api/v1/secrets?owner=…`). ENUMERATION, NOT DISCLOSURE: this route returns secret NAMES only — no value is read, decrypted or returned (that is `secret.read`, at injection time, which carries `grant_id`/`jti`). Emitted ONLY for an explicit `?owner=`, because a member listing their own namespace is the console's ordinary poll on every page load and auditing it would bury the cross-user reads this row exists for. It closes the asymmetry where every WRITE through `?owner=` stamped `secret_owner` and the READ recorded nothing. `Target` is the namespace | `names` (how many were enumerated — a COUNT; the names themselves are never recorded), `secret_owner` (the namespace read) | `internal/api/secrets.go` (`handleListSecrets`) | internal |
| `secret.read` | A registered secret is resolved for injection into a run (or fails to resolve) | `grant_id`, `host`, `jti`, `owner` (0.7: the namespace consulted — the run creator's principal string; for an operator-created run that is the operator's own identity string (`admin-token`, `local:<op>`, an admin's sub), under which no row is ever stamped, so the lookup falls back to the operator row), `purpose`, `reason`, `source`, `credential_source` (0.7.6, on the captured-AWS-SSO injection arm only: which lane's session was resolved) | `internal/api/injection.go:118`, `internal/api/injection_awssso.go:187` (every captured-AWS-SSO refusal), `internal/api/injection_awssso.go:270` (the resolve that hands back a live session) | internal |
| `secret.rekey` | `wardynd -rotate-age-key` re-encrypted the whole store to a new age identity. SUCCESS only, and written by the one-shot maintenance process rather than the serving daemon (`actor_type` `system`, `actor` `wardyn/rotate-age-key`); an aborted rotation commits nothing and emits nothing. `target` is the store name (`pg`). Deliberately carries NO secret names — the sibling rows above each name one secret because each event IS one secret, whereas a single event listing the whole inventory would hand every configured sink the full set of names at once. `public_recipient` is the new age recipient (public by construction: it is what ciphertext is encrypted *to*), which is what lets an operator confirm which key the store now answers to | `public_recipient`, `secrets` | `cmd/wardynd/rekey.go:177` | internal |
| `credential.mint` | A credential is minted against an `APPROVED` approval (invariant 2 — mint IS the approval, same transaction). `lease` marks a mint the human did NOT individually decide: a `git_pat` whose approval carried `decision_scope=run` re-mints for the rest of the run under that one decision (the B2 per-run lease), and `decision_scope` beside it is the RAW stored value the lease turned on. Absent on every ordinary mint — a lease widens what one approval authorized, so it is stamped rather than left inferable | `approval_id`, `decision_scope`, `grant_id`, `host`, `jti`, `kind`, `lease`, `reason`, `scope` | `internal/broker/broker.go:583` (`mintEvent`), `internal/api/internal.go:565` | internal |
| `credential.reauth.requested` | A run's OWN model credential lapsed MID-RUN and could not be renewed, so the proxy is holding the sandbox's next credential exchange and the credential's owner has been asked to sign in again (Finding 4, 0.7.6 — the `credential_reauth` approval kind, migration `0064`). This is the row that makes a held run answerable: WHOSE credential (`owner` + `credential_source`), WHY it is dead (`reason`, a closed vocabulary: `spent` = the refresh token is gone at AWS, `unavailable` = renewal failed transiently with nothing left to serve, `not_found` = there is no stored session for that namespace at all) and WHICH request the console is showing (`approval_id`). Actor is `system` (`wardynd`), not the agent: the sandbox asked for a credential, it did not ask for a human. `reason` is deliberately NOT part of the approval's `requested_scope` — the scope is the dedup key, so a `spent`→`unavailable` flip between two resolves would otherwise raise a second row for one lapse. At most ONE PENDING row per run and scope (the 0022 partial unique index now covers this kind), and at most `maxReauthHolds` such workflows per run | `approval_id`, `credential_source`, `detail`, `owner`, `provider`, `reason` | `internal/api/injection_awssso.go` (`holdOrRefuseCredentialReauth`) | internal |
| `credential.reauth.resolved` | The owner signed in again and the held request is answered: the `credential_reauth` row moves to `APPROVED` and the proxy's next poll wakes the parked call. Written in the SAME transaction as that state change (`store.PG.ResolveReauthApproval`) — if this row cannot be written the row stays `PENDING`, because a credential-bearing retry with no auditable predecessor is the thing invariant I7 forbids. NOT `approval.decide`: nobody decided anything, and the kind is refused a decision outright (409). `resolved_by` is the principal whose sign-in landed and `capture_run_id` the login run it landed from — the two facts that make "resolved by whom" answerable without a join. Emitted by BOTH the eager path (the capture handler, after its own `harness.credential.captured` row) and the idempotent reconcile-on-read that repairs a crash between those two writes; exactly one of them ever wins, because the state change is a `WHERE state='PENDING'` CAS | `approval_id`, `capture_run_id`, `owner`, `provider`, `resolved_by` | `internal/api/injection_awssso.go` (`resolveReauth`) | internal |
| `credential.revoke` | A minted credential is revoked (kill-switch, run stop). One row per credential the run ACTUALLY minted — the cascade enumerates the run's successful `credential.mint` rows UNION the approvals whose `minted_jti` was burnt (`mintedCredentialsSQL`), so an auto-mintable grant (which creates no approval row at all) and a leased `git_pat`'s 2nd..Nth mint are covered too — `github_token`, `api_key`, `git_pat` and `ssh_key` alike. `outcome=success` records the audit join, not an invalidation. `note` is the honest per-KIND revocation story (`revokeNote`, `internal/broker/revoke.go`), not one GitHub-shaped sentence: a `github_token` relies on TTL expiry (<=1h) plus the identity denylist because `RevokeRun` does not call GitHub's `DELETE /installation/token` — that endpoint must be presented the token itself and `RevokeRun` holds only the `jti`, no copy of the value addressed by it. Scoped to `RevokeRun` deliberately: Wardyn DOES make that call in two places — `VerifyRefRuleset`'s probe hand-back and `discardMinted`, the mint's own discard door (B11a-F1) — but both surrender a token that was NEVER returned to the run, so neither can reach the credential this row is about, and a blanket claim would be false; a `git_pat`/`ssh_key` is an operator-managed secret Wardyn can neither revoke, expire nor down-scope (the operator must rotate it at the forge, and the identity denylist only stops further mints); an `api_key` stays proxy-side and is never resident in the sandbox. NOTHING here invalidates a credential already handed out | `jti`, `kind` (absent when the grant row behind the mint is gone), `note` | `internal/broker/revoke.go:92` (`RevokeRun`, `revokeNote`) | internal |
| `identity.mint` | A per-run SPIFFE identity (JWT-SVID) is minted | — | `internal/identity/embedded/embedded.go:187` | internal |
| `identity.renew` | A run's identity JWT is renewed | `expires_at`, `prev_jti`, `reason`, `run_state` | `internal/api/internal.go:830` | internal |
| `identity.revoke` | A run's identity is revoked at teardown | — | `internal/identity/embedded/embedded.go:286` | internal |
| `run.identity.expired` | A NON-TERMINAL run presented an EXPIRED identity on the internal lane (`/api/v1/internal/*`), so its token renews are not landing and every control-plane call it makes is now refused. Emitted ONCE per run (an in-memory guard keyed on the run id; a `wardynd` restart may emit a second row for the same run — two rows for one incident, never a row per request). The run is deliberately LEFT RUNNING: mid-flight work is the owner's to abandon, and the remedy is to kill it and start a new one. A TERMINAL run emits nothing — its token is supposed to be dead, and an unreaped sidecar is not news. Only an expired token reaches this row at all: the identity provider reports expiry as a typed error carrying the run id (`identity.ExpiredTokenError`), while a revoked, forged or wrong-audience token names no run and stays as coarse as the `auth.failed` row beside it. Paired with the proxy sidecar's renew backoff + give-up (`internal/egress/proxy/renew.go`), which is what stopped this case from flooding the trail with one `auth.failed` row a minute | `path`, `run_state` | `internal/api/http.go:859` (`auditRunIdentityExpired`) | internal |
| `approval.decide` | A human decides a pending approval (approve/deny, any scope) | `approval_id`, `decision`, `reason` | `internal/approval/approval.go:167` | internal |
| `approval.expire` | The approval sweeper expires an undecided approval past its TTL | — | `internal/approval/approval.go:264` | internal |
| `approval.cancelled` | A run reached a terminal state (killed, completed, failed, stopped) while one or more of its approvals were still `PENDING`, so the terminal cascade moved them to `CANCELLED` — nobody decided them and nothing was approved or denied. ONE row per run transition, not per approval: they all belong to a single run ending, and the row is keyed to the run so it lands in that run's evidence rail. `reason` names the transition (`run_killed`, `run_completed`, `run_failed`, `run_stopped`, or `run_terminated` when the state could not be read back), `count` how many approvals moved. A run with nothing pending emits NO row at all, which is what makes a re-kill idempotent in the append-only log as well as in the store. **`outcome=failure` with an `error` field is a PARTIAL cascade**: a store error part-way through leaves the approvals it already moved durably `CANCELLED`, so the row goes out with the count of what actually moved rather than being skipped — an operator's queue emptying with nothing in the trail is the failure this row exists to prevent. Actor is `system` (`wardyn/approval-cancel`) — see `cancelRunApprovals`, the terminal-cascade caller in `internal/api/runs_lifecycle.go:252` | `count`, `error`, `reason`, `run_id` | `internal/approval/approval.go:337` (`CancelForRun`) | internal |
| `harness.credential.captured` | A harness login flow (Claude Code subscription, etc.) captures a credential. The AWS SSO emit also carries `owner` + `credential_source`: "" / `shared` is the ONE credential every run inherits, a subject / `per_user` is that person's own — the pair is what makes a per-user estate's capture rows tellable apart, and "whose session" the first answerable question after an incident. `captured: true` is the RESPONSE body of the paste door, not a datum — no emit writes it | `credential_source`, `owner`, `provider`, `source` | `internal/api/harnesscred.go:795`, `internal/api/ssotoken.go:246` | internal |
| `harness.credential.refused` | An AWS SSO credential upload was REFUSED, so nothing was stored. Every refusal on this route used to be a bare 400/409 with no row at all, which is what let a wrong-account capture land as "no diff, no audit row naming the change, and no warning" — the reporting operator's own words. `reason` is from a FIXED vocabulary, never sandbox-chosen text, so an incident review can GROUP refusals: `blob_shape` (unparseable, or missing required fields — the sentence names the EMPTY ones, `account_id`/`role_name` included, since a portal miss is the commonest way a capture comes back short), `field_unsafe` (a control character or whitespace in a field baked verbatim into the generated `~/.aws/config`), `field_shape` (an `account_id` that is not 12 digits, or a `role_name` outside IAM's own grammar — the same two shapes the roster-save door holds the admin's pin to, so one rule now governs both doors), `region_mismatch` / `start_url_mismatch` (the F006 bindings to the region and access portal this login run was LAUNCHED with), `account_role_pin_mismatch` (the blob names an account/role other than the one the roster row pinned at launch), `model_account_mismatch` (the session is for one account and the configured Bedrock model lives in another — a run using it would be refused by IAM; UNPINNED launches only, since an explicit roster pin is the admin's deliberate answer and outranks the model's account), `unstamped_scope` (a login run with no launch-time scope stamp on a deployment that now reads `per_user`), `already_captured` (this login run already captured once), `stamp_unreadable` / `store_error` (the daemon could not read the launch stamp, the existing credential, or — on the re-read below — the login run itself), `run_killed` (the login run was KILLED — cancelled, or superseded by that person's next sign-in — and a terminal run may still reach this upload door inside `terminalUploadGrace`, so the run's own state is the belt under a best-effort revocation). Actor is the AGENT — the refusal is attributed to the login sandbox that attempted the write, and `Target` is the reserved harness secret name it attempted to write — the operator-wide one, even for a per_user refusal, because that is the name the upload was attempting. **`owner` + `credential_source` ride the refusals that happen AFTER the scope is decided** (`already_captured`, every `store_error` arm, and the `run_killed` arm decided inside the lock) — the same pair the captured row carries, and the one that makes a per_user estate's refusal stream groupable by person instead of a join back through each row's `run_id` to its `harness.login.started`. The EARLIER refusals carry neither: there is no decided scope yet, and that run's stamp already holds the pair. **Order matters when reading these:** the identity binding runs BEFORE the once-only guard, so a sandbox replaying after a genuine capture with a WRONG pair reads as `account_role_pin_mismatch`, not `already_captured` — a replay count taken from this reason alone under-counts. **Rows are unbounded per run:** nothing rate-limits the upload route, so a loop inside a login sandbox can write one row per attempt for the run's idle cap. Accepted rather than throttled — the rows are small, the reason vocabulary is closed so they group and prune, and nothing is stored; the run is time-capped regardless. **Which refusals take the per-owner lock:** `blob_shape`, `field_unsafe`, `field_shape`, `region_mismatch`, `start_url_mismatch`, `account_role_pin_mismatch`, `model_account_mismatch`, `stamp_unreadable` and `unstamped_scope` are decided BEFORE `lockAWSSSOOwner`, so a flood of those cannot contend with credential renewal. `already_captured`, every `store_error` arm and a SECOND `run_killed` arm are decided INSIDE it — the once-only guard is a read-then-put and has to be, and the run state is re-read there as the last statement before the write because a supersede landing in the window between the top-of-route check and the store would otherwise let the ended sandbox's capture overwrite the one that replaced it. `run_killed` therefore appears on BOTH sides of the lock: the early arm carries no `owner` pair, the late one does — so a sandbox replaying a CORRECTLY BOUND blob after its genuine capture takes and releases the refresher's per-owner single-flight lock once per attempt, for the run's idle cap | `credential_source`, `owner`, `provider`, `reason` | `internal/api/awssso_pin.go:391` (`refuseCapture`) | internal |
| `harness.credential.disconnected` | A harness credential is disconnected | `captured`, `provider` | `internal/api/harnesscred.go:851` | internal |
| `harness.login.started` | A harness SSO login flow starts. It also STAMPS the launch-time credential scope — `credential_source` + `owner` — because that is what the capture upload binds to: the login sandbox lives to its idle cap, so re-resolving the scope at upload time let a roster edit mid-run re-point a member's capture at the operator-wide credential every run inherits. `sso_start_url` is operator config, never a credential. From 0.7.3 it also stamps the roster row's account/role PIN (`sso_account_id` + `sso_role_name`) for the same reason: the upload refuses a blob that disagrees with the pin as it read AT LAUNCH, so a roster edit while the login sandbox is still alive cannot re-point a capture already in flight | `credential_source`, `egress`, `owner`, `provider`, `sso_account_id`, `sso_role_name`, `sso_start_url` | `internal/api/harnesscred.go:526` | internal |
| `harness.credential.refresh` | Wardyn renews a captured AWS SSO credential CONTROL-PLANE side at dispatch (SSO-OIDC `CreateToken` over the `refresh_token` grant) — the run-less counterpart to `harness.credential.captured`, because the credential is per-principal, not per-run. `outcome=success` once the rotated pair is persisted; `outcome=failure` both when the redeem itself failed (`spent` says whether the refresh token is gone — `invalid_grant`/`expired_token`/`invalid_client`/`unauthorized_client` — or whether it was transient, in which case the credential is untouched and the next dispatch redeems normally — and if the access token has not actually expired yet that run is still served from it, since renewal starts a skew window ahead of expiry) and when the redeem SUCCEEDED but the re-`Put` did not (`persist_error`): that run is still served from the in-memory renewed blob, since the stored pair is already spent at AWS and re-redeeming it is impossible. `attempts` rides every failure row (1 or 2 — a transient failure gets exactly one immediate retry, so `attempts: 2` with a transport error on both is a network story, not a credential one; two EOFs read differently from an EOF then `invalid_grant`, and `error` joins both attempts' text when there were two) AND a success row when the retry is what made it succeed (a first-attempt success carries no `attempts` field at all) | `attempts`, `credential_source`, `error`, `expires_at`, `owner`, `persist_error`, `provider`, `registration_expires_at`, `rotated`, `spent` | `internal/api/awssso_refresh.go:497` | internal |
| `token.create` | A signed-in human mints a per-user API token for themselves (`POST /me/tokens`). The actor is that human and `Target` is the token id — never the credential, which is returned once and stored only as a hash | `name`, `role` | `internal/api/apitokens.go:373` | internal |
| `token.revoke` | A per-user API token is revoked, by its owner (`DELETE /me/tokens/{id}`) or by an admin revoking anyone's (`DELETE /tokens/{id}`). `principal` names the token's OWNER, which is the point on the admin lane: the actor is the admin, the subject is someone else. A bulk sweep writes ONE ROW PER CREDENTIAL too, carrying `scope:"sweep"` beside the same two keys — the aggregate `tokens_revoked` on `session.revoke` / `access.role_mapping.*` says how many, and on the role-mapping lane's unanswerable-snapshot arm that number covers every elevated-stamp token in the deployment, so without these rows the log cannot say WHICH credentials an edit killed. | `name`, `principal`, `scope` (`"sweep"`, absent on the single-token doors) | `internal/api/apitokens.go:454`; the sweep at `internal/api/sessions.go:240` | internal |
| `session.revoke` | An admin revokes active OIDC console sessions — `target` is the revoked `sub`, or `*` for a revoke-all (`POST /api/v1/sessions/revoke`, D16). Also revokes every unrevoked per-user API token the target holds — a `wdn_` bearer is that human's session in another form, so "revoke a human now" covers both ("all" covers every token in the deployment); `tokens_revoked` counts THIS call only (a retry after a failure counts just the remainder), and outcome `failure` marks a partial application — sessions revoked, token sweep errored mid-way | `scope` (`sub` or `all`), `sub` (sub-scoped only), `tokens_revoked` | `internal/api/sessions.go:120,125,135,140` | internal |
| `scm.ado.signin.captured` | A per-user Azure DevOps sign-in is captured. TWO paths write it and `source` says which: `login` is the PRIMARY one — the console's own SSO login carried the Azure DevOps scopes and its callback kept the refresh token, so an admin configures the org once and a member who signs in is connected with nothing else to do — and `signin` is the dedicated fallback door (`GET /api/v1/scm/azure-devops/callback`) for a row in another tenant, a login that predates the row, or a consent since withdrawn. The login path writes NO row when the grant carries no Azure DevOps scope (consent declined, a Conditional Access policy, a tenant that will not issue them): that is one row per login per person on an org that has not finished setting this up, and the absence of a stored credential already says it. That login still succeeds. ONE action for both outcomes: `success` once the refresh token is stored under the person's OWN namespace at the reserved harness name, and `failure` with a `reason` for every refusal, so a review reads one action instead of correlating two. `reason` is from a FIXED vocabulary, never caller-chosen text: `identity_binding` (the identity token's `sub` is not the session's subject — the fail-closed binding, and the one refusal that means somebody else's credential was about to be stored under this person), `consent_required` / `interaction_required` (the tenant refused the authorization or the exchange — the person's or their admin's to resolve), `exchange_failed` (the code exchange did not complete for any other reason: a code-challenge mismatch, a spent code, an unreachable authority), `unusable_grant` (the exchange succeeded but returned no refresh token or no granted scope, so there is nothing renewable to store) and `store_error` (everything checked out and the write failed). Actor is the HUMAN whose session started the sign-in and `Target` is the reserved secret name the capture writes, so "whose Azure DevOps access" is answerable without a join. `identity_binding`, `consent_required`, `interaction_required` and `exchange_failed` are reachable only from the dedicated door — at login the identity binding holds by construction, since the subject compared against IS the identity token's own. The attack-shaped refusals — a forged `state`, a missing one-time cookie, a caller with no session subject, a row configured against another application — emit NO row: nothing about a credential happened, and a scanner hitting the callback must not be able to write to the audit log. Never carries a token, a code or a verifier. `scopes` is the GRANTED set the tenant reported, never the requested one: Entra does not narrow an Azure DevOps token to the scopes a request names, so the granted set is the only honest record of what this credential can reach — and what bounds a single run is Wardyn's own capability check in front of the resource, not the token | `client_id`, `error`, `expires_at`, `provider`, `reason`, `scopes`, `source`, `tenant_id` | `adoSignInCapturedAction`, `internal/api/ado_entra_store.go:570` | internal |

## Policy, capability & authorization

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `policy.create` | A named egress/run policy is created | `min_confinement_class`, `name` | `internal/api/policies.go:289` | internal |
| `policy.update` | A named policy is updated | `min_confinement_class`, `name` | `internal/api/policies.go:333` | internal |
| `policy.delete` | A named policy is deleted | — | `internal/api/policies.go:354` | internal |
| `policy.inline` | A one-off (inline, unsaved) policy is used to launch a run | `eligible_grants`, `min_confinement_class`, `workspace_mounts` | `internal/api/inline_policy.go:243` | internal |
| `capability.grant.created` | `POST /permissions/grants` (new) | `capability`, `effect`, `subject`, `subject_type`, `value` | `internal/api/permissions.go:343` | internal |
| `capability.grant.updated` | `POST /permissions/grants` (upsert on existing key) | `capability`, `effect`, `subject`, `subject_type`, `value` | `internal/api/permissions.go:345` | internal |
| `capability.grant.deleted` | `DELETE /permissions/grants/{id}` | — | `internal/api/permissions.go:375` | internal |
| `capability.enforcement.write` | `PUT /permissions/enforcement` (whole-map replace) | (saved enforcement map) | `internal/api/permissions.go:424`; documented `docs/OPERATIONS.md`'s capability-grants section | internal |
| `governance.profile.write` | `POST /governance/profiles` (create) or `PUT /governance/profiles/{id}` (update): a named, assignable ceiling is saved. `Target` is the profile id | `allow_all_egress`, `limits`, `min_confinement_class`, `name` | `internal/api/governance.go:219` | internal |
| `governance.profile.delete` | `DELETE /governance/profiles/{id}`. Refused `409` while the profile is still assigned, so this row never records a silent widening — the unassign is its own event below | — | `internal/api/governance.go:281` | internal |
| `governance.assignment.write` | `POST /governance/assignments`: a profile is bound to a user, a group, or everyone (upsert on the natural `(subject_type, subject)` key — 201 vs 200 tells create from update apart, this action name does not) | `priority`, `profile_id`, `subject`, `subject_type` | `internal/api/governance.go:390` | internal |
| `governance.assignment.delete` | `DELETE /governance/assignments/{id}`: the supported way to widen a principal back to the deployment ceiling, which is why it is audited on its own line rather than riding a profile delete | — | `internal/api/governance.go:417` | internal |
| `drive.write` | `POST /drives` (create) or `PUT /drives/{id}` (update): a user drive — the persistent storage an admin registers and allocates — is saved. `Target` is the drive id. `host_root` is recorded because it is the host tree this row authorizes binding into other people's sandboxes; a drive carries no secret (a share credential is the operator's, held host-side, never by Wardyn). `rehomed` is true when the edit RE-POINTED the storage every allocated member is bound to — a rename, a host-root correction or a template change, which this one action otherwise covers indistinguishably from a cosmetic edit; such a change is refused 409 unless the caller confirmed it. `rehomed_fields` and `rehomed_subjects` are present EXACTLY when `rehomed` is true, and carry what the overridden refusal said: which identity fields moved (every allocated person's object name is derived from them) and how many allocations went with them — without those two the log cannot answer which objects were orphaned | `backend`, `home_template`, `host_root`, `name`, `reclaim`, `rehomed`, `rehomed_fields`, `rehomed_subjects`, `size_mib`, `storage_class`, `writable` | `internal/api/user_drives.go:438` | internal |
| `drive.delete` | `DELETE /drives/{id}`. Refused `409` while the drive is still allocated (`ON DELETE RESTRICT`), so this row never records a silent un-allocation — the deallocation is its own event below | — | `internal/api/user_drives.go:735` | internal |
| `drive.grant.write` | `POST /drives/grants`: a drive is allocated to a user, a group, or everyone (upsert on the natural `(subject_type, subject)` key — 201 vs 200 tells create from update apart, this action name does not). `home_override_set` is a **boolean**, not the value: which directory an admin pinned is a person's username as often as not, and whether one was pinned is the governance fact | `drive_id`, `enabled`, `home_override_set`, `priority`, `size_mib_override`, `subject`, `subject_type`, `writable_override` | `internal/api/user_drives.go:925` | internal |
| `drive.grant.delete` | `DELETE /drives/grants/{id}`: the product-side half of offboarding, and the half that deletes **no data** — which is why the row carries the drive's declared `reclaim` intent, so the log says what the operator was told to do about the directory this allocation was the last pointer to. **`drive` here is the drive OBJECT's name** (`user_drives.name`), with `drive_id` its uuid — the same key on `run.drive.mount` carries the per-person home segment instead, because that row is about one person's directory and this one is about the object | `drive`, `drive_id`, `reclaim`, `subject`, `subject_type` | `userDriveGrantAuditData`, `internal/api/user_drives.go:963` | internal |
| `run.drive.mount` | Dispatch attached a member's USER DRIVE to a sandbox — the one object in a run's spec that OUTLIVES the run, which is why it is recorded on its own line rather than left implicit in the run's policy snapshot. Actor `system`: a member ticked a checkbox, and what is logged is dispatch's own resolution of that flag into a storage object. Emitted only when a drive is actually attached. **`Target` names the storage, and WHICH name depends on the backend** — because this is the one field on the row whose reader is not the operator. The run page's Audit tab renders a row's `target` and nothing from `data`, and `auditScope` lets a run's CREATOR read their own run's rows (`GET /audit?run_id=`). A **managed** object's name is Wardyn's own (`wardyn-drive-<drive-slug>-<home>`, volume and claim alike): it says which volume or claim the run was handed, discloses nothing about the host, and is carried verbatim. A **`host_path`** object name is `<host_root>/<home>` — an absolute path on the operator's filesystem — so the row carries **`<drive name>/<home>`** instead, in the `Target` **and** in `object`, which answers the same question without handing a member the share's layout (the rule `driveShareIsBindable` and `applyUserDriveEnv` already state for the same reader). **The payload is not a side door**: `GET /audit?run_id=` hands the run's creator the whole event, `data` included, so masking only the rendered field would have moved the same disclosure one key over. An operator reads a share's root from `GET /drives`, which is operator-only and already carries it. A **managed** row is unchanged on both fields, so the reclaim command still reads the exact volume or claim name straight off it; **`drive` here is the per-person HOME SEGMENT**, not the drive object's name — that is what the same key carries on the `drive.grant.*` rows. Neither ever enters the sandbox env, which carries the target and the mode and nothing else (`applyUserDriveEnv`). `enforcement` (`filesystem`/`request`/`external`/`none`) is what actually binds the drive's bytes, logged beside the mount so a later dispute reads the size with its caveat attached. Emitted AFTER `CreateSandbox` returns, so a `success` row means the drive really was bound: the driver re-runs the host-root ceiling and the bind deny-list at create time and can still refuse, and that refusal surfaces as a `run.create` failure with no mount row at all | `backend`, `drive`, `enforcement`, `mode`, `object` | `auditDriveMount`, `internal/api/runs_dispatch_mounts.go:579`; called after `CreateSandbox` in `internal/api/runs_dispatch.go` | internal |
| `directory.search_failed` | `GET /access/directory/search` could not answer — a lapsed secret, a revoked consent, an unreachable tenant — so an operator sees the connector failing without an admin reporting "the picker is empty". FAILURE ONLY: a successful lookup is never audited. Deliberately **content-free** — the fields are the connector's typed `directory.ProviderError` values, never the query, which is the name of a person an admin was looking up. `Target` is the request path | `op`, `provider`, `upstream_status` | `internal/api/directory_search.go:153` (`auditDirectoryFailure`) | internal |
| `access.role_mapping.write` | `POST /access/mappings` (Phase 2 lane A, migration `0051`, the People step): a console role mapping is added, or re-added over an existing value (upsert on the natural `value` key — 201 vs 200 tells the two apart, this action name does not) | `role`, `value`, `stale_token_snapshots`, `tokens_revoked` | `internal/api/access.go:557` | internal |
| `access.role_mapping.delete` | `DELETE /access/mappings/{id}`. Records the DELETED row's `value`/`role` — captured from the matched row before the store delete, since the row (and its id-to-value mapping) is gone by the time the event is written | `role`, `value`, `stale_token_snapshots`, `tokens_revoked` | `internal/api/access.go:658` | internal |
| `approval.second_human.bypass` | `WARDYN_EGRESS_SECOND_HUMAN` is set and the decider was the `admin-token` principal, so the four-eyes rule was BYPASSED (break-glass). A shared token carries no per-human identity to compare against, so it is exempt by design — this event is what keeps that exemption from being silent. The row is written on both of Decide's arms: outcome `success` sits beside the `actor_type=system` `approval.decide` the decision itself writes, and outcome `failure` records a bypass that got past the gate on a decision that then failed — there is no `approval.decide` beside that one, which is what the outcome says | `reason`, `switch`, `error` | `internal/api/approvals.go:450` (`requireSecondHuman`) | internal |
| `authz.denied` | Every member denial that isn't a plain foreign-resource 404 — plus, since 0.7.4, the RUN-TOKEN tier: `internalAuth`'s liveness gate refuses every `/internal/*` door to a run that has gone terminal (`reason` `run_terminal`, with the terminal state beside it as `run_state`) or whose row is gone (`run_not_found`), with `actor_type` `agent` and the request path as the target. Token verification alone could not catch it — `revokeRunCascade` is best-effort, so a killed run whose revocation write failed still presents a verifiable token (`internal/api/internal_live_run.go:192`). The tail-upload doors (recordings, scan results, the captured SSO cache) are exempt for five minutes after the terminal transition, because those uploads race the watcher that writes it. — see `docs/OPERATIONS.md`'s "Every denial that isn't a 404" for the full `reason` vocabulary (`admin_surface`, `security_admin_surface`, `not_owner`, `attach_ticket_foreign_run`, `byoi_member`, `capability_workspace`, `capability_egress_host`, `capability_secret`, `capability_agent`, `capability_integration`, `capability_workspace_provider`, `governance_profile`, `grant_pairing_not_eligible`, `groups_snapshot_stale`, `second_human_required`, `harness_login_mechanism_principal`, `run_terminal`, `run_not_found`). The `governance_profile` reason covers the user-drive door too, at target `runs.drive` (`denyMemberDrive`, `internal/api/user_drives_run.go:239`) — a new target, not a new reason. 0.7 does add three reasons, all listed above: `security_admin_surface` (the second admin tier's own refusal), `attach_ticket_foreign_run`, and `groups_snapshot_stale` (the resolver cannot answer this caller's group tier, so every ceiling-bounded seam refuses 403 — emitted ONCE per request at each of the TWO sites that decide it: `internal/api/governance.go`'s `ceilingWithUnusableGroups` at target `governance.ceiling`, and `internal/api/user_drives_resolve.go`'s `driveWithUnusableGroups` at target `runs.drive`. They are separate decisions over separate group-tier reads — `HasGroupTierAssignments` and `HasGroupTierDriveGrants` — and a deployment can have either without the other, so a denial stream that named only the first was empty for a drives-only deployment). 0.7.2 adds one more, also listed above: `capability_workspace_provider` (a member's work would come from a git provider row they hold no grant for — six doors, two targets, all detailed in OPERATIONS' table). 0.7.3 adds one more, also listed above: `harness_login_mechanism_principal` (the shared admin bearer token attempted a `per_user` harness-login capture with OIDC configured, target `setup.harness_login`, `422` rather than `403` — see OPERATIONS' table) 0.7.4 adds the two RUN-TOKEN-tier reasons named above (`run_terminal`, `run_not_found`) and one FIELD: `member_mode: true` rides the datum at every ADMIN-TIER gate — the two universal chokepoints (`requireOperator`, `requireSecurityOperator`) and the in-handler refusals that raise the identical `admin_surface` / `security_admin_surface` datum (`getWorkspaceAuthorized`'s operator-owned-workspace arm, `internal/api/helpers.go`; the `?owner=` secrets gate, `internal/api/secrets.go`; `resolveAlwaysTarget`'s operator-only `always` scope, `internal/api/approvals.go`; `denyMemberField`, which carries the `workspaces.llm_cred` arm, `internal/api/runs_create_validate.go`) — when the refused caller is an admin exercising **"view as member"** — so a denial stream reads as an admin walking the member path rather than as an incident. It is a MARKER, not a field every row answers: it is omitted entirely when the mode is off, and it changes no `reason` and no status code (`authzDeniedDatum`, `internal/api/membermode.go`). | `dropped`, `host`, `member_mode`, `method`, `reason`; and on the run-token tier `actor` plus `run_state` (the terminal state the liveness gate found) | multiple sites; documented `docs/OPERATIONS.md:2047-2054` | **stable** (documented, closed `reason` enum) |
| `auth.failed` | An authentication attempt fails, on EITHER lane. **Public lane**: an `adminAuth` 401 (admin token not configured, missing bearer, or a bearer that doesn't match), OR a presented OIDC session cookie was rejected (tampered/malformed or expired) — whichever reason is more specific wins when both apply on the same request. **Internal lane** (the untrusted sandbox / host sensor boundary): an `internalAuth` 401 on a missing or unverifiable run token, an `internalAuthGroundtruth` 401 on a missing or unverifiable host-sensor token, and `handleInternalRequestApproval`'s two 400s — a sidecar asking for an approval kind it may not raise (`credential` is broker-only, never sidecar-raised) or omitting `requested_scope`. Actor is always `system`, and it names WHICH boundary refused: `wardyn/adminAuth`, `wardyn/csrf`, `wardyn/internalAuth`, `wardyn/internalAuthGroundtruth`, or `wardyn/internalApproval` (no verified caller identity exists at any of these points except `wardyn/csrf`'s, and public-lane stuffing vs. a probing sidecar vs. a cross-origin page are different incidents). Content-free: `reason` is a closed enum (`admin_token_not_configured`, `missing_bearer_token`, `invalid_admin_token`, `invalid_session`, `expired_session`, `revoked_session`, `session_revocation_unavailable` — the last two only when session revocation is wired — plus `cross_origin_refused`, the 0.7.3 CSRF guard's refusal of a cookie-authenticated cross-origin mutation in EITHER mode (`internal/api/csrf.go`'s `sameOriginOrRefuse` on the SSO branch, the LocalMode arm's own check; a **403**, not a 401, and the only reason here that is not an authentication failure but a request refused for where it came from — the caller was already past authentication (a valid session, or, in LocalMode, the no-auth bypass), which is also why its Actor is `wardyn/csrf` and not `wardyn/adminAuth`. The PTY-attach WebSocket's own cross-origin refusal emits the same pair) — plus the internal lane's `identity_provider_not_configured`, `missing_run_token`, `invalid_run_token`, `missing_sensor_token`, `invalid_sensor_token`, `unsupported_internal_approval_kind`, `missing_requested_scope`), never a user-supplied string; the request path is `Target`, the TCP peer is `SourceIP`. Rate-bound (process-global token bucket, 1/sec with a burst of 5) so a scanner throwing 401s cannot flood the append-only log — **so this action's row count is NOT the failure rate**: past the burst the trail flattens while the real volume grows, and the dropped emits are counted as `wardyn_auth_failed_suppressed_total` on `/metrics` ([OPERATIONS.md](OPERATIONS.md) → Monitoring). Alert on that series, not on rows of this action. **COALESCED as well as rate-bound since 0.7.2** (`WARDYN_AUDIT_COALESCE_WINDOW`, default `5m`, `0` = off — [ENV.md](ENV.md)): a run of IDENTICAL consecutive refusals (same actor, `reason`, path and `SourceIP`) is folded into its FIRST row plus one **new** summary row carrying `count`, `first_seen` and `last_seen` — never an update of the first row, because this table is append-only and hash-chained, so a summary is always another append. Two rows on a summary, then, and the FIRST row of a streak carries only `reason` exactly as before; the window is the maximum GAP between two identical rows, and a streak also closes at 1000, so one row can never stand for unbounded volume. The folded rows are counted in the same `wardyn_auth_failed_suppressed_total` series as the rate-limited ones. It exists because the rate limit did nothing about a SLOW permanent drip: a sidecar retrying a renew it would never be granted wrote one row a minute forever and evicted every real security event out of the console's window — see `run.identity.expired` above for the other half of that fix | `count`, `first_seen`, `last_seen`, `reason` | `internal/api/http.go` (`auditAuthFailedAs` — the one rate-bound emit both lanes funnel through, `auditAuthFailed` is the public-lane wrapper), `internal/api/internal.go` (`handleInternalRequestApproval`); session-rejection reason from `internal/auth/oidc/oidc.go`'s `Middleware`/`SessionRejectedFromContext` | internal |
| `egress.*` | The proxy reports an egress decision for a run — the literal suffix is the decision itself: `egress.allow`, `egress.deny`, or `egress.pending` (`egress.Decision`, `internal/egress/egress.go:27-31`). A synthetic `blind` scan decision emits only `llm.scan.blind`, never a duplicate `egress.allow` for the tunnel. **A second synthetic shape:** when the proxy's decision buffer overflows, it posts a synthetic `egress.deny` with `target=""`, every Data field zero/empty, and `rule_source` carrying the extensible `egress.decisions.dropped:<n>` marker (`droppedSummaryLog`, `internal/egress/proxy/decisions.go`) — an audit-fidelity alert, not a policy denial of an actual request; `n` (the drop count) is embedded in that string, not a separate queryable field. **A third synthetic shape:** when the agent retries a `builtin:private-ip` refusal, the proxy answers the identical attempt from a per-run memo (`privateIPMemo`, `internal/egress/proxy/private_ip_memo.go`) rather than re-resolving and re-deciding, so those attempts get NO row of their own; when that streak closes — the memo evicts the entry, or the run ends — one NEW `egress.deny` is posted carrying `repeat`, the number of attempts refused without a row. `repeat` is absent on every ordinary decision, it is a count and never a rewrite of the row that opened the streak (the chain is append-only), and it is the one Data field that is queryable rather than embedded in `rule_source`. **`cause`/`via` ride a dial-shaped `builtin:dial-failed`/`builtin:gateway-vet-failed`/`builtin:upstream-protocol-mismatch` deny only** (`egress.DecisionLog.Cause`/`.Via`, `internal/egress/egress.go`): `cause` is the STAGE (`tcp dial`, `tls handshake`, `upstream proxy connect`, `dial`) plus the underlying error — EXCEPT on `builtin:upstream-protocol-mismatch`, which carries the fixed sentence in its own row below, masked and topology-redacted the same two ways the sandbox's own error body is (`Proxy.httpError`, `internal/egress/proxy/sandbox_error.go`) — this is a run's OWN CREATOR's row too (`auditScope` below), not just the operator's, so the redaction runs before the field is ever built; `via` is the hop CLASS attempted, `direct` or `upstream-proxy`, never an address. Both absent on every decision that is not dial-shaped | `approval_id`, `cause`, `host`, `method`, `path`, `port`, `repeat`, `rule_source`, `via` | `internal/api/internal.go:93` | internal |

### `egress.*`'s `rule_source` values

The row above names `rule_source` as a field but enumerates none of its
values; they were scattered ad hoc across `docs/ENV.md`, `docs/OPERATIONS.md`
and `docs/POLICIES.md` prose, with no single table. This is every
`ruleSource*` constant defined in `internal/egress/proxy` (excluding
`_test.go`) except `ruleSourceRequireTLS`, which is a `policy:` value and is
documented with that family in "The evaluator's own inline sources" below. It is the BROKERED and site-config vocabulary only: the
decisions `evaluate` itself writes pass their `rule_source` as an inline literal
rather than a named constant and are NOT in this table (`builtin:*`, `policy:*`,
`approval:*`, `internal/egress/proxy/proxy.go`), and the synthetic
`egress.decisions.dropped:<n>` marker is documented in the `egress.*` row above:

| `rule_source` | Meaning | Defined at |
|---|---|---|
| `brokered:mint` | A brokered `POST /wardyn/v1/credentials/mint` request | `internal/egress/proxy/local_routes.go:47` |
| `brokered:approvals` | A brokered tool-call approval create/poll (`/wardyn/v1/approvals`) | `internal/egress/proxy/local_routes.go:48` |
| `brokered:recording` | A brokered recording upload (`/wardyn/v1/recordings/`) | `internal/egress/proxy/local_routes.go:49` |
| `brokered:scan-result` | A brokered scan-result upload (`/wardyn/v1/scan-results/`) | `internal/egress/proxy/local_routes.go:50` |
| `policy:tool-allow` | A tool call the run's own `tool_rules` waved through with no human asked | `internal/egress/proxy/local_routes.go:54` |
| `policy:tool-deny` | A tool call `tool_rules` refused with no human asked | `internal/egress/proxy/local_routes.go:55` |
| `brokered:sso-token` | A brokered AWS SSO session upload from a container-login run (`/wardyn/v1/sso-token/`) | `internal/egress/proxy/local_routes.go:56` |
| `artifact:mitm` | A corp artifact-registry request TLS-MITM'd to inject the operator's registry token — not an LLM/inspection path | `internal/egress/proxy/local_routes.go:60` |
| `brokered:git` | Every decision the GitHub App-broker route (`/wardyn/gh/`) itself made — the per-repo allowlist ALLOW, and equally the DENY for an unparseable path, an ungranted repo, a non-smart-HTTP verb, or a mint/build failure (`ruleSourceGit`, `internal/egress/proxy/git_broker.go:167`, `:177`, `:182`, `:218`, `:232`, `:248`); the three `brokered:git:branch-ns*` rows below are the only decisions carved out of it — two refusals (`brokered:git:branch-ns`, `brokered:git:branch-ns-encoding`) and one ALLOW that REPLACES the ordinary `brokered:git` allow when the parser is opted out (`brokered:git:branch-ns-off`, `allowSrc`, `internal/egress/proxy/git_broker.go:269`) | `internal/egress/proxy/git_broker.go:43` |
| `brokered:git:branch-ns` | A brokered push denied by push branch-namespace confinement (as opposed to the per-repo allowlist). Emitted by BOTH broker lanes since 0.7.2 - the App lane always, the `git_pat` lane when `WARDYN_GIT_PAT_BROKER_ENFORCE_BRANCH_NS` is on - from the one shared `confinePush` step, against each lane's own host | `internal/egress/proxy/git_broker.go:55` |
| `brokered:git:branch-ns-encoding` | A brokered push refused because its body had a non-identity `Content-Encoding` the proxy could not inspect while branch-namespace confinement is on; same two lanes as the row above | `internal/egress/proxy/git_broker.go:59` |
| `brokered:git:branch-ns-off` | A brokered push forwarded WITHOUT parsing because branch-namespace confinement is opted out - deployment-wide (`WARDYN_GIT_BROKER_ENFORCE_BRANCH_NS=false`, `BranchNSEnforced`) or for this one run (`git_push_any_branch`, `internal/types/policy.go:259`); both App-lane switches take the same branch (`internal/egress/proxy/git_broker.go:205`), so the marker never says which one was set - see `docs/POLICIES.md`'s "`git_push_any_branch`: the per-run opt-out". Since 0.7.2 the `git_pat` lane emits it too, for its own THIRD switch's counterpart case: `WARDYN_GIT_PAT_BROKER_ENFORCE_BRANCH_NS` is ON and this run's `git_push_any_branch` opted out (`PATBranchNSEnforced`, `internal/egress/proxy/pat_broker.go:182`). With that switch off - the default - a PAT push is not confined at all and keeps the ordinary `brokered:git-pat` allow, so this row never appears for it | `internal/egress/proxy/git_broker.go:81` |
| `brokered:git-pat` | A PAT-broker-terminated ADO/GitLab (or unbrokered-GitHub) request, `WARDYN_GIT_PAT_BROKER=on` (`/wardyn/git/<host>/`) | `internal/egress/proxy/pat_broker.go:49` |
| `brokered:git-pat:denied` | ANY PAT-broker refusal — an unparseable `/wardyn/git/<host>/<rest>` path, a host not in the grant's allowlist, a non-smart-HTTP verb, or a mint / host-vet / request-build failure (`ruleSourcePATDenied`, `internal/egress/proxy/pat_broker.go:131`, `:140`, `:161`, `:197`, `:214`, `:223`); the row names the LANE that refused, never which of the six causes | `internal/egress/proxy/pat_broker.go:50` |
| `site-config:internal-host` | An allow via the operator's `internal_hosts` lift of the private/reserved-IP guard | `internal/egress/proxy/egress_target.go:39` |
| `site-config:egress-redirect` | An allow via an EXACT literal-IP entry an `egress_redirects` `to` substitution wrote (see `docs/OPERATIONS.md`, "Egress redirects: two tiers") | `internal/egress/proxy/egress_target.go:52` |
| `brokered:llm` | A brokered Anthropic/OpenAI LLM passthrough request (`/wardyn/llm/<provider>/`) | `internal/egress/proxy/llm_routes.go:46` |
| `scan:blocked` | An LLM request refused by content inspection (`on_scanner_error=block` or a blocking finding) | `internal/egress/proxy/llm_routes.go:50` |
| `scan:opaque-tunnel` | An opaque CONNECT to an LLM host inspection could not see into — the honest coverage-gap signal, paired with a synthetic `llm.scan.blind` audit row | `internal/egress/proxy/llm_routes.go:51` |
| `scan:mitm` | An LLM request inspected via TLS-MITM interception of an otherwise-opaque CONNECT tunnel (the subscription-OAuth path) | `internal/egress/proxy/llm_routes.go:54` |

#### The evaluator's own inline sources

`evaluate()` (`internal/egress/proxy/proxy.go`) writes these `rule_source`
values directly at the `decisionLog` call site rather than through a named
constant — the table above is the BROKERED and site-config vocabulary only;
this is the rest of it, plus the `approval:<approval-id>` shape whose UUID
rides in the value itself.

One row here is NOT the evaluator's: `policy:require-tls` is emitted by the
plain forward lane (`internal/egress/proxy/plain_lane.go`) from a named
`ruleSource*` constant, for a refusal the evaluator cannot make — it has
already allowed the host, and what the lane refuses is the TRANSPORT the
operator's injection rule named. It sits in this table rather than the one
above because an operator reading a decision row looks up the `policy:`
family here:

| `rule_source` | Meaning | Defined at |
|---|---|---|
| `builtin:upstream-proxy` | A configured corp upstream proxy is in the path; recorded once at proxy construction, the hop the SSRF re-check is deferred to | `internal/egress/proxy/proxy.go:526` |
| `builtin:private-ip` | The literal-IP guard denied a private/loopback/link-local/metadata literal no `internal_hosts` lift or `egress_redirects` `to` trusts exactly — step 0 and the post-`egressTarget` re-check alike. An identical retry against a host already refused here is answered from a per-run memo (`privateIPMemo`, `internal/egress/proxy/private_ip_memo.go`) and counted into one summary row instead of emitting a row per attempt — see the `egress.*` row's third synthetic shape | `internal/egress/proxy/literal_ip_guard.go:78` (step 0), `internal/egress/proxy/proxy.go:729` (post-`egressTarget`) |
| `builtin:resolve-failed` | The name never resolved — a resolver outage, NXDOMAIN, or a zero-answer lookup — so no address was ever vetted. A DNS fault, NOT the address-range guard it used to be audited as: the two have opposite fixes, and this one is fixed at the sandbox's resolver rather than by declaring an `internal_hosts` lift | `internal/egress/proxy/proxy.go:726` (post-`egressTarget`, `errHostUnresolved`) |
| `policy:evaluator-error` | The pluggable evaluator returned an error: fail-closed deny | `internal/egress/proxy/proxy.go:602` |
| `policy:denied` | The run policy's deny list names the host (deny beats allow) | `internal/egress/proxy/proxy.go:615` |
| `approval:denied` | A first-use approval for this host was refused | `internal/egress/proxy/proxy.go:671` |
| `approval:pending` | A first-use approval is open and undecided — the request is held, not denied. Selected beside the cap label it is paired with (`approvalPendingReason`), so the two states and their audit labels cannot drift apart | `internal/egress/proxy/approvals.go:150` |
| `approval:host-cap` | The run's first-use approval table is FULL (`maxApprovalHosts`, 4096), so no entry was made, no approval was raised and none ever will be for this host. Same fail-closed verdict as `approval:pending` and a different fact with a different fix, which is why it is its own label rather than folded into it: `pending` means a human has yet to answer, this means nobody was asked. Carries no `approval_id` — there is none. The sidecar also logs a one-shot `ERROR … host cap reached` | `internal/egress/proxy/approvals.go:160` |
| `policy:default-deny` | No allowlist entry names the host and no approval covers it | `internal/egress/proxy/proxy.go:682` |
| `policy:method` | The host is allowed but the request's HTTP method is not | `internal/egress/proxy/proxy.go:639` |
| `policy:allowed` | An ordinary standing allowlist allow (`allowLog`, `approvalID == Nil`) | `internal/egress/proxy/proxy.go:752` |
| `policy:require-tls` | The plain forward lane refused a cleartext request to a host whose `api_key` injection rule sets `require_tls` (`docs/POLICIES.md`, `eligible_grants[].scope`): the operator declared the credential TLS-only, so the request is DENIED rather than merely forwarded uncredentialed. The lane writes its own 403 and this deny row REPLACES the allow the evaluator had already granted the host - nothing was forwarded. The one value here declared as a named `ruleSource*` constant rather than written inline, because it is the one decision the lane makes for itself | `internal/egress/proxy/plain_lane.go:35` |
| `credential:reauth-timeout` | The proxy PARKED a sandbox's AWS SSO `GetRoleCredentials` call while the credential's owner was asked to sign in again (the `credential_reauth` approval, 0.7.6) and nobody signed in before the hold's budget ended (`WARDYN_CREDENTIAL_REAUTH_TIMEOUT`). A DENY, and deliberately NOT folded into the credential-refresh failure beside it: "nobody signed in" and "the credential could not be refreshed" have different fixes, and only one of them is a person's to make. The sandbox gets a modelled 401 `UnauthorizedException` rather than the 502 a refresh failure gives, because both AWS SDKs read a 502 as a transport error and RETRY it — three more full holds for one lapse. The approval row STAYS `PENDING`: the sign-in is still wanted, and the 24h sweeper or the run's terminal cascade closes it, so this row — not the approval — is what says a hold ran out | `internal/egress/proxy/local_routes.go:73` |
| `approval:<approval-id>` | Allowed by one specific approval — the UUID rides IN the value, so the trail self-joins to the approval (`allowLog`) | `approvalID.String()`, `internal/egress/proxy/proxy.go:754` |
| `builtin:dial-failed` | The vetted dial itself failed, so the earlier allow is superseded by a deny rather than over-reported (E3) — never the configured LLM gateway's OWN guard refusal, which carries `builtin:gateway-vet-failed` instead (its own row, below) | `internal/egress/proxy/proxy.go:865` (CONNECT tunnel), `internal/egress/proxy/plain_lane.go:198` (plain forward), `internal/egress/proxy/llm_routes.go:420` (brokered LLM round trip) |
| `builtin:gateway-vet-failed` | The operator's OWN configured LLM gateway host was refused by `vetTrustedHost`'s guard (`errGatewayVet`, `internal/egress/proxy/egress_target.go`) — it resolved to loopback/link-local/this proxy's own control-plane network, or did not resolve at all. A CONFIG problem the operator's own gateway can never satisfy by retrying, never a network-lost-it dial (`builtin:dial-failed`, above) and never the allow-shaped `brokered:llm` | `internal/egress/proxy/egress_target.go:437` (`ruleSourceGatewayVetFailed`), `internal/egress/proxy/llm_routes.go:307` |
| `builtin:upstream-protocol-mismatch` | `internal/egress/proxy/egress_target.go:448` (`ruleSourceUpstreamProtocolMismatch`), `internal/egress/proxy/llm_routes.go:416` (MITM + brokered LLM), `internal/egress/proxy/plain_lane.go:190` (plain forward, via `refuseH2Mismatch`) | `internal/egress/proxy/egress_target.go` (`ruleSourceUpstreamProtocolMismatch`), `internal/egress/proxy/llm_routes.go`, `internal/egress/proxy/upstream_protocol.go` |

## Auth

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `auth.member_mode` | `POST /me/member-mode`: a signed-in human turned Wardyn's **"view as member"** mode on or off for their own session (0.7.4). The first SUCCESS-side auth action in the vocabulary — every other `auth.*` row is a failure — and it is here because the mode's whole claim is that it is not impersonation: `Actor` is the caller's own OIDC sub, unchanged, on this row and on every row the session writes while the mode is on. `enabled` is the requested state (an absent/empty body means `false`, which is the exit); `real_role` is the STAMPED session role the mode pauses, read off the cookie by `SetMemberMode` rather than from the request context, which is already clamped to `member` while the mode is on. No row is written for a caller with no per-human role (admin token, local mode, no IdP) — that request is refused `400` before the toggle, because there is nothing to pause. `no_credential` (0.7.5) marks the SECOND posture — "view as a new member (not signed in)", in which the caller's own per-user model credential reads as absent — and it rides the datum ONLY on a row where the server GRANTED that posture — the mode was turned on, the caller holds a role to pause, and the model-access agent's roster row is `per_user` — so every row a 0.7.4 deployment could write stays byte-identical and the key never claims a posture the session is not in. See [OPERATIONS.md](OPERATIONS.md)'s "Exercising member mode as an admin" for the mode's ceilings | `enabled`, `no_credential`, `real_role` | `internal/api/membermode.go:159` | internal |

## Kernel ground-truth (eBPF/Tetragon stream)

Defined as named constants (not scattered literals) in
`internal/groundtruth/groundtruth.go:44-64` — the `kernel.` prefix is enforced
by the ground-truth ingest endpoint (any action without it is rejected,
fail-closed) and is the field SIEM rules key on alongside `data.stream=ebpf`.
This stream is **detection, not prevention** (see the package doc comment) —
it never blocks, and it is blind inside CC3/Kata microVM guests.

| Action | When | Where | Stable? |
|---|---|---|---|
| `kernel.process.exec` | A process `execve` observed by the kernel sensor | `internal/groundtruth/groundtruth.go:50` | **stable** (const, documented) |
| `kernel.network.connect` | An outbound TCP connect observed by the sensor | `internal/groundtruth/groundtruth.go:52` | **stable** (const, documented) |
| `kernel.file.write` | A write to a sensitive path observed by the sensor | `internal/groundtruth/groundtruth.go:54` | **stable** (const, documented) |
| `kernel.sensor.heartbeat` | Periodic liveness beat from the ingest sidecar (`run_id` NULL) — `/healthz` keys `ebpf_groundtruth` state off this | `internal/groundtruth/groundtruth.go:60` | **stable** (const, documented) |
| `kernel.sensor.blind` | One-time event for a run the host eBPF sensor cannot see into (CC3/Kata) | `internal/groundtruth/groundtruth.go:64` | **stable** (const, documented) |

## Base images, integrations & site config

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `base_image.write` | An admin creates/updates a base agent image | `image`, `kind`, `steps` | `internal/api/base_images.go:113` | internal |
| `base_image.delete` | An admin deletes a base image | `detached_from`, `forced` | `internal/api/base_images.go:155` | internal |
| `integration.write` | An admin creates/updates an integration (secrets/egress/config/delivery) | `default_for`, `egress`, `header`, `kind` | `internal/api/setup_integrations.go:432` | internal |
| `integration.delete` | An admin deletes an integration | `credentials`, `egress`, `kind` | `internal/api/setup_integrations.go:473` | internal |
| `setup.onboarding.completed` | An operator finished (or deliberately left) the Getting Started funnel — stamps `SiteConfig.OnboardingCompletedAt`, the install-side fact the console's landing, welcome hero and setup gate read. Emitted once per install: the handler is idempotent and a later re-finish never moves the timestamp | `completed_at` | `internal/api/setup_onboarding.go:79` | internal |
| `site_config.write` | `PUT /site-config` (full-document replace). `egress_redirects`/`internal_hosts`/`upstream_proxy_url`/`upstream_proxy_secret_ref` are recorded IN THE CLEAR (B7-F4) — the same disclosure `workspace_provider.write`'s `base_urls` already makes: topology, not a credential (a secret ref is a NAME, never the value it names). Without them, two PUTs naming two different proxy URLs produced the identical row, and an MDM-applied narrowing/opening of the redirect table or internal-host allowlist was unreviewable from the log alone. `egress_redirects` is capped at 50 from→to pairs (`egress_redirects_count` stays the honest, unbounded total; `egress_redirects_truncated` appears only past the cap) | `agent_providers`, `egress_redirects`, `egress_redirects_count`, `egress_redirects_truncated` (only past the 50-pair cap), `git_providers`, `internal_hosts`, `internal_hosts_count`, `scm_hosts_count`, `sources_no_longer_admitted` (only when the body NAMED `workspace_providers` — this is the CLI/MDM door a laptop re-applies on every boot, so an MDM-tightened base URL narrows here with nobody watching a console), `storage_configured`, `upstream_proxy_configured`, `upstream_proxy_secret_ref`, `upstream_proxy_url` | `internal/api/site_config.go:774` | internal |
| `site_config.test_proxy` | The site-config "test upstream proxy" probe runs | `custom_target`, `elapsed_ms`, `intercepted`, `state`, `target_host` | `internal/api/site_config_probe.go:904` | internal |
| `site_config.test_redirect` | The site-config "test egress redirect" probe runs | `elapsed_ms`, `from_host`, `state`, `to_host` | `internal/api/site_config_probe.go:983` | internal |
| `site_config.test_probe` | An egress-redirect probe run's finalize step (via `finalizeRunTail`; `reclaimProbeRun`'s doc comment explains why the audited name must be this endpoint's own, never `run.compose`) | — | `internal/api/site_config_probe.go:763` | internal |

## Workspace and agent providers

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `workspace_provider.write` | An admin replaces the whole workspace-provider block (`PUT /workspace-providers`). `base_urls` is recorded IN THE CLEAR — a provider address is topology, not a credential, the same disclosure `integration.write` already makes with `egress` — and a narrowing applied by MDM is unreviewable without it. `sources_no_longer_admitted` is how many already-onboarded repo locators the new block refuses, so narrowing is never silent. A block written through `PUT /site-config` instead (the CLI/MDM door) audits as `site_config.write`, whose `git_providers`/`storage_configured` fields exist for exactly that case | `base_urls`, `disabled`, `git_count`, `kinds`, `lanes`, `sources_no_longer_admitted` | `internal/api/workspace_providers.go:788` | internal |
| `agent_provider.write` | An admin replaces the whole agent roster (`PUT /agent-providers`) — which agents this deployment offers, the one model-access lane each may use, and whose credential that is. From 0.7.3 `pins` carries each row's `sso_account_id`/`sso_role_name` as `<account>/<role>`, sorted — the identity this deployment signs with, which a review of a refused capture (`harness.credential.refused`) has no other way to learn. `sso_start_url` IS NEVER RECORDED, and the difference is deliberate: it is the one field of the block that names an organisation's identity provider, the audit log is read by more people than the providers page is, and no review question needs it. `credential_sources` renders an unset row as its effective value, `shared`. A roster written through `PUT /site-config` instead (the CLI/MDM door) audits as `site_config.write`, whose `agent_providers` count exists for exactly that case | `agent_count`, `credential_sources`, `disabled`, `ids`, `mechanisms`, `pins` | `internal/api/agent_providers.go:445` | internal |

## System/reaper sources (no HTTP caller)

These are emitted by background sweepers, not request handlers — `ActorType`
is `types.ActorSystem` and `Actor` is a fixed component name
(`wardyn/lifecycle-reaper`, `wardyn/approval-sweeper`,
`wardyn/recording-sweeper`).

| Action | When | Data fields | Where | Stable? |
|---|---|---|---|---|
| `recording.retention.sweep` | The recordings age-based retention sweep runs (the retention knob `docs/ENV.md:47` names — see `docs/OPERATIONS.md`'s audit-retention paragraph for the asymmetry with the audit log itself, which has no such knob) | — | `cmd/wardynd/adapters.go:849` | internal |
| `k8s.netpol_unenforced` | Boot-time NetworkPolicy canary verdict (`k8sNetpolVerdict`, also published on the anonymous `/healthz`'s `network_policy` field) grades this k8s substrate `unenforced` or `acknowledged` — every sandbox on it runs without a proven default-deny NetworkPolicy. Nil run id (deployment-wide, not tied to a run); silent on `enforced` and on every non-k8s driver | `verdict`, `driver` | `internal/api/reconcile.go:158` (`auditK8sNetpolIfUnenforced`) | internal |

## Notes on completeness

- This table is hand-curated from a source grep, not generated by a build
  step, but it is CI-gated like the closed-enum DB `CHECK` constraints
  `internal/db`'s `TestClosedEnumChecksMatchConstants` pins: a newly-added
  action literal with no row here fails `TestAuditActionsDoc_EveryEmitHasRow`
  / `TestAuditActionsForwardGuardCoversEveryEmitShape`, and a row whose
  citation no longer names what it claims fails
  `TestAuditActionsDocCitationsAreLive` (all three `cmd/wardynd`, gating
  `make ci`'s `test-race`). What is NOT enforced is the `Data` field list
  inside a row — see the next point.
- `Data` field lists were read from the `mustJSON(map[string]any{...})` or
  `Data:` literal at the cited call site; a field marked here can still be
  `omitempty`'d away on a given event (e.g. `error` only appears on a
  `failure` outcome).
- Two related documents cover ground already: `docs/OPERATIONS.md`'s "Every
  denial that isn't a 404" owns `authz.denied`'s `reason` vocabulary in full,
  and `docs/SSH.md`'s "Audit actions" owns the four `ssh.*` actions — both are
  referenced above rather than restated.
