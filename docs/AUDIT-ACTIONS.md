# Audit action vocabulary

Every row in the append-only audit log (`internal/types/types.go`'s
`AuditEvent`) carries a dotted `action` string. There is no enum or schema
file for it — call sites are the source of truth — so this page is the
generated-by-hand vocabulary reference: every action literal live in the tree
today, grep-curated from `internal/api`, `internal/broker`, `internal/lifecycle`,
`internal/approval`, `internal/identity/embedded`, and `internal/groundtruth`,
with its `Data` field names read from that call site. **OPERATIONS.md and
SSH.md point here** rather than re-listing actions themselves.

To reproduce or extend this table: `grep -rn 'auditEvent(\|"[a-z_]\+\.[a-z_.]\+"' internal/api/*.go`
(excluding `_test.go`) finds every call site; each site's `mustJSON(map[string]any{...})`
or `Data:` literal is the field list. **Citations here name a SYMBOL, not a line:**
`path/file.go#Symbol` for a top-level func, `path/file.go#Type.Method` for a method,
`path/doc.md#heading-slug` for a section of another document. CI enforces doc↔emit parity
in both directions: `TestAuditActionsDocCitationsAreLive` resolves every one of them with
`go/parser` and fails unless the action appears inside that symbol's own body
(`cmd/wardynd/audit_actions_doc_guard_test.go`, whose neighbour
`TestAuditActionsDocCitesSymbolsNotLineNumbers` keeps the old `file:line` form from
creeping back), and
`TestAuditActionsDoc_EveryEmitHasRow` plus
`TestAuditActionsForwardGuardCoversEveryEmitShape` check the other direction —
every emitted action literal has either a row here or an entry in
`auditActionAllow` (`cmd/wardynd/audit_actions_forward_guard_test.go`'s own
escape hatch for a literal deliberately outside the registry — empty today,
so nothing is currently excluded this way); all three run in
`cmd/wardynd` and gate `make ci` via `cover-check`'s `test-report` suite. The residual gap is
narrower now than "no CI check" — a `Data:` field can still go undocumented or
drift unguarded (see `internal/api/audit.go`'s `parseAuditFilter` for the
filterable envelope fields, which are stable regardless of action): the
action SET is guarded, the per-action field LISTS are not.

**Stable vs internal**, as the Status column uses them: *stable* means another Wardyn doc
already commits to the name and its fields as an integration point (a SIEM
rule, a filter, an operator runbook) — treat it as a compatibility surface.
*Internal* means today's implementation detail: the name and fields exist
because that's what the call site happened to write, not because anything
outside `wardynd` depends on the shape. Nothing here is versioned; an
internal action can rename across releases without notice.

**Consumers** names what inside Wardyn reads the action back by name — the
places a rename breaks a feature, not only an external SIEM rule. `—` means
nothing in the tree reads it back. A Go reader is cited `file#symbol` and is
checked by the same citation guard as the Where column; a console reader is
named in words.

**Envelope fields added by the hash chain.** Since migration `0047` every event
also carries `prev_hash` and `row_hash` (hex SHA-256, computed by Postgres —
see [OPERATIONS.md](OPERATIONS.md#the-hash-chain--what-a-rewritten-row-looks-like)).
They are action-independent, and they ride the **audit-sink stream**
(webhook/syslog/file) whenever the Postgres write that computes them succeeded,
which is what puts a head hash in your SIEM. An event spooled during a store
outage streams *without* them and is not re-streamed when the spool drains
(OPERATIONS.md, "The audit log can't quietly rot"). `GET /audit`
and `GET /audit/export` do not select them, so they are absent there, and
neither is filterable; the chain is checked through
`GET /api/v1/audit/chain/verify`, not by reading rows back. The sweep emits no
audit action of its own — writing an integrity finding into the log the finding
is *about* would record it in the one place already under suspicion.

## Grammar

Every action is `<noun>[.<sub>].<verb>`: two or three dot-separated
`snake_case` segments, the last an imperative verb from the list below —
never a past tense (`capability.grant.create`, not
`capability.grant.created`) and never a noun (`run.policy.resolve`, not
`run.policy.effective`). A wildcard family row (`egress.*`, `llm.scan.*`) is
held to the same rule through every suffix its row names.
`TestAuditActionsDoc_Grammar` (`cmd/wardynd/audit_actions_doc_guard_test.go`)
checks every action row, and every named family suffix, against this section;
the list is closed, so a new verb is an edit to it that review sees.

**Verbs:** `add` `admit` `alert` `allow` `apply` `approve` `attach` `authenticate` `authorize` `autostop` `block` `build` `bypass` `cancel` `capture` `close` `collide` `complete` `configure` `connect` `create` `decide` `delete` `deny` `detach` `disconnect` `dispatch` `drop` `ending_soon` `enrol` `erase` `exec` `expire` `fail` `forward` `grant` `hold` `ingest` `inject` `kill` `list` `migrate` `mint` `mount` `open` `ping` `promote` `read` `reassert` `reassign` `reconcile` `record` `redirect` `refresh` `refuse` `reject` `rekey` `renew` `request` `reset` `resolve` `revive` `revoke` `rewrap` `scan` `selftest` `set` `skip` `start` `sweep` `synthesize` `takeover` `test` `transfer` `update` `upload` `veto` `write`

Seven exceptions, listed in that test's `auditActionGrammarAllow`: `authz.denied`
keeps its past tense (see "Renamed in 0.8" below for why — the single
heaviest-cited action in the tree and a compatibility surface OPERATIONS.md
already commits to by name). `run.ended`, `run.ended.expired` (the lease's
end-of-run rows, #568) and `auth.signin_unserialized` (the sign-in lock's
no-capacity row) landed on main after this grammar did; an audit action name
is an append-only public code consumers key on, so none of the three is
renamed to fit — the allowlist is the honest record of the gap rather than a
silent exception. The same holds for `run.lost` and `run.lost.expired`, the
lost-run rows (#574) that sit beside `run.ended` and `run.ended.expired` and
name the same kind of kept-run state. `auth.member_mode` is the one compat
row: it is dual-emitted beside `auth.user_view.set` through 0.8.x under its
exact pre-0.8 name (OD-18, #617), which is its whole purpose, and is removed
in 0.9.

A `rule_source` value (the two tables under the `egress.*` row) is
`family:kebab` — lowercase kebab-case segments joined by colons, never a dot;
the same test checks every row of those tables.

## Runs

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `run.create` | Two families. **success**: a run row is created (`POST /runs`, or system-created for a follow-on workspace-step run). **failure**: the dispatch-failure family re-uses this action rather than adding one — sandbox create, a lost `sandbox_ref`, MITM CA provisioning, the subscription and Bedrock inject-grant authors, the `require_inspectable_llm` refusal, the declared-mechanism refusal (an `AgentProviders` row names how this agent reaches its model and the transport that resolved is not that one), the stored-AWS-identity refusal (the captured AWS SSO session this run would carry names an account/role the roster row no longer pins — the pin is bound at capture time, so a capture that predates one is never otherwise compared), an `api_key` grant whose scope would not compile, the per-person Azure DevOps lane's refusals (no per-run certificate authority, an unissuable token mode, a capability outside the row's ceiling, a grant write — `reason` names which), and a run whose autonomy level generates managed settings on a runner whose capabilities could not be read, and the Amazon Bedrock autonomy-grade refusal (#504: the run's autonomy level was graded without a Bedrock model credential and dispatch would hand it one — `reason` `autonomy_grade_drift`), and the model-provider refusals (#528, #529, #530, joined in #551: the provider block cannot be read; the provider the run chose is gone, turned off or no longer serves its agent; its owner's own credential for it is not there — no key or token stored, not signed in to Claude or to AWS, or an AWS sign-in for another pinned account and role or another access portal; the install cannot serve it — no secret store, no Claude sign-in image, a Bedrock provider with no region or model, or the admin token, which is no person; an AWS sign-in renewal AWS did not answer; its credential could not be read; the provider credential grant could not be written; or (owner ruling 2026-09-25) the run's policy carries an `env_secret` grant that would set a model-credential variable, which a governing block refuses whether or not a provider was chosen — `provider` names it) | success: `agent`, `repo`, `policy_id`, `confinement_class`, `confinement_source` (`requested`/`defaulted` — 0.7.8: an unspecified request's enforced class is now the strongest the runner advertises at or above the policy floor, live-probed, so this is the one field that still says whether the caller named `confinement_class` or a runtime resolved it for them), `jti`, `inline_policy`; conditional: `user_type` (0.8: the user type the creator resolved as — the type chosen in the user view, else the one stamped at sign-in; absent for a run with no human creator), `user_view` (0.8: `true` on a run launched in the user view, absent otherwise), `interactive_start`, `seed_auto_tools`, `task_mode`, `tool_approvals`, `clamp_warnings` (every way launch narrowed what the caller asked for — the run-bound record the run-detail "Effective policy" widget reads), `autonomy` (0.8: the resolved autonomy level, the three-axis posture that produced it and `bound_by`, EVERY rubric field that tied at that level — present only for a run under a governance profile whose rubric caps this posture, and the only provenance for the posture, which is graded on a spec launch goes on to widen), `credential_confinement` (closed vocabulary, one value today: `below_floor` — #150: this run's model credential resolved to a stored AWS SSO session delivered to the sandbox at dispatch, after the enforced class was already resolved, under a confinement class weaker than the CC3 floor such a credential would otherwise require; absent covers everything else, never published as a false negative), `model_provider` (`{id, kind}` — #527: this run's model-provider choice, the multi-provider design §2.4 resolution `chooseModelProvider` made. The run row freezes the id alone, `AgentRun.model_provider_id`; this event is the only provenance for the KIND at the moment of choice, since a provider's kind can change later (a kind change mints a fresh UID, #521) and the row would then read a kind the id no longer has. Absent for a run under no provider block, or under a block serving no provider for this agent — today's path). failure: `error`, or `sandbox_ref` + `set_sandbox_ref_error`; the declared-mechanism refusal also carries `reason` (`model_credential` — the machine-readable class the console grades a run's ending by) and `mechanism` (the DECLARED lane, so a console door binds to the refused run's own lane); a model-provider refusal carries `provider` (the id the run chose) and, whenever its kind is known — every state but a deleted/unknown id or a block that could not be read (#532) — `kind` plus that same value under `mechanism`; a credential refusal — one the run owner's own sign-in or stored key repairs: no key or token stored, not signed in to Claude or to AWS, or an AWS sign-in for another pinned account and role or another access portal — also carries `reason` (`model_credential`); the `env_secret` refusal carries `variable` (the sandbox variable, one of `modelEnvNames`) and `grant` (the secret the grant names), and no `reason`; the console's audit reader reads `mechanism` only on such a row, until MP-24 moves it off that key. The create/Review 422 (`enforceRunModelProvider`) carries this same `provider` and `kind` in the wire body whenever it names a provider, and `reason: model_credential` only on a credential refusal, the one class a sign-in or a stored key repairs — the structural fix for #146 defect 2; its 503 for a credential or provider block that cannot be read carries the sentence alone | `internal/api/runs.go#Server.handleCreateRun` (create); `internal/api/runs_dispatch.go#Server.dispatchRun`, `internal/api/runs_dispatch_llm.go#Server.provisionDispatchMITMCA,Server.authorBedrockBearerInjection,Server.enforceInspectableLLM`, `internal/api/provider_subscription.go#Server.authorOAuthSentinelGrant`, `internal/api/runs_dispatch_sso_inject.go#Server.authorBedrockSSOInjection`, `internal/api/runs_dispatch_llm_mechanism.go#Server.enforceConfiguredLLMMechanism`, `internal/api/runs_dispatch_ado_inject.go#Server.refuseADOEntraDispatch`, `internal/api/runs_autonomy_bedrock.go#Server.bedrockCredGradeHolds`, `internal/api/runs_dispatch_provider.go#Server.refuseProviderDispatch`, `internal/api/runs_create.go#Server.persistRunGrants` (failure — and that last one stamps the **grant uuid** as Target, not the run id) | internal | `internal/api/workspace_run.go#dispatchFailureReason`; console run card and New Run review |
| `run.dispatch` | Sandbox dispatch attempted or completed. A **failure** row is also written when the detached launch that runs after `POST /runs` has answered (or after a harness-login run is created) panics — `note` says so — and when a harness-login launch cannot resolve the dispatch ceiling (`error`); in each case the run is then failed with a failure hint rather than left non-terminal | `note`, `sandbox_ref`; failure: `note` (dispatch refused on an unresolved ceiling, dispatch aborted by a concurrent kill/stop, launch panic), `error` (a store error on a lost claim, harness-login ceiling unresolved), `sandbox_ref` (the STARTING->RUNNING transition was lost), `teardown_error` (orphan-sandbox teardown failed; written by `dispatchRun`'s orphan teardown through `stopSandboxOrAudit`) | `internal/api/runs_dispatch.go#Server.dispatchRun`, `internal/api/runs_create_launch.go#Server.finishCreateRunLaunch`, `internal/api/harnesscred_launch.go#Server.finishHarnessLoginLaunch` | internal | `internal/api/workspace_run.go#dispatchFailureReason` |
| `run.build` | BYOI/devcontainer image build for a run, and the workspace-image resolve/build | `byoi_base`, `devcontainer_repo`, `error`, `image`; the workspace path also carries `workspace_id` | `internal/api/runs_create.go#Server.resolveCreateRunImage`, `internal/api/workspace_run_image.go#Server.resolveWorkspaceImage` | internal | `internal/api/workspace_build.go#Server.handleBuildWorkspace` |
| `run.complete` | Run reaches a terminal state | `error`, `exit_code`, `panic`, `state` | `internal/api/runs_lifecycle.go#Server.startCompletionWatcher` | internal | `cmd/wardyn/commands.go#agentExitCode` (the CLI's exit code), `internal/api/site_config_probe.go#hasRunComplete,Server.probeFailureDetail`; console run detail |
| `run.fail` | Run fails before the agent starts | `error`, `from` | `internal/api/runs_lifecycle.go#Server.failAndRevoke` | internal | — |
| `run.kill` | Operator or owner kills a run — or WARDYN ITSELF supersedes one: starting a new harness-login run (AWS SSO or the Claude container login) ends that same person's older login sandboxes for that agent (one live sign-in per person, `supersedeCallerLoginRuns`), because an abandoned one completes its login unattended and its late capture overwrites the one the person just made. A supersede row is actor `system`/`wardynd` and carries `reason` = `superseded_by_new_login`, `superseded_for` (whose sandbox it was) and `superseded_by_run` (the sign-in that replaced it); it audits `success` like any clean kill. WARDYN also ends a sign-in run whose AWS SSO capture it has just STORED, after a short grace so the in-sandbox helper receives its answer first (`killSignInRunAfterCapture`, #151): actor `system`/`wardynd`, `reason` = `sign_in_captured`, `captured_for` (the credential namespace the capture landed in — a person's subject under `per_user`, empty for the shared credential); a run already ended by then (the console's own kill, the banner door) is left alone and gets no second row. **`superseded_for` is the run's `created_by`, which is not always a PERSON:** `admin-token` is the literal name of the shared admin bearer, and a local-mode caller's is the operator name (or whatever `X-Wardyn-Principal` said), so two humans sharing one of those supersede each other and the row names the CREDENTIAL they shared, not which of them clicked. Nothing widens — both are already full operators who can kill any run through the kill route — but a reviewer counting sign-ins per person must read an SSO `sub` as a person and those two as a credential — the reason rides the row's data and is deliberately NOT in the per-step error map, which is what decides `outcome`. **A FAILED supersede reads differently from a failed kill:** the row goes out `outcome=failure` carrying the failing step (`identity_error`, `runner_error`, `broker_error`) BESIDE the supersede fields, and the supersede adds no `run.revoke` row for that failed teardown step and no 500 — the new sign-in proceeds rather than locking the person out over the old run's teardown, and the capture upload's `run_killed` refusal is what contains it. (The cascade's approval-cancel half can still write its own `run.revoke` / `approval_cancel_error` on a supersede, as on any terminal transition — see the `run.revoke` row below.) A `run.revoke` row carrying `identity_error`/`runner_error`/`broker_error` therefore never belongs to a supersede: it is a requested kill, or a stop/completion/sweep/reaper revoke | (run state transition), `broker_error`, `captured_for`, `identity_error`, `reason`, `runner_error`, `superseded_by_run`, `superseded_for` | `internal/api/runs_lifecycle.go#Server.killTeardownTail` | internal | console run detail and audit screen |
| `run.exec` | Exec into a dispatched sandbox. TWO failure sites, not one: the `Exec` call itself, and the `SetRunAgentExecID` write that follows it — a lost exec-id write leaves the boot reconciler unable to tell a healthy run from one whose dispatcher died, so it fails the run here rather than letting the trail say `success` | `argv`, `error` | `internal/api/runs_dispatch.go#Server.startAgentOrIdle` | internal | `internal/api/site_config_probe.go#Server.execSucceeded`; console audit facets |
| `run.files.fail` | A workspace file-browse operation on a run fails | `error` | `internal/api/run_files.go#Server.auditRunFilesFailure` | internal | — |
| `run.interactive.start` | `interactive_start` path taken | `note`, `sandbox_ref` | `internal/api/runs_dispatch.go#Server.startAgentOrIdle` | internal | — |
| `run.resources.fail` | A run-resources query/update fails | `error` | `internal/api/run_resources.go#Server.handleRunResources` | internal | — |
| `run.selftest` | Post-dispatch selftest gate (agent-image liveness check before a run is usable) | `confinement_class`, `detail`, `error`, `exit_code`, `fail_closed` | `internal/api/runs_dispatch.go#Server.startAgentOrIdle` | internal | — |
| `run.record.start` | A Record Mode session starts or fails to start. Carries the O5 cross-user marker `workspace_owner` when the launching admin is not the workspace's owner — recording a MEMBER's workspace mounts their directory and injects their credentials, so it is exactly the act that marker makes queryable | `allow_all_egress`, `confined`, `confinement`, `confinement_class`, `detail`, `label`, `mode`, `record_run_id`, `workspace_owner` | `internal/api/record.go#Server.handleRecordWorkspace` | internal | — |
| `run.record.synthesize` | Record→Promote profile synthesis | `allowed_domains`, `anomalies`, `eligible_grants` | `internal/api/profile.go#Server.handleSynthesizeProfile` | internal | — |
| `run.revoke` | Credential/identity revoked on stop (API path), or the lifecycle reaper's revoke attempt fails. `approval_cancel_error` is the terminal cascade's approval half failing (`cancelRunApprovals`, `internal/api/runs_lifecycle.go#Server.cancelRunApprovals`) — the run is already terminal by then, so like every other field here it is reported rather than propagated, and the stale-PENDING sweeper is the backstop | `approval_cancel_error`, `broker_error`, `identity_error`, `runner_error` | `internal/api/runs_lifecycle.go#Server.revokeRunCascade`; reaper failure at `internal/lifecycle/lifecycle.go#Reaper.emitRevokeFailure` | internal | — |
| `run.autostop` | Idle-timeout autostop fires | `idle_for_sec`, `reason`, `threshold_sec` | `internal/lifecycle/lifecycle.go#Reaper.emitAutoStop` | internal | console run detail |
| `run.ending_soon` | A run's end (`ends_at`) is near: sent once per threshold per end, at 24 hours (only for a run whose end is more than 2 days after its start), 1 hour and 10 minutes before it. A pass that finds a run inside several thresholds sends only the closest. Moving the end re-arms them. Actor `system`. Audit fan-out is the only outbound channel: Wardyn sends no mail | `ends_at`, `files_kept_sec`, `left_sec`, `threshold_sec` | `internal/api/run_lease.go#Server.warnRunEnding` | internal | — |
| `run.end.set` | A person moved a run's end (`ends_at`) through `PATCH /runs/{id}` (#569). Extending within the run's captured `max_end_ahead_sec` (measured from now) is always allowed; shortening, setting an end on a run with none, and `No end` (`to: null`, only where `allow_no_end`) need the run's captured `user_changes_limits`. An over-ask is cut to the limit (`capped: true`). `limits_exempt: true` marks a super admin, bounded by the deployment alone; a security admin is bounded like anyone and reaches only their own runs. **`outcome=denied`** with `reason` is a change the limits refused; nothing was written. A later end or `No end` also re-checks the run OWNER's authority, whoever asks: **`outcome=denied`** with `subject` (the owner) and `reason` `profile_unreadable` (the store could not be read to resolve the owner's governance profile), `profile_gone` (the captured profile no longer exists), `capability_<kind>` (the owner no longer holds the run's `agent`, a `workspace` or a git `workspace_provider`; when someone else asks, the owner is known by sub and stamped user type, as for `run.revive`, so under an enforced kind an admin-owned run is extended by its owner or with an `all`, owner-sub or owner-type allow row) or `owner_unverifiable` | `capped`, `from`, `limits_exempt`, `max`, `reason`, `subject`, `to` | `internal/api/run_end_wait.go#Server.handleSetRunEndAndWait` | internal | — |
| `run.wait_budget.set` | A person changed how long a run's requests wait for a decision (`wait_budget_sec`) through `PATCH /runs/{id}` (#569). Needs the run's captured `user_changes_limits`; capped at the tighter of the run's captured `max_wait_sec` and `WARDYN_APPROVAL_EXPIRY_AFTER` (`max`, `capped`). Open requests follow at once: their `expires_at` is read from the run. `limits_exempt` and **`outcome=denied`** as for `run.end.set` | `capped`, `from`, `limits_exempt`, `max`, `reason`, `to` | `internal/api/run_end_wait.go#Server.handleSetRunEndAndWait` | internal | — |
| `run.ended` | A run reached its end (`ends_at`). **`kept: true`**: it is stopped and kept — pending approvals cancelled (`run_ended`), broker credentials revoked, the agent container stopped but not removed, the proxy sidecar stopped (kept for a revive to read its config back) so it has no network — and stays `RUNNING` (holding its quota slot) with `lost_reason=ended` until `kept_until` (`WARDYN_ENDED_RUN_GRACE`) or a kill. The run identity is not revoked: nothing holds its token once the proxy is gone. The broker revoke on a kept run is re-asserted once per PROCESS (an in-memory guard, not a claim), so on a multi-replica deployment each other replica (and each restart) re-asserts it once, harmless (revoke is idempotent) but expected. **`kept: false`**: it was torn down at its end instead (`STOPPED`, the full revoke cascade) because the grace is 0, the substrate cannot keep a stopped sandbox, or ending it failed (`end_error` — the fail-closed arm); a failed teardown adds a `failure` row carrying `teardown_error` | `end_error`, `ends_at`, `kept`, `kept_until`, `teardown_error` | `internal/api/run_lease.go#Server.endRun` | internal | — |
| `run.ended.expired` | A run kept after its end passed `WARDYN_ENDED_RUN_GRACE` and was torn down (`STOPPED`, the full revoke cascade, the sandbox and its files removed); a failed teardown adds a `failure` row carrying `teardown_error` | `ended_at`, `grace_sec`, `teardown_error` | `internal/api/run_lease.go#Server.leaseRun` | internal | — |
| `run.lost` | A run lost its sandbox. `reason` `reboot`: the watcher found an interactive run's agent container exited but still there. `reason` `outage`: the run's token had not been renewed for over an hour and five minutes (`token_renewed_at`), so its identity is dead and its proxy had given up renewing. **`kept: true`**: it is kept — pending approvals cancelled (`run_lost`), broker credentials revoked, the proxy sidecar stopped (kept for a revive to read its config back) so it has no egress; a `reboot` run's agent stays stopped, an `outage` run's keeps running — and stays `RUNNING` with `lost_reason` until `kept_until` (its end plus `WARDYN_ENDED_RUN_GRACE`; absent when it has no end) or a kill. The run identity is not revoked. **`kept: false`**: the proxy could not be stopped, or the substrate cannot keep a sandbox (`lost_error`), so it was torn down with the full revoke cascade — `FAILED`, or for a `reboot` the state its exit code gives. A headless run, or one past its end and grace, is not kept and gets no `run.lost` row: it is finalized as `run.reconcile` | `kept`, `kept_until`, `lost_error`, `reason`, `teardown_error` | `internal/api/run_lost.go#Server.loseRun`; the re-assert's `kept: false` at `internal/api/run_lease.go#Server.leaseRun` | internal | — |
| `run.lost.expired` | A lost run passed its end plus `WARDYN_ENDED_RUN_GRACE` and was torn down (`STOPPED`, the full revoke cascade, the sandbox and its files removed); a failed teardown adds a `failure` row carrying `teardown_error` | `ends_at`, `grace_sec`, `lost_at`, `reason`, `teardown_error` | `internal/api/run_lease.go#Server.leaseRun` | internal | — |
| `run.revive` | A run was given a new proxy sidecar: a proxy-only **revive** of a run lost to an `outage` (`from` `outage`), a **revive after a reboot** (`from` `reboot`, `agent_started: true`: the kept agent container started again only once the new proxy runs), or an admin's **restart with current limits** of a live run (`from` `live`, `POST /admin/runs/restart`). The actor is whoever asked; the authority is the run's OWNER (`subject`): the new proxy is the old one's own config read back from the stopped sidecar, with a fresh run token minted for the owner and the owner's CURRENT governance profile (`profile_id`, the one captured at create) re-asserted over the frozen policy — its denies unioned in (`denied_added`, `denies_added`), and every injection (`dropped_injection_hosts`) and brokered PAT host (`dropped_broker_lanes`) the profile denies dropped. The policy is never re-resolved and the per-run MITM CA carries over. `proxy_release` is the release that started the new proxy. Refused before anything changes (no row): a profile that no longer exists, a profile that now denies a host the run's git broker needs, a run that `ended`, a run lost to a `reboot` in a bulk restart or on a runner that cannot start its agent, past its end, or one that ended, was lost or was revived while this revive ran. **`outcome=denied`** with `subject` and `reason`, before anything changes: the owner no longer holds the run's `agent`, a `workspace` or a git `workspace_provider` (`capability_<kind>`; an admin's revive or restart knows the owner by sub and the user type stamped on the run, so any deny row covering the value refuses and only `all` rows or allow rows for the owner's sub or stamped type count (a stamp naming a deleted type refuses; an unstamped run counts no type rows) — not the owner's admin role either, so under an enforced kind an admin-owned run is revived by its owner or with such a row), or the model credential the proxy would inject is gone (`model_credential_erased`) or its integration disabled (`model_provider_disabled`). The upstream proxy, trusted CA and model gateways are taken from the current configuration, and the internal-host lift only narrows. **`failure`** with `error` and no `lost_again`: a live run's replace failed before its old proxy was touched (a proxy image that cannot be pulled, a failed inspect), so the run keeps that proxy and its recorded release. **`failure`** with `error` and `lost_again: true`: the run was lost, or its old proxy was or may have been removed with no new one started, so the run was marked lost (`outage`) again with no proxy (a `run.lost` row follows), or torn down when it cannot be kept; a rebooted run whose new proxy or agent did not come up is lost (`reboot`) again with both stopped (`error` starts `start agent:` when the agent failed) | `agent_started`, `denied_added`, `denies_added`, `dropped_broker_lanes`, `dropped_injection_hosts`, `error`, `from`, `lost_again`, `profile`, `profile_id`, `proxy_release`, `reason`, `subject` | `internal/api/run_revive.go#Server.reviveRunProxy` | internal | — |
| `run.agent_policy.write` | The AGENT-SIDE half of a run's resolved autonomy level was generated at dispatch: the managed-settings document `internal/agentpolicy` maps that level to, for the one agent that reads one. Present for a claude-code run whose level is `L0`, `L1` or `L2`, and for every claude-code run on the hold lane (`tool_approvals` `hold`, not interactive) whatever its level, including none: a hold run at no level, `L2` or `L3` gets the `L1` document, whose `allowManagedPermissionRulesOnly` stops a repository or user allow rule from running a tool before the approval gate is asked. Off the hold lane, an unrestricted level, a run no governance rubric bound, and every other agent get no file and therefore no row (the honest signal for an agent that has no agent-side layer is the launch warning on the 201 and the line the image's launcher prints). Written once the agent's container exists — after the sandbox is created, or on an exec-less (krun) runner after the agent's exec succeeds — so a run whose file the driver refused (the Docker driver refuses a managed-file run whose image USER is root or whose `/etc` the workload could write) gets `run.create` or `run.exec` `failure` instead, and no row. **`delivered` is the field to read:** `true` only when the runner advertises `Capabilities.ManagedFiles` and the sandbox spec carried the file, which the driver places root-owned before the agent starts; `false` means the run is going under its CLI-flag levers alone, with `reason` saying why (the create response carries the same fact as a warning). On the exec-less krun runtime the file is still placed but the row records `false` with `reason` "unverified on this runtime: libkrun may run the guest as root", because libkrun may not apply the image's non-root `USER`. A run whose runner's capabilities cannot be read is failed at dispatch (`run.create` `failure`) rather than recorded here — the difference between a ceiling the agent is running under and one only the control plane knows about. `actor_type` `system` (`wardynd`) | `agent`, `bytes` (the document's size, never its body), `delivered`, `level`, `path`; conditional: `reason` (only when `delivered` is false), `tool_approvals` (`"hold"`, only on a hold-lane run; at `level` `""`, `L2` or `L3` it is why the row exists) | `internal/api/runs_dispatch_agentpolicy.go#Server.auditAgentPolicy` | internal | — |
| `run.reconcile` | The orphan-sandbox reconciler acts on a run at boot | (reconcile outcome) | `internal/api/reconcile.go#Server.reconcileFinalize` | internal | — |
| `sandbox.orphan.sweep` | The boot-time orphan reconciler (`reconcileOrphanedSandbox`) FAILS to tear down a terminal run's still-live sandbox — emitted on failed teardown only; a still-failing teardown leaves the ref set for the next boot to retry | `sandbox_ref`, `teardown_error` | `internal/api/reconcile.go#Server.reconcileOrphanedSandbox` (`stopSandboxOrAudit`, `internal/api/reconcile.go#Server.reconcileOrphanedSandbox`) | internal | — |
| `sandbox.sweep.request` | An OPERATOR triggered a sandbox sweep via `POST /api/v1/admin/sandboxes/sweep` — emitted on SUCCESS, recording who asked and how many were swept. Distinct from `sandbox.sweep`, which the primitive emits on failed teardown only. The boot pass runs the same primitive without this row, since nobody asked for it | `swept` | `internal/api/runs_lifecycle.go#Server.handleSweepSandboxes` | operator | — |
| `sandbox.sweep` | `SweepTerminalSandboxes` (the callable sweep primitive over terminal runs with a live probed sandbox) FAILS to tear one down — emitted on failed teardown only, like its boot-time sibling | `sandbox_ref`, `teardown_error` | `internal/api/runs_lifecycle.go#Server.SweepTerminalSandboxes` (`stopSandboxOrAudit`, `internal/api/runs_lifecycle.go#Server.SweepTerminalSandboxes`) | internal | — |
| `run.artifact.redirect` | Package-registry (artifact) redirect configured for a run | `detail`, `ecosystem`, `error`, `header`, `host`, `integration_id`, `port`, `secret_name` | `internal/api/artifact_redirect.go#Server.planArtifactRedirect` | internal | — |
| `run.policy.resolve` | The effective (post-merge) policy snapshot is recorded at dispatch | (full policy snapshot); plus `disk_mib_filled` when the scratch size was filled in from the org's `storage.ephemeral.default_disk_mib` rather than authored on the policy — the provenance a reader needs to know a host that cannot keep that size runs the sandbox uncapped instead of refusing it at create | `internal/api/runs_dispatch.go#Server.dispatchRun` | internal | `internal/api/runs_policy.go#Server.effectiveUIApps` (the UI gateway's app list) |
| `run.ceiling.reassert` | A governance profile applies to the run's creator, so dispatch re-asserts the profile's denied hosts AFTER the phases that add corporate hosts and credential lanes. Emitted on EVERY walled run from EVERY dispatch lane — an ordinary create, a record/verify session, a workspace source scan, a site-config probe and the managed-harness login all hand dispatch a resolved ceiling, so the absence of this row means "no profile applies", never "this door skipped the ceiling" — including one with nothing to drop — "which ceiling did this run actually run under" is the question `run.policy.resolve` cannot answer, and the drops themselves are invisible there because injections and broker grants ride `ProxyConfig`, not the spec. `actor_type` `system` (`wardynd`) | `denied_added`, `dropped_broker_lanes` (the git/PAT broker keys withheld, plus `bedrock` when the profile denies a host this run's Bedrock lane reaches — the resident SigV4 keys, the captured-SSO env, the host `~/.aws` bind-mount and the per-run MITM entry all go with it), `dropped_injection_hosts`, `note`, `profile`; and when the profile sets `max_ephemeral_disk_mib` (0.7.2), that limit plus the `ephemeral_disk_mib` the run ended up with — the size half of the same ceiling, absent for a profile that sets no size | `internal/api/runs_dispatch_ceiling.go#Server.reassertCeilingDenies` (`reassertCeilingDenies`) | internal | — |
| `run.upstream_proxy.resolve` | Upstream (corporate) proxy resolution for a run | `reason` | `internal/api/runs_dispatch_mounts.go#Server.resolveRunUpstreamProxy` (`resolveRunUpstreamProxy`) | internal | — |
| `run.bedrock.configure` | Bedrock LLM credential/config wired into a run | `detail`, `endpoint`, `hosts`, `mode`, `model`, `provider` (0.8: the model provider the run chose, absent on the legacy lanes), `region` | `internal/api/runs_dispatch_llm.go#Server.resolveLLMInjections` | internal | — |
| `run.subscription.inject` | Subscription OAuth proxy-side inject wired into a run (see `subscription-proxy-inject` — the live OAuth token is injected proxy-side, never resident in the sandbox). `source=provider` (0.8) is a run on an `anthropic_subscription` model provider, credentialed by its owner's own Claude sign-in | `detail`, `host`, `source`, `tls_mitm`, `provider` (0.8, `source=provider` only: the chosen provider's id) | `internal/api/provider_subscription.go#Server.authorOAuthSentinelGrant` | internal | console audit facets |
| `run.llm_inspection.resolve` | LLM egress content-inspection secret names resolved for a run | `missing`, `names`, `reason`, `resolved`; conditional: `reserved_skipped` (names refused by the sink guard, and `bedrock-api-key`, which is read only for the grant dispatch authors) | `internal/api/runs_dispatch_secrets.go#Server.resolveLLMInspectionSecrets` | internal | — |
| `llm.scan.*` | An outbound LLM content-inspection pass completes for a run's egress — the literal suffix is `sc.Action` (`llm.scan.alert`, `llm.scan.block`, `llm.scan.skip`, `llm.scan.bypass`, or `llm.scan.fail` when the scanner failed or the body did not parse, per `egress.ScanSummary.Action`); security-relevant, and deliberately CONTENT-FREE — `findings` carries detector names, field paths, offsets, counts and masked samples only, never the matched bytes. `finding_count` is the pre-cap total the proxy COUNTS as it produces findings and sends as `findings_total` — it is NOT `len(findings)`. `findings_reported` is `len(sc.Findings)`, the count the proxy itself reported to the daemon after the proxy's own cap; `finding_count` exceeds `findings_reported` ONLY when `findings_capped` is true — the converse does NOT hold, since block mode's severity keep-backs can leave the two equal on a capped scan, so `findings_capped` (not the comparison) is the truncation signal. The embedded `findings` array is separately truncated to the first 100 records (`maxAuditFindings`) by this handler, so on a capped scan `findings` is SHORTER than `findings_reported`, and neither `finding_count` nor `findings_reported` equals `len(findings)`. `findings_past_cap` is an upper bound on what was pushed out past the cap, because block mode's severity keep-backs are counted past the cap and still reported alongside it. `llm.scan.fail` records `outcome=failure`; every other suffix follows the usual success/denied rule | `channel`, `coverage`, `finding_count`, `findings`, `findings_capped`, `findings_past_cap`, `findings_reported`, `host`, `mode`, `scanned`, `skipped`, `skip_reason` | `internal/api/internal.go#Server.recordLLMScanAudit` (`recordLLMScanAudit`) | internal | — |
| `run.injection.drop` | Dispatch removed a proxy injection before the proxy was handed its list (one event per injection). `reason` is one of three: `bedrock_bearer_not_dispatch_authored` — an injection naming `bedrock-api-key` that dispatch did not author (a stored or default policy's grant, a recorded profile's); the key is injected only through the grant dispatch authors, which records whose key it is, and the sink refuses any other; `model_credential_not_provider_authored` — under a model-provider block, the provider path's one strip, which runs before any arm authors: every injection that would credential the run's model other than the grant its chosen provider's arm authors after it (a provider key or sign-in name, a subscription, managed or AWS SSO sentinel, `bedrock-api-key`, or any grant on a model vendor's host (every harness's, not only the run's own), the boot gateway that re-points one, or a host the chosen provider's arm credentials — its route-through or endpoint host, its Bedrock data-plane and control hosts, its AWS access-portal host), so no operator credential serves a provider run; on a run that chose a provider the row also carries `provider`; `model_provider_not_dispatch_authored` — with no provider chosen, an injection naming a `wardyn-provider-` credential, which only the provider arm authors. `outcome=denied`; `Target` is the dropped **grant uuid**, not the run id. The run continues, on the grant dispatch authored for it if it has one | `grant_id`, `host`, `reason`, `secret_name` | `internal/api/runs_dispatch_llm.go#Server.dropUnauthoredBedrockBearerInjections`, `internal/api/runs_dispatch_provider.go#Server.auditDroppedInjection` | internal | — |
| `run.env_secret.resolve` | One `env_secret` grant resolved store→sandbox env at dispatch (one event per grant). `outcome=failure` means the grant was SKIPPED — `reason` says why (unreadable scope, reserved secret name, `bedrock-api-key` — proxy-injected only, never resident — no secret store, unresolvable secret, or a refusal to overwrite a platform-authored variable that is already set to a non-empty value) — and the variable is left ABSENT, never blank-set. Carries the variable name and the SECRET NAME only; the value is never logged, and is mask-registered for the run | `name`, `reason`, `secret_name` | `internal/api/runs_dispatch_secrets.go#Server.resolveEnvSecretGrants` | internal | — |
| `run.egress.add` | A launch lane widened the run's egress allowlist — one row per lane that added anything, `kind` naming the lane: `workspace` (hosts an operator approved for a referenced workspace), `workspace_clone` (a referenced workspace's non-GitHub clone host), `site_config` (the site-config SCM hosts, unioned only when the run declares a repo), `ssh` (the SSH gateway's CONNECT endpoints), `git_pat` (a `git_pat` grant's hosts) | `added_domains`, `kind` | `internal/api/runs_create.go#Server.unionRunEgress` | internal | — |
| `run.git_pat.drop` | A `git_pat` grant is withheld at dispatch because the run is brokered for that forge — the git-broker route is its only route to it by name, so withholding the PAT is load-bearing, not belt-and-braces | `dropped_hosts`, `note` | `internal/api/runs_dispatch.go#Server.dispatchRun` (`auditBrokeredGrantDrop`, `internal/api/runs_dispatch.go#Server.dispatchRun`) | internal | — |
| `run.ssh.drop` | An `ssh_key` grant's hosts are dropped from a run, `reason` saying why: `brokered_forge` — withheld at dispatch for the same reason as `run.git_pat.drop` (target: the run id); `unsupported_agent` — an SSH-enabled run launched against an agent image without SSH support, so the whole SSH host set is dropped at create (target: the agent) | `dropped_hosts`, `note` (`brokered_forge` only), `reason` | `internal/api/runs_dispatch.go#Server.dispatchRun` (`auditBrokeredGrantDrop`, `internal/api/runs_dispatch.go#Server.dispatchRun`), `internal/api/runs_create.go#Server.applySSHLaneWarnings` | internal | — |
| `run.provider.veto` | A git credential lane the deployment's workspace-provider policy does not PERMIT, so the run was wired without it. Providers mint nothing — they veto — and this is the veto: the `applySSHLaneWarnings` shape, drop the wiring and say so on the 201. The lane is decided by the row that ADMITTED the repository (two rows of one kind on one host are a supported configuration, so "whichever row claims the host" is not an answer); only a grant whose scope names no repository at all falls back to the union of the rows claiming its host, permitting any lane any of them permits. The eligibility GRANT ROW stays where the policy authored one (it records what the policy asked for, not an issued credential); what is dropped is the sandbox wiring, and for the `pat` lane the ADO egress bundle that arm would have added with it — nothing else adds those domains, so a veto that left them would hand the run reachability for a forge it can no longer authenticate to. **`Target` is the provider KIND, never the row id**: `auditScope` lets a run's creator read their own run's rows, and a row id is on the far side of the disclosure rule (the operator-facing 201 warning names it; this durable row a member can read does not). Emitted from all five grant sites: the three `persistRunGrants` arms (`github_token`/`git_pat`/`ssh_key`) and the two launcher-side helpers (`maybeGitHubReadGrant`, `maybeSSHKeyGrant`), which have no warnings channel — for those this row IS the record. `actor_type` `system` (`wardynd`) | `grant`, `kind`, `lane` | `internal/api/workspace_admission.go#Server.laneVetoed` (`laneVetoed`) | internal | — |
| `run.provider.admit` | A repository was admitted over SSH at the HOST level by a row whose base URLs carry an ORG PATH — the documented SSH scoping ceiling, recorded the one time it actually costs something. An SSH clone URL carries no path a base URL can be compared against (`git@github.com:acme/x.git` is not `/acme/x`), so `https://github.com/acme` admits an SSH clone of ANY org on github.com with the deployment's `ssh-key-github-com` secret. This is the ONLY admission outcome that is WIDER than the policy reads, so it is never silent: run create also puts it on the 201 as a warning, and the remedy is the row's own `lanes` list (drop `ssh` and the addresses bind again, over the one transport that carries a path). The `run.provider.veto` SHAPE exactly — outcome `failure`, **`Target` is the provider KIND, never the row id**, for the same disclosure reason: a run's creator can read their own run's rows. Emitted only from run create, the one door with a warnings channel to pair it with; a source onboarded at another door is re-asked here at launch. Absent on a deployment with no provider rows, and on a row whose base URLs bound the whole host anyway — nothing was widened. `actor_type` `system` (`wardynd`) | `host`, `kind`, `lane` | `internal/api/workspace_admission.go#Server.sshHostLevelWarnings` (`sshHostLevelWarnings`) | internal | — |
| `run.workspace.collide` | Launch refused because the workspace is already in use by another run | `other_runs` | `internal/api/runs.go#Server.warnWorkspaceCollision` | internal | — |
| `run.workspace_cred.resolve` | Workspace-scoped integration credential resolved for a run | `integration_ref`, `type` | `internal/api/runs_create_fields.go#Server.recordCreateFolds` | internal | — |
| `run.requirement.allow` | A workspace-needs-scanner egress requirement is satisfied at launch | `added_domains`, `workspace_id` | `internal/api/runs_create_requirements.go#Server.applyWorkspaceRequirementsFor` | internal | — |
| `run.requirement.inject` | A workspace-needs-scanner integration requirement is satisfied at launch | `header`, `injected_hosts`, `integration_id` | `internal/api/integrations_run.go#Server.applyIntegrationRequirement` | internal | — |
| `run.requirement.grant` | A workspace-needs-scanner secret requirement is satisfied at launch | `host`, `secret_name` | `internal/api/runs_create_requirements.go#Server.applyRequiredSecretGrant` | internal | — |

## Workspaces & sources

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `workspace.create` | `POST /workspaces` | `name`, `owned_by`, `sources` | `internal/api/workspaces.go#Server.handleCreateWorkspace` | internal | — |
| `workspace.update` | `PUT /workspaces/{id}` | `image_changed`, `name`, `rescan_required`, `sources` | `internal/api/workspaces.go#Server.handleUpdateWorkspace` | internal | — |
| `workspace.delete` | `DELETE /workspaces/{id}` | — | `internal/api/workspaces.go#Server.handleDeleteWorkspace` | internal | — |
| `workspace.scan` | A workspace directory/repo scan (needs-scanner) runs | `detail`, `reason`, `scan_run_ids`, `sources`, `workspace_id` | `internal/api/workspace_run.go#Server.reconcileWorkspaceRun`, `internal/api/source_scan.go#Server.scanAttachedSources` | internal | — |
| `workspace.record` | The "workspace record" onboarding-import run completes | `anomalies`, `domains`, `kernel_sensor_blind`, `minted_grants`, `mode`, `task` | `internal/api/workspace_run.go#Server.reconcileRecordRun` | internal | — |
| `workspace.requirement.write` | An admin/member edits a workspace-needs requirement from the approval flow | (requirement diff) | `internal/api/approvals_writeback.go#Server.learnVerifyEgress` | internal | — |
| `workspace.requirements.write` | An admin/member replaces the workspace's whole requirements contract (`PUT /workspaces/{id}/requirements`) — distinct from the singular `workspace.requirement.write` above (a single-requirement edit from the approval flow); this is the map-wide replace | `count` | `internal/api/workspace_requirements.go#Server.handleSetWorkspaceRequirements` | internal | — |
| `workspace.envcode.write` | The onboarding "env code" (devcontainer/setup snippet) is written for a workspace | `files`, `skipped`, `skipped_files`, `written_files` | `internal/api/workspace_envcode.go#Server.handleWriteEnvAsCode` | internal | — |
| `workspace.egress.approve` | Operator/member approves a pending workspace egress decision (`always`/`session` scope write-back), **or the boot heal replays one** — `source` tells them apart (`approval:<id>` vs `boot-heal`), and the heal also emits `failure` for a verdict the live path would now refuse | `domains`, `source` | `internal/api/approvals_writeback.go#Server.persistWorkspaceEgressDecision`, `internal/api/approvals_reconcile.go#Server.auditHealDecision`, `internal/api/record.go#Server.handlePromoteRecordEgress` | internal | — |
| `workspace.egress.deny` | Operator/member denies a workspace egress decision, **or the boot heal replays one** (same `source` split as the approve row above) | `domains`, `source` | `internal/api/approvals_writeback.go#Server.persistWorkspaceEgressDecision`, `internal/api/approvals_reconcile.go#Server.auditHealDecision`, `internal/api/workspaces.go#Server.handleSetDeniedEgress` | internal | — |
| `workspace.llm_cred.set` | An operator binds (or clears) the model/harness credential for a workspace/container (`PUT /workspaces/{id}/llm-cred`): an AI integration (`integration_ref`) and/or a model provider (`provider_ref`), each `""` when unset | `integration_ref`, `provider_ref` | `internal/api/workspaces.go#Server.handleSetWorkspaceLLMCred` | internal | — |
| `workspace.reassign` | An admin returns a member-owned workspace to the operator, `owned_by=""` (`POST /workspaces/{id}/reassign`) — the offboarding path for a member who has left | `from_owner` (the departed member's principal; `""` when the row was already operator-owned) | `internal/api/workspace_owner.go#Server.handleReassignWorkspace` | internal | — |
| `workspace.provider.admit` | A repository was admitted ONLY because `site_config.scm_hosts` still names its host and NO provider row claims it — the one-release grace that keeps an onboarded GitLab/Bitbucket repo launchable the day the first GitHub provider row is added, and the state an admin has until 0.8 to close. Emitted at EVERY admission door (`admitRepoSources`, `admitLauncherRepo`), not only run create: the two server-side launchers carry no warnings field, so a record/scan clone on such a host would otherwise pass in silence. Every door that ANSWERS A BODY also says it out loud as a warning on the success response — run create's 201 and the three onboarding doors, `POST`/`PUT /workspaces` and `POST /sources` (V2/F1: onboarding a GitLab workspace through the wizard used to succeed silently, so the admin who must close the state first heard about the host from whoever launched the first run). One row per HOST rather than per repo, because the fact is about the host; no run id, because most of these doors have none. Absent on a deployment with no provider rows — nothing is admitted "only because" of the legacy list when the legacy list is the whole policy. `actor_type` `system` (`wardynd`) | `host` | `internal/api/workspace_admission.go#Server.auditLegacyHostAdmissions` (`auditLegacyHostAdmissions`) | internal | — |
| `source.write` | A workspace source (dir/repo) is added or updated | `kind`, `locator`, `ref`, `writable` | `internal/api/sources.go#Server.handleCreateSource` | internal | — |
| `source.delete` | A workspace source is removed | `detached_from`, `forced` | `internal/api/sources.go#Server.handleDeleteSource` | internal | — |
| `source.scan` | A single source's onboarding scan runs | `ai_advisor`, `ai_changed`, `confidence`, `detail`, `leak_findings`, `reason`, `scan_run_id`, `secret_reqs` | `internal/api/workspace_run.go#Server.reconcileWorkspaceRun`, `internal/api/source_scan.go#Server.handleScanSource` | internal | — |

**`workspace_owner` — the cross-user marker on every workspace-scoped write.**
Any event in this section whose target is a MEMBER-OWNED workspace
(`workspaces.owned_by` non-empty, migration `0048`) carries one extra Data
field, `workspace_owner`, **whenever the actor is not that owner** — i.e.
whenever an admin acts on a member's workspace for support or offboarding. That
includes the approval write-backs (`workspace.egress.approve`,
`workspace.egress.deny`, `workspace.requirement.write`), which are the most
common cross-user path of all: deciding an approval is owner-OR-admin, so an
admin choosing `always` on a member's run durably rewrites that member's
workspace. One rule stamps them all, in `internal/api/workspace_owner.go` —
`auditWorkspaceData` for a handler that holds the request,
`auditWorkspaceDataFor` for a writer that holds the acting principal as a
string — and it is what makes cross-user admin access QUERYABLE rather than
merely present in the log: filter `?actor=<admin>` and keep the rows where
`workspace_owner` is set and differs.

The deliberate exception is two EMIT SITES, not two actions: the `wardynd`-actor
emits that settle a governed run — `reconcileWorkspaceRun`'s `workspace.scan` and
`reconcileRecordRun`'s `workspace.record`, both in `internal/api/workspace_run.go`
— have no acting human to compare the owner against, so they never stamp. The
human who launched that run is recorded on the run, not on these events. The
`workspace.scan` ACTION is not exempt: emitted from `scanAttachedSources`
(`internal/api/source_scan.go`) it carries the requesting human as actor and
stamps `workspace_owner` like every other row above.

The actor is never rewritten to match. An admin acting on a member's workspace
is recorded as the ADMIN — there is no impersonation anywhere on this path, and
`TestWorkspaceOwner_NoImpersonation` (`internal/api/workspace_reassign_test.go`)
pins it across the write surface. An operator-owned workspace (`owned_by=""`,
every pre-0.6 row) never stamps the field, so an admin-only deployment's audit
Data is unchanged.

## Sessions, SSH & UI relay

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `session.attach` | A human attaches to a run's tmux session (console or CLI) | `cols`, `error`, `lane`, `reason`, `rows`, `sandbox_ref`, `transport` | `internal/api/attach.go` | internal | — |
| `session.detach` | A session detaches | `read_only`, `reason`, `transport` | `internal/api/attach.go` | internal | — |
| `session.takeover` | A second viewer takes over a held session (the write-lane single-holder rule) | `held_since`, `previous_holder`, `previous_source`, `taken_over` | `internal/api/attach_holder.go#Server.handleAttachTakeover` | internal | — |
| `session.promote` | A read-only observer became the WRITER on the socket it already had: the writer released the PTY and the oldest queued observer inherited it, or a take-over promoted the taking principal's own observer. Write authority over a live sandbox changed hands without anyone asking for it, which is why it is recorded even though it grants no new capability — every observer passed the same owner-or-admin gate when it connected. The actor is the PROMOTED principal; a take-over's own `session.takeover` row names the taker | `previous_holder`, `principal`, `source` | `internal/api/attach_holder.go#Server.announceAttachPromotion` | internal | — |
| `session.recording.write` | A recording is attached to / detached from a session | — | `internal/api/attach.go#Server.newSessionRecorder` | internal | console run detail (the session recordings list) |
| `recording.upload` | A sandbox uploads an asciinema-cast chunk for a run's recording session — audited on both outcomes, like every sibling recording lane, since a full store or an over-cap upload is exactly how a long session's provenance gets lost | `error` (failure only) | `internal/api/recording.go#Server.handleUploadRecording` | internal | — |
| `ssh.authenticate` | Every SSH-gateway connection attempt, success or failure — `docs/SSH.md` names this one **stable** and documents it as the residual-#19 correlate | `override`, `reason` | `internal/api/sshgateway.go#Server.sshAuditAuthFailure` (failure), `internal/api/sshgateway.go#Server.sshVerifiedAuth` (success); documented `docs/SSH.md#bounds` | **stable** (documented) | — |
| `ssh.channel.reject` | An SSH channel was REFUSED by the per-run concurrent-channel cap (`maxSSHSessionsPerRun`). Emitted for BOTH channel types, and `channel_type` distinguishes them — `session` (shell/exec/sftp) and `direct-tcpip` (`-L` forwards) draw on ONE shared counter, inverting the OpenSSH model where `MaxSessions` scopes to session channels only. Before 0.7 a refusal was invisible to the deployment: the client saw `ResourceShortage` and nothing was recorded — which matters now that the desktop envelope ships `WARDYN_SSH_LISTEN` on by default | `channel_type`, `reason`, `max` | `internal/api/sshgateway.go#Server.sshAuditChannelRejected` | human | — |
| `ssh.exec` | A command executed over the SSH gateway (argv + exit code only — no content) | `argv`, `error`, `exit` | `internal/api/sshgateway_channels.go#Server.bridgeSSHExec`; documented `docs/SSH.md#bounds` | **stable** (documented) | — |
| `ssh.sftp.transfer` | An sftp transfer over the SSH gateway (byte count only — no payload/filenames) | `bytes`, `error` | `internal/api/sshgateway_channels.go`; documented `docs/SSH.md#bounds` | **stable** (documented) | — |
| `ssh.forward` | An `ssh -L` port-forward session | `bytes`, `error`, `port` | `internal/api/sshgateway_channels.go`; documented `docs/SSH.md#bounds` | **stable** (documented) | — |
| `ssh_key.add` | A human registers an SSH public key (`POST /me/ssh-keys`). `capped` is a marker, present (`true`) only on a key registered while the session was in the user view (member mode), which never gains the admin override (migration `0070_ssh_key_view_capped`, `docs/SSH.md#bounds`) | `capped`, `name` | `internal/api/sshkeys.go#Server.handleAddSSHKey` | internal | — |
| `ssh_key.delete` | A human removes a registered SSH public key | — | `internal/api/sshkeys.go#Server.handleDeleteSSHKey` | internal | — |
| `ui.authorize` | A UI-sandbox relay session is authorized or denied | `app`, `host`, `port`, `reason` | `internal/api/uigateway.go#Server.handleUIEnter`, `internal/api/uigateway_session.go#Server.uiDenyReassert` | internal | — |
| `ui.open` | A UI-sandbox relay session is opened | `app`, `port` | `internal/api/uigateway.go#Server.uiDial` | internal | — |
| `ui.close` | A UI-sandbox relay session closes | `app`, `duration_sec`, `port` | `internal/api/uigateway.go#Server.uiDial` | internal | — |
| `ui.start` | The relay starts (or fails to start) the sandbox-side app process | `app`, `launcher`, `port`, `reason` | `internal/api/uigateway.go#Server.uiEnsureApp` | internal | — |

## Secrets, credentials & identity

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `secret.write` | An admin or member writes a secret in their OWN namespace (0.7, migration `0050`: self-service — see OPERATIONS.md's "Secret ownership"). Outcome `denied` with `reason` `owner_param`: an admin named `?owner=` on the PUT, which is refused for everyone since 0.8 (credential-storage design K7-A) — a credential is set only by the person it belongs to, so the row records the attempt and nothing is written (a member naming `?owner=` gets `authz.denied` instead, as on the other secrets routes). Outcome `failure` with `reason` `row`: in store mode the value reached the external store but its row was not written (credential-storage design rule 18), so the store may already serve the new value behind the 500; Wardyn's own writes (a captured or refreshed Azure DevOps or AWS sign-in, actor `system`; a pasted harness credential) record the same failure | `secret_owner` (the non-"" namespace written; absent for the operator's own "" namespace), `reason` (`owner_param` on the refusal, `row` on the failure above) | `internal/api/secrets.go#Server.handlePutSecret`, `internal/api/secrets.go#Server.auditRowNotWritten` | internal | — |
| `secret.delete` | An admin or member deletes a secret, same ownership rule as `secret.write` | `secret_owner` (absent for ""), `owner_known` (present, and `false`, only when an admin's `?owner=` matched no principal this deployment knows and was taken verbatim — byte-identical, from the outside, to a typo whose namespace nobody uses; the marker is what tells the two apart); when the value lived in an external store that reports what it kept (`azurekv`): `store`, `purged` (false when the vault refused the purge or `WARDYN_AZURE_KV_PURGE=never`), `recoverable_days` (the vault's soft-delete retention, when not purged) | `internal/api/secrets.go#Server.handleDeleteSecret` | internal | — |
| `secret.list` | An admin enumerates a namespace through the admin-only `?owner=` surface (`GET /api/v1/secrets?owner=…`). ENUMERATION, NOT DISCLOSURE: this route returns secret NAMES only — no value is read, decrypted or returned (that is `secret.read`, at injection time, which carries `grant_id`/`jti`). Emitted ONLY for an explicit `?owner=`, because a member listing their own namespace is the console's ordinary poll on every page load and auditing it would bury the cross-user reads this row exists for. It closes the asymmetry where every WRITE through `?owner=` stamped `secret_owner` and the READ recorded nothing. `Target` is the namespace | `names` (how many were enumerated — a COUNT; the names themselves are never recorded), `secret_owner` (the namespace read) | `internal/api/secrets.go` (`handleListSecrets`) | internal | — |
| `secret.read` | A stored secret is read, or a secret is resolved for injection into a run (or fails to). **Every store read is recorded exactly once** (credential-storage fail-closed rule 23): the `secretstore.Audited` decorator wardynd wraps its one store in records each `Get` with the purpose its caller put in the context; the injection sinks below record their own, richer row and mark the read `secretstore.SiteAudited`, so the decorator stays silent for it; the boot conversion of legacy (v0) rows records one row per row it converted (`purpose` `boot`) — an aborted conversion (a v0 row that will not decrypt) commits nothing and records nothing — the rows it opened first are recorded by the boot that converts them; the abort itself is in the boot log, naming the row; `wardynd -migrate-secrets` records one per value it reads on its way to another store (`purpose` `migrate`, `actor` `wardyn/migrate-secrets`, `to` the target). A `Get` that finds no row records nothing: no value was read. A `Get` whose context carries neither mark is refused before the store is read and recorded as a failure with purpose `unmarked`. `TestEverySecretReadIsAuditedOnce` (`cmd/wardynd/secret_read_audit_guard_test.go`) type-checks every `Get` site, and every call of the pg store's bulk readers that open values without a `Get` (`ConvertV0`, `Migrate`), and fails on one whose context is neither a mark nor a parameter every caller passes marked; it also fails on a new pg-store function that opens a value (locally or through the external store, `vaultkv` and `azurekv` included) without being classified there. `wardynd -reconcile` reads store metadata only, never a value, so it records `secret.reconcile`, not `secret.read`. A decorator row goes through the same recorder chain as every audit writer (masked, fanned out, and on a failed write spooled to the local fallback, `-audit-spool`); a read never waits on or fails for its audit write. The captured AWS SSO, Azure DevOps and managed-subscription sinks read a stored blob to derive what they inject, so each resolve there is two rows with different targets: the stored blob's read (`sso-refresh`, `ado-refresh`, `managed-token`) and the injected sentinel's | Decorator rows (actor `wardynd`, system): `purpose` (`boot`, `broker-mint`, `dispatch`, `managed-token`, `migrate`, `sso-refresh`, `ado-refresh`, `status` — a readiness or status check that reads a value to grade it, never to use it; `unmarked` — a refused read whose caller set no purpose), `owner` (the namespace the read was made for; `""` is the operator's), `store` (the backend: `pg`, or `vaultkv` or `azurekv` in store mode), `ref` (where the row keeps its value — for `pg`, the row's `kek_id`; absent on a legacy v0 row, which has none), `row_owner` (whose row was opened: `""` when a member's read fell back to the operator's row; present whenever the store found the row, a `migrate` row included). `ref`, `row_owner` and a `migrate` row's `Target` are the row's own text, so each is cut to 512 bytes and its non-printing characters become `?`. `Outcome` is `failure` when the row exists but is refused; the store's error text is never recorded. Migrator rows (actor `wardyn/migrate-secrets`, system): `purpose` `migrate`, `owner`, `to` (the migrator's target). Sink rows: `grant_id`, `host`, `jti`, `owner` (0.7: the namespace consulted — the run creator's principal string; for an operator-created run that is the operator's own identity string (`admin-token`, `local:<op>`, an admin's sub), under which no row is ever stamped, so the lookup falls back to the operator row), `purpose`, `reason` (on a failed read of a stored credential — the api_key and Bedrock bearer sinks answer it through `internal/api/injection.go#storeReadRefusal`: `store_unavailable` — the store or the database did not answer, answered 503, transient: the proxy keeps its last value for up to 15 minutes past its expiry; otherwise definitive, and the proxy drops the value at once: `not-found` — no such row, 424; `refused` — the row exists but the store refused it, 424; `store_error` on the captured-AWS-SSO arm and `store_refused` on the per-person Azure DevOps arm, 403; `invalid_header_name` and `reserved_secret_name` — refused before any read, 403), `source`, `credential_source` (0.7.6, on the captured-AWS-SSO injection arm: which lane's session was resolved; on the Bedrock bearer arm: whose namespace the key was read from). On the Bedrock bearer arm `owner` is the namespace the key was READ from — the one dispatch recorded on its grant, `""` for the operator's — not the run creator; on the per-person Azure DevOps arm (0.7.10): `provider_row`, `organisation`, `capabilities`, `drift` (which snapshot field moved, on a `scope_changed` refusal) and `granted_scope` — the scope string the Entra authority reported for the minted token, verbatim, never parsed out of the token; on the model-provider key arm (#528, and since #551 a Bedrock key provider's too): `owner` is the run's own subject, the only namespace the key is read from, and `provider_uid` names the provider the dispatch-authored grant recorded; its refusals carry `reason` = `missing_scope_snapshot` (the grant records no provider, or names a different key), `owner_mismatch` (the grant records a subject other than the run's), `store_unreadable` (the owner's namespace could not be read) or `own_key_absent` (the owner has no key stored for that provider), and (#531) `run_unreadable` / `providers_unreadable` (the run or the provider block could not be re-read) or `provider_changed` (the run's provider, re-read by UID, is gone, off, no longer serves the agent, is no longer a kind that takes a key, or now derives a host, header or format other than the grant's); its success also carries `provider`. On the two legacy subscription sentinels (#531): `reason` = `model_providers_configured` (a provider block is set, so the operator's shared token is not injected) or `providers_unreadable` (the block could not be read). The generic api-key, Bedrock bearer and model-provider key sinks add `store`, `ref` and `row_owner` for the row they read | `internal/secretstore/audited.go#RecordRead` (every decorator row, and the boot conversion's, which wardynd's `convertSecretStore` records per converted row), `internal/api/injection.go#Server.handleInternalInjection`, `internal/api/injection.go#Server.resolveSubscriptionSentinelInjection` (the two subscription sentinels), `internal/api/injection_awssso.go#Server.resolveAWSSSOInjection` (every captured-AWS-SSO refusal), `internal/api/injection_awssso.go#Server.resolveAWSSSOInjection` (the resolve that hands back a live session), `internal/api/injection_bedrock_bearer.go#Server.resolveBedrockBearerInjection` (the Bedrock bearer resolve and its refusals), `internal/api/injection_ado.go#Server.resolveADOInjection` (every per-person Azure DevOps refusal, and the resolve that hands back a live token), `internal/api/injection_provider_key.go#Server.resolveProviderKeyInjection` (0.8, `source=provider`: a person's own model-provider key — for a key, endpoint or Bedrock key provider alike, since they share the one `wardyn-provider-<uid>-key` name — and its refusals), `internal/api/provider_subscription.go#Server.resolveProviderSubscriptionInjection` (0.8, `source=provider`: a run's owner's own Claude sign-in for its model provider, and its refusals; `owner` is the run token's subject, `provider` the provider's id); on the captured-AWS-SSO arm a run on a Bedrock provider refuses a lapsed session with `reason=provider_signin_<not_found|spent|unavailable>` rather than raising a re-auth request, `cmd/wardynd/migrate_secrets.go#migrateMode` (one per value the migrator reads) | internal | — |
| `secret.rekey` | `wardynd -rotate-age-key` rewrapped every stored secret's data key under the key-encryption key of a new age identity (the sealed values are neither decrypted nor rewritten). SUCCESS only, and written by the one-shot maintenance process rather than the serving daemon (`actor_type` `system`, `actor` `wardyn/rotate-age-key`); an aborted rotation commits nothing and emits nothing. `target` is the store name (`pg`). Deliberately carries NO secret names — the sibling rows above each name one secret because each event IS one secret, whereas a single event listing the whole inventory would hand every configured sink the full set of names at once. `public_recipient` is the new age recipient (public by construction; each row's `kek_id` is a fingerprint of it), which is what lets an operator confirm which key the store now answers to | `public_recipient`, `secrets` | `cmd/wardynd/rekey.go#emitRekeyAudit` | internal | — |
| `secret.rewrap` | `wardynd -rewrap` rewrapped stored secrets' data keys onto the key a write uses under this configuration: the local key of their purpose (credential-storage design §2.13 c) — rows written before the platform/credential split, and, once `WARDYN_PLATFORM_KEY_FILE` is set, the boot keys still under the age key — or the key service `WARDYN_KEK=transit` selects, at its latest version, or, with the key service read-only, back to the local key (§2.3). The rewrap is client-side — each data key is unwrapped under its row's own key and wrapped again, bound to the row; Transit's server-side `rewrap` is never called — and the sealed values are neither decrypted nor rewritten, so no `secret.read` is written. SUCCESS only, written by the one-shot maintenance process (`actor_type` `system`, `actor` `wardyn/rewrap`); an aborted run commits nothing and emits nothing. `target` is the store name (`pg`). Carries NO secret names, for the reason `secret.rekey` gives | `secrets`, `platform_key_separate`, `key_service` (present only with `WARDYN_KEK=transit`: its `kek_id`), `key_version` (present only for a versioned key service, Transit: the version every row under it is now wrapped under) | `cmd/wardynd/rewrap.go#emitRewrapAudit` | internal | — |
| `secret.migrate` | `wardynd -migrate-secrets -to=<target>` moved stored secrets between local sealing (`local`) and the organisation's store (`vaultkv` or `azurekv`), one row per transaction (credential-storage design §2.3a.9). Written by the one-shot maintenance process (`actor_type` `system`, `actor` `wardyn/migrate-secrets`), once per run: `success`, or `failure` with `reason` `aborted` on any abort (then `count` is how many rows were committed before the row the abort names; the row itself, and any value the abort left behind in the store, are named in the command's error, not here). `target` is the target store. Carries NO secret names, for the reason `secret.rekey` gives; each value read on the way is its own `secret.read` | `from`, `to`, `count`, `reason`, `soft_deleted` (present only when a `-to=local` run left old copies soft-deleted in Key Vault, recoverable by the organisation; `wardynd -reconcile` lists them) | `cmd/wardynd/migrate_secrets.go#migrateMode` | internal | — |
| `secret.reconcile` | `wardynd -reconcile` compared the pointer rows with the values in the organisation's store (credential-storage design §2.3a.1). It reads store metadata only, never a value, and changes nothing; written once per run by the one-shot maintenance process (`actor_type` `system`, `actor` `wardyn/reconcile`). `outcome` is `success` when both sides agree, else `failure` with `reason` `drift` (pointers without a value, or values without a pointer) or `aborted` (the store did not answer). Carries counts only; the command's output names each row and path. A store-mode `secret.write` whose value reached the store while its row did not, and could not be removed again, is not yet its own row: the error names it and `-reconcile` lists it (per-read and failure coverage is CS-13) | `checked`, `dangling`, `orphans`, `reason` | `cmd/wardynd/migrate_secrets.go#reconcileMode` | internal | — |
| `credential.mint` | A credential is minted against an `APPROVED` approval (invariant 2 — mint IS the approval, same transaction). `lease` marks a mint the human did NOT individually decide: a `git_pat` whose approval carried `decision_scope=run` re-mints for the rest of the run under that one decision (the B2 per-run lease), and `decision_scope` beside it is the RAW stored value the lease turned on. Absent on every ordinary mint — a lease widens what one approval authorized, so it is stamped rather than left inferable. An auto-mintable grant re-mints on every proxy re-resolve, one row each: a person's model-provider key (#531) re-resolves about every 10 minutes while its run makes model calls (a 15-minute lease less the proxy's 5-minute refresh margin), each alongside a `secret.read` | `approval_id`, `decision_scope`, `grant_id`, `host`, `jti`, `kind`, `lease`, `reason`, `scope` | `internal/broker/broker.go#Broker.mint` (`mintEvent`), `internal/api/internal.go#Server.handleInternalMint` | internal | `internal/recordmode/recordmode.go#Capture`; console run detail and audit screen |
| `credential.reauth.request` | A run's OWN model credential lapsed MID-RUN and could not be renewed, so the proxy is holding the sandbox's next credential exchange and the credential's owner has been asked to sign in again (Finding 4, 0.7.6 — the `credential_reauth` approval kind, migration `0064`). This is the row that makes a held run answerable: WHOSE credential (`owner` + `credential_source`), WHY it is dead (`reason`, a closed vocabulary: `spent` = the refresh token is gone at AWS, `unavailable` = renewal failed transiently with nothing left to serve, `not_found` = there is no stored session for that namespace at all) and WHICH request the console is showing (`approval_id`). Actor is `system` (`wardynd`), not the agent: the sandbox asked for a credential, it did not ask for a human. `reason` is deliberately NOT part of the approval's `requested_scope` — the scope is the dedup key, so a `spent`→`unavailable` flip between two resolves would otherwise raise a second row for one lapse. At most ONE PENDING row per run and scope (the 0022 partial unique index now covers this kind), and at most `maxReauthHolds` such workflows per run. The per-person Azure DevOps lane raises the same row, with `provider: azure_devops`, when a mid-run renewal of the person's sign-in is refused (`reason`: `dead_credential` = the refresh token is gone, `interaction_required` = a Conditional Access policy wants the person present); its scope carries `mechanism: entra_signin` and no `credential_source`, and it counts against the same `maxReauthHolds`. A run that chose a Bedrock SSO model provider (0.8) is held the same way, and its row carries `model_provider`; its approval's scope also names the provider (`provider` + `provider_uid`), so only its owner's sign-in for THAT provider answers it — a roster sign-in never does, and a provider sign-in never answers a roster hold | `approval_id`, `credential_source`, `detail`, `model_provider` (provider runs only), `owner`, `provider`, `reason` | `internal/api/injection_awssso.go` (`holdOrRefuseCredentialReauth`), `internal/api/injection_ado_signin.go` (`holdForADOSignIn`) | internal | — |
| `credential.reauth.resolve` | The owner signed in again and the held request is answered: the `credential_reauth` row moves to `APPROVED` and the proxy's next poll wakes the parked call. Written in the SAME transaction as that state change (`store.PG.ResolveReauthApproval`) — if this row cannot be written the row stays `PENDING`, because a credential-bearing retry with no auditable predecessor is the thing invariant I7 forbids. NOT `approval.decide`: nobody decided anything, and the kind is refused a decision outright (409). `resolved_by` is the principal whose sign-in landed and `capture_run_id` the login run it landed from — the two facts that make "resolved by whom" answerable without a join. Emitted by BOTH the eager path (the capture handler, after its own `harness.credential.capture` row) and the idempotent reconcile-on-read that repairs a crash between those two writes; exactly one of them ever wins, because the state change is a `WHERE state='PENDING'` CAS. An Azure DevOps sign-in or consent request is resolved the same way by the person's next Azure DevOps capture (either door, after its `ado.signin.capture` row) or the reconcile-on-read, with `provider: azure_devops` and no `capture_run_id`. A model provider's hold (0.8) also carries `model_provider` | `approval_id`, `capture_run_id`, `model_provider` (provider holds only), `owner`, `provider`, `resolved_by` | `internal/api/injection_awssso.go` (`resolveReauth`), `internal/api/injection_ado_capability.go` (`reconcileADOReauthOnRead`) | internal | — |
| `credential.capability.request` | A run on the per-person Azure DevOps lane asked for access it was not dispatched with (a push, a pull request, a policy edit — a grantable capability inside the administrator's ceiling), and the control plane RAISED a `tool_call` approval for a person to decide. Raised only by the control plane (`answerADOCapability` → `raiseADOCapability`), with the grant in the row's `grant_id` — which the sandbox's own approval route can never set, and which is what lets the run's owner decide it. The approval's `requested_scope` is canonical (lane, provider row, organisation, grant, capability, repository, ref class, and a `tool`/`cmd` composed from them), so two requests differing only in their raw path are ONE pending row and this action is written once, by whoever raised it; the raw `method`/`path` ride THIS row instead. `first_use` is the run's mode as its proxy holds it: under `wait_for_review` the proxy holds the request until the decision, under `deny_with_review` the attempt is refused and a retry after approval goes through. Actor is `system` (`wardynd`). At most `maxADOCapabilityHoldsPerRun` such requests (and consent requests) per run. A consent a capability needs and the person has not given is a `credential.reauth.request` row with `provider: azure_devops`, `reason: consent_required` instead, resolved by the person's next sign-in (`reconcileADOReauthOnRead`) | `approval_id`, `capability`, `detail`, `first_use`, `method`, `organisation`, `owner`, `path`, `provider_row`, `ref_class`, `repo` | `internal/api/injection_ado_capability.go` (`raiseADOCapability`) | internal | — |
| `credential.revoke` | A minted credential is revoked (kill-switch, run stop). One row per credential the run ACTUALLY minted — the cascade enumerates the run's successful `credential.mint` rows UNION the approvals whose `minted_jti` was burnt (`mintedCredentialsSQL`), so an auto-mintable grant (which creates no approval row at all) and a leased `git_pat`'s 2nd..Nth mint are covered too — `github_token`, `api_key`, `git_pat` and `ssh_key` alike. `outcome=success` records the audit join, not an invalidation. `note` is the honest per-KIND revocation story (`revokeNote`, `internal/broker/revoke.go`), not one GitHub-shaped sentence: a `github_token` relies on TTL expiry (<=1h) plus the identity denylist because `RevokeRun` does not call GitHub's `DELETE /installation/token` — that endpoint must be presented the token itself and `RevokeRun` holds only the `jti`, no copy of the value addressed by it. Scoped to `RevokeRun` deliberately: Wardyn DOES make that call in two places — `VerifyRefRuleset`'s probe hand-back and `discardMinted`, the mint's own discard door (B11a-F1) — but both surrender a token that was NEVER returned to the run, so neither can reach the credential this row is about, and a blanket claim would be false; a `git_pat`/`ssh_key` is an operator-managed secret Wardyn can neither revoke, expire nor down-scope (the operator must rotate it at the forge, and the identity denylist only stops further mints); an `api_key` stays proxy-side and is never resident in the sandbox. NOTHING here invalidates a credential already handed out | `jti`, `kind` (absent when the grant row behind the mint is gone), `note` | `internal/broker/revoke.go#Broker.RevokeRun` (`RevokeRun`, `revokeNote`) | internal | — |
| `credential.erase` | An admin or `security_admin` erased every credential one person has stored (`DELETE /api/v1/people/{principal}/credentials`, 0.8, credential-storage design §2.5 — offboarding in one act). The principal resolves as `?owner=` does on the secrets routes; the operator namespace is never erased (it holds the platform keys). Each credential is deleted as `secret.delete` would delete it — in store mode the value leaves Vault or Key Vault before its row. `outcome=success` only when the namespace is empty afterwards; `outcome=failure` when any credential could not be deleted or one was written while the erase ran, with `count` saying how many WERE deleted (the rest stay, and a retry resumes). `Target` is the namespace. Carries a COUNT, never the names or values. Wardyn cannot revoke a credential upstream: AWS, Anthropic, Azure DevOps and a gateway still honour what they issued until it is revoked there | `count`, `secret_owner`, `owner_known` (as on `secret.delete`), and when the values lived in an external store that reports what it kept (`azurekv`): `store`, `purged`, `recoverable_days` | `internal/api/credential_erase.go#Server.handleErasePersonCredentials` | internal | — |
| `credential.expired.delete` | Wardyn deleted a stored sign-in nothing can use any more (credential-storage design §2.7, least retention): `reason` `invalid_grant` when the provider refused its refresh token for good at a renewal (the AWS SSO `invalid_grant` family, or an Azure DevOps dead refresh token — a Conditional Access refusal is NOT this, the token still lives), `reason` `expired` when the daily sweep found its `expires_at` past (an AWS SSO sign-in: its access token's expiry without a refresh token, its client registration's with one). Actor `system` (`wardynd`); `Target` is the secret name. `outcome=failure` (with `error`) when the delete itself failed on the `invalid_grant` arm: the sign-in stays until the next refusal or the person's next capture replaces it; a sweep that could not delete a row writes nothing and retries it the next day | `reason`, `provider` (`aws`, `ado`; on the `invalid_grant` arm), `expires_at` (on the `expired` arm), `secret_owner` (absent for the operator's "" namespace), `error` | `internal/api/credential_erase.go#Server.SweepExpiredCredentials`, `internal/api/credential_erase.go#Server.deleteDeadCredential` | internal | — |
| `identity.mint` | A per-run SPIFFE identity (JWT-SVID) is minted | — | `internal/identity/embedded/embedded.go#Provider.MintRunIdentity` | internal | — |
| `identity.renew` | A run's identity JWT is renewed (the run's `token_renewed_at` is stamped before the token is handed out). A refused renew is `denied` with `reason` `run_not_found`, `run_terminal:<state>`, or `run_lost`/`run_lost:<lost_reason>` for a kept (ended or lost) run | `expires_at`, `prev_jti`, `reason`, `run_state` | `internal/api/internal.go#Server.handleInternalTokenRenew` | internal | — |
| `identity.revoke` | A run's identity is revoked at teardown | — | `internal/identity/embedded/embedded.go#Provider.RevokeRun` | internal | — |
| `identity.jti.revoke` | A single token is revoked by its own jti, without revoking the whole run: a revive (O2, `run.revive`) retires the run's OLD token this way the moment a fresh one is minted | — | `internal/identity/embedded/embedded.go#Provider.RevokeJTI` | internal | — |
| `run.identity.expire` | A NON-TERMINAL run presented an EXPIRED identity on the internal lane (`/api/v1/internal/*`), so its token renews are not landing and every control-plane call it makes is now refused. Emitted ONCE per run (an in-memory guard keyed on the run id; a `wardynd` restart may emit a second row for the same run — two rows for one incident, never a row per request). The row itself does not end the run; once its token has gone unrenewed for over an hour and five minutes, the lapsed-token sweep keeps an interactive run as lost with its proxy stopped, or fails a headless one (see `run.lost`). A TERMINAL run emits nothing — its token is supposed to be dead, and an unreaped sidecar is not news. Only an expired token reaches this row at all: the identity provider reports expiry as a typed error carrying the run id (`identity.ExpiredTokenError`), while a revoked, forged or wrong-audience token names no run and stays as coarse as the `auth.fail` row beside it. Paired with the proxy sidecar's renew backoff + give-up (`internal/egress/proxy/renew.go`), which is what stopped this case from flooding the trail with one `auth.fail` row a minute | `path`, `run_state` | `internal/api/http.go#Server.auditRunIdentityExpired` (`auditRunIdentityExpired`) | internal | — |
| `approval.decide` | A human decides a pending approval (approve/deny, any scope) | `approval_id`, `decision`, `reason` | `internal/approval/approval.go#Decide` | internal | console audit screen |
| `approval.expire` | An undecided approval moves to `EXPIRED` — either the periodic sweep, past the deployment TTL (`WARDYN_APPROVAL_EXPIRY_AFTER`) or past its run-level `expires_at` = min(requested_at + the run's wait_budget_sec, the run's ends_at) (#567) (actor `system`), or (#811) the run's own agent withdrawing a `tool_call` row it raised, which `wardyn-toolgate` does the moment its wait deadline is reached instead of leaving it `PENDING` for the sweep to catch up to (actor `agent`, the run's SPIFFE ID). The sweep (`ExpireStaleByKind`) writes `approval_id`, `cutoff` and `expires_at`; the withdrawal (`ExpireOne`) writes `approval_id` and `reason` (`client_withdrawn`) | `approval_id`, `cutoff`, `expires_at`, `reason` | `internal/approval/approval.go#ExpireStaleByKind,ExpireOne` | internal | — |
| `approval.cancel` | A run reached a terminal state (killed, completed, failed, stopped) while one or more of its approvals were still `PENDING`, so the terminal cascade moved them to `CANCELLED` — nobody decided them and nothing was approved or denied. ONE row per run transition, not per approval: they all belong to a single run ending, and the row is keyed to the run so it lands in that run's evidence rail. `reason` names the transition (`run_killed`, `run_completed`, `run_failed`, `run_stopped`, `run_ended` when the run's end passed — kept or torn down, see `run.ended` — `run_lost` when it lost its sandbox, see `run.lost`, or `run_terminated` when the state could not be read back), `count` how many approvals moved and `by_kind` the same count split by what each row IS (e.g. `{"egress_domain":2,"tool_call:azure_devops":1}`) — both count only rows the cancel itself moved, never one a human decided first. **`by_kind` keys** (`approval.TallyKey`, read off the stored row, no extra column): `egress_domain` and `credential` as their kind; `tool_call` is a hook's tool call and `tool_call:azure_devops` an Azure DevOps capability escalation (a `tool_call` row whose `grant_id` column is set — only the control plane sets it — and whose scope names that grant on the `azure_devops` lane); `credential_reauth:aws_sso` is a lapsed AWS SSO session's re-auth (scope with a `credential_source` and no `lane`), `credential_reauth:azure_devops_signin` and `credential_reauth:azure_devops_consent` an Azure DevOps sign-in or consent request (scope `lane` `azure_devops`, `mechanism` `entra_signin` / `entra_consent`); a `credential_reauth` row of none of those shapes stays plain `credential_reauth`. `wardyn_credential_reauth_total{outcome="cancelled"}` counts `credential_reauth:aws_sso` only. A run with nothing pending emits NO row at all, which is what makes a re-kill idempotent in the append-only log as well as in the store. **`outcome=failure` with an `error` field is a PARTIAL cascade**: a store error part-way through leaves the approvals it already moved durably `CANCELLED`, so the row goes out with the count of what actually moved rather than being skipped — an operator's queue emptying with nothing in the trail is the failure this row exists to prevent. Actor is `system` (`wardyn/approval-cancel`) — see `cancelRunApprovals`, the terminal-cascade caller in `internal/api/runs_lifecycle.go#Server.cancelRunApprovals` | `by_kind`, `count`, `error`, `reason`, `run_id` | `internal/approval/approval.go#CancelForRun` (`CancelForRun`) | internal | — |
| `approval.push_paths.record` | A held push's `push_content` approval was raised and its complete review-matched path list stored with it (#1066). The approval's `requested_scope` names ten paths — it is the dedup key and the card's list — so this row is where the rest are in the run's audit trail and its export (`GET /api/v1/audit/export?run_id=`), whatever state the run ends in; `GET /api/v1/approvals/{id}/paths` reads the same list back to whoever may see the approval. The list is bounded — 10,000 paths or 1 MiB of path text, `truncated: true` past either (`types.PushPathList`) — and verified before anything is stored: sorted, distinct, opening with the scope's paths, and, when whole, `paths_total` long with `paths_digest` over it; a list that fails is a 400 and writes neither the approval nor this row. Once per approval: a re-raise that joins the same `PENDING` row writes nothing. Actor is the run's agent (its SPIFFE ID); `Target` is the approval id. The largest row Wardyn writes (up to 1 MiB of paths): a SIEM sink that caps a message below that loses it — syslog over UDP drops the row, a webhook collector whose request limit is smaller drops the whole batch the row rides in — while Postgres keeps both copies | `approval_id`, `paths`, `paths_digest`, `paths_total`, `truncated` | `internal/api/approvals_push.go#Server.recordPushPathList` | internal | — |
| `harness.credential.capture` | A harness login flow (Claude Code subscription, etc.) captures a credential. The AWS SSO emit also carries `owner` + `credential_source`: "" / `shared` is the ONE credential every run inherits, a subject / `per_user` is that person's own — the pair is what makes a per-user estate's capture rows tellable apart, and "whose session" the first answerable question after an incident. `captured: true` is the RESPONSE body of the paste door, not a datum — no emit writes it. A sign-in through a model provider's own door (0.8) also carries `model_provider`, and `Target` is the provider's own per-person name (`wardyn-provider-<uid>-sso` or `-oauth`) in the signer's namespace: an AWS one from the helper upload, a Claude one from `PUT /model-providers/{id}/sign-in`, which also carries `owner` + `credential_source` | `credential_source`, `model_provider` (provider door only), `owner`, `provider`, `source` | `internal/api/harnesscred.go#Server.handleHarnessCredentialPaste`, `internal/api/ssotoken.go#Server.handleUploadSSOToken`, `internal/api/provider_signin.go#Server.handleProviderSignInCapture` | internal | console sign-in capture confirmation |
| `harness.credential.refuse` | An AWS SSO credential upload was REFUSED, so nothing was stored. Every refusal on this route used to be a bare 400/409 with no row at all, which is what let a wrong-account capture land as "no diff, no audit row naming the change, and no warning" — the reporting operator's own words. `reason` is from a FIXED vocabulary, never sandbox-chosen text, so an incident review can GROUP refusals: `blob_shape` (unparseable, or missing required fields — the sentence names the EMPTY ones, `account_id`/`role_name` included, since a portal miss is the commonest way a capture comes back short), `field_unsafe` (a control character or whitespace in a field baked verbatim into the generated `~/.aws/config`), `field_shape` (an `account_id` that is not 12 digits, or a `role_name` outside IAM's own grammar — the same two shapes the roster-save door holds the admin's pin to, so one rule now governs both doors), `region_mismatch` / `start_url_mismatch` (the F006 bindings to the region and access portal this login run was LAUNCHED with), `account_role_pin_mismatch` (the blob names an account/role other than the one the roster row pinned at launch), `model_account_mismatch` (the session is for one account and the configured Bedrock model lives in another — a run using it would be refused by IAM; UNPINNED launches only, since an explicit roster pin is the admin's deliberate answer and outranks the model's account), `unstamped_scope` (a login run with no launch-time scope stamp on a deployment that now reads `per_user`), `already_captured` (this login run already captured once), `stamp_unreadable` / `store_error` (the daemon could not read the launch stamp, the existing credential, or — on the re-read below — the login run itself), `run_killed` (the login run was KILLED — cancelled, or superseded by that person's next sign-in — and a terminal run may still reach this upload door inside `terminalUploadGrace`, so the run's own state is the belt under a best-effort revocation), `provider_changed` (0.8: a sign-in through a model provider's own door whose provider was removed, re-created under a new UID, or given another address (rule 8's: its base URL, Bedrock region or base URL, header scheme, or a harness's path or header — a harness added or removed counts too), access portal or pin while the login sandbox was open — checked under the same lock rule 8's credential purge holds, and carrying `owner` + `credential_source`), `signin_busy` (a **503**: the per-person sign-in lock could not be taken inside its 5s budget, so the capture was not serialized against that person's other sign-ins and nothing was stored; it carries the `owner` pair, since the scope is decided before the lock). On a provider sign-in, `region_mismatch` binds to the provider's region as stamped at launch rather than the boot config, and `unstamped_scope` also answers a provider sign-in whose stamp names another owner than the run token's. Actor is the AGENT — the refusal is attributed to the login sandbox that attempted the write, and `Target` is the reserved harness secret name it attempted to write — the operator-wide one, even for a per_user refusal, because that is the name the upload was attempting. **`owner` + `credential_source` ride the refusals that happen AFTER the scope is decided** (`already_captured`, every `store_error` arm, and the `run_killed` arm decided inside the lock) — the same pair the captured row carries, and the one that makes a per_user estate's refusal stream groupable by person instead of a join back through each row's `run_id` to its `harness.login.start`. The EARLIER refusals carry neither: there is no decided scope yet, and that run's stamp already holds the pair. **Order matters when reading these:** the identity binding runs BEFORE the once-only guard, so a sandbox replaying after a genuine capture with a WRONG pair reads as `account_role_pin_mismatch`, not `already_captured` — a replay count taken from this reason alone under-counts. **Rows are unbounded per run:** nothing rate-limits the upload route, so a loop inside a login sandbox can write one row per attempt for the run's idle cap. Accepted rather than throttled — the rows are small, the reason vocabulary is closed so they group and prune, and nothing is stored; the run is time-capped regardless. **Which refusals take the per-owner lock:** `blob_shape`, `field_unsafe`, `field_shape`, `region_mismatch`, `start_url_mismatch`, `account_role_pin_mismatch`, `model_account_mismatch`, `stamp_unreadable` and `unstamped_scope` are decided BEFORE `lockAWSSSOOwner`, so a flood of those cannot contend with credential renewal. `already_captured`, every `store_error` arm and a SECOND `run_killed` arm are decided INSIDE it — the once-only guard is a read-then-put and has to be, and the run state is re-read there as the last statement before the write because a supersede landing in the window between the top-of-route check and the store would otherwise let the ended sandbox's capture overwrite the one that replaced it. `run_killed` therefore appears on BOTH sides of the lock: the early arm carries no `owner` pair, the late one does — so a sandbox replaying a CORRECTLY BOUND blob after its genuine capture takes and releases the refresher's per-owner single-flight lock once per attempt, for the run's idle cap | `credential_source`, `owner`, `provider`, `reason` | `internal/api/awssso_pin.go#Server.refuseCapture` (`refuseCapture`) | internal | — |
| `harness.credential.disconnect` | A harness credential is disconnected | `captured`, `provider` | `internal/api/harnesscred.go#Server.handleHarnessDisconnect` | internal | — |
| `harness.login.start` | A harness SSO login flow starts. It also STAMPS the launch-time credential scope — `credential_source` + `owner` — because that is what the capture upload binds to: the login sandbox lives to its idle cap, so re-resolving the scope at upload time let a roster edit mid-run re-point a member's capture at the operator-wide credential every run inherits. `sso_start_url` is operator config, never a credential. From 0.7.3 it also stamps the roster row's account/role PIN (`sso_account_id` + `sso_role_name`) for the same reason: the upload refuses a blob that disagrees with the pin as it read AT LAUNCH, so a roster edit while the login sandbox is still alive cannot re-point a capture already in flight. A sign-in through a model provider's own door (`POST /model-providers/{id}/sign-in`, 0.8) also stamps `model_provider` + `model_provider_uid` (the capture lands under that provider's own name and answers only its holds) and the provider's `sso_region` and `model` as they read at launch — what its AWS upload binds to in place of the boot config; its start URL and pin come from the provider record, never the request. `model` is from the harnesses the caller may launch on that provider (one naming an AWS account when any does; an unpinned provider whose models name two accounts is refused before launch), and `model_provider_address` is a SHA-256 digest of the provider's address as rule 8 reads it — never the address itself — which both capture doors compare, so a sign-in given for one address never lands after the provider moved | `credential_source`, `egress`, `model` (provider door only), `model_provider` (provider door only), `model_provider_address` (provider door only), `model_provider_uid` (provider door only), `owner`, `provider`, `sso_account_id`, `sso_region` (provider door only), `sso_role_name`, `sso_start_url` | `internal/api/harnesscred.go#Server.launchHarnessLoginRun` | internal | `internal/api/harnesscred.go#Server.loginRunStamp` |
| `harness.credential.refresh` | Wardyn renews a captured AWS SSO credential CONTROL-PLANE side at dispatch (SSO-OIDC `CreateToken` over the `refresh_token` grant) — the run-less counterpart to `harness.credential.capture`, because the credential is per-principal, not per-run. `outcome=success` once the rotated pair is persisted; `outcome=failure` both when the redeem itself failed (`spent` says whether the refresh token is gone — `invalid_grant`/`expired_token`/`invalid_client`/`unauthorized_client` — or whether it was transient, in which case the credential is untouched and the next dispatch redeems normally — and if the access token has not actually expired yet that run is still served from it, since renewal starts a skew window ahead of expiry) and when the redeem SUCCEEDED but the re-`Put` did not (`persist_error`): that run is still served from the in-memory renewed blob, since the stored pair is already spent at AWS and re-redeeming it is impossible. `attempts` rides every failure row (1 or 2 — a transient failure gets exactly one immediate retry, so `attempts: 2` with a transport error on both is a network story, not a credential one; two EOFs read differently from an EOF then `invalid_grant`, and `error` joins both attempts' text when there were two) AND a success row when the retry is what made it succeed (a first-attempt success carries no `attempts` field at all) | `attempts`, `credential_source`, `error`, `expires_at`, `owner`, `persist_error`, `provider`, `registration_expires_at`, `rotated`, `spent` | `internal/api/awssso_refresh.go#Server.auditAWSSSORefresh` | internal | — |
| `token.create` | A signed-in human mints a per-user API token for themselves (`POST /me/tokens`). The actor is that human and `Target` is the token id — never the credential, which is returned once and stored only as a hash. `user_type` is the user type the token is stamped with | `name`, `role`, `user_type` | `internal/api/apitokens.go#Server.handleCreateAPIToken` | internal | — |
| `token.revoke` | A per-user API token is revoked, by its owner (`DELETE /me/tokens/{id}`) or by an admin revoking anyone's (`DELETE /tokens/{id}`). `principal` names the token's OWNER, which is the point on the admin lane: the actor is the admin, the subject is someone else. A bulk sweep writes ONE ROW PER CREDENTIAL too, carrying `scope:"sweep"` beside the same two keys — the aggregate `tokens_revoked` on `session.revoke` / `access.role_mapping.*` says how many, and on the role-mapping lane's unanswerable-snapshot arm that number covers every elevated-stamp or old-type-stamped token in the deployment (on the first type assignment to a value that derived Standard user, every Standard-user token whose snapshot is missing or partial — more than the row's `stale_token_snapshots`, which counts only the tokens naming the value), so without these rows the log cannot say WHICH credentials an edit killed. | `name`, `principal`, `scope` (`"sweep"`, absent on the single-token doors) | `internal/api/apitokens.go#Server.revokeAPIToken`; the sweep at `internal/api/sessions.go#Server.revokeAPITokensFor` | internal | — |
| `session.revoke` | An admin revokes active OIDC console sessions — `target` is the revoked `sub`, or `*` for a revoke-all (`POST /api/v1/sessions/revoke`, D16). Also revokes every unrevoked per-user API token the target holds — a `wdn_` bearer is that human's session in another form, so "revoke a human now" covers both ("all" covers every token in the deployment); `tokens_revoked` counts THIS call only (a retry after a failure counts just the remainder), and outcome `failure` marks a partial application — sessions revoked, token sweep errored mid-way | `scope` (`sub` or `all`), `sub` (sub-scoped only), `tokens_revoked` | `internal/api/sessions.go#Server.handleRevokeSessions` | internal | — |
| `ado.signin.capture` | A per-user Azure DevOps sign-in is captured. TWO paths write it and `source` says which: `login` is the PRIMARY one — the console's own SSO login carried the Azure DevOps scopes and its callback kept the refresh token, so an admin configures the org once and a member who signs in is connected with nothing else to do — and `signin` is the dedicated fallback door (`GET /api/v1/scm/azure-devops/callback`) for a row in another tenant, a login that predates the row, or a consent since withdrawn. The login path writes NO row when the grant carries no Azure DevOps scope (consent declined, a Conditional Access policy, a tenant that will not issue them): that is one row per login per person on an org that has not finished setting this up, and the absence of a stored credential already says it. That login still succeeds. ONE action for both outcomes: `success` once the refresh token is stored under the person's OWN namespace at the reserved harness name, and `failure` with a `reason` for every refusal, so a review reads one action instead of correlating two. `reason` is from a FIXED vocabulary, never caller-chosen text: `identity_binding` (the identity token's `sub` is not the session's subject — the fail-closed binding, and the one refusal that means somebody else's credential was about to be stored under this person), `consent_required` / `interaction_required` (the tenant refused the authorization or the exchange — the person's or their admin's to resolve), `exchange_failed` (the code exchange did not complete for any other reason: a code-challenge mismatch, a spent code, an unreachable authority), `unusable_grant` (the exchange succeeded but returned no refresh token, or no granted scope inside the row's configured scopes, so there is nothing renewable a run could use) and `store_error` (everything checked out and the write failed — the cause is in the daemon log, never on the row). Actor is the HUMAN whose session started the sign-in and `Target` is the reserved secret name the capture writes, so "whose Azure DevOps access" is answerable without a join. `identity_binding`, `consent_required`, `interaction_required` and `exchange_failed` are reachable only from the dedicated door — at login the identity binding holds by construction, since the subject compared against IS the identity token's own. The attack-shaped refusals — a forged `state`, a missing one-time cookie, a caller with no session subject, a row configured against another application — emit NO row: nothing about a credential happened, and a scanner hitting the callback must not be able to write to the audit log. Never carries a token, a code or a verifier. `scopes` is the tenant's granted set narrowed to the row's configured scopes (what a run can use; identity scopes and anything outside the row's ceiling are dropped), never the requested set. Entra does not narrow the token itself to those scopes, so what bounds a single run is Wardyn's own capability check in front of the resource, not the token | `client_id`, `expires_at`, `provider`, `reason`, `scopes`, `source`, `tenant_id` | `adoSignInCapturedAction`, `internal/api/ado_entra_store.go#Server.auditADOCapture` | internal | — |

## Policy, capability & authorization

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `policy.create` | A named egress/run policy is created | `min_confinement_class`, `name` | `internal/api/policies.go#Server.handleCreatePolicy` | internal | — |
| `policy.update` | A named policy is updated | `min_confinement_class`, `name` | `internal/api/policies.go#Server.handleUpdatePolicy` | internal | — |
| `policy.delete` | A named policy is deleted | — | `internal/api/policies.go#Server.handleDeletePolicy` | internal | — |
| `policy.inline.apply` | A one-off (inline, unsaved) policy is used to launch a run | `eligible_grants`, `min_confinement_class`, `workspace_mounts` | `internal/api/inline_policy.go#Server.resolveRunPolicy` | internal | — |
| `capability.grant.create` | `POST /permissions/grants` (new) | `capability`, `effect`, `subject`, `subject_type`, `value` | `internal/api/permissions.go#Server.handleUpsertCapabilityGrant` | internal | — |
| `capability.grant.update` | `POST /permissions/grants` (upsert on existing key) | `capability`, `effect`, `subject`, `subject_type`, `value` | `internal/api/permissions.go#Server.handleUpsertCapabilityGrant` | internal | — |
| `capability.grant.delete` | `DELETE /permissions/grants/{id}` | — | `internal/api/permissions.go#Server.handleDeleteCapabilityGrant` | internal | — |
| `capability.enforcement.write` | `PUT /permissions/enforcement` (whole-map replace) | (saved enforcement map) | `internal/api/permissions.go#Server.handlePutCapabilityEnforcement`; documented `docs/OPERATIONS.md`'s capability-grants section | internal | — |
| `capability.availability.write` | `PUT /permissions/availability/{kind}/{value}` (0.8: one value's "Available to" turned to Only… or back to Everyone; Target is the kind) | `kind`, `value`, `restricted` | `internal/api/permissions_availability.go#Server.handlePutAvailability` | internal | — |
| `governance.profile.write` | `POST /governance/profiles` (create) or `PUT /governance/profiles/{id}` (update): a named, assignable ceiling is saved. `Target` is the profile id | `allow_all_egress`, `limits`, `min_confinement_class`, `name` | `internal/api/governance.go#Server.writeGovernanceProfile` | internal | — |
| `governance.profile.delete` | `DELETE /governance/profiles/{id}`. Refused `409` while the profile is still assigned, so this row never records a silent widening — the unassign is its own event below | — | `internal/api/governance.go#Server.handleDeleteGovernanceProfile` | internal | — |
| `governance.assignment.write` | `POST /governance/assignments`: a profile is bound to a user, a group, a user type (0.8, `subject_type` `user_type`), or everyone (upsert on the natural `(subject_type, subject)` key — 201 vs 200 tells create from update apart, this action name does not) | `priority`, `profile_id`, `subject`, `subject_type` | `internal/api/governance.go#Server.handleUpsertGovernanceAssignment` | internal | — |
| `governance.assignment.delete` | `DELETE /governance/assignments/{id}`: the supported way to widen a principal back to the deployment ceiling, which is why it is audited on its own line rather than riding a profile delete | — | `internal/api/governance.go#Server.handleDeleteGovernanceAssignment` | internal | — |
| `user_type.write` | `POST /user-types` (create, `201`) or `PUT /user-types/{id}` (update, `200`): an org-defined user type is saved (migration `0071_user_types`). `Target` is the type id | `built_in`, `id`, `name`, `priority` | `internal/api/user_types.go#Server.auditUserTypeWrite` | internal | — |
| `user_type.delete` | `DELETE /user-types/{id}`. Refused `409` while the chart's role map or default role, or a permission, profile or drive row, still names the type, or a live API token carries it, and always for the built-in `standard` type — so this row never records a type vanishing from under someone still mapped to it | `id` | `internal/api/user_types.go#Server.handleDeleteUserType` | internal | — |
| `drive.write` | `POST /drives` (create) or `PUT /drives/{id}` (update): a user drive — the persistent storage an admin registers and allocates — is saved. `Target` is the drive id. `host_root` is recorded because it is the host tree this row authorizes binding into other people's sandboxes; a drive carries no secret (a share credential is the operator's, held host-side, never by Wardyn). `rehomed` is true when the edit RE-POINTED the storage every allocated member is bound to — a rename, a host-root correction or a template change, which this one action otherwise covers indistinguishably from a cosmetic edit; such a change is refused 409 unless the caller confirmed it. `rehomed_fields` and `rehomed_subjects` are present EXACTLY when `rehomed` is true, and carry what the overridden refusal said: which identity fields moved (every allocated person's object name is derived from them) and how many allocations went with them — without those two the log cannot answer which objects were orphaned | `backend`, `home_template`, `host_root`, `name`, `object_scheme`, `reclaim`, `rehomed`, `rehomed_fields`, `rehomed_subjects`, `size_mib`, `storage_class`, `writable` | `internal/api/user_drives.go#Server.writeUserDrive` | internal | — |
| `drive.delete` | `DELETE /drives/{id}`. Refused `409` while the drive is still allocated (`ON DELETE RESTRICT`), so this row never records a silent un-allocation — the deallocation is its own event below | — | `internal/api/user_drives.go#Server.handleDeleteUserDrive` | internal | — |
| `drive.grant.write` | `POST /drives/grants`: a drive is allocated to a user, a group, a user type (0.8, `subject_type` `user_type`), or everyone (upsert on the natural `(subject_type, subject)` key — 201 vs 200 tells create from update apart, this action name does not). `home_override_set` is a **boolean**, not the value: which directory an admin pinned is a person's username as often as not, and whether one was pinned is the governance fact | `drive_id`, `enabled`, `home_override_set`, `priority`, `size_mib_override`, `subject`, `subject_type`, `writable_override` | `internal/api/user_drives.go#Server.handleUpsertUserDriveGrant` | internal | — |
| `drive.grant.delete` | `DELETE /drives/grants/{id}`: the product-side half of offboarding, and the half that deletes **no data** — which is why the row carries the drive's declared `reclaim` intent, so the log says what the operator was told to do about the directory this allocation was the last pointer to. **`drive` here is the drive OBJECT's name** (`user_drives.name`), with `drive_id` its uuid — the same key on `run.drive.mount` carries the per-person home segment instead, because that row is about one person's directory and this one is about the object | `drive`, `drive_id`, `reclaim`, `subject`, `subject_type` | `userDriveGrantAuditData`, `internal/api/user_drives.go#Server.handleDeleteUserDriveGrant` | internal | — |
| `run.drive.mount` | Dispatch attached a member's USER DRIVE to a sandbox — the one object in a run's spec that OUTLIVES the run, which is why it is recorded on its own line rather than left implicit in the run's policy snapshot. Actor `system`: a member ticked a checkbox, and what is logged is dispatch's own resolution of that flag into a storage object. Emitted only when a drive is actually attached. **`Target` names the storage, and WHICH name depends on the backend** — because this is the one field on the row whose reader is not the operator. The run page's Audit tab renders a row's `target` and nothing from `data`, and `auditScope` lets a run's CREATOR read their own run's rows (`GET /audit?run_id=`). A **managed** object's name is Wardyn's own (`wardyn-drive-<drive-slug>-<home>`, volume and claim alike): it says which volume or claim the run was handed, discloses nothing about the host, and is carried verbatim. A **`host_path`** object name is `<host_root>/<home>` — an absolute path on the operator's filesystem — so the row carries **`<drive name>/<home>`** instead, in the `Target` **and** in `object`, which answers the same question without handing a member the share's layout (the rule `driveShareIsBindable` and `applyUserDriveEnv` already state for the same reader). **The payload is not a side door**: `GET /audit?run_id=` hands the run's creator the whole event, `data` included, so masking only the rendered field would have moved the same disclosure one key over. An operator reads a share's root from `GET /drives`, which is operator-only and already carries it. A **managed** row is unchanged on both fields, so the reclaim command still reads the exact volume or claim name straight off it; **`drive` here is the per-person HOME SEGMENT**, not the drive object's name — that is what the same key carries on the `drive.grant.*` rows. Neither ever enters the sandbox env, which carries the target and the mode and nothing else (`applyUserDriveEnv`). `enforcement` (`filesystem`/`request`/`external`/`none`) is what actually binds the drive's bytes, logged beside the mount so a later dispute reads the size with its caveat attached. Emitted AFTER `CreateSandbox` returns, so a `success` row means the drive really was bound: the driver re-runs the host-root ceiling and the bind deny-list at create time and can still refuse, and that refusal surfaces as a `run.create` failure with no mount row at all | `backend`, `drive`, `enforcement`, `mode`, `object` | `auditDriveMount`, `internal/api/runs_dispatch_mounts.go#Server.auditDriveMount`; called after `CreateSandbox` in `internal/api/runs_dispatch.go` | internal | — |
| `directory.search.fail` | `GET /access/directory/search` could not answer — a lapsed secret, a revoked consent, an unreachable tenant — so an operator sees the connector failing without an admin reporting "the picker is empty". FAILURE ONLY: a successful lookup is never audited. Deliberately **content-free** — the fields are the connector's typed `directory.ProviderError` values, never the query, which is the name of a person an admin was looking up. `Target` is the request path | `op`, `provider`, `upstream_status` | `internal/api/directory_search.go#Server.auditDirectoryFailure` (`auditDirectoryFailure`) | internal | — |
| `access.role_mapping.write` | `POST /access/mappings` (Phase 2 lane A, migration `0051`, the People step): a console role mapping is added, or re-added over an existing value (upsert on the natural `value` key — 201 vs 200 tells the two apart, this action name does not). `user_type` is the row's user type on a `user` row, `""` on an admin-tier row | `role`, `user_type`, `value`, `stale_token_snapshots`, `tokens_revoked` | `internal/api/access.go#Server.handleUpsertRoleMapping` | internal | — |
| `access.role_mapping.delete` | `DELETE /access/mappings/{id}`. Records the DELETED row's `value`/`role` — captured from the matched row before the store delete, since the row (and its id-to-value mapping) is gone by the time the event is written; `user_type` likewise (`""` on an admin-tier row) | `role`, `user_type`, `value`, `stale_token_snapshots`, `tokens_revoked` | `internal/api/access.go#Server.handleDeleteRoleMapping` | internal | — |
| `approval.second_human.bypass` | `WARDYN_EGRESS_SECOND_HUMAN` is set and the decider was the `admin-token` principal, so the four-eyes rule was BYPASSED (break-glass). A shared token carries no per-human identity to compare against, so it is exempt by design — this event is what keeps that exemption from being silent. The row is written on both of Decide's arms: outcome `success` sits beside the `actor_type=system` `approval.decide` the decision itself writes, and outcome `failure` records a bypass that got past the gate on a decision that then failed — there is no `approval.decide` beside that one, which is what the outcome says | `reason`, `switch`, `error` | `internal/api/approvals.go#Server.decide` (`requireSecondHuman`) | internal | — |
| `authz.denied` | Every member denial that isn't a plain foreign-resource 404 — plus, since 0.7.4, the RUN-TOKEN tier: `internalAuth`'s liveness gate refuses every `/internal/*` door to a run that has gone terminal (`reason` `run_terminal`, with the terminal state beside it as `run_state`) or whose row is gone (`run_not_found`), with `actor_type` `agent` and the request path as the target. Token verification alone could not catch it — `revokeRunCascade` is best-effort, so a killed run whose revocation write failed still presents a verifiable token (`internal/api/internal_live_run.go#Server.auditInternalDenied`). The tail-upload doors (recordings, scan results, the captured SSO cache) are exempt for five minutes after the terminal transition, because those uploads race the watcher that writes it. — see `docs/OPERATIONS.md`'s "Every denial that isn't a 404" for the full `reason` vocabulary (`admin_surface`, `security_admin_surface`, `not_owner`, `attach_ticket_foreign_run`, `byoi_user`, `capability_workspace`, `capability_egress_host`, `capability_secret`, `capability_agent`, `capability_integration`, `capability_workspace_provider`, `capability_feature`, `capability_model_provider`, `capability_policy`, `governance_profile`, `grant_pairing_not_eligible`, `groups_snapshot_stale`, `second_human_required`, `harness_login_mechanism_principal`, `harness_login_not_per_user`, `run_terminal`, `run_not_found`, `user_type_unknown`, `user_view_type_deleted`). The `governance_profile` reason covers the user-drive door too, at target `runs.drive` (`internal/api/user_drives_run.go#Server.denyUserDrive`) — a new target, not a new reason. 0.7 does add three reasons, all listed above: `security_admin_surface` (the second admin tier's own refusal), `attach_ticket_foreign_run`, and `groups_snapshot_stale` (the resolver cannot answer this caller's group tier, so every ceiling-bounded seam refuses 403 — emitted ONCE per request at each of the TWO sites that decide it: `internal/api/governance.go`'s `ceilingWithUnusableGroups` at target `governance.ceiling`, and `internal/api/user_drives_resolve.go`'s `driveWithUnusableGroups` at target `runs.drive`. They are separate decisions over separate group-tier reads — `HasGroupTierAssignments` and `HasGroupTierDriveGrants` — and a deployment can have either without the other, so a denial stream that named only the first was empty for a drives-only deployment). 0.7.2 adds one more, also listed above: `capability_workspace_provider` (a member's work would come from a git provider row they hold no grant for — six doors, two targets, all detailed in OPERATIONS' table). 0.7.3 adds one more, also listed above: `harness_login_mechanism_principal` (the shared admin bearer token attempted a `per_user` harness-login capture with OIDC configured, target `setup.harness_login`, `422` rather than `403` — see OPERATIONS' table) 0.7.4 adds the two RUN-TOKEN-tier reasons named above (`run_terminal`, `run_not_found`) and one FIELD, renamed in 0.8 from `member_mode` to `user_view` (docs/OPERATIONS.md's "Renamed in 0.8" appendix; history not rewritten — a pre-0.8 row still carries `member_mode`): `user_view: true` rides the datum at every ADMIN-TIER gate — the two universal chokepoints (`requireOperator`, `requireSecurityOperator`) and the in-handler refusals that raise the identical `admin_surface` / `security_admin_surface` datum (`getWorkspaceAuthorized`'s operator-owned-workspace arm, `internal/api/helpers.go`; the `?owner=` secrets gate, `internal/api/secrets.go`; `resolveAlwaysTarget`'s operator-only `always` scope, `internal/api/approvals.go`; the `workspaces.llm_cred` arm, `internal/api/workspaces.go`) — when the refused caller is an admin exercising the **user view** — so a denial stream reads as an admin walking the user path rather than as an incident. It is a MARKER, not a field every row answers: it is omitted entirely when the view is off, and it changes no `reason` and no status code. 0.8 routes every row through one emitter (`refuse`, `internal/api/refusal.go`) and one datum (`authz.Datum`, `internal/authz/authz.go`), so the marker now rides every `authz.denied` row a user-view admin meets — `groups_snapshot_stale` included — and every row a request produced carries `method`; the reasons themselves are the registry in `internal/authz/registry.go`, which also adds `harness_login_not_per_user` (a member's `POST /setup/harness-login` when the agent's model credential is not `per_user`, target `setup.harness_login`, `403` — emitted since 0.7.2 and undocumented until now). 0.8 also adds `capability_feature` (a person tried to add an SSH key or mint an API token and that feature is not available to them, targets `me.ssh_keys` and `me.tokens`). 0.8 also adds `capability_policy` (a member selected a stored policy they hold no grant for, target `runs.policy`). | `answered` (0.8, on `user_view_type_deleted` only: `admin_view` when the request was a launch door, so the row records that the caller was actually answered 409 `admin_view` rather than the row's own 403 body), `dropped`, `host`, `user_view`, `method`, `reason`, `user_type` (0.8: the caller's stamped user type, whenever they carry one — a reserved Datum key, not something a door sets by hand. On `user_type_unknown` it is the type that no longer exists; on `user_view_type_deleted` and beside every `user_view` marker, the type the user view looks through; on every other row from a signed-in human, simply their own stamped type); and on the run-token tier `actor` plus `run_state` (the terminal state the liveness gate found) | multiple sites; documented `docs/OPERATIONS.md#every-denial-that-isnt-a-404` | **stable** (documented, closed `reason` enum) | — |
| `auth.fail` | An authentication attempt fails, on EITHER lane. **Public lane**: an `adminAuth` 401 (admin token not configured, missing bearer, or a bearer that doesn't match), OR a presented OIDC session cookie was rejected (tampered/malformed or expired) — whichever reason is more specific wins when both apply on the same request. **Internal lane** (the untrusted sandbox / host sensor boundary): an `internalAuth` 401 on a missing or unverifiable run token, an `internalAuthGroundtruth` 401 on a missing or unverifiable host-sensor token, and `handleInternalRequestApproval`'s two 400s — a sidecar asking for an approval kind it may not raise (`credential` is broker-only, never sidecar-raised) or omitting `requested_scope`. Actor is always `system`, and it names WHICH boundary refused: `wardyn/adminAuth`, `wardyn/csrf`, `wardyn/internalAuth`, `wardyn/internalAuthGroundtruth`, `wardyn/internalApproval`, `wardyn/deviceAuth` (the hybrid device lane, `/api/v1/devices/{id}/*`), or `wardyn/oidcCallback` (the SSO sign-in callback refusing a login the identity provider approved, because of the user type the role map gives that person) (no verified caller identity exists at any of these points except `wardyn/csrf`'s, and public-lane stuffing vs. a probing sidecar vs. a cross-origin page are different incidents). Content-free: `reason` is a closed enum (`admin_token_not_configured`, `missing_bearer_token`, `invalid_admin_token`, `invalid_session`, `expired_session`, `revoked_session`, `session_revocation_unavailable` — the last two only when session revocation is wired — plus `cross_origin_refused`, the 0.7.3 CSRF guard's refusal of a cookie-authenticated cross-origin mutation in EITHER mode (`internal/api/csrf.go`'s `sameOriginOrRefuse` on the SSO branch, the LocalMode arm's own check; a **403**, not a 401, and the only reason here that is not an authentication failure but a request refused for where it came from — the caller was already past authentication (a valid session, or, in LocalMode, the no-auth bypass), which is also why its Actor is `wardyn/csrf` and not `wardyn/adminAuth`. The PTY-attach WebSocket's own cross-origin refusal emits the same pair) — plus the internal lane's `identity_provider_not_configured`, `missing_run_token`, `invalid_run_token`, `missing_sensor_token`, `invalid_sensor_token`, `unsupported_internal_approval_kind`, `missing_requested_scope`, `reserved_scope_key` — a sidecar naming a `lane` key in `requested_scope`, the key a control-plane-raised Azure DevOps escalation carries — and the device lane's `missing_device_token`, `invalid_device_token` — unknown and revoked alike — and `device_store_unavailable` — and the sign-in callback's `user_type_ambiguous` (the values the person matched name two or more custom user types at the same top priority, and Wardyn never picks one) and `user_type_unknown` (a matched value, or `WARDYN_OIDC_DEFAULT_ROLE`, names a user type that does not exist; never a wider default); neither is ever an `admin` sign-in, which lands on the built-in type instead. The type ids are in the `wardynd` log line beside the row, not in the row), never a user-supplied string; the request path is `Target`, the TCP peer is `SourceIP`. Rate-bound (process-global token bucket, 1/sec with a burst of 5) so a scanner throwing 401s cannot flood the append-only log — **so this action's row count is NOT the failure rate**: past the burst the trail flattens while the real volume grows, and the dropped emits are counted as `wardyn_auth_failed_suppressed_total` on `/metrics` ([OPERATIONS.md](OPERATIONS.md) → Monitoring). Alert on that series, not on rows of this action. **COALESCED as well as rate-bound since 0.7.2** (`WARDYN_AUDIT_COALESCE_WINDOW`, default `5m`, `0` = off — [ENV.md](ENV.md)): a run of IDENTICAL consecutive refusals (same actor, `reason` and path — since 0.8 NOT the peer, so a caller rotating its source address still folds) is folded into its FIRST row plus one **new** summary row carrying `count`, `peers` (distinct peer IPs folded, saturating at 100), `peer_ips` (those addresses, sorted, port stripped, at most 100), `peers_truncated` (true when a further peer arrived after the cap), `first_seen` and `last_seen`; the summary's `SourceIP` is the streak's opening peer — never an update of the first row, because this table is append-only and hash-chained, so a summary is always another append. Two rows on a summary, then, and the FIRST row of a streak carries only `reason` exactly as before; the window is the maximum GAP between two identical rows, and a streak also closes at 1000, so one row can never stand for unbounded volume. The folded rows are counted in the same `wardyn_auth_failed_suppressed_total` series as the rate-limited ones. It exists because the rate limit did nothing about a SLOW permanent drip: a sidecar retrying a renew it would never be granted wrote one row a minute forever and evicted every real security event out of the console's window — see `run.identity.expire` above for the other half of that fix | `count`, `first_seen`, `last_seen`, `peer_ips`, `peers`, `peers_truncated`, `reason` | `internal/api/http.go` (`auditAuthFailedAs` — the one rate-bound emit both lanes funnel through, `auditAuthFailed` is the public-lane wrapper), `internal/api/internal.go` (`handleInternalRequestApproval`), `internal/api/devices_auth.go` (`deviceAuth`), `internal/api/signin_denied.go` (`auditSignInDenied`, fed by `internal/auth/oidc/oidc_callback.go`'s `CallbackHandlerWithDenials`); session-rejection reason from `internal/auth/oidc/oidc.go`'s `Middleware`/`SessionRejectedFromContext` | internal | — |
| `egress.*` | The proxy reports an egress decision for a run — the literal suffix is the decision itself: `egress.allow`, `egress.deny`, or `egress.hold` (`egress.Decision`, `internal/egress/egress.go#Decision`). A synthetic `blind` scan decision emits only `llm.scan.bypass`, never a duplicate `egress.allow` for the tunnel. **A second synthetic shape:** when the proxy's decision buffer overflows, it posts a synthetic `egress.deny` with `target=""`, every Data field zero/empty, and `rule_source` carrying the extensible `egress:dropped-decisions-<n>` marker (`droppedSummaryLog`, `internal/egress/proxy/decisions.go`) — an audit-fidelity alert, not a policy denial of an actual request; `n` (the drop count) is embedded in that string, not a separate queryable field. **A third synthetic shape:** when the agent retries a `builtin:private-ip` refusal, the proxy answers the identical attempt from a per-run memo (`privateIPMemo`, `internal/egress/proxy/private_ip_memo.go`) rather than re-resolving and re-deciding, so those attempts get NO row of their own; when that streak closes — the memo evicts the entry, or the run ends — one NEW `egress.deny` is posted carrying `repeat`, the number of attempts refused without a row. `repeat` is absent on every ordinary decision, it is a count and never a rewrite of the row that opened the streak (the chain is append-only), and it is the one Data field that is queryable rather than embedded in `rule_source`. **`cause`/`via` ride a dial-shaped `builtin:dial-failed`/`builtin:gateway-vet-failed`/`builtin:upstream-protocol-mismatch` deny only** (`egress.DecisionLog.Cause`/`.Via`, `internal/egress/egress.go`): `cause` is the STAGE (`tcp dial`, `tls handshake`, `upstream proxy connect`, `dial`) plus the underlying error, masked and topology-redacted the same two ways the sandbox's own error body is (`Proxy.httpError`, `internal/egress/proxy/sandbox_error.go`) — this is a run's OWN CREATOR's row too (`auditScope` below), not just the operator's, so the redaction runs before the field is ever built — EXCEPT on `builtin:upstream-protocol-mismatch`, whose `cause` is the FIXED sentence given in that source's own row in the `rule_source` table below, never a stage prefix or an underlying error string (there is no dial stage to name: the round trip completed); `via` is the hop CLASS attempted, `direct` or `upstream-proxy`, never an address. Both absent on every decision that is not dial-shaped. **`upstream_fault` rides an `egress.allow` whose relayed Bedrock data-plane call (`/model/…`) AWS refused** (`egress.DecisionLog.UpstreamFault`, `internal/egress/proxy/bedrock_fault.go`): `AccessDeniedException` (403 — an SCP or IAM deny, or model access not enabled) or `ThrottlingException` (429), read off the status and `x-amzn-ErrorType` only, never a body; `recovered` marks the first clean model call on that host after one. The proxy relays the response unchanged and never retries; the control plane copies the class into the run's `failure_hint` (`internal/api/bedrock_dataplane_fault.go`), which run reads serve only on a FAILED run. Absent on every other decision | `approval_id`, `cause`, `host`, `method`, `path`, `port`, `repeat`, `rule_source`, `upstream_fault`, `via` | `internal/api/internal.go#Server.handlePostDecision` | internal | `internal/recordmode/recordmode.go`, `internal/api/workspace_egress.go#Server.handleObservedEgress` (`egress.deny`); console egress tile and audit screen |

### `egress.*`'s `rule_source` values

The row above names `rule_source` as a field but enumerates none of its
values; they were scattered ad hoc across `docs/ENV.md`, `docs/OPERATIONS.md`
and `docs/POLICIES.md` prose, with no single table. This is every
`ruleSource*` constant defined in `internal/egress/proxy` (excluding
`_test.go`) except `ruleSourceRequireTLS`, which is a `policy:` value and is
documented with that family in "The evaluator's own inline sources" below. It is the BROKERED and site-config vocabulary only: the
decisions `evaluate` itself writes pass their `rule_source` as an inline literal
rather than a named constant and are NOT in this table (`builtin:*`, `policy:*`,
`approval:*`, `internal/egress/proxy/proxy.go`), and the synthetic
`egress:dropped-decisions-<n>` marker is documented in the `egress.*` row above:

| `rule_source` | Meaning | Defined at |
|---|---|---|
| `brokered:mint` | A brokered `POST /wardyn/v1/credentials/mint` request | `internal/egress/proxy/local_routes.go#ruleSourceMint` |
| `brokered:approvals` | A brokered tool-call approval create/poll (`/wardyn/v1/approvals`) | `internal/egress/proxy/local_routes.go#ruleSourceApprovals` |
| `brokered:recording` | A brokered recording upload (`/wardyn/v1/recordings/`) | `internal/egress/proxy/local_routes.go#ruleSourceRecordings` |
| `brokered:scan-result` | A brokered scan-result upload (`/wardyn/v1/scan-results/`) | `internal/egress/proxy/local_routes.go#ruleSourceScanResults` |
| `policy:tool-allow` | A tool call the run's own `tool_rules` waved through with no human asked | `internal/egress/proxy/local_routes.go#ruleSourceToolAllow` |
| `policy:tool-deny` | A tool call `tool_rules` refused with no human asked | `internal/egress/proxy/local_routes.go#ruleSourceToolDeny` |
| `brokered:sso-token` | A brokered AWS SSO session upload from a container-login run (`/wardyn/v1/sso-token/`) | `internal/egress/proxy/local_routes.go#ruleSourceSSOToken` |
| `artifact:mitm` | A corp artifact-registry request TLS-MITM'd to inject the operator's registry token — not an LLM/inspection path | `internal/egress/proxy/local_routes.go#ruleSourceArtifactMITM` |
| `brokered:git` | Every decision the GitHub App-broker route (`/wardyn/gh/`) itself made — the per-repo allowlist ALLOW, and equally the DENY for an unparseable path, an ungranted repo, a non-smart-HTTP verb, or a mint/build failure (`ruleSourceGit`, `internal/egress/proxy/git_broker.go#Proxy.handleGitBroker`, `:177`, `:182`, `:218`, `:232`, `:248`); the three `brokered:git:branch-ns*` rows and the five `brokered:git:push-*` rows below are the only decisions carved out of it — seven refusals (`brokered:git:branch-ns`, `brokered:git:branch-ns-encoding`, `brokered:git:push-rules`, `brokered:git:push-too-large`, `brokered:git:push-uninspectable`, `brokered:git:push-held`, `brokered:git:push-held-unattended`) and one ALLOW that REPLACES the ordinary `brokered:git` allow when the parser is opted out (`brokered:git:branch-ns-off`, `allowSrc`, `internal/egress/proxy/git_broker.go#Proxy.handleGitBroker`) | `internal/egress/proxy/git_broker.go#ruleSourceGit` |
| `brokered:git:branch-ns` | A brokered push denied by push branch-namespace confinement (as opposed to the per-repo allowlist). Emitted by BOTH broker lanes since 0.7.2 - the App lane always, the `git_pat` lane when `WARDYN_GIT_PAT_BROKER_ENFORCE_BRANCH_NS` is on - from the one shared `confinePush` step, against each lane's own host | `internal/egress/proxy/git_broker.go#ruleSourceGitRef` |
| `brokered:git:branch-ns-encoding` | A brokered push refused because its body had a non-identity `Content-Encoding` the proxy could not inspect while branch-namespace confinement is on; same two lanes as the row above | `internal/egress/proxy/git_broker.go#ruleSourceGitEnc` |
| `brokered:git:branch-ns-off` | A brokered push forwarded WITHOUT parsing because branch-namespace confinement is opted out - deployment-wide (`WARDYN_GIT_BROKER_ENFORCE_BRANCH_NS=false`, `BranchNSEnforced`) or for this one run (`git_push_any_branch`, `internal/types/policy.go#RunPolicySpec`); both App-lane switches take the same branch (`internal/egress/proxy/git_broker.go#Proxy.handleGitBroker`), so the marker never says which one was set - see `docs/POLICIES.md`'s "`git_push_any_branch`: the per-run opt-out". Since 0.7.2 the `git_pat` lane emits it too, for its own THIRD switch's counterpart case: `WARDYN_GIT_PAT_BROKER_ENFORCE_BRANCH_NS` is ON and this run's `git_push_any_branch` opted out (`PATBranchNSEnforced`, `internal/egress/proxy/pat_broker.go#Proxy.handlePATBroker`). With that switch off - the default - a PAT push is not confined at all and keeps the ordinary `brokered:git-pat` allow, so this row never appears for it | `internal/egress/proxy/git_broker.go#ruleSourceGitNSOff` |
| `brokered:git:push-rules` | A brokered push denied because a path it introduces matched the run's `push_rules.deny_paths`. Emitted by every broker lane — the App lane, the `git_pat` lane and the per-person Azure DevOps (Entra) lane, where it comes before the capability check — whenever the run's policy carries content rules, INDEPENDENTLY of branch-namespace confinement and of either switch for it: `git_push_any_branch` opts out of WHERE a push may land and must never switch off WHAT it may contain (`internal/egress/proxy/push_rules.go#Proxy.inspectPush`). On the Azure DevOps Entra lane it is also emitted by the REST gate for a Git Pushes - Create body naming a denied path (`internal/egress/proxy/ado_content.go#Proxy.governADOContent`). The offending paths ride the sidecar's structured log and the refusal body git shows the person - at most ten of them - and NEVER this row, whose free-text fields (`cause`, `via`) are reserved for dial-shaped refusals | `internal/egress/proxy/push_rules.go#ruleSourceGitRules` |
| `brokered:git:push-too-large` | A brokered push refused because the request is bigger than `push_rules.max_inspect_pack_mib` (default `defaultInspectPackMiB`), so the content rules could not be applied to it. Refused rather than held: holding would ask a person to approve a push nobody inspected. The remedy is an operator raising that ceiling, which is an auditable authoring act | `internal/egress/proxy/push_rules.go#ruleSourceGitPackBig` |
| `brokered:git:push-uninspectable` | A brokered push refused because the inspector could not answer from the request's own bytes while content rules are in force: a thin pack whose delta bases stayed on the forge, a body in a non-identity `Content-Encoding`, a malformed pack or one past one of `internal/gitpack`'s ceilings, a `deny_paths` list too long to evaluate against the push, a `deny_paths` entry the broker cannot read (`types.DenyPathSegments`), or a wait for the sidecar's inspection slot that ran out (`503`) On the Azure DevOps Entra lane's REST gate, while the run has push rules: a Git Pushes - Create body the gate cannot read whole, and any other REST route that puts content on a branch without naming its paths (import, server-side commit/merge/cherry-pick/revert, fork sync, annotated tag, a ref pointed at a commit, a pull-request completion, a wiki page, a TFVC check-in) (`internal/egress/proxy/ado_content.go#Proxy.governADOContent`). Worded like the `brokered:git:branch-ns-encoding` refusal, and a distinct row so audit never reads an unreadable push as a matched rule | `internal/egress/proxy/push_rules.go#ruleSourceGitPackBlind` |
| `brokered:git:push-held` | A brokered push a `push_rules.require_review_paths` pattern matched was HELD for an admin's `push_content` approval and then refused: the admin denied it, nobody decided within `hold_seconds`, the approval closed undecided (expired or cancelled), the sidecar could not raise it, or the run already has too many pushes held or remembered. A push the admin approves writes the lane's ordinary ALLOW instead. Every broker lane, on the same trigger as `brokered:git:push-rules`, and only when no deny path matched. The approval itself goes through the existing `approval.decide` / `approval.expire` / `approval.cancel` actions; the matched paths ride the sidecar's structured log and at most ten of them the refusal body, never this row | `internal/egress/proxy/push_hold.go#ruleSourceGitPushHeld` |
| `brokered:git:push-held-unattended` | A brokered push a `push_rules.require_review_paths` pattern matched on an UNATTENDED run (a non-interactive task run, `proxy.Config.Unattended`), refused at once with no approval raised — there is nobody to ask. Distinct from `brokered:git:push-held` so audit can tell "nobody could be asked" from "someone said no" | `internal/egress/proxy/push_hold.go#ruleSourceGitPushHeldUnattended` |
| `brokered:git:forge-read` | The broker's OWN reads of `api.github.com` on a push's behalf, made with the run's credential when content rules need to show that a path the pack does not carry is unchanged. One ALLOW row per push that read the forge at all (up to 64 reads), host `api.github.com:443`; the read count rides the sidecar's structured log line, not this row. A push refused or passed from its own bytes reads nothing and records none | `internal/egress/proxy/push_rules.go#ruleSourceGitForgeRead` |
| `brokered:git-pat` | A PAT-broker-terminated ADO/GitLab (or unbrokered-GitHub) request, `WARDYN_GIT_PAT_BROKER=on` (`/wardyn/git/<host>/`). The `brokered:git:branch-ns*` and `brokered:git:push-*` rows are carved out of it on this lane too: one vocabulary, two brokers | `internal/egress/proxy/pat_broker.go#ruleSourcePAT` |
| `brokered:git-pat:denied` | ANY PAT-broker refusal — an unparseable `/wardyn/git/<host>/<rest>` path, a host not in the grant's allowlist, a non-smart-HTTP verb, or a mint / host-vet / request-build failure (`ruleSourcePATDenied`, `internal/egress/proxy/pat_broker.go#Proxy.handlePATBroker`, `:141`, `:162`, `:205`, and the shared outbound leg `forwardBrokeredGit` (handed `ruleSourcePATDenied` as its deny source) at `:240`, `:249`); the row names the LANE that refused, never which of the six causes | `internal/egress/proxy/pat_broker.go#ruleSourcePATDenied` |
| `brokered:ado` | An Azure DevOps REST request on a host the run's Azure DevOps grant covers, forwarded (with the brokered credential) because the capability catalogue (`internal/adoscope`) classified it and the run holds that capability; emitted by the MITM forward once the gate has passed it (`gateADO`, `internal/egress/proxy/mitm.go#Proxy.serveMITMRequest`) | `internal/egress/proxy/ado_gate.go#ruleSourceADO` |
| `brokered:ado:denied` | ANY Azure DevOps REST gate refusal - another organisation than the grant pins, a backslash or control character in the path, git smart-HTTP on the intercepted connection, a body the peek cannot see whole, a request the catalogue cannot classify, a denied area (tokens, service hooks, extensions, the internal contribution API), an unrecognized write, or a capability the run was not granted and that no person approved in time (a grantable capability is first HELD while the control plane raises an approval — `awaitADOCapability`, `internal/egress/proxy/ado_hold.go` — and forwarded under `brokered:ado` if it is approved), or any request to a covered host that arrives on the plain forward lane (an absolute-form request-line with no CONNECT), which never runs the gate and so refuses them all (`refuseADOPlain`); answered 403 in Azure DevOps' own error shape (`refuseADO`, `internal/egress/proxy/ado_gate.go#Proxy.refuseADO`); the row names the LANE that refused, never which cause | `internal/egress/proxy/ado_gate.go#ruleSourceADODenied` |
| `brokered:ado-git` | A git smart-HTTP request through the `/wardyn/git/<host>/` broker on a host the run's per-person Azure DevOps (Entra) grant covers, forwarded with the person's bearer after the organisation pin and the capability check passed (`serveADOGit`). A push is first put to the run's `push_rules`; the `brokered:git:push-*` refusals are carved out of this lane too, and come before the capability check | `internal/egress/proxy/pat_broker_entra.go#ruleSourceADOGit` |
| `brokered:ado-git:denied` | ANY refusal of git through the `/wardyn/git/<host>/` broker on a host the run's per-person Azure DevOps (Entra) grant covers - another organisation than the grant pins, a receive-pack command section it cannot read (encoded, malformed, oversized), a capability the run was not granted (clone/fetch and the receive-pack advertisement need `read`; a push needs `code_write`, or `policy_bypass` when it moves any ref outside the run's own `refs/heads/wardyn/<run-id>/` namespace), or a credential resolve / host-vet / request-build failure; capability and organisation refusals go through `refuseADOGit` (`internal/egress/proxy/pat_broker_entra.go#Proxy.refuseADOGit`), which first HOLDS a missing grantable capability for a person's approval (`awaitADOCapability`; a push is decided on its pack upload, so a `once` approval is spent there and never by the receive-pack advertisement or git's ref-less probe POST), and answers git in its own terms, never 401 | `internal/egress/proxy/pat_broker_entra.go#ruleSourceADOGitDenied` |
| `brokered:ado:upstream-not-signed-in` | An Azure DevOps REST request the gate forwarded was answered 203 with a `text/html` sign-in page: no credential Azure DevOps could read reached it. The client gets a 401 in Azure DevOps' JSON error shape (`typeKey` `NotSignedInException`) saying the run is not signed in, never the HTML page as a success (`relayUpstream`). Written AFTER the lane's own forwarding row, for the same request, as an ALLOW: Wardyn forwarded it and nothing in its policy refused it, so it never moves `wardyn_egress_denies_total`; the class comes from Azure DevOps' answer (F-LIVE-7), recorded by `noteADOUpstream` (`internal/egress/proxy/ado_refusal.go#adoUpstreamClass.ruleSource`) | `internal/egress/proxy/ado_refusal.go#ruleSourceADOUpstreamNotSignedIn` |
| `brokered:ado:upstream-refused` | An Azure DevOps REST request the gate forwarded was answered 401. A token missing the scope, an expired token and a revoked token answer identically, so the row never says which; the body passes through unchanged and `X-Wardyn-Egress-Detail` names all three possibilities (`relayUpstream`). Written AFTER the lane's own forwarding row, for the same request, as an ALLOW: Wardyn forwarded it and nothing in its policy refused it, so it never moves `wardyn_egress_denies_total`; the class comes from Azure DevOps' answer (F-LIVE-7), recorded by `noteADOUpstream` (`internal/egress/proxy/ado_refusal.go#adoUpstreamClass.ruleSource`) | `internal/egress/proxy/ado_refusal.go#ruleSourceADOUpstreamRefused` |
| `brokered:ado:upstream-no-access` | An Azure DevOps REST request the gate forwarded was answered 403 or 404 WITH `X-VSS-UserData`: the credential authenticated and the identity is not permitted (for example `TF401019`, `TF402455`). The body passes through unchanged and `X-Wardyn-Egress-Detail` says the account lacks access (`relayUpstream`). Written AFTER the lane's own forwarding row, for the same request, as an ALLOW: Wardyn forwarded it and nothing in its policy refused it, so it never moves `wardyn_egress_denies_total`; the class comes from Azure DevOps' answer (F-LIVE-7), recorded by `noteADOUpstream` (`internal/egress/proxy/ado_refusal.go#adoUpstreamClass.ruleSource`) | `internal/egress/proxy/ado_refusal.go#ruleSourceADOUpstreamNoAccess` |
| `brokered:ado-git:upstream-not-signed-in` | The `/wardyn/git/<host>/` broker's Entra lane: Azure DevOps answered 203 with a sign-in page. git gets the reason in its own terms (`refuseADOGitUpstream`: a rejected ref for a push that asked for report-status, else plain text), never the page. Written AFTER the lane's own forwarding row, for the same request, as an ALLOW: Wardyn forwarded it and nothing in its policy refused it, so it never moves `wardyn_egress_denies_total`; the class comes from Azure DevOps' answer (F-LIVE-7), recorded by `noteADOUpstream` (`internal/egress/proxy/ado_refusal.go#adoUpstreamClass.ruleSource`) | `internal/egress/proxy/ado_refusal.go#ruleSourceADOGitUpstreamNotSignedIn` |
| `brokered:ado-git:upstream-refused` | The broker's Entra lane: Azure DevOps answered 401. git gets a sentence naming an expired sign-in, a revoked one or a missing permission as the possibilities, never a credential prompt (`refuseADOGitUpstream`). Written AFTER the lane's own forwarding row, for the same request, as an ALLOW: Wardyn forwarded it and nothing in its policy refused it, so it never moves `wardyn_egress_denies_total`; the class comes from Azure DevOps' answer (F-LIVE-7), recorded by `noteADOUpstream` (`internal/egress/proxy/ado_refusal.go#adoUpstreamClass.ruleSource`) | `internal/egress/proxy/ado_refusal.go#ruleSourceADOGitUpstreamRefused` |
| `brokered:ado-git:upstream-no-access` | The broker's Entra lane: Azure DevOps answered 403 or 404 with `X-VSS-UserData`. git gets a sentence saying the account lacks access to the repository (`refuseADOGitUpstream`). Written AFTER the lane's own forwarding row, for the same request, as an ALLOW: Wardyn forwarded it and nothing in its policy refused it, so it never moves `wardyn_egress_denies_total`; the class comes from Azure DevOps' answer (F-LIVE-7), recorded by `noteADOUpstream` (`internal/egress/proxy/ado_refusal.go#adoUpstreamClass.ruleSource`) | `internal/egress/proxy/ado_refusal.go#ruleSourceADOGitUpstreamNoAccess` |
| `site-config:internal-host` | An allow via the operator's `internal_hosts` lift of the private/reserved-IP guard | `internal/egress/proxy/egress_target.go#ruleSourceInternalHost` |
| `site-config:egress-redirect` | An allow via an EXACT literal-IP entry an `egress_redirects` `to` substitution wrote (see `docs/OPERATIONS.md`, "Egress redirects: two tiers") | `internal/egress/proxy/egress_target.go#ruleSourceEgressRedirect` |
| `brokered:llm` | A brokered Anthropic/OpenAI LLM passthrough request (`/wardyn/llm/<provider>/`) | `internal/egress/proxy/llm_routes.go#ruleSourceLLM` |
| `scan:blocked` | An LLM request refused by content inspection (`on_scanner_error=block` or a blocking finding) | `internal/egress/proxy/llm_routes.go#ruleSourceLLMBlocked` |
| `scan:opaque-tunnel` | An opaque CONNECT to an LLM host inspection could not see into — the honest coverage-gap signal, paired with a synthetic `llm.scan.bypass` audit row | `internal/egress/proxy/llm_routes.go#ruleSourceLLMBlind` |
| `scan:mitm` | An LLM request inspected via TLS-MITM interception of an otherwise-opaque CONNECT tunnel (the subscription-OAuth path) | `internal/egress/proxy/llm_routes.go#ruleSourceLLMMITM` |

#### The evaluator's own inline sources

`evaluate()` (`internal/egress/proxy/proxy.go`) writes these `rule_source`
values directly at the `decisionLog` call site rather than through a named
constant — the table above is the BROKERED and site-config vocabulary only;
this is the rest of it, plus the `approval:<approval-id>` shape whose UUID
rides in the value itself.

One row here is NOT the evaluator's: `policy:require-tls` is emitted by the
plain forward lane (`internal/egress/proxy/plain_lane.go`) from a named
`ruleSource*` constant, for a refusal the evaluator cannot make — it has
already allowed the host, and what the lane refuses is the TRANSPORT the
operator's injection rule named. It sits in this table rather than the one
above because an operator reading a decision row looks up the `policy:`
family here:

| `rule_source` | Meaning | Defined at |
|---|---|---|
| `builtin:upstream-proxy` | A configured corp upstream proxy is in the path; recorded once at proxy construction, the hop the SSRF re-check is deferred to | `internal/egress/proxy/proxy.go#newProxy` |
| `builtin:private-ip` | The literal-IP guard denied a private/loopback/link-local/metadata literal no `internal_hosts` lift or `egress_redirects` `to` trusts exactly — step 0 and the post-`egressTarget` re-check alike. An identical retry against a host already refused here is answered from a per-run memo (`privateIPMemo`, `internal/egress/proxy/private_ip_memo.go`) and counted into one summary row instead of emitting a row per attempt — see the `egress.*` row's third synthetic shape | `internal/egress/proxy/literal_ip_guard.go#Proxy.literalIPGuard` (step 0), `internal/egress/proxy/proxy.go#Proxy.evaluate` (post-`egressTarget`) |
| `builtin:resolve-failed` | The name never resolved — a resolver outage, NXDOMAIN, or a zero-answer lookup — so no address was ever vetted. A DNS fault, NOT the address-range guard it used to be audited as: the two have opposite fixes, and this one is fixed at the sandbox's resolver rather than by declaring an `internal_hosts` lift | `internal/egress/proxy/proxy.go#Proxy.evaluate` (post-`egressTarget`, `errHostUnresolved`) |
| `policy:evaluator-error` | The pluggable evaluator returned an error: fail-closed deny | `internal/egress/proxy/proxy.go#Proxy.evaluate` |
| `policy:denied` | The run policy's deny list names the host (deny beats allow) | `internal/egress/proxy/proxy.go#Proxy.evaluate` |
| `approval:denied` | A first-use approval for this host was refused | `internal/egress/proxy/proxy.go#Proxy.evaluate` |
| `approval:pending` | A first-use approval is open and undecided — the request is held, not denied. Selected beside the cap label it is paired with (`approvalPendingReason`), so the two states and their audit labels cannot drift apart | `internal/egress/proxy/approvals.go#approvalPendingReason` |
| `approval:host-cap` | The run's first-use approval table is FULL (`maxApprovalHosts`, 4096), so no entry was made, no approval was raised and none ever will be for this host. Same fail-closed verdict as `approval:pending` and a different fact with a different fix, which is why it is its own label rather than folded into it: `pending` means a human has yet to answer, this means nobody was asked. Carries no `approval_id` — there is none. The sidecar also logs a one-shot `ERROR … host cap reached` | `internal/egress/proxy/approvals.go#ruleSourceApprovalHostCap` |
| `policy:default-deny` | No allowlist entry names the host and no approval covers it | `internal/egress/proxy/proxy.go#Proxy.evaluate` |
| `policy:method` | The host is allowed but the request's HTTP method is not | `internal/egress/proxy/proxy.go#Proxy.evaluate` |
| `policy:allowed` | An ordinary standing allowlist allow (`allowLog`, `approvalID == Nil`) | `internal/egress/proxy/proxy.go#Proxy.allowLog` |
| `policy:require-tls` | The plain forward lane refused a cleartext request to a host whose `api_key` injection rule sets `require_tls` (`docs/POLICIES.md`, `eligible_grants[].scope`): the operator declared the credential TLS-only, so the request is DENIED rather than merely forwarded uncredentialed. The lane writes its own 403 and this deny row REPLACES the allow the evaluator had already granted the host - nothing was forwarded. The one value here declared as a named `ruleSource*` constant rather than written inline, because it is the one decision the lane makes for itself | `internal/egress/proxy/plain_lane.go#ruleSourceRequireTLS` |
| `credential:reauth-timeout` | The proxy PARKED a sandbox's AWS SSO `GetRoleCredentials` call while the credential's owner was asked to sign in again (the `credential_reauth` approval, 0.7.6) and nobody signed in before the hold's budget ended (`WARDYN_CREDENTIAL_REAUTH_TIMEOUT`). A DENY, and deliberately NOT folded into the credential-refresh failure beside it: "nobody signed in" and "the credential could not be refreshed" have different fixes, and only one of them is a person's to make. The sandbox gets a modelled 401 `UnauthorizedException` rather than the 502 a refresh failure gives, because both AWS SDKs read a 502 as a transport error and RETRY it — three more full holds for one lapse. The approval row STAYS `PENDING`: the sign-in is still wanted, and the 24h sweeper or the run's terminal cascade closes it, so this row — not the approval — is what says a hold ran out. A held Azure DevOps request (the per-person Azure DevOps lane's sign-in hold) writes the same row once per hold, and its sandbox gets Azure DevOps' own 403 error shape (REST) or plain text (git) instead of the AWS body. Unlike every other decision, it does not reset the run's idle clock (`agent_runs.updated_at`, `internal/api/internal_touch.go#Server.shouldTouch`), because it reports that nobody answered, not agent activity | `internal/egress/proxy/local_routes.go#ruleSourceCredentialReauthTimeout` |
| `approval:<approval-id>` | Allowed by one specific approval — the UUID rides IN the value, so the trail self-joins to the approval (`allowLog`) | `approvalID.String()`, `internal/egress/proxy/proxy.go#Proxy.allowLog` |
| `builtin:dial-failed` | The vetted dial itself failed, so the earlier allow is superseded by a deny rather than over-reported (E3) — never the configured LLM gateway's OWN guard refusal, which carries `builtin:gateway-vet-failed` instead (its own row, below) | `internal/egress/proxy/proxy.go#Proxy.handleConnect` (CONNECT tunnel), `internal/egress/proxy/upstream_protocol.go#Proxy.failUpstream` (plain forward and the three git broker lanes: GitHub App, git_pat, Azure DevOps), `internal/egress/proxy/llm_routes.go#Proxy.forwardInspectedLLM` (brokered LLM round trip) |
| `builtin:gateway-vet-failed` | The operator's OWN configured LLM gateway host was refused by `vetTrustedHost`'s guard (`errGatewayVet`, `internal/egress/proxy/egress_target.go`) — it resolved to loopback/link-local/this proxy's own control-plane network, or did not resolve at all. A CONFIG problem the operator's own gateway can never satisfy by retrying, never a network-lost-it dial (`builtin:dial-failed`, above) and never the allow-shaped `brokered:llm` | `internal/egress/proxy/egress_target.go#ruleSourceGatewayVetFailed` (`ruleSourceGatewayVetFailed`), `internal/egress/proxy/llm_routes.go#Proxy.proxyLLMRequest` |
| `builtin:upstream-protocol-mismatch` | A round trip that GOT AN ANSWER: the peer sent an HTTP/2 frame (a SETTINGS frame on stream 0) on a TLS connection that negotiated no ALPN protocol, and the request could not be completed over HTTP/2 either — its body could not be replayed, or the HTTP/2 resend failed too (`Proxy.roundTripUpstream`, `internal/egress/proxy/upstream_protocol.go`). A request the HTTP/2 resend completes is an ordinary allow, and the host goes straight to HTTP/2 for the rest of the run. Never `builtin:dial-failed`: the network did not lose this one, and a protocol mismatch never succeeds on retry, so the sandbox gets a 400 (an `UpstreamProtocolMismatchException` on the AWS lane) instead of the 500 an SDK retries. `cause` is a FIXED sentence, not a stage-prefixed error text (contrast the `egress.*` row above): `peer answered HTTP/2 to an HTTP/1.1 request (ALPN: <negotiated protocol, or "none">)`, followed by why the HTTP/2 resend did not run (the body cannot be replayed) or the resend's own error, masked and topology-redacted like any dial cause. Detection reads the first bytes after the TLS handshake (`sniffH2`), which catches a peer that speaks first whatever its SETTINGS payload holds, and falls back to net/http's own response-parse error text (`isH2Preface`) for a peer that only answers once it has read a request | `internal/egress/proxy/egress_target.go#ruleSourceUpstreamProtocolMismatch` (`ruleSourceUpstreamProtocolMismatch`), `internal/egress/proxy/llm_routes.go#Proxy.forwardInspectedLLM`, `internal/egress/proxy/upstream_protocol.go#Proxy.failUpstream` (plain forward and the three git broker lanes) |

## Auth

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `auth.signin_unserialized` | A sign-in launch (`POST /setup/harness-login`) or its credential capture ran WITHOUT the per-person sign-in lock, because the database pool could not spare a connection for the hold — at `pool_max_conns=2` that is every sign-in (see [ENV.md](ENV.md)'s `WARDYN_PG_DSN`). The pass proceeds, so a person with two sign-ins at once can end up with two live login sandboxes; this row is the evidence. A lock that exists but cannot be taken in time is NOT this row: that sign-in is refused `503` and nothing is started or stored (a capture's refusal is `harness.credential.refuse` with `reason` `signin_busy`). Actor `system`/`wardynd`, `Target` the person (the login run's creator), `RunID` the login run. `reason` is a closed enum with one value, `lock_no_capacity` | `reason` | `internal/api/harnesscred_supersede.go#Server.lockLoginSupersede` | internal | — |
| `auth.user_view.set` | `POST /me/view`: a signed-in human turned Wardyn's user view — which looks through a chosen user type — on or off for their own session (0.8; renamed from `auth.member_mode`/"view as member", 0.7.4 — docs/OPERATIONS.md's "Renamed in 0.8" appendix; history not rewritten, so a pre-0.8 row still reads `auth.member_mode`). The TOGGLE is dual-emitted for one minor (0.8.x, OD-18) alongside the row below under the OLD action name, same `Data`, so a dashboard or SIEM rule still filtering on the old name keeps seeing rows; the compat row is removed in 0.9. The first SUCCESS-side auth action in the vocabulary — every other `auth.*` row is a failure — and it is here because the view's whole claim is that it is not impersonation: `Actor` is the caller's own OIDC sub, unchanged, on this row and on every row the session writes while the view is on. `enabled` is the requested state (`{"view":"user"}` is on, with the `user_type` it looks through — the type asked for, else the admin's previous choice, else their own type, else the built-in one; an absent/empty body or `{"view":"admin"}` means `false`, the exit); `real_role` is the STAMPED session role the view pauses, read off the cookie by `SetUserView` rather than from the request context, which is already clamped to `user` while the view is on. No row is written for a caller with no per-human role (admin token, local mode, no IdP) — that request is refused `400` before the toggle, because there is nothing to pause. `no_credential` marks the SECOND posture — "view as a new user (not signed in)", in which the caller's own per-user model credential reads as absent — and it rides the datum ONLY on a row where the server GRANTED that posture — the view was turned on, the caller holds a role to pause, and the model-access agent's roster row is `per_user` — so the key never claims a posture the session is not in. Also written, single-emit (no pre-0.8 equivalent to dual-emit against), by `GET /me` when it finds the viewed type deleted and drops the session back to the Admin view: `enabled: false`, the deleted `user_type`, and `reason` `user_type_deleted`. See [OPERATIONS.md](OPERATIONS.md)'s "Exercising member mode as an admin" for the view's ceilings | `enabled`, `no_credential`, `real_role`, `reason`, `user_type` | `internal/api/user_view.go#Server.handleSetUserView,Server.userViewGate` | internal | — |
| `auth.member_mode` | The pre-0.8 name of `auth.user_view.set` above (docs/OPERATIONS.md's "Renamed in 0.8" appendix). Dual-emitted alongside it for one minor (0.8.x, OD-18) — identical `Data`, same emit site — so a caller still filtering on this name keeps seeing rows; removed in 0.9. A row under this name from before the rename predates it entirely and is not rewritten | `enabled`, `no_credential`, `real_role` | `internal/api/user_view.go#Server.handleSetUserView` | internal | — |

## Devices (hybrid enrolment)

The organisation side of hybrid enrolment ([PLAN.md](design/0.8/PLAN.md), "Hybrid enrolment and audit federation"):
an admin mints a single-use enrolment token, a laptop's first boot trades it for a `wdd_` device
credential, and the device then pushes its own chained audit rows upward. The rows the organisation
writes ABOUT a device (the actions below) name it `system` / `device:<id>`. A FEDERATED row — one the
device pushed — is different: its `actor_type`, `action`, `target` and `outcome` are the device's
CLAIMS, so a laptop's `human` row stays `human`, and it is marked as forwarded three ways. Its `actor`
is `device:<id>/<claimed actor>` (`FederatedActor` in internal/store/store_devices.go), so no filter or
rule keyed on an organisation principal matches a laptop's claim to be that principal. Its
`data.device_origin` (`mergeDeviceOrigin`) holds the device id, its local seq, the hashes it
claimed and the `actor` and `source_ip` it claimed. And every audit read and the NDJSON export
carry a top-level `device_id` on it (absent on the organisation's own rows), and filter on
`?origin=device` or `?origin=organisation` — a row counts as forwarded only when the `actor` prefix
and `data.device_origin.device_id` name the same device (`FederatedDeviceID`). There is no action of
its own, so no filter keyed on `action` has to learn about federated rows. Three fields are never
the device's to choose: `actor` as above, `source_ip` is the peer the organisation saw the push
arrive from, and a row whose `run_id` names one of the organisation's own runs refuses the whole
batch. Every accepted row's claim recomputes from the stored row alone (`FederatedClaimHashSQL` in
internal/store/store_devices.go; a data-less claim is named by `device_origin.data_null`), so a
claim that could only be stored by changing it — data that is not an object or null, a top-level
`device_origin` key of its own, a `device_id` of its own, a target the laptop's own cap would have
shortened — is refused. A device-route authentication refusal is
`auth.fail` with Actor `wardyn/deviceAuth` (see Auth).

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `device.enrolment_token.create` | An admin mints a single-use enrolment token for one named laptop (`POST /admin/devices/enrolment-tokens`). `target` is the token row's id — never the token, which is returned once and stored only as a hash | `device_name`, `expires_at` | `internal/api/devices.go#Server.handleMintEnrolmentToken` | internal | — |
| `device.enrolment_token.revoke` | An admin or security admin cancels an enrolment token before any laptop redeems it (`DELETE /admin/devices/enrolment-tokens/{id}`); a later redemption answers as an unknown token does. `target` is the token row's id. A token already redeemed, revoked or expired is a 404 and writes no row | `device_name`, `minted_by` | `internal/api/devices.go#Server.handleRevokeEnrolmentToken` | internal | — |
| `device.enrol` | **success**: a laptop redeemed an enrolment token and was issued its device credential — actor `device:<id>`, `target` the new device id, `source_ip` the TCP peer. **failure**: the anonymous route refused a redemption — actor `wardyn/deviceEnrol`, `target` the path; `reason` is `invalid_enrolment_token` (unknown, expired and already-used are deliberately one reason) or `device_create_failed` (the token was spent but the device row was not written — re-mint). Failure rows are bounded the way `auth.fail` is: identical consecutive refusals (same `reason`, same path — deliberately NOT the peer, which an anonymous caller can rotate) fold into the streak's first row plus one summary row carrying `count`, `first_seen`, `last_seen` (`WARDYN_AUDIT_COALESCE_WINDOW`), and every row written pays the same process-wide bucket as `auth.fail`, whose folds and drops count in `wardyn_auth_failed_suppressed_total`; the route itself is limited per peer (an IPv6 peer by its /64), and a 429 writes no row | success: `name`, `enrolment_token_id`, `minted_by`; failure: `reason`; a summary adds `count`, `first_seen`, `last_seen` | `internal/api/devices_auth.go#Server.handleDeviceEnrol,Server.auditEnrolFailure` | internal | — |
| `device.revoke` | An admin or security admin revokes a device (`DELETE /admin/devices/{id}`); its next push or heartbeat answers 401. A second revoke is a 404 and writes no row | `name` | `internal/api/devices.go#Server.handleRevokeDevice` | internal | — |
| `device.audit.ingest` | FAILURES ONLY: an authenticated device's audit push was refused, nothing was stored and its cursor did not move. `reason` is closed: `invalid_body` (unreadable or not a strict array of rows), `batch_too_large` (over 500 rows), `invalid_row` (seq not strictly increasing, an `actor_type`/`outcome` outside the enum, a claim that could not re-check from the stored row, or a value Postgres cannot represent — a `\u0000` escape, a NUL in text), `chain_mismatch` (a claimed hash does not recompute, or the batch does not attach to the head this organisation recorded — answered 422), `org_run` (a row's `run_id` names one of this organisation's own runs — answered 422), `revoked` (the device was revoked after its request was authenticated — answered 401, as its next request is), `store_error`. A successful push writes no row of its own: the rows it carried are the record. Bounded per device: identical consecutive refusals from one device (same `reason`) fold into one streak with a summary row, and every row written pays that device's own bucket (5, then one a minute), so a laptop replaying a refused batch costs two rows however long it retries; folds and drops count in `wardyn_device_ingest_failures_suppressed_total`. A second push from a device whose previous push is still in progress is answered 429 and writes no row | `reason`, `rows` (submitted count), `acked_seq` (this organisation's recorded cursor for the device); a summary carries `reason`, `count`, `first_seen`, `last_seen` | `internal/api/devices_auth.go#Server.auditIngestFailure` | internal | — |
| `device.chain.reset` | A push was ACCEPTED whose first new row is a genesis row (empty `prev_hash`) although a chain was already recorded for the device — a purge of the laptop's own table. Accepted, because refusing would strand every later row, and recorded, because a developer with root can purge: this row is the evidence the set is not continuous | `prior_seq`, `prior_row_hash`, `accepted` | `internal/api/devices_auth.go#Server.handleDeviceAuditIngest` | internal | — |
| `device.local.enrol` | LAPTOP side: this daemon enrolled with the organisation at `WARDYN_ORG_URL` at boot — first boot, or a boot with a fresh `WARDYN_ORG_ENROLMENT_TOKEN` after a revocation (re-enrolment is a new device, so the forwarder's cursor restarts at 0 and the whole local table is pushed again). Actor `system` / `wardyn/federation`, `target` the device id the organisation issued. The credential itself is stored in the secret store under the reserved name `wardyn-org-device-credential` and never reaches a row | `name` (the name the organisation recorded) | `cmd/wardynd/boot_hybrid.go#bootHybrid` | internal | — |
| `device.local.revoke` | LAPTOP side: the organisation answered 401 with the device realm, or 410 — this device's credential is revoked. The forwarder stops, the revocation is recorded in the local `org_federation` row (so a restart, the organisation reachable or not, comes back still revoked; only a re-enrolment clears it), `/healthz`'s `org_federation.enrolled` turns false, and every run-creating path answers 503 naming re-enrolment. Runs already going continue, and a run created between the revocation and the forwarder's next call (at most one tick, 15 s, or one backoff interval, up to 5 min, while the organisation was unreachable) is legitimately local. Actor `system` / `wardyn/federation`, `target` the device id | `status` (401 or 410), `acked_seq` (the local cursor the organisation had acknowledged) | `internal/federation/forwarder.go#Forwarder.revoke` | internal | — |

## Kernel ground-truth (eBPF/Tetragon stream)

Defined as named constants (not scattered literals) in
`internal/groundtruth/groundtruth.go#ActionProcessExec` — the `kernel.` prefix is enforced
by the ground-truth ingest endpoint (any action without it is rejected,
fail-closed) and is the field SIEM rules key on alongside `data.stream=ebpf`.
This stream is **detection, not prevention** (see the package doc comment) —
it never blocks, and it is blind inside CC3/Kata microVM guests.

| Action | When | Where | Status | Consumers |
|---|---|---|---|---|
| `kernel.process.exec` | A process `execve` observed by the kernel sensor | `internal/groundtruth/groundtruth.go#ActionProcessExec` | **stable** (const, documented) | `internal/recordmode/recordmode.go#Capture`, `internal/api/healthz.go#groundtruthKinds` |
| `kernel.network.connect` | An outbound TCP connect observed by the sensor | `internal/groundtruth/groundtruth.go#ActionNetworkConnect` | **stable** (const, documented) | `internal/recordmode/recordmode.go#Capture`, `internal/api/healthz.go#groundtruthKinds` |
| `kernel.file.write` | A write to a sensitive path observed by the sensor | `internal/groundtruth/groundtruth.go#ActionFileWrite` | **stable** (const, documented) | `internal/recordmode/recordmode.go#Capture` |
| `kernel.sensor.ping` | Periodic liveness beat from the ingest sidecar (`run_id` NULL) — `/healthz` keys `ebpf_groundtruth` state off this | `internal/groundtruth/groundtruth.go#ActionSensorHeartbeat` | **stable** (const, documented) | `internal/api/healthz.go#Server.ebpfGroundtruthStatus` (`/healthz`) |
| `kernel.sensor.bypass` | One-time event for a run the host eBPF sensor cannot see into (CC3/Kata) | `internal/groundtruth/groundtruth.go#ActionSensorBlind` | **stable** (const, documented) | — |

## Base images, integrations & site config

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `base_image.write` | An admin creates/updates a base agent image | `image`, `kind`, `steps` | `internal/api/base_images.go#Server.handleCreateBaseImage` | internal | — |
| `base_image.delete` | An admin deletes a base image | `detached_from`, `forced` | `internal/api/base_images.go#Server.handleDeleteBaseImage` | internal | — |
| `integration.write` | An admin creates/updates an integration (secrets/egress/config/delivery) | `default_for`, `egress`, `header`, `kind` | `internal/api/setup_integrations.go#Server.handlePutIntegration` | internal | — |
| `integration.delete` | An admin deletes an integration | `credentials`, `egress`, `kind` | `internal/api/setup_integrations.go#Server.handleDeleteIntegration` | internal | — |
| `setup.onboarding.complete` | An operator finished (or deliberately left) the Getting Started funnel — stamps `SiteConfig.OnboardingCompletedAt`, the install-side fact the console's landing, welcome hero and setup gate read. Emitted once per install: the handler is idempotent and a later re-finish never moves the timestamp | `completed_at` | `internal/api/setup_onboarding.go#Server.handleSetupOnboardingComplete` | internal | — |
| `site_config.write` | `PUT /site-config` (full-document replace). `egress_redirects`/`internal_hosts`/`upstream_proxy_url`/`upstream_proxy_secret_ref` are recorded IN THE CLEAR (B7-F4) — the same disclosure `workspace_provider.write`'s `base_urls` already makes: topology, not a credential (a secret ref is a NAME, never the value it names). Without them, two PUTs naming two different proxy URLs produced the identical row, and an MDM-applied narrowing/opening of the redirect table or internal-host allowlist was unreviewable from the log alone. `egress_redirects` is capped at 50 from→to pairs (`egress_redirects_count` stays the honest, unbounded total; `egress_redirects_truncated` appears only past the cap). `sign_in_help_text`/`sign_in_help_url` (#484) are recorded IN THE CLEAR too, on every write: the anonymous `/healthz` publishes both, so the log holds nothing a signed-out reader cannot see | `agent_providers`, `egress_redirects`, `egress_redirects_count`, `egress_redirects_truncated` (only past the 50-pair cap), `git_providers`, `internal_hosts`, `internal_hosts_count`, `model_providers` (only once a model-provider block exists: the count of providers not turned off, so a deployment with none writes the row it always wrote), `per_user_credentials_invalidated` (while a model-provider block exists, or when this write removed one: how many people's own model-provider credentials it deleted first, as on `model_provider.write`), `scm_hosts_count`, `sign_in_help_text`, `sign_in_help_url`, `sources_no_longer_admitted` (only when the body NAMED `workspace_providers` — this is the CLI/MDM door a laptop re-applies on every boot, so an MDM-tightened base URL narrows here with nobody watching a console), `storage_configured`, `upstream_proxy_configured`, `upstream_proxy_secret_ref`, `upstream_proxy_url` | `internal/api/site_config.go#Server.handlePutSiteConfig` | internal | — |
| `site_config.proxy.test` | The site-config "test upstream proxy" probe runs | `custom_target`, `elapsed_ms`, `intercepted`, `state`, `target_host` | `internal/api/site_config_probe.go#Server.handleTestSiteConfigProxy` | internal | — |
| `site_config.redirect.test` | The site-config "test egress redirect" probe runs | `elapsed_ms`, `from_host`, `state`, `to_host` | `internal/api/site_config_probe.go#Server.handleTestSiteConfigRedirect` | internal | — |
| `site_config.probe.kill` | An egress-redirect probe run's finalize step (via `finalizeRunTail`; `reclaimProbeRun`'s doc comment explains why the audited name must be this endpoint's own, never `run.compose`) | — | `internal/api/site_config_probe.go#Server.reclaimProbeRun` | internal | — |

## Workspace and agent providers

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `workspace_provider.write` | An admin replaces the whole workspace-provider block (`PUT /workspace-providers`). `base_urls` is recorded IN THE CLEAR — a provider address is topology, not a credential, the same disclosure `integration.write` already makes with `egress` — and a narrowing applied by MDM is unreviewable without it. `sources_no_longer_admitted` is how many already-onboarded repo locators the new block refuses, so narrowing is never silent. A block written through `PUT /site-config` instead (the CLI/MDM door) audits as `site_config.write`, whose `git_providers`/`storage_configured` fields exist for exactly that case | `base_urls`, `disabled`, `git_count`, `kinds`, `lanes`, `sources_no_longer_admitted` | `internal/api/workspace_providers.go#Server.handlePutWorkspaceProviders` | internal | — |
| `agent_provider.write` | An admin replaces the whole agent roster (`PUT /agent-providers`) — which agents this deployment offers, the one model-access lane each may use, and whose credential that is. From 0.7.3 `pins` carries each row's `sso_account_id`/`sso_role_name` as `<account>/<role>`, sorted — the identity this deployment signs with, which a review of a refused capture (`harness.credential.refuse`) has no other way to learn. `sso_start_url` IS NEVER RECORDED, and the difference is deliberate: it is the one field of the block that names an organisation's identity provider, the audit log is read by more people than the providers page is, and no review question needs it. `credential_sources` renders an unset row as its effective value, `shared`. A roster written through `PUT /site-config` instead (the CLI/MDM door) audits as `site_config.write`, whose `agent_providers` count exists for exactly that case | `agent_count`, `credential_sources`, `defaults` (only when a row names a `default_provider`: `<agent>:<provider>` pairs, sorted), `disabled`, `ids`, `mechanisms`, `pins` | `internal/api/agent_providers.go#Server.handlePutAgentProviders` | internal | — |
| `model_provider.write` | An admin replaces the whole model-provider block (`PUT /model-providers`) — which kinds of model credential the deployment supports and which agents each serves. `harnesses` names each `<agent>:<provider>` pair; `pins` carries each Bedrock SSO provider's `<provider>:<account>/<role>`; `base_url_changed` names the providers whose address or header scheme changed (base URL, a per-agent path, the Bedrock region or data plane, the auth header), the change that must not carry a person's credential to a new destination; `kind_changed` names the providers whose kind changed, which are given a fresh `uid` so no credential given for the old kind follows the id. `per_user_credentials_invalidated` is how many people's own credentials the write deleted, in every namespace, before saving (rule 8): every credential for a provider whose address or header scheme changed, whose kind changed, or that was removed — a failed deletion refuses the write. `BaseURL` and `sso_start_url` ARE NEVER RECORDED, for the reason `agent_provider.write` never records the start URL. A block written through `PUT /site-config` instead (the CLI/MDM door) audits as `site_config.write`, whose `model_providers` count exists for exactly that case | `base_url_changed`, `disabled`, `harnesses`, `ids`, `kind_changed`, `kinds`, `per_user_credentials_invalidated`, `pins`, `provider_count` | `internal/api/model_providers_api.go#Server.handlePutModelProviders` | internal | — |
| `model_provider.credential.write` | A person stores their own key or token for a key or endpoint model provider (`PUT /model-providers/{id}/credential`) — in their own namespace only, admins included, never the operator's. `target` is the provider id. `owner` is the namespace it landed in. The value is never recorded | `owner`, `provider` | `internal/api/model_provider_credentials.go#Server.handlePutProviderCredential` | internal | — |
| `model_provider.credential.delete` | A person removes their own key or token for a model provider (`DELETE /model-providers/{id}/credential`). Idempotent: removing one never stored writes the row too | `owner`, `provider` | `internal/api/model_provider_credentials.go#Server.handleDeleteProviderCredential` | internal | — |

## System/reaper sources (no HTTP caller)

These are emitted by background sweepers, not request handlers — `ActorType`
is `types.ActorSystem` and `Actor` is a fixed component name
(`wardyn/lifecycle-reaper`, `wardyn/approval-sweeper`,
`wardyn/recording-sweeper`).

| Action | When | Data fields | Where | Status | Consumers |
|---|---|---|---|---|---|
| `recording.retention.sweep` | The recordings age-based retention sweep runs (the retention knob `docs/ENV.md#wardynd-control-plane` names — see `docs/OPERATIONS.md`'s audit-retention paragraph for the asymmetry with the audit log itself, which has no such knob) | — | `cmd/wardynd/adapters.go#runRecordingSweeper` | internal | — |
| `k8s.netpol.fail` | Boot-time NetworkPolicy canary verdict (`k8sNetpolVerdict`, also published on the anonymous `/healthz`'s `network_policy` field) grades this k8s substrate `unenforced` or `acknowledged` — every sandbox on it runs without a proven default-deny NetworkPolicy. Nil run id (deployment-wide, not tied to a run); silent on `enforced` and on every non-k8s driver | `verdict`, `driver` | `internal/api/reconcile.go#Server.auditK8sNetpolIfUnenforced` (`auditK8sNetpolIfUnenforced`) | internal | — |

## Notes on completeness

- This table is hand-curated from a source grep, not generated by a build
  step, but it is CI-gated like the closed-enum DB `CHECK` constraints
  `internal/db`'s `TestClosedEnumChecksMatchConstants` pins: a newly-added
  action literal with no row here fails `TestAuditActionsDoc_EveryEmitHasRow`
  / `TestAuditActionsForwardGuardCoversEveryEmitShape`, and a row whose
  citation no longer names what it claims fails
  `TestAuditActionsDocCitationsAreLive` (all three `cmd/wardynd`, gating
  `make ci`'s `cover-check`). What is NOT enforced is the `Data` field list
  inside a row — see the next point.
- `Data` field lists were read from the `mustJSON(map[string]any{...})` or
  `Data:` literal at the cited call site; a field marked here can still be
  `omitempty`'d away on a given event (e.g. `error` only appears on a
  `failure` outcome).
- Two related documents cover ground already: `docs/OPERATIONS.md`'s "Every
  denial that isn't a 404" owns `authz.denied`'s `reason` vocabulary in full,
  and `docs/SSH.md`'s "Audit actions" owns the four `ssh.*` actions — both are
  referenced above rather than restated.

## Renamed in 0.8

Wardyn has no users yet (owner ruling, 2026-09-22, #205/#203/#206), so this
lands as a clean break: no alias period, no dual emission, no
`superseded_by` marker on an old row. A consumer keyed on a name in the LEFT
column starts missing rows the moment this ships; the fix is to key on the
RIGHT column instead. Rows written before the upgrade keep the name they were
written with — the log is append-only and is never rewritten — so a query
that spans the upgrade has to name both.

One action is the exception to "no dual emission": the user view's toggle
(#617) writes `auth.member_mode` beside `auth.user_view.set`, with identical
`Data`, through 0.8.x (OD-18), and the compat row is removed in 0.9. The rest
of that rename — the `authz.denied` datum `member_mode` → `user_view` and the
reason `byoi_member` → `byoi_user` — is tabled in
[OPERATIONS.md's "Renamed in 0.8"](OPERATIONS.md#renamed-in-08).

Every name below was renamed to meet the grammar in "Grammar" above: 21 were
past tense (`scm.ado.signin.captured` also had four segments), 37 ended in
something other than a single verb — a noun, an adjective, or a compound
segment like `test_proxy` — and the three `run.workspace.requirement.*`
names had four segments. Where several old names became one, the third
column names the `Data` field that tells the merged rows apart. The `egress.*` and `llm.scan.*` suffixes are wire values
the egress proxy sends the control plane, so `egress.Decision`'s held value
is now `hold` (was `pending`) and `egress.ScanSummary.Action` sends `fail`
and `bypass` (were `error` and `blind`); the proxy and `wardynd` ship from
one release and move together.

| Old (0.7) | New (0.8) | Tells the merged rows apart |
|---|---|---|
| `approval.cancelled` | `approval.cancel` | — |
| `auth.failed` | `auth.fail` | — |
| `auth.member_mode` | `auth.user_view.set` | — |
| `capability.grant.created` | `capability.grant.create` | — |
| `capability.grant.deleted` | `capability.grant.delete` | — |
| `capability.grant.updated` | `capability.grant.update` | — |
| `credential.capability.requested` | `credential.capability.request` | — |
| `credential.reauth.requested` | `credential.reauth.request` | — |
| `credential.reauth.resolved` | `credential.reauth.resolve` | — |
| `device.audit.chain_reset` | `device.chain.reset` | — |
| `directory.search_failed` | `directory.search.fail` | — |
| `egress.pending` | `egress.hold` | — |
| `harness.credential.captured` | `harness.credential.capture` | — |
| `harness.credential.disconnected` | `harness.credential.disconnect` | — |
| `harness.credential.refused` | `harness.credential.refuse` | — |
| `harness.login.started` | `harness.login.start` | — |
| `k8s.netpol_unenforced` | `k8s.netpol.fail` | — |
| `kernel.sensor.blind` | `kernel.sensor.bypass` | — |
| `kernel.sensor.heartbeat` | `kernel.sensor.ping` | — |
| `llm.scan.blind` | `llm.scan.bypass` | — |
| `llm.scan.error` | `llm.scan.fail` | — |
| `llm.scan.skipped` | `llm.scan.skip` | — |
| `policy.inline` | `policy.inline.apply` | — |
| `run.agent_policy` | `run.agent_policy.write` | — |
| `run.files` | `run.files.fail` | — |
| `run.git_pat.brokered_forge` | `run.git_pat.drop` | `reason` `brokered_forge` |
| `run.git_pat.egress` | `run.egress.add` | `kind` `git_pat` |
| `run.identity.expired` | `run.identity.expire` | — |
| `run.injection.dropped` | `run.injection.drop` | — |
| `run.interactive` | `run.interactive.start` | — |
| `run.llm.bedrock` | `run.bedrock.configure` | — |
| `run.llm.subscription_inject` | `run.subscription.inject` | — |
| `run.llm_inspection.secrets_resolve` | `run.llm_inspection.resolve` | — |
| `run.policy.effective` | `run.policy.resolve` | — |
| `run.provider.lane_dropped` | `run.provider.veto` | — |
| `run.provider.ssh_host_level` | `run.provider.admit` | `reason` `ssh_host_level` |
| `run.resources` | `run.resources.fail` | — |
| `run.site_config.egress` | `run.egress.add` | `kind` `site_config` |
| `run.ssh.brokered_forge` | `run.ssh.drop` | `reason` `brokered_forge` |
| `run.ssh.egress` | `run.egress.add` | `kind` `ssh` |
| `run.ssh.unsupported_agent` | `run.ssh.drop` | `reason` `unsupported_agent` |
| `run.workspace.clone_egress` | `run.egress.add` | `kind` `workspace_clone` |
| `run.workspace.collision` | `run.workspace.collide` | — |
| `run.workspace.creds` | `run.workspace_cred.resolve` | — |
| `run.workspace.egress` | `run.egress.add` | `kind` `workspace` |
| `run.workspace.requirement.egress` | `run.requirement.allow` | — |
| `run.workspace.requirement.integration` | `run.requirement.inject` | — |
| `run.workspace.requirement.secret` | `run.requirement.grant` | — |
| `sandbox.orphan_sweep` | `sandbox.orphan.sweep` | — |
| `sandbox.sweep_requested` | `sandbox.sweep.request` | — |
| `scm.ado.signin.captured` | `ado.signin.capture` | — |
| `session.recording` | `session.recording.write` | — |
| `setup.onboarding.completed` | `setup.onboarding.complete` | — |
| `site_config.test_probe` | `site_config.probe.kill` | — |
| `site_config.test_proxy` | `site_config.proxy.test` | — |
| `site_config.test_redirect` | `site_config.redirect.test` | — |
| `ssh.auth` | `ssh.authenticate` | — |
| `ssh.channel_rejected` | `ssh.channel.reject` | — |
| `ssh.sftp` | `ssh.sftp.transfer` | — |
| `ui.auth` | `ui.authorize` | — |
| `workspace.provider.legacy_host` | `workspace.provider.admit` | `reason` `legacy_host` |

`authz.denied` keeps its past tense. It is the single heaviest-cited action
in the tree (31 non-test call sites) and a compatibility surface
`docs/OPERATIONS.md`'s "Every denial that isn't a 404" already commits to by
name, so its rename is its own reviewed change rather than a rider on this
one. `TestAuditActionsDoc_Grammar` carries it in `auditActionGrammarAllow`.

`secret.read`'s refusal `reason` values are now all snake_case. Eleven were
kebab-case: the four host pins — `host-not-organisation` (per-person Azure
DevOps), `sso-host-not-portal` (captured AWS SSO),
`oauth-host-not-anthropic` (subscription OAuth) and `oauth-host-not-provider`
(a per-person Claude subscription provider) — the stored-credential read
refusals `store-unavailable` and `not-found`, and the generic injection
path's `invalid-header-name`, `no-oauth-provider`, `reserved-secret-name`,
`resolve-failed` and `shared-subscription-posture`; each is now the same
words joined by underscores. Other actions' `reason` vocabularies are
unchanged.

The `egress.decisions.dropped:<n>` `rule_source` value (not an audit action —
see the `egress.*` row's second synthetic shape above) was the one
`rule_source` with dots in its family and a trailing colon. It is now
`egress:dropped-decisions-<n>` (family `egress`, kebab `dropped-decisions`,
the count appended with a hyphen).
