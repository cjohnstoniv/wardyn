<!-- GENERATED by scripts/test-gaps.sh — DO NOT EDIT BY HAND. -->
# Test gaps — untested exported surface (inventory)

_Generated 2026-09-21 by `scripts/test-gaps.sh` from `test/reports/go/union/coverage-func.txt`
(union coverage total **78.5%**), cross-checked against the Postgres lane (`test/reports/go/pg/coverage-func.txt`)._

This is an **inventory, not a promise**. An exported func listed here is not
automatically a bug: a thin pass-through, a driver only a live daemon exercises,
or a helper covered indirectly can legitimately read 0.0%. The list exists so the
*untested* public surface stays a watched number, kept separate from the
service-gated code a per-PR run structurally cannot reach. Only exported funcs are
counted (the package's public surface); unexported helpers are omitted. **The union
profile is built WITH `-coverpkg=./...`** (`scripts/test-report.sh`), so a func called
only from a DIFFERENT package's tests is counted covered here too — "Untested" below
means no test anywhere in the tree reaches it, not merely no same-package test.
Statement coverage also cannot see a branch inside an already-covered func that no
input exercises.

Refresh: `make cover-check` (+ `make test-report-pg` for the PG cross-check), then `./scripts/test-gaps.sh`.

## Summary

| Bucket | Exported funcs at 0.0% in the union |
|---|---:|
| **PG-gated** (proven covered by `ci test-pg`) | 168 |
| **Docker-gated** (needs `WARDYN_TEST_DOCKER=1`) | 10 |
| **Kubernetes-gated** (needs a real cluster) | 3 |
| **Pass-through** (delegating method, covered by its caller) | 33 |
| **Untested** (no test in the tree reaches it) | 31 |
| Total 0.0% exported | 245 |
| _(of 755 exported funcs in the union)_ | |

## Untested — no test in the tree reaches these

Exported funcs no test anywhere in the module reaches (module-wide instrumentation
via `-coverpkg=./...`) — the closest number to a real backlog this profile can
measure, not a proof nothing calls them. PG-package funcs appear here only when the
Postgres lane ALSO leaves them at 0.0%.

- **cmd/wardynd**: IsRevoked, ListRoleMappings, Record, RevokeJTI, RevokeRun, StopRun, Write
- **internal/api**: StartDrain
- **internal/broker**: WithSIEM
- **internal/contentscan**: Scan
- **internal/identity**: New
- **internal/lifecycle**: Run
- **internal/recording**: Sweep
- **internal/secretstore**: New
- **internal/store**: ActiveRunsAtWorkspacePath, ClearSourceActiveRun, CountApprovalsForRun, GetFederationCursor, ListGovernanceProfiles, ListWorkspacesPageForOwner, Ping, PruneAWSSSOSpentTokens, QueryAuditEventsFilteredPage, RunWatcherFresh, SetFederationCursor, SetWorkspaceBuiltImage, SetWorkspaceImportState, SetWorkspaceLLMCred, SetWorkspaceOwner, TouchDevice, UpdateSourceConfig

## Pass-through — interface/delegating methods, covered by their caller

Hand-classified (#174): a one-line `net.Conn`/`net.Addr`/`error`/
`Sink.Name()`-style wrapper or an adapter forwarding straight to the type
it adapts. 0.0% here is not a gap — the forward has no branch of its own to
miss, and whatever exercises the CALLER (or the interface's real
implementation) is what actually proves it works. See PASSTHROUGH_LIST in
`scripts/test-gaps.sh` for the reviewed list and its re-verification note.

- **cmd/wardyn-toolgate**: Error
- **cmd/wardynd**: BuildDevcontainer, BuildFromDevcontainerFiles, CancelForRun, CountForRun, Decide, FinalizeBase, Get, ListApprovalsPage, ListApprovalsPageByRun, ListApprovalsPageByRunCreator, Request, SweepOrphanedBuilds
- **internal/api**: Error, LocalAddr, Network, Peek, RemoteAddr, String
- **internal/audit/sinks**: Name, Name, Name
- **internal/broker**: Error, Error
- **internal/directory**: Error
- **internal/egress/evaluatortest**: MethodAllowed, Name
- **internal/egress/proxy**: Addr, Addr, Name, Read
- **internal/identity**: Error
- **internal/secretstore/pg**: Name

## PG-gated — covered by the Postgres lane, not by the per-PR run

0.0% in the enforced union (which can't set WARDYN_TEST_PG) but >0% in
`test/reports/go/pg`. Exercised on the `ci test-pg` job / `make test-report-pg`.

- **cmd/wardynd**: IsSessionRevoked, List, ListRunningWithPolicy, Record, RevokeAll, RevokeSub
- **internal/broker**: BeginReadCommitted, Commit, Exec, MintedCredentials, NewPgxStore, QueryRow, Rollback, Scan
- **internal/db**: AuditChainCanary, AuditDDLBypassRoutes, AuditDDLProtected, Connect, Migrate, TryAdvisoryLock
- **internal/recording**: NewPGStore, OpenCast, SaveCast, SaveCastNamed, StatAndTail
- **internal/secretstore/pg**: Delete, For, Get, List, Put, Rekey
- **internal/store**: AWSSSOTokenSpent, ActiveRunsByCreator, AddSSHKey, AddWorkspaceEgressDecision, ClaimSourceActiveRun, ClaimStaleRunWatchers, ClaimWorkspaceActiveRun, ClearWorkspaceActiveRun, ConsumeAttachTicket, ConsumeEnrolmentToken, CountActiveRunsBy, CreateAPIToken, CreateApproval, CreateDevice, CreateGrant, CreatePolicy, CreateRun, CreateWorkspace, DecideApproval, DeleteBaseImage, DeleteCapabilityGrant, DeleteGovernanceAssignment, DeleteGovernanceProfile, DeletePolicy, DeleteRef, DeleteRoleMapping, DeleteSSHKey, DeleteSource, DeleteUserDrive, DeleteUserDriveGrant, DeleteWorkspace, GetAPITokenByRaw, GetApproval, GetBaseImage, GetBaseImagesByIDs, GetCapabilityEnforcement, GetDeviceByRaw, GetGovernanceProfile, GetPolicy, GetRef, GetRun, GetRunLayout, GetSSHKeyByFingerprint, GetSiteConfig, GetSource, GetSourcesByIDs, GetUserDrive, GetWorkspace, HasGroupTierAssignments, HasGroupTierDriveGrants, HeartbeatRunWatcher, IngestDeviceAudit, InsertAuditEvent, LatestAuditEventByAction, ListAPITokens, ListAPITokensByPrincipal, ListApprovals, ListApprovalsPage, ListApprovalsPageByRun, ListApprovalsPageByRunCreator, ListAuditEventsAfterSeq, ListBaseImages, ListCapabilityGrants, ListCapabilityGrantsFor, ListDevices, ListGovernanceAssignments, ListGrantsByRun, ListGroupDenyGrants, ListPolicies, ListPoliciesPage, ListRoleMappings, ListRuns, ListRunsPage, ListRunsPageByCreator, ListSSHKeysByPrincipal, ListSources, ListUserDriveGrants, ListUserDriveGrantsPage, ListUserDrives, ListWorkspaces, ListWorkspacesPage, MarkAWSSSOTokenSpent, MergeWorkspaceRequirements, MintAttachTicket, MintEnrolmentToken, NewPG, PutCapabilityEnforcement, PutRef, PutRunLayout, PutSiteConfig, QueryAuditEvents, QueryAuditEventsPage, QueryRecentAuditEvents, QueryRecentAuditEventsPage, Record, RefreshAPITokenRoles, RefreshSSHKeyRoles, ResolveGovernanceProfile, ResolveReauthApproval, ResolveUserDrive, RevokeAPIToken, RevokeDevice, SetRunAgentExecID, SetRunFailureHint, SetRunImage, SetRunStatusDetail, SetSandboxRef, SetSourceScanResult, SetSourceScanResultUnfenced, SetWorkspaceApprovedEgress, SetWorkspaceDeniedEgress, SetWorkspaceRecordResult, SetWorkspaceRequirements, TouchAPIToken, TouchRun, UpdateBaseImageName, UpdatePolicy, UpdateRunStateIf, UpdateRunStateIfIdle, UpdateWorkspace, UpsertBaseImage, UpsertCapabilityGrant, UpsertGovernanceAssignment, UpsertGovernanceProfile, UpsertRoleMapping, UpsertSource, UpsertUserDrive, UpsertUserDriveGrant, VerifyAuditChain, WorkspacesAttaching, WorkspacesUsingBaseImage
- **pkg/client**: CreateSource, DeleteWorkspace, GetSource, GetWorkspace, ListSources, ScanSource

## Docker-gated — need a live daemon (WARDYN_TEST_DOCKER=1)

Real-daemon driver funcs; the fakeDocker tests self-skip them, so they read
0.0% wherever no daemon is present. Classified by package.

- **internal/envbuild**: New
- **internal/runner/docker**: ImagePresent, Read
- **test/awsssofake**: BedrockCalls, ReleaseParkedRoleCreds, RoleCredentialCallTimes, RoleCredentialsSeen, RunAWSCommand, RunDeviceCodeLogin, SetParkRoleCreds

## Kubernetes-gated — need a real cluster (WARDYN_TEST_K8S=1)

internal/runner/k8s's session funcs (Attach/Close/ExecStream/Read/Resize/Write)
— X1c-F13: these used to fall into "Untested" above with nothing distinguishing
"nobody has ever exercised the k8s substrate" from "this needs a cluster no
per-PR run has." Classified by package (no cluster here to prove it).

- **internal/runner/k8s**: ExecStream, Read, Write

### Manual cluster lanes no coverage profile can see at all

No workflow runs the first two, and `-coverpkg` never observes any of the
three — not even as the K8S-gated bucket above, since they exercise code far
beyond `internal/runner/k8s`. Listed here so the k8s surface is not read as
untouched. `make test-conformance-k8s` is the exception: CI runs it on kind +
Calico (the `conformance-k8s` job, busybox conformance-agent image, runc
only) — the other two self-skip without `WARDYN_TEST_K8S=1` and a green
result for them is evidence only for the tip somebody actually ran it on.

- `scripts/kind-sso-walk.sh` — **the AWS SSO walk** (0.7.4). Two Dex principals
  on a kind cluster, the containerized `aws sso login` against an on-cluster
  fake IAM Identity Center, and a member's Bedrock run whose role credentials
  the fake confirms were minted for THAT member's pinned account/role. Covers
  the per-user credential path end to end — `internal/api`'s `resolveBedrockAuth`
  ssoInject branch, `harnesscred.go`'s login run, `awssso_refresh.go`'s
  dispatch-time renewal — plus `internal/runner/k8s`'s Attach/ExecStream, which
  read 0.0% in the K8S-gated bucket above. Prereqs: `make kind-quickstart` +
  `make kind-sso`; recipe in docs/OPERATIONS.md, "Testing AWS SSO without an
  AWS tenant".
- `scripts/run-e2e-ssh-k8s.sh` (`make test-e2e-ssh-k8s`) — the SSH gateway over
  the k8s exec lane: `internal/runner/k8s`'s Attach/Close/ExecStream/Read/
  Resize/Write, every one of them listed in the K8S-gated bucket above.
- `make test-conformance-k8s` — the containment conformance suite against a
  NetworkPolicy-enforcing CNI.
