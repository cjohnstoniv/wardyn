{
  "//": "Typecheck-only config. `tsc -p tsconfig.check.json` cleanly typechecks the app sources + tests.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["vitest/globals", "@testing-library/jest-dom", "node"]
  },
  "include": ["src"]
}
