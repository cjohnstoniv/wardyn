Wardyn

Copyright 2025 The Wardyn Authors

This distribution includes third-party open-source components that are
distributed under their own licenses. Go module dependencies and bundled UI
dependencies are predominantly licensed under Apache-2.0, MIT, and BSD-style
licenses. The asciinema recorder binary is licensed under the GNU General Public
License (GPL) and is invoked only as an external subprocess by the wardyn-rec
component; it is never compiled into or dynamically linked with Wardyn.
Continuous integration gates Go dependency licenses (`go-licenses check`, which
fails the build on forbidden or restricted license types); per-dependency
attribution for Go modules can be regenerated locally with
`go-licenses report ./...`.

The console UI bundles the Inter and JetBrains Mono typefaces
(`@fontsource/inter`, `@fontsource/jetbrains-mono`; see
`ui/src/styles/index.css`), both licensed under the SIL Open Font License,
Version 1.1 (OFL-1.1). The OFL permits bundling, use, and redistribution as
part of a larger software distribution; each `@fontsource` package publishes
its own OFL-1.1 `LICENSE` file alongside the font files it installs.
Per-dependency attribution for UI (npm) dependencies, including these, can be regenerated
locally with `pnpm licenses list --prod --long` (CI does not yet publish a
generated THIRD-PARTY-LICENSES artifact — a follow-up item, not yet built).
